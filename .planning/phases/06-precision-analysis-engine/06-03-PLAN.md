---
phase: 06-precision-analysis-engine
plan: 03
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - pipeline.py
  - tests/test_pipeline.py
  - api/tasks.py
autonomous: true
requirements:
  - PREC-02
  - PREC-03
  - PREC-04
  - PREC-05
  - TUNING-03
  - QUAL-01

must_haves:
  truths:
    - "detect_key(wav_path, tuning_hz) usa Essentia KeyExtractor(profileType='edma', tuningFrequency=tuning_hz)"
    - "detect_key recebe tuning_hz como segundo argumento e passa para KeyExtractor antes de computar HPCP"
    - "analyze_audio() chama detect_tuning() ANTES de detect_key() e passa tuning_hz como argumento"
    - "analyze_audio() retorna dict com campo tuning_hz (float ou None)"
    - "json.dumps(analyze_audio(real_wav)) não levanta TypeError"
    - "api/tasks.py process_job retorna tuning_hz no dict de retorno"
    - "todos os testes da fase passam: test_detect_key_uses_tuning_hz, test_key_detection, test_json_output_shape"
  artifacts:
    - path: "pipeline.py"
      provides: "detect_key() Essentia + analyze_audio() atualizado"
      contains: "def detect_key(wav_path: Path, tuning_hz: float | None)"
    - path: "pipeline.py"
      provides: "campo tuning_hz no retorno de analyze_audio"
      contains: "\"tuning_hz\":"
    - path: "api/tasks.py"
      provides: "tuning_hz propagado para resposta da API"
      contains: "tuning_hz"
  key_links:
    - from: "pipeline.analyze_audio"
      to: "pipeline.detect_tuning"
      via: "tuning_hz = detect_tuning(wav_path)"
      pattern: "tuning_hz = detect_tuning"
    - from: "pipeline.analyze_audio"
      to: "pipeline.detect_key"
      via: "detect_key(wav_path, tuning_hz)"
      pattern: "detect_key\\(wav_path, tuning_hz\\)"
    - from: "pipeline.detect_key"
      to: "essentia.standard.KeyExtractor"
      via: "tuningFrequency=freq"
      pattern: "tuningFrequency=freq"
---

<objective>
Substituir detect_key() por Essentia KeyExtractor(profileType="edma"), atualizar analyze_audio() com a nova sequência de execução e campo tuning_hz, limpar código legado do librosa, propagar tuning_hz para api/tasks.py, e fazer todos os testes da fase ficarem GREEN.

Purpose: Este é o plan que fecha o ciclo TDD da fase: os stubs RED do Plan 01 ficam verdes, analyze_audio() entrega o contrato completo da Phase 6, e tasks.py expõe tuning_hz à API para que a Phase 7 possa exibi-lo na interface.
Output: pipeline.py com toda a migração concluída; api/tasks.py com tuning_hz; suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-precision-analysis-engine/06-RESEARCH.md
@.planning/phases/06-precision-analysis-engine/06-02-SUMMARY.md

<interfaces>
<!-- Estado de pipeline.py após Plan 02 (o que já existe ao chegar neste plan) -->

<!-- JÁ EXISTEM após Plan 02: -->
import essentia.standard as es
def detect_tuning(wav_path: Path) -> float | None: ...   # HPSS gate + librosa
def detect_bpm(wav_path: Path) -> float: ...             # Essentia RhythmExtractor2013

<!-- AINDA EXISTEM do código legado (serão substituídos/removidos neste plan): -->
_MAJOR_PROFILE = [6.35, 2.23, ...]
_MINOR_PROFILE = [6.33, 2.68, ...]
_NOTES = ["C", "C#", ...]
def _pick_best_tempo(...)  # NOTA: já removida no Plan 02 — confirmar ausência
def _detect_key_from_chroma(chroma): ...
def detect_key(wav_path: Path) -> tuple[str, float]: ...   # assinatura SEM tuning_hz

<!-- NOVA assinatura após este plan: -->
def detect_key(wav_path: Path, tuning_hz: float | None) -> tuple[str, float]:
    # usa es.MonoLoader(sampleRate=44100) + es.KeyExtractor(profileType="edma", tuningFrequency=freq)
    # freq = tuning_hz if tuning_hz is not None else 440.0
    # retorna (str(f"{key} {scale}"), float(strength))

<!-- analyze_audio() após este plan: -->
def analyze_audio(wav_path: Path) -> dict[str, Any]:
    # NOVA SEQUÊNCIA:
    # 1. validate_wav(wav_path) -> duration_sec
    # 2. detect_tuning(wav_path) -> tuning_hz   ← NOVO (antes de detect_key!)
    # 3. detect_bpm(wav_path) -> bpm            ← Essentia (já implementado)
    # 4. detect_key(wav_path, tuning_hz) -> (key, key_confidence)  ← NOVO argumento
    # 5. key_to_camelot(key) -> camelot
    # RETORNO: dict com campo "tuning_hz" adicionado

<!-- api/tasks.py — linha 71 retorno de process_job: -->
# ANTES:
return {
    "status": "done",
    "bpm": result["bpm"],
    ...
    "download_url": f"/files/{self.request.id}",
}
# DEPOIS — adicionar "tuning_hz":
return {
    "status": "done",
    "bpm": result["bpm"],
    "bpm_half": result["bpm_half"],
    "bpm_double": result["bpm_double"],
    "key": result["key"],
    "camelot": result["camelot"],
    "duration_sec": result["duration_sec"],
    "wav_path": result["wav_path"],
    "tuning_hz": result.get("tuning_hz"),   # ← NOVO: float ou None
    "download_url": f"/files/{self.request.id}",
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Substituir detect_key() por Essentia KeyExtractor e limpar código legado</name>

  <read_first>
    - pipeline.py — ler COMPLETO (estado atual após Plan 02); focar nas funções detect_key(), _detect_key_from_chroma(), nas constantes _MAJOR_PROFILE/_MINOR_PROFILE/_NOTES, e em analyze_audio()
    - tests/test_pipeline.py — ler test_detect_key_uses_tuning_hz (stub RED do Plan 01), test_key_detection (test existente a ser atualizado), test_bpm_half_double_calculation (mock de detect_key — verificar se assinatura muda quebra algo)
    - .planning/phases/06-precision-analysis-engine/06-RESEARCH.md — seção "Pattern 4: detect_key() com Essentia KeyExtractor", "Pitfall 3: detect_key recebe tuning_hz=None sem fallback", "Anti-Patterns a Evitar"
  </read_first>

  <files>pipeline.py, tests/test_pipeline.py</files>

  <behavior>
    - detect_key(sample_wav_path, tuning_hz=440.0) retorna tuple (str, float) onde str começa com "A " e termina com "major" ou "minor" (fixture é 440Hz — pitch class A)
    - detect_key(any_wav, tuning_hz=None) não levanta TypeError — usa 440.0 como fallback interno
    - detect_key(any_wav, tuning_hz=432.0) instancia KeyExtractor com tuningFrequency=432.0 (verificado por test_detect_key_uses_tuning_hz)
    - test_detect_key_uses_tuning_hz passa (GREEN)
    - test_key_detection passa (GREEN) com assinatura atualizada: detect_key(sample_wav_path, tuning_hz=None)
  </behavior>

  <action>
**Parte A: Substituir detect_key() em pipeline.py**

REMOVER as seguintes funções e constantes de pipeline.py:
- Constantes `_MAJOR_PROFILE`, `_MINOR_PROFILE`, `_NOTES`
- Função `_detect_key_from_chroma(chroma: np.ndarray) -> str`
- Função `detect_key(wav_path: Path) -> tuple[str, float]` (versão librosa/Krumhansl-Schmuckler)

ADICIONAR a nova detect_key() no lugar (após detect_bpm(), antes de _CAMELOT):

```python
# Stage 3c: Key detection (PREC-02, PREC-03, PREC-05)
def detect_key(wav_path: Path, tuning_hz: float | None) -> tuple[str, float]:
    """Tonalidade via Essentia KeyExtractor(profileType='edma') com correção de tuning.

    CRÍTICO: tuning_hz deve ser passado APÓS detect_tuning() — não antes.
    O parâmetro tuningFrequency é de instanciação: os bins HPCP são computados
    uma única vez. Se tuning_hz for None (beat percussivo), usa 440.0 Hz como
    fallback neutro — KeyExtractor ainda funciona, apenas sem correção de pitch.

    Args:
        wav_path: Path para o arquivo WAV.
        tuning_hz: Frequência de referência em Hz retornada por detect_tuning(),
                   ou None para beat percussivo. Nunca passar None sem verificação.

    Returns:
        Tuple de (key_string, confidence) onde key_string é '<NOTA> <major|minor>'
        (ex: 'F# minor', 'A major') e confidence é float em [0.0, 1.0].
    """
    audio = es.MonoLoader(filename=str(wav_path), sampleRate=44100)()
    freq = tuning_hz if tuning_hz is not None else 440.0
    key, scale, strength = es.KeyExtractor(
        profileType="edma",
        tuningFrequency=freq,
    )(audio)
    return str(f"{key} {scale}"), float(strength)
```

IMPORTANTE: A tabela `_CAMELOT` e a função `key_to_camelot()` devem ser mantidas sem alteração — já cobrem todos os 24 casos Essentia verificados.

Verificar que `import scipy.stats` pode ser removido agora (era usado apenas por detect_bpm() librosa). Remover se não houver outro uso no arquivo.

**Parte B: Atualizar analyze_audio() em pipeline.py**

Substituir o corpo de `analyze_audio()` pela nova sequência:

```python
def analyze_audio(wav_path: Path) -> dict[str, Any]:
    """Executa o pipeline completo de análise e retorna o dict JSON-serializable.

    Pipeline order:
      1. validate_wav  — ffprobe sanity check + duration retrieval.
      2. detect_tuning — librosa HPSS: concert pitch Hz ou None se percussivo.
      3. detect_bpm    — Essentia RhythmExtractor2013(method='multifeature').
      4. detect_key    — Essentia KeyExtractor(profileType='edma', tuningFrequency=tuning_hz).
                         tuning_hz passado ANTES dos bins HPCP serem computados (PREC-03).
      5. key_to_camelot — lookup O(1) na tabela estática.
      6. Derivar bpm_half e bpm_double (aritmética pura).

    O WAV NÃO é deletado por esta função — Phase 2 gerencia o ciclo de vida (D-09).

    Args:
        wav_path: Path para um arquivo WAV produzido por download_audio().

    Returns:
        Dict com os seguintes campos (todos tipos Python nativos — JSON-safe):
            bpm           (float)        — BPM primário detectado
            bpm_half      (float)        — bpm / 2 arredondado a 1 decimal
            bpm_double    (float)        — bpm * 2 arredondado a 1 decimal
            key           (str)          — '<NOTA> <major|minor>' (ex: 'F# minor')
            camelot       (str)          — código Camelot (ex: '11A') ou '?' se não mapeado
            key_confidence (float)       — confiança do KeyExtractor em [0.0, 1.0]
            tuning_hz     (float | None) — concert pitch em Hz, ou None se beat percussivo
            duration_sec  (float)        — duração do arquivo em segundos (via ffprobe)
            wav_path      (str)          — path do WAV como string (não Path object)

    Raises:
        ValueError: Se validate_wav falhar (arquivo ausente, corrompido, ou erro de ffprobe).
    """
    wav_path = Path(wav_path)
    duration_sec = validate_wav(wav_path)
    tuning_hz = detect_tuning(wav_path)           # Step 2: ANTES de detect_key (PREC-03)
    bpm = detect_bpm(wav_path)                    # Step 3: Essentia
    key, key_confidence = detect_key(wav_path, tuning_hz)  # Step 4: tuning_hz já calculado
    camelot = key_to_camelot(key)

    return {
        "bpm":            float(bpm),
        "bpm_half":       round(float(bpm) / 2, 1),
        "bpm_double":     round(float(bpm) * 2, 1),
        "key":            str(key),
        "camelot":        str(camelot),
        "key_confidence": float(key_confidence),
        "tuning_hz":      tuning_hz,    # float ou None — JSON-serializable (TUNING-03)
        "duration_sec":   round(float(duration_sec), 1),
        "wav_path":       str(wav_path),
    }
```

**Parte C: Atualizar testes que usam detect_key() sem tuning_hz**

Em `tests/test_pipeline.py`, localizar e atualizar os seguintes trechos:

1. `test_key_detection` (linha ~138): a chamada `pipeline.detect_key(sample_wav_path)` deve tornar-se `pipeline.detect_key(sample_wav_path, tuning_hz=None)` — a assinatura antiga sem segundo argumento quebrará com TypeError.

2. `test_bpm_half_double_calculation` (linha ~156): o mock `patch.object(pipeline, "detect_key", return_value=("C major", 1.0))` não precisa de alteração — o mock substitui a função inteira, não depende da assinatura.

3. `test_json_output_shape` (linha ~201): atualizar o conjunto `required` para incluir `"tuning_hz"` e atualizar o mock de `detect_key` para a nova assinatura se necessário. O mock atual usa `patch.object(pipeline, "detect_key", return_value=("F# minor", 1.0))` — ainda válido (mock ignora assinatura). Mas o `required` set deve ser:
   ```python
   required = {"bpm", "key", "camelot", "bpm_half", "bpm_double", "wav_path", "duration_sec", "tuning_hz"}
   ```
   E adicionar mock para detect_tuning:
   ```python
   with patch.object(pipeline, "detect_bpm", return_value=140.0), \
        patch.object(pipeline, "detect_key", return_value=("F# minor", 1.0)), \
        patch.object(pipeline, "detect_tuning", return_value=440.0), \
        patch.object(pipeline, "validate_wav", return_value=183.0):
   ```

Após as alterações, rodar:

```bash
.venv/bin/python3 -m pytest tests/test_pipeline.py -m "not e2e" -q 2>&1 | tail -15
```

Todos os testes não-e2e devem passar.
  </action>

  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_pipeline.py::test_detect_key_uses_tuning_hz tests/test_pipeline.py::test_key_detection tests/test_pipeline.py::test_json_output_shape -x -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - pipeline.py contém `def detect_key(wav_path: Path, tuning_hz: float | None) -> tuple[str, float]:`
    - pipeline.py contém `es.KeyExtractor(`
    - pipeline.py contém `profileType="edma"`
    - pipeline.py contém `tuningFrequency=freq`
    - pipeline.py contém `freq = tuning_hz if tuning_hz is not None else 440.0`
    - pipeline.py NÃO contém `_detect_key_from_chroma` (função legada removida)
    - pipeline.py NÃO contém `_MAJOR_PROFILE` (constante legada removida)
    - pipeline.py contém `"tuning_hz":` no corpo de analyze_audio() (campo no dict de retorno)
    - pipeline.py contém `tuning_hz = detect_tuning(wav_path)` em analyze_audio()
    - pipeline.py contém `detect_key(wav_path, tuning_hz)` em analyze_audio() (tuning_hz passado como argumento)
    - tests/test_pipeline.py contém `"tuning_hz"` no set `required` de test_json_output_shape
    - `pytest tests/test_pipeline.py::test_detect_key_uses_tuning_hz -x -q` sai com 0 (PASSED — stub RED virou GREEN)
    - `pytest tests/test_pipeline.py::test_key_detection -m integration -x -q` sai com 0 (PASSED)
    - `pytest tests/test_pipeline.py::test_json_output_shape -x -q` sai com 0 (PASSED)
  </acceptance_criteria>

  <done>detect_key() Essentia implementada; código legado Krumhansl-Schmuckler removido; test_detect_key_uses_tuning_hz GREEN; analyze_audio() com campo tuning_hz e nova sequência.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagar tuning_hz para api/tasks.py e executar suite completa</name>

  <read_first>
    - api/tasks.py — ler a função process_job() inteira; focar no dict de retorno (linhas 71-81) para identificar onde adicionar tuning_hz
    - tests/test_pipeline.py — ler test_json_output_shape para confirmar que o campo tuning_hz foi adicionado corretamente na Task 1
    - .planning/phases/06-precision-analysis-engine/06-RESEARCH.md — seção "Pitfall 6: tasks.py não expõe tuning_hz no resultado"
  </read_first>

  <files>api/tasks.py, tests/test_pipeline.py</files>

  <behavior>
    - process_job() retorna dict com campo "tuning_hz" (float ou None)
    - json.dumps(result) dentro do worker Celery não levanta TypeError (tuning_hz é float ou None — ambos JSON-safe)
    - Suite completa não-e2e: 0 falhas
  </behavior>

  <action>
**Parte A: Adicionar tuning_hz ao dict de retorno de process_job() em api/tasks.py**

Localizar o dict de retorno na função `process_job()` (linha ~71):

```python
return {
    "status": "done",
    "bpm": result["bpm"],
    "bpm_half": result["bpm_half"],
    "bpm_double": result["bpm_double"],
    "key": result["key"],
    "camelot": result["camelot"],
    "duration_sec": result["duration_sec"],
    "wav_path": result["wav_path"],
    "download_url": f"/files/{self.request.id}",
}
```

Adicionar `"tuning_hz": result.get("tuning_hz"),` antes de `"download_url"`. O dict após a mudança deve ser:

```python
return {
    "status": "done",
    "bpm": result["bpm"],
    "bpm_half": result["bpm_half"],
    "bpm_double": result["bpm_double"],
    "key": result["key"],
    "camelot": result["camelot"],
    "duration_sec": result["duration_sec"],
    "wav_path": result["wav_path"],
    "tuning_hz": result.get("tuning_hz"),   # float ou None — TUNING-03; Phase 7 exibe "A = X Hz"
    "download_url": f"/files/{self.request.id}",
}
```

Usar `.get("tuning_hz")` em vez de `["tuning_hz"]` como defesa contra regressão — se analyze_audio() por algum motivo não retornar o campo, retorna None em vez de levantar KeyError.

**Parte B: Verificar test_json_output_shape com WAV real (QUAL-01)**

Confirmar que o teste de integração test_json_output_shape com o fixture real está no estado correto. O teste deve:
- Chamar `analyze_audio(sample_wav_path)` com o WAV real (não mocks)
- Verificar que `tuning_hz` está no conjunto `required`
- Verificar que `json.dumps(result)` não levanta TypeError
- Verificar que `parsed["tuning_hz"]` é float ou None

Revisar o teste atual em tests/test_pipeline.py. Se ele ainda usa apenas mocks (patch.object), adicionar uma versão `@pytest.mark.integration` separada que usa o WAV real:

```python
@pytest.mark.integration
def test_json_output_shape_integration(sample_wav_path):
    """analyze_audio() em WAV real deve ter tuning_hz e json.dumps não deve levantar TypeError.

    QUAL-01: valida que todos os tipos no dict são JSON-nativos (sem numpy types).
    """
    import json as _json
    pipeline = pytest.importorskip("pipeline", reason="pipeline.py não implementado")
    result = pipeline.analyze_audio(sample_wav_path)
    required = {"bpm", "key", "camelot", "bpm_half", "bpm_double",
                "wav_path", "duration_sec", "tuning_hz"}
    assert required.issubset(result.keys()), f"Missing: {required - set(result.keys())}"
    serialized = _json.dumps(result)  # não deve levantar TypeError — QUAL-01
    parsed = _json.loads(serialized)
    assert parsed["tuning_hz"] is None or isinstance(parsed["tuning_hz"], float), (
        f"tuning_hz deve ser float ou None, got {type(parsed['tuning_hz'])}"
    )
    assert isinstance(parsed["bpm"], float), f"bpm deve ser float, got {type(parsed['bpm'])}"
    assert isinstance(parsed["key"], str), f"key deve ser str, got {type(parsed['key'])}"
```

**Parte C: Executar suite completa não-e2e**

```bash
.venv/bin/python3 -m pytest tests/test_pipeline.py -m "not e2e" -q 2>&1 | tail -20
```

Esperado: todos os testes passam. Nenhum FAILED ou ERROR. Em particular:
- test_detect_tuning_harmonic PASSED
- test_detect_tuning_percussive PASSED
- test_detect_key_uses_tuning_hz PASSED
- test_bpm_accuracy PASSED
- test_key_detection PASSED
- test_json_output_shape PASSED
- test_json_output_shape_integration PASSED
- test_bpm_half_double_calculation PASSED
- test_camelot_mapping PASSED

Se algum teste falhar, investigar e corrigir antes de concluir este plan. Não marcar como done com falhas.
  </action>

  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_pipeline.py -m "not e2e" -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - api/tasks.py contém `"tuning_hz": result.get("tuning_hz"),` no dict de retorno de process_job()
    - `grep "tuning_hz" api/tasks.py` retorna pelo menos 1 match
    - tests/test_pipeline.py contém `def test_json_output_shape_integration(`
    - tests/test_pipeline.py contém `"tuning_hz"` no set required de test_json_output_shape_integration
    - `pytest tests/test_pipeline.py -m "not e2e" -q` sai com código 0 e imprime "X passed" sem FAILED ou ERROR
    - `python3 -c "import json; import pipeline; result = pipeline.analyze_audio('tests/fixtures/sample.wav'); json.dumps(result)"` sai com código 0 (sem TypeError)
  </acceptance_criteria>

  <done>tuning_hz propagado para tasks.py; suite completa não-e2e verde; QUAL-01 satisfeito com WAV real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wav_path → detect_key | Path vindo de analyze_audio(); já sanitizado por validate_wav() upstream |
| Celery worker → JSON serializer | dict de retorno de process_job() serializado pelo Celery — numpy types quebrariam silenciosamente |
| API consumer → tuning_hz | Campo exposto no GET /jobs/{id}; valor é float ou None — sem PII, sem vetores de ataque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | numpy type no dict de retorno | mitigate | `float()` defensivo em todos os valores Essentia em detect_key(); `result.get("tuning_hz")` em tasks.py retorna None (não levanta) se campo ausente por regressão |
| T-06-03-02 | Information Disclosure | wav_path no dict de retorno de tasks.py | accept | wav_path é um caminho interno /tmp/sg_*.wav — não é dado do usuário; já exposto na API v1 (campo existente desde Phase 2) |
| T-06-03-03 | Denial of Service | detect_key com WAV corrompido | accept | validate_wav() (ffprobe) já rejeita WAVs corrompidos antes de chegar a detect_key; exceção ValueError é capturada em process_job() e mapeada para JobFailure sanitizado |
| T-06-03-04 | Elevation of Privilege | essentia.standard.KeyExtractor | accept | Roda in-process como biblioteca Python pura — sem subprocessos, sem acesso elevado |
</threat_model>

<verification>
Suite completa não-e2e deve estar verde após este plan:

```bash
.venv/bin/python3 -m pytest tests/test_pipeline.py -m "not e2e" -q
```

Saída esperada: todos os testes passam, incluindo os 3 novos (test_detect_tuning_harmonic, test_detect_tuning_percussive, test_detect_key_uses_tuning_hz) e o integration (test_json_output_shape_integration).

Verificação de serialização direta:

```bash
.venv/bin/python3 -c "
import json, pipeline
result = pipeline.analyze_audio('tests/fixtures/sample.wav')
print('tuning_hz:', result.get('tuning_hz'))
print('json ok:', len(json.dumps(result)), 'chars')
"
```

Deve imprimir tuning_hz como float ou None e "json ok: N chars" sem erro.
</verification>

<success_criteria>
- `grep "tuning_hz = detect_tuning" pipeline.py` retorna match (ordem correta em analyze_audio)
- `grep "detect_key(wav_path, tuning_hz)" pipeline.py` retorna match (tuning_hz passado)
- `grep "\"tuning_hz\":" pipeline.py` retorna match (campo no dict de retorno)
- `grep "tuning_hz" api/tasks.py` retorna match
- `pytest tests/test_pipeline.py -m "not e2e" -q` — 0 falhas, todos PASSED
- `python3 -c "import json, pipeline; json.dumps(pipeline.analyze_audio('tests/fixtures/sample.wav'))"` — sai sem TypeError
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-precision-analysis-engine/06-03-SUMMARY.md`
</output>
