---
phase: 06-precision-analysis-engine
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - tests/test_pipeline.py
autonomous: true
requirements:
  - PREC-01
  - PREC-02
  - PREC-03
  - TUNING-01
  - TUNING-02
  - QUAL-01

must_haves:
  truths:
    - "requirements.txt contém essentia==2.1b6.dev1389 e `import essentia.standard` no venv não levanta exceção"
    - "test_detect_tuning_harmonic existe em test_pipeline.py e falha (RED) antes da implementação"
    - "test_detect_tuning_percussive existe em test_pipeline.py e falha (RED) antes da implementação"
    - "test_detect_key_uses_tuning_hz existe em test_pipeline.py e falha (RED) antes da implementação"
  artifacts:
    - path: "requirements.txt"
      provides: "Dependência essentia fixada"
      contains: "essentia==2.1b6.dev1389"
    - path: "tests/test_pipeline.py"
      provides: "Stubs RED para tuning e key com tuning_hz"
      contains: "test_detect_tuning_harmonic"
  key_links:
    - from: "tests/test_pipeline.py::test_detect_tuning_harmonic"
      to: "pipeline.detect_tuning"
      via: "importorskip"
      pattern: "pipeline\\.detect_tuning"
    - from: "tests/test_pipeline.py::test_detect_key_uses_tuning_hz"
      to: "pipeline.detect_key"
      via: "mock KeyExtractor"
      pattern: "tuningFrequency"
---

<objective>
Instalar Essentia no venv e criar os 3 stubs TDD RED que servem de contrato para os plans 02 e 03.

Purpose: Os stubs garantem que o executor dos plans seguintes implemente exatamente o comportamento testável definido aqui. Sem os stubs RED primeiro, é impossível verificar se a implementação satisfaz os requisitos TUNING-01, TUNING-02 e PREC-03.
Output: `requirements.txt` com `essentia==2.1b6.dev1389`; 3 novos testes em `tests/test_pipeline.py` em estado RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-precision-analysis-engine/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar essentia ao requirements.txt e verificar import no venv</name>

  <read_first>
    - requirements.txt — ver dependências atuais para não duplicar nem conflitar
    - .planning/phases/06-precision-analysis-engine/06-RESEARCH.md — seção "Standard Stack" e "Instalação" para versão exata
  </read_first>

  <files>requirements.txt</files>

  <action>
Adicionar a linha abaixo ao final de requirements.txt (após `redis==6.4.0`):

```
essentia==2.1b6.dev1389
```

Em seguida instalar a dependência no venv:

```bash
.venv/bin/pip install essentia==2.1b6.dev1389
```

Verificar que o import funciona:

```bash
.venv/bin/python3 -c "import essentia.standard; print('essentia.standard OK')"
```

A saída deve ser `essentia.standard OK` sem exceção. Se levantar ImportError ou ModuleNotFoundError, NÃO prosseguir — reportar o erro imediatamente.

PITFALL: não usar `import essentia` sem `.standard` — as classes algorítmicas estão em `essentia.standard`.
  </action>

  <verify>
    <automated>grep "essentia==2.1b6.dev1389" requirements.txt && .venv/bin/python3 -c "import essentia.standard; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - requirements.txt contém a linha exata `essentia==2.1b6.dev1389`
    - `.venv/bin/python3 -c "import essentia.standard"` sai com código 0 e imprime "OK"
    - requirements.txt não contém `essentia-tensorflow` (seria ~2GB de dependências desnecessárias)
  </acceptance_criteria>

  <done>essentia 2.1b6.dev1389 listado em requirements.txt e importável no venv sem exceção.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar stubs TDD RED — test_detect_tuning_harmonic, test_detect_tuning_percussive, test_detect_key_uses_tuning_hz</name>

  <read_first>
    - tests/test_pipeline.py — ver estrutura atual, fixtures usadas, padrão de marcadores, onde inserir os novos testes
    - tests/conftest.py — ver fixtures disponíveis (sample_wav_path, etc.)
    - .planning/phases/06-precision-analysis-engine/06-RESEARCH.md — seção "Wave 0 Gaps" e "Mapeamento Requisitos → Testes"
  </read_first>

  <files>tests/test_pipeline.py</files>

  <behavior>
    - test_detect_tuning_harmonic: chama pipeline.detect_tuning(sample_wav_path) onde sample_wav_path é o WAV 440Hz de 5s; espera retornar um float > 400 e < 480 (raio de ±10% em torno de 440 Hz), não None, não numpy type
    - test_detect_tuning_percussive: cria um sinal de ruído branco puro (sem componente harmônico) via numpy, salva como WAV temporário, chama pipeline.detect_tuning(); espera retornar None (razão harmônica < 0.2 dispara o gate HPSS)
    - test_detect_key_uses_tuning_hz: usa unittest.mock.patch para substituir essentia.standard.KeyExtractor por um mock que captura os kwargs de instanciação; chama pipeline.detect_key(wav_path, tuning_hz=432.0); verifica que KeyExtractor foi instanciado com tuningFrequency=432.0
  </behavior>

  <action>
Inserir os 3 testes a seguir em `tests/test_pipeline.py`, logo após o bloco `# ANALYSIS-04: Camelot mapping` e antes do bloco `# D-05: JSON output shape`. Usar exatamente as assinaturas e lógica abaixo:

```python
# ---------------------------------------------------------------------------
# TUNING-01: detect_tuning retorna Hz em sinal harmônico (Wave 0 RED stub)
# ---------------------------------------------------------------------------

@pytest.mark.integration
def test_detect_tuning_harmonic(sample_wav_path):
    """detect_tuning() em WAV 440Hz deve retornar float próximo de 440 Hz.

    O fixture sample.wav é um tom puro de 440Hz — energia harmônica dominante.
    Espera tuning_hz entre 400 e 480 Hz (±10% de 440), não None, não numpy type.
    TUNING-01.
    """
    pipeline = pytest.importorskip("pipeline", reason="pipeline.py não tem detect_tuning ainda (Plan 02)")
    if not hasattr(pipeline, "detect_tuning"):
        pytest.fail("pipeline.detect_tuning não existe — implementar no Plan 02")
    result = pipeline.detect_tuning(sample_wav_path)
    assert result is not None, "detect_tuning retornou None em sinal harmônico 440Hz — HPSS gate incorreto"
    assert isinstance(result, float), f"detect_tuning deve retornar float nativo, got {type(result)}"
    assert 400.0 < result < 480.0, f"tuning_hz fora do range esperado para 440Hz: {result}"


# ---------------------------------------------------------------------------
# TUNING-02: detect_tuning retorna None em sinal percussivo (Wave 0 RED stub)
# ---------------------------------------------------------------------------

@pytest.mark.integration
def test_detect_tuning_percussive(tmp_path):
    """detect_tuning() em WAV de ruído branco deve retornar None.

    Ruído branco tem ratio de energia harmônica ~0 (< 0.2), portanto o HPSS gate
    deve ativar e retornar None. TUNING-02.
    """
    import numpy as np
    import soundfile as sf

    pipeline = pytest.importorskip("pipeline", reason="pipeline.py não tem detect_tuning ainda (Plan 02)")
    if not hasattr(pipeline, "detect_tuning"):
        pytest.fail("pipeline.detect_tuning não existe — implementar no Plan 02")

    # Criar WAV de ruído branco puro (sem pitch) — 2 segundos a 22050 Hz
    rng = np.random.default_rng(seed=42)
    noise = rng.uniform(-1.0, 1.0, size=22050 * 2).astype(np.float32)
    percussive_wav = tmp_path / "percussive.wav"
    sf.write(str(percussive_wav), noise, 22050)

    result = pipeline.detect_tuning(percussive_wav)
    assert result is None, (
        f"detect_tuning deve retornar None em ruído branco (sinal percussivo), got {result}"
    )


# ---------------------------------------------------------------------------
# PREC-03: detect_key passa tuning_hz para KeyExtractor (Wave 0 RED stub)
# ---------------------------------------------------------------------------

def test_detect_key_uses_tuning_hz():
    """detect_key() deve instanciar KeyExtractor com tuningFrequency=tuning_hz.

    Usa mock para capturar os kwargs de instanciação de KeyExtractor.
    Verifica que PREC-03 está cumprido: o tuning_hz detectado é passado como
    tuningFrequency antes que os bins HPCP sejam computados.
    """
    from unittest.mock import MagicMock, patch

    pipeline = pytest.importorskip("pipeline", reason="pipeline.py não tem detect_key Essentia ainda (Plan 03)")
    if not hasattr(pipeline, "detect_key"):
        pytest.fail("pipeline.detect_key não existe")

    captured_kwargs: dict = {}

    def fake_key_extractor_factory(**kwargs):
        captured_kwargs.update(kwargs)
        mock_instance = MagicMock()
        mock_instance.return_value = ("A", "minor", 0.85)
        return mock_instance

    with patch("essentia.standard.KeyExtractor", side_effect=fake_key_extractor_factory), \
         patch("essentia.standard.MonoLoader") as mock_loader:
        mock_loader.return_value.return_value = MagicMock()
        pipeline.detect_key(Path("/tmp/fake.wav"), tuning_hz=432.0)

    assert "tuningFrequency" in captured_kwargs, (
        f"KeyExtractor não recebeu tuningFrequency. kwargs capturados: {captured_kwargs}"
    )
    assert abs(captured_kwargs["tuningFrequency"] - 432.0) < 0.01, (
        f"tuningFrequency esperado 432.0, got {captured_kwargs['tuningFrequency']}"
    )
    assert captured_kwargs.get("profileType") == "edma", (
        f"profileType deve ser 'edma', got {captured_kwargs.get('profileType')!r}"
    )
```

IMPORTANTE: O stub de `test_detect_key_uses_tuning_hz` faz `patch("essentia.standard.KeyExtractor", ...)`. Para o patch funcionar em runtime, o módulo `essentia.standard` deve estar importado em `pipeline.py` como `import essentia.standard as es` e o KeyExtractor deve ser referenciado como `es.KeyExtractor(...)`. Não usar `from essentia.standard import KeyExtractor` (mudaria o namespace do patch).

Após inserir os testes, rodar a suite para confirmar que os 3 novos testes FALHAM (RED):

```bash
.venv/bin/python3 -m pytest tests/test_pipeline.py -k "test_detect_tuning_harmonic or test_detect_tuning_percussive or test_detect_key_uses_tuning_hz" -x -q 2>&1 | head -40
```

Os testes devem falhar com `pytest.fail(...)` ou `AttributeError` (função não existe ainda). Se algum PASSAR neste momento, a lógica do stub está incorreta — revisar.
  </action>

  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_pipeline.py -k "test_detect_tuning_harmonic or test_detect_tuning_percussive or test_detect_key_uses_tuning_hz" -q 2>&1 | grep -E "(FAILED|ERROR|passed)" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_pipeline.py contém `def test_detect_tuning_harmonic(`
    - tests/test_pipeline.py contém `def test_detect_tuning_percussive(`
    - tests/test_pipeline.py contém `def test_detect_key_uses_tuning_hz(`
    - tests/test_pipeline.py contém `tuningFrequency` (verifica que o stub captura o parâmetro correto)
    - tests/test_pipeline.py contém `profileType` dentro do bloco test_detect_key_uses_tuning_hz
    - Rodar `pytest -k "test_detect_tuning_harmonic or test_detect_tuning_percussive or test_detect_key_uses_tuning_hz"` deve resultar em 3 FAILEDs ou ERRORs (nunca 3 passed — isso seria stub incorreto)
    - Os testes pré-existentes (test_camelot_mapping, test_bpm_half_double_calculation, etc.) continuam passando: `pytest tests/test_pipeline.py -m "not integration and not e2e" -q` sai com 0
  </acceptance_criteria>

  <done>3 stubs RED existem em test_pipeline.py; suite não-integration permanece verde; essentia instalado e importável.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → filesystem | Testes de integração leem WAV de tests/fixtures/ — caminho fixo, sem input do usuário |
| pip install → venv | Instalação de essentia==2.1b6.dev1389 de PyPI — versão fixada elimina risco de supply chain drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | requirements.txt | accept | Arquivo commitado em git; alterações são auditáveis; versão fixada com == (não >=) conforme convenção do projeto |
| T-06-01-02 | Information Disclosure | tests/test_pipeline.py | accept | Testes são código de desenvolvimento; nenhum dado sensível — apenas caminhos de fixture e valores de frequência |
| T-06-01-03 | Denial of Service | pip install durante CI | mitigate | Versão fixada (`==`) impede resolução de nova versão instável; wheel manylinux já verificado no PyPI |
</threat_model>

<verification>
Suite não-integration permanece verde após este plan:

```bash
.venv/bin/python3 -m pytest tests/test_pipeline.py -m "not integration and not e2e" -q
```

Deve sair com 0 falhas. Os 3 novos testes integration devem estar RED (FAILED ou ERROR).
</verification>

<success_criteria>
- `grep "essentia==2.1b6.dev1389" requirements.txt` retorna match
- `.venv/bin/python3 -c "import essentia.standard"` sai com código 0
- `grep "test_detect_tuning_harmonic\|test_detect_tuning_percussive\|test_detect_key_uses_tuning_hz" tests/test_pipeline.py` retorna 3 matches
- `pytest tests/test_pipeline.py -m "not integration and not e2e" -q` — 0 falhas
- Os 3 stubs novos falham quando executados isoladamente (RED confirmado)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-precision-analysis-engine/06-01-SUMMARY.md`
</output>
