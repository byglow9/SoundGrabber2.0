---
phase: 10-failure-hardening-and-e2e-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 10-02-PLAN.md
files_modified:
  - start-all.sh
  - railway.toml
  - railway-worker.toml
  - .planning/phases/10-failure-hardening-and-e2e-validation/10-SMOKE-TEST.md
autonomous: false
requirements:
  - PIPE-07
must_haves:
  truths:
    - "start-all.sh existe na raiz do projeto com set -e, Celery em background e exec uvicorn"
    - "railway.toml aponta para bash start-all.sh como startCommand"
    - "railway-worker.toml está aposentado (contém comentário de aposentadoria)"
    - "Deploy no Railway usa o serviço único — Uvicorn e Celery no mesmo container"
    - "3 beats reais atingem status=done com WAV válido, BPM plausível e key em notação padrão"
    - "10-SMOKE-TEST.md documenta resultados E2E assinados pelo operador"
  artifacts:
    - path: "start-all.sh"
      provides: "Script de startup Railway: Celery worker background + Uvicorn foreground"
      contains: "exec uvicorn"
    - path: "railway.toml"
      provides: "Configuração Railway do serviço único"
      contains: "bash start-all.sh"
    - path: ".planning/phases/10-failure-hardening-and-e2e-validation/10-SMOKE-TEST.md"
      provides: "Evidência documentada da validação E2E no Railway"
      contains: "PASS"
  key_links:
    - from: "railway.toml"
      to: "start-all.sh"
      via: "startCommand"
      pattern: "bash start-all.sh"
    - from: "start-all.sh"
      to: "uvicorn api.main:app"
      via: "exec (PID 1 para Railway SIGTERM)"
      pattern: "exec uvicorn"
---

<objective>
Migrar Railway para container único (D-01) e validar pipeline E2E com beats reais (PIPE-07).

Purpose: O bug de containers separados (documentado em 09-01-SMOKE-TEST.md) faz o GET /files/{id}
retornar 410 porque o WAV está em /tmp do container Celery mas o Uvicorn procura em /tmp do
seu próprio container. A migração para single-container via start-all.sh resolve isso nativamente
sem mudar uma linha de código de produção (D-02).

Output:
- start-all.sh: script bash que inicia ambos os processos (Celery em background, Uvicorn em foreground com exec)
- railway.toml: startCommand atualizado para "bash start-all.sh"
- railway-worker.toml: aposentado com comentário explicativo
- 10-SMOKE-TEST.md: evidência de validação E2E assinada pelo operador
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-failure-hardening-and-e2e-validation/10-CONTEXT.md
@.planning/phases/10-failure-hardening-and-e2e-validation/10-RESEARCH.md
@.planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md
@.planning/phases/10-failure-hardening-and-e2e-validation/10-02-SUMMARY.md

<interfaces>
<!-- Contratos existentes que esta implementação deve respeitar -->

De railway.toml (estado ATUAL antes desta task):
```toml
[deploy]
startCommand = "uvicorn api.main:app --host 0.0.0.0 --port $PORT --limit-concurrency 100 --timeout-keep-alive 5"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
```

De railway-worker.toml (estado ATUAL):
```toml
[deploy]
startCommand = "celery -A api.tasks worker --loglevel=info --concurrency=3"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
```

De 10-RESEARCH.md §Padrão 3 (start-all.sh completo verificado):
```bash
#!/usr/bin/env bash
# start-all.sh — Railway single-container startup
# Inicia Celery Worker em background e Uvicorn em foreground (PID monitorado pelo Railway).
set -e

celery -A api.tasks worker --loglevel=info --concurrency=3 &

exec uvicorn api.main:app \
    --host 0.0.0.0 \
    --port "${PORT:-8000}" \
    --limit-concurrency 100 \
    --timeout-keep-alive 5
```

De 10-RESEARCH.md §Pitfall 4 (CRÍTICO):
```
Após migrar para serviço único, se o serviço "celery-worker" Railway ainda aponta para
railway-worker.toml, ele continua sendo deployado — duplicando workers.
O plano DEVE incluir task explícita de desativação do serviço Celery Worker no dashboard Railway.
```

De 09-01-SMOKE-TEST.md (formato de referência para o smoke test):
```markdown
# Tabela de gate checks por critério de sucesso
# Resultado PASS/FAIL/BLOQUEADO por gate
# Assinatura de data no final
```

De 09-01-SMOKE-TEST.md §Task 4 (URLs Railway existentes):
```
public_url: https://soundgrabber-test.up.railway.app
# Esta URL pode ter mudado após redeploys — o operador deve verificar no momento do teste
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar start-all.sh e atualizar railway.toml; aposentar railway-worker.toml</name>
  <files>start-all.sh, railway.toml, railway-worker.toml</files>

  <read_first>
    - railway.toml (LER — estado atual do startCommand que será substituído)
    - railway-worker.toml (LER — confirmar conteúdo antes de aposentar)
    - .planning/phases/10-failure-hardening-and-e2e-validation/10-RESEARCH.md §Padrão 3 (start-all.sh exato)
    - .planning/phases/10-failure-hardening-and-e2e-validation/10-RESEARCH.md §Pitfall 2 (SIGTERM + exec)
    - .planning/phases/10-failure-hardening-and-e2e-validation/10-RESEARCH.md §Pitfall 4 (serviço duplicado Railway)
    - start.sh (se existir — referência de padrão local; verificar antes de criar start-all.sh)
  </read_first>

  <action>
**Arquivo 1 — Criar `start-all.sh` na raiz do projeto:**

```bash
#!/usr/bin/env bash
# start-all.sh — Railway single-container startup (Uvicorn + Celery Worker)
#
# Inicia Celery Worker em background e Uvicorn em foreground.
# Railway monitora o processo foreground (Uvicorn, PID 1 após exec) para health checks.
# SIGTERM do Railway chega diretamente ao Uvicorn via exec — graceful shutdown funciona.
#
# Por que exec: sem exec, o bash seria PID 1 e precisaria repassar sinais manualmente.
# Com exec, uvicorn substitui o processo bash e recebe SIGTERM diretamente.
#
# Por que & (background) para Celery: Railway monitora apenas o processo foreground.
# Celery recebe SIGTERM do kernel quando o grupo de processos termina (task_acks_late=True
# já configurado em api/tasks.py — Celery termina após completar o job atual).
#
# Substitui o padrão de dois containers separados (railway.toml + railway-worker.toml).
# Referência: D-01 de .planning/phases/10-failure-hardening-and-e2e-validation/10-CONTEXT.md
set -e

celery -A api.tasks worker --loglevel=info --concurrency=3 &

exec uvicorn api.main:app \
    --host 0.0.0.0 \
    --port "${PORT:-8000}" \
    --limit-concurrency 100 \
    --timeout-keep-alive 5
```

Após criar o arquivo, aplicar permissão de acordo com o Security Gate do CLAUDE.md
(scripts de operação devem ter chmod 750):
```bash
chmod 750 start-all.sh
```

**Arquivo 2 — Atualizar `railway.toml`:**

Substituir a linha `startCommand` mantendo todo o resto inalterado:

```toml
# Railway deployment configuration — SEC-INFRA-02, SEC-INFRA-03, SEC-INFRA-04 (Phase 7).
#
# Uvicorn binds to 0.0.0.0:$PORT — Railway injects $PORT per deploy and isolates the
# container from direct internet access via its edge proxy (D-11). HTTPS termination
# and HTTP->HTTPS 301 redirect happen at the Railway edge automatically (D-04, D-05).
#
# start-all.sh starts both Uvicorn (foreground, PID 1) and Celery Worker (background).
# This single-service approach replaces the previous two-service setup that had isolated
# /tmp directories (bug documented in 09-01-SMOKE-TEST.md Task 4).
#
# Schema reference: https://docs.railway.com/config-as-code/reference

[deploy]
startCommand = "bash start-all.sh"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
```

**Arquivo 3 — Aposentar `railway-worker.toml`:**

Substituir o conteúdo por comentário de aposentadoria (manter arquivo para histórico):

```toml
# APOSENTADO — Phase 10 (2026-05-11)
#
# Este arquivo foi substituído por start-all.sh + railway.toml single-service setup.
# Uvicorn e Celery Worker agora rodam no mesmo container para compartilhar /tmp.
# Bug original: containers separados isolavam /tmp, causando GET /files/{id} → 410.
# Solução: D-01 de .planning/phases/10-failure-hardening-and-e2e-validation/10-CONTEXT.md
#
# Para reativar workers separados: descomentar o bloco abaixo e configurar o serviço
# Railway para apontar para este arquivo (Settings → Config-as-code → Railway Config File).
# Requer object storage (S3/R2) para compartilhar WAV entre serviços.
#
# [deploy]
# startCommand = "celery -A api.tasks worker --loglevel=info --concurrency=3"
# restartPolicyType = "ON_FAILURE"
# restartPolicyMaxRetries = 3
```

**Verificações locais após criação:**
```bash
# Verificar sintaxe do script
bash -n start-all.sh
echo "Sintaxe OK"

# Verificar que exec está presente
grep "exec uvicorn" start-all.sh

# Verificar que celery está em background (&)
grep "celery.*&$" start-all.sh

# Verificar railway.toml atualizado
grep "bash start-all.sh" railway.toml

# Verificar set -e
grep "set -e" start-all.sh
```

**NOTA OPERACIONAL (documentar no resultado):**
O serviço "celery-worker" Railway (ID: 145135bf-201e-4fd7-8ea2-ed790d334dbf) deve ser
DESATIVADO manualmente no dashboard Railway após o deploy do serviço único. Caso contrário,
dois workers processarão jobs do mesmo Redis (workers duplicados).
Instruções: Railway Dashboard → Projeto → Serviço "celery-worker" → Settings → Danger Zone → Remove Service.
  </action>

  <verify>
    <automated>bash -n start-all.sh && grep "exec uvicorn" start-all.sh && grep "bash start-all.sh" railway.toml && echo "ALL OK"</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n start-all.sh` sai com código 0 (sem erros de sintaxe)
    - `grep "set -e" start-all.sh` retorna `set -e`
    - `grep "exec uvicorn" start-all.sh` retorna a linha com exec
    - `grep "celery.*&$" start-all.sh` retorna a linha do celery em background
    - `grep 'PORT:-8000' start-all.sh` retorna a linha com o fallback de porta
    - `grep "bash start-all.sh" railway.toml` retorna `startCommand = "bash start-all.sh"`
    - `grep "APOSENTADO" railway-worker.toml` retorna o comentário de aposentadoria
    - `ls -la start-all.sh` mostra permissões -rwxr-x--- (750)
  </acceptance_criteria>

  <done>start-all.sh criado com exec+background e chmod 750, railway.toml atualizado, railway-worker.toml aposentado. Sintaxe do script verificada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy no Railway + Validação E2E com 3 beats reais (PIPE-07)</name>

  <read_first>
    - .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md (formato de referência para o smoke test)
    - .planning/phases/10-failure-hardening-and-e2e-validation/10-CONTEXT.md §D-12, D-13, D-14
    - .planning/ROADMAP.md §Phase 10 Success Criteria (3 critérios exatos a verificar)
  </read_first>

  <what-built>
    Task 1 criou start-all.sh e atualizou railway.toml. O próximo passo requer deploy
    no Railway para que Uvicorn e Celery Worker rodem no mesmo container, compartilhando /tmp,
    e a validação E2E com 3 beats reais confirme o pipeline completo.
  </what-built>

  <how-to-verify>
**Pré-requisito: desativar o serviço celery-worker separado**

Antes do deploy, verificar se o serviço "celery-worker" (Railway ID: 145135bf-201e-4fd7-8ea2-ed790d334dbf)
ainda está ativo. Se estiver, desativá-lo no dashboard Railway para evitar workers duplicados.

**Passo 1 — Commit e push para disparar redeploy Railway:**
```bash
git add start-all.sh railway.toml railway-worker.toml
git commit -m "feat(10): single-container deploy — start-all.sh replaces two-service setup"
git push
```

**Passo 2 — Aguardar deploy e verificar logs de startup:**

Via Railway Dashboard (ou MCP railway-agent):
- Verificar que o serviço "web" deploiou com o novo startCommand
- Nos logs, devem aparecer AMBAS as saídas:
  - `celery@... ready.` (worker iniciou em background)
  - `Application startup complete.` (uvicorn iniciou em foreground)
- Não deve aparecer: "BGUTIL_BASE_URL not set", "connection refused", "ConnectionRefusedError"

**Passo 3 — Verificar /health:**
```bash
curl -sS https://soundgrabber-test.up.railway.app/health
# Esperado: {"status":"ok"}
```

**Passo 4 — Escolher 3 beats instrumentais (D-13):**
Critérios: instrumentais de 3-7 minutos, gêneros diferentes (ex: trap, lo-fi, house).
Usar URLs do YouTube com menos de 15 minutos. NÃO usar URLs fixas do contexto — escolher
URLs frescas para evitar vídeos removidos.

**Passo 5 — Submeter os 3 jobs e aguardar:**
```bash
# Beat 1:
curl -sS -X POST https://soundgrabber-test.up.railway.app/jobs \
  -H "Content-Type: application/json" \
  -d '{"youtube_url": "URL_BEAT_1"}'
# Anotar job_id_1

# Beat 2:
curl -sS -X POST https://soundgrabber-test.up.railway.app/jobs \
  -H "Content-Type: application/json" \
  -d '{"youtube_url": "URL_BEAT_2"}'
# Anotar job_id_2

# Beat 3:
curl -sS -X POST https://soundgrabber-test.up.railway.app/jobs \
  -H "Content-Type: application/json" \
  -d '{"youtube_url": "URL_BEAT_3"}'
# Anotar job_id_3
```

**Passo 6 — Polling até status=done (intervalo 5s):**
```bash
# Repetir para cada job até status=done:
curl -sS https://soundgrabber-test.up.railway.app/jobs/{job_id}
```

**Passo 7 — Verificar campos obrigatórios em cada resposta done:**
- `"status": "done"` presente
- `"bpm"` é número inteiro plausível (60-200 para beats instrumentais)
- `"key"` está em notação padrão (ex: "F# minor", "A major", "C major")
- `"camelot"` não é "?" (chave mapeada)
- `"download_url"` presente

**Passo 8 — Verificar download do WAV (critério de sucesso #3 do ROADMAP):**
```bash
# Para cada job_id:
curl -sS -o /tmp/beat_test.wav \
  https://soundgrabber-test.up.railway.app/files/{job_id}

# Verificar que não é 0-byte e não é HTML:
ls -la /tmp/beat_test.wav
# Esperado: > 1MB (arquivo WAV real de beat de 3-7 minutos)

file /tmp/beat_test.wav
# Esperado: RIFF (little-endian) data, WAVE audio
```

**Passo 9 — Documentar resultados em 10-SMOKE-TEST.md:**
Seguir exatamente o formato de 09-01-SMOKE-TEST.md:
- Tabela de gate checks por critério (PASS/FAIL/BLOQUEADO)
- Resultado JSON de cada job (status=done)
- Tamanho do WAV baixado
- Data e assinatura
  </how-to-verify>

  <acceptance_criteria>
    - `grep "Application startup complete" [logs Railway]` — presente nos logs do serviço único
    - `grep "celery@.*ready" [logs Railway]` — Celery também iniciou no mesmo container
    - `curl .../jobs/{job_id_1}` → `"status":"done"`, `"bpm":` numérico, `"key":` string com "major" ou "minor"
    - `curl .../jobs/{job_id_2}` → mesmos campos presentes
    - `curl .../jobs/{job_id_3}` → mesmos campos presentes
    - `curl .../files/{job_id_1}` → HTTP 200 (não 410 "File expired") com Content-Type: audio/wav
    - Arquivo WAV baixado é > 1MB e `file` reporta "RIFF ... WAVE audio"
    - `cat .planning/phases/10-failure-hardening-and-e2e-validation/10-SMOKE-TEST.md` existe e contém "PASS"
    - Nenhum dos 3 jobs retornou `"error_type": "bgutil_unavailable"` (bgutil está operacional)
  </acceptance_criteria>

  <action>
Após verificação bem-sucedida, criar o arquivo de smoke test:

Criar `.planning/phases/10-failure-hardening-and-e2e-validation/10-SMOKE-TEST.md` com:
- Cabeçalho (data, phase, executor)
- Seção por cada tarefa verificada (startup logs, /health, jobs, WAV download)
- Tabela de gate checks por critério de sucesso do ROADMAP.md §Phase 10
- Resultados JSON dos 3 jobs (status=done com bpm/key/camelot)
- Tamanho dos 3 WAVs baixados
- Assinatura de data e resultado geral (PASS/FAIL)

Seguir exatamente o formato de `.planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md`.
  </action>

  <resume-signal>
    Digitar "aprovado" após:
    - Deploy no Railway com single-container funcionando (Celery + Uvicorn nos logs)
    - 3 beats atingiram status=done com bpm/key/camelot
    - GET /files/{id} retornou HTTP 200 com WAV válido (não 410)
    - 10-SMOKE-TEST.md criado com resultados documentados
    
    Ou descrever o problema encontrado para diagnóstico.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Railway container → /tmp | Uvicorn e Celery Worker compartilham /tmp no mesmo container |
| start-all.sh → Railway PID 1 | exec garante que SIGTERM chega ao Uvicorn diretamente |
| operator → Railway Dashboard | Desativação do serviço celery-worker requer ação manual no dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Denial of Service | Celery Worker + Uvicorn no mesmo container | accept | Escala independente sacrificada (D-03). Aceitável para escala underground. Mitigação futura: object storage (S3/R2) para separar novamente. |
| T-10-03-02 | Elevation of Privilege | Celery worker com superuser warning (visto nos logs da Phase 9) | accept | Railway executa todos os processos como root no container. Warning do Celery é informativo. Sem vetor de escalação real no ambiente isolado do Railway. |
| T-10-03-03 | Information Disclosure | /tmp compartilhado entre Uvicorn e Celery | accept | /tmp é isolado no container Railway (não exposto externamente). WAV files têm chmod 0o600 (SEC-FILE-01). Nenhum outro processo tem acesso ao /tmp do container. |
| T-10-03-04 | Tampering | start-all.sh executável no repositório | mitigate | chmod 750 (Security Gate §scripts: "chmod 750 para scripts de operação"). Script contém apenas comandos de startup sem eval de input externo. set -e na primeira linha operacional. |
| T-10-03-05 | Denial of Service | Workers duplicados se celery-worker Railway não for desativado | mitigate | Task 2 instrui explicitamente a desativação do serviço. Pitfall documentado em 10-RESEARCH.md §Pitfall 4. |

Controles do Security Gate CLAUDE.md verificados para start-all.sh:
- `set -e` na primeira linha: presente
- Sem `eval` de input externo: confirmado (apenas comandos hardcoded)
- chmod 750 aplicado em Task 1 (conforme Security Gate §scripts para scripts de operação)
</threat_model>

<verification>
Verificação de fase completa para o plano 03 (pré-deploy):

```bash
# Sintaxe do script
bash -n start-all.sh && echo "Sintaxe OK"

# Campos obrigatórios em start-all.sh
grep "set -e" start-all.sh
grep "exec uvicorn" start-all.sh
grep "celery.*&$" start-all.sh
grep "PORT:-8000" start-all.sh

# Permissão correta (750)
ls -la start-all.sh | grep "rwxr-x---"

# railway.toml atualizado
grep "bash start-all.sh" railway.toml

# railway-worker.toml aposentado
grep "APOSENTADO" railway-worker.toml
```

Verificação E2E (após deploy — pelo operador):
- Logs Railway mostram `celery@... ready.` E `Application startup complete.`
- 3 jobs atingem status=done com bpm/key/camelot
- GET /files/{id} retorna HTTP 200 com WAV > 1MB
- 10-SMOKE-TEST.md criado com todos os gates PASS
</verification>

<success_criteria>
- start-all.sh existe na raiz com set -e, Celery em background, exec uvicorn em foreground, chmod 750
- railway.toml tem startCommand = "bash start-all.sh"
- railway-worker.toml tem comentário de aposentadoria
- Após deploy: Uvicorn e Celery no mesmo container, /tmp compartilhado
- 3 beats atingem status=done no Railway com WAV válido, BPM e key
- 10-SMOKE-TEST.md documenta todos os resultados (mesmo padrão de 09-01-SMOKE-TEST.md)
- PIPE-07 verificado end-to-end na infra real do Railway
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/10-failure-hardening-and-e2e-validation/10-03-SUMMARY.md`
com: arquivos criados/modificados, resultado do deploy Railway, resumo do smoke test E2E.
</output>
