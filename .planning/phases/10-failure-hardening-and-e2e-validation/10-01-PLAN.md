---
phase: 10-failure-hardening-and-e2e-validation
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_pipeline_fixes.py
autonomous: true
requirements:
  - PIPE-06
must_haves:
  truths:
    - "Os 4 novos stubs de teste para PIPE-06 existem em test_pipeline_fixes.py"
    - "Todos os 4 stubs FALHAM (RED) ao rodar pytest — sem implementação ainda"
    - "Os stubs existentes de PIPE-01..05 e bgutil 0.8.x continuam passando (nenhuma regressão)"
  artifacts:
    - path: "tests/test_pipeline_fixes.py"
      provides: "4 novos stubs RED para PIPE-06 + todos os testes anteriores intactos"
      contains: "test_pipe06_bgutil_probe_connect_error_raises"
  key_links:
    - from: "tests/test_pipeline_fixes.py"
      to: "pipeline.BgutilUnavailable"
      via: "pytest.raises"
      pattern: "BgutilUnavailable"
---

<objective>
Criar stubs RED para PIPE-06 em tests/test_pipeline_fixes.py.

Purpose: Estabelecer os contratos de comportamento testáveis ANTES de qualquer implementação.
Os 4 testes devem falhar (RED) porque pipeline.BgutilUnavailable não existe ainda e o probe
HTTP não está implementado. Isso garante que o plano 02 realmente valida o comportamento,
não apenas passa por coincidência.

Output: 4 novos casos de teste RED em tests/test_pipeline_fixes.py. Nenhum arquivo de
produção é tocado neste plano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-failure-hardening-and-e2e-validation/10-CONTEXT.md
@.planning/phases/10-failure-hardening-and-e2e-validation/10-RESEARCH.md
@.planning/phases/10-failure-hardening-and-e2e-validation/10-VALIDATION.md

<interfaces>
<!-- Contratos do pipeline.py ATUAL — executor não precisa explorar o codebase -->
<!-- Nota: BgutilUnavailable NÃO existe ainda — o teste deve falhar referenciando-a -->

De pipeline.py (assinatura atual de download_audio):
```python
def download_audio(url: str, cookies_path: str, po_token: str, bgutil_base_url: str = "") -> Path:
    """Raises:
        RuntimeError: If yt-dlp fails.
        FileNotFoundError: If WAV not produced.
    """
```

De tests/test_pipeline_fixes.py (padrão de mocking estabelecido nas fases anteriores):
```python
import inspect
import pipeline
from unittest.mock import patch, MagicMock
import pytest

# Padrão de mock para módulo-level: patch("pipeline.X") intercepta chamadas em pipeline.py
# Padrão de inspeção: inspect.getsource(pipeline.func) verifica código-fonte sem chamar a função
```

De api/tasks.py (exception chain existente):
```python
from pipeline import check_duration, download_audio, analyze_audio
# BgutilUnavailable ainda NÃO importado — o plano 02 adiciona
```

De 10-RESEARCH.md (Padrão 5 — mock path verificado):
```python
# Mock path CORRETO: "pipeline.httpx.get"
# NÃO usar: "httpx.get" (não intercepta chamadas feitas dentro de pipeline.py)
# Motivo: após `import httpx` em pipeline.py, o módulo tem sua própria referência ao httpx
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Escrever 4 stubs RED para PIPE-06 em test_pipeline_fixes.py</name>
  <files>tests/test_pipeline_fixes.py</files>

  <read_first>
    - tests/test_pipeline_fixes.py (LER INTEIRO — preservar todos os 11 testes existentes, adicionar apenas no final)
    - pipeline.py linhas 1-35 (imports existentes — confirmar que httpx NÃO está importado ainda)
    - api/tasks.py linhas 1-15 (imports existentes — confirmar que BgutilUnavailable NÃO está importado ainda)
    - .planning/phases/10-failure-hardening-and-e2e-validation/10-RESEARCH.md §Padrão 5 (mock correto)
  </read_first>

  <behavior>
    - Stub 1 (test_pipe06_bgutil_probe_connect_error_raises): mock "pipeline.httpx.get" levantando httpx.ConnectError → download_audio com bgutil_base_url não-vazio deve levantar exceção com "bgutil" na mensagem. RED porque pipeline.httpx não existe (httpx não importado em pipeline.py) e BgutilUnavailable não existe.
    - Stub 2 (test_pipe06_bgutil_probe_timeout_raises): mock "pipeline.httpx.get" levantando httpx.ConnectTimeout → mesma verificação de "bgutil" na mensagem. RED pelo mesmo motivo.
    - Stub 3 (test_pipe06_no_probe_when_bgutil_url_empty): mock "pipeline.httpx.get" como MagicMock sem side_effect → download_audio com bgutil_base_url="" deve chamar yt-dlp (pode falhar com qualquer erro) mas NÃO deve ter chamado httpx.get. RED porque "pipeline.httpx" não existe.
    - Stub 4 (test_pipe06_tasks_bgutil_error_type): mock pipeline.download_audio levantando RuntimeError("bgutil unavailable test") → process_job deve produzir JobFailure com error_type="bgutil_unavailable". RED porque tasks.py não captura BgutilUnavailable como tipo distinto — o except RuntimeError genérico produz error_type="download_error".
  </behavior>

  <action>
Adicionar ao final de tests/test_pipeline_fixes.py (DEPOIS dos 11 testes existentes, sem modificar nada acima):

1. Adicionar bloco de imports necessários para os novos testes (se ainda não presentes):
   ```python
   import httpx
   import pytest
   from unittest.mock import patch, MagicMock, call
   ```
   Verificar quais já existem no arquivo antes de adicionar duplicatas.

2. Adicionar separador de seção:
   ```python
   # ---------------------------------------------------------------------------
   # PIPE-06 — bgutil probe: falha explícita quando bgutil inacessível (RED stubs)
   # Estes testes FALHAM até que 10-02-PLAN.md implemente:
   #   - import httpx em pipeline.py
   #   - class BgutilUnavailable(RuntimeError) em pipeline.py
   #   - probe HTTP em download_audio() antes de ydl_opts
   #   - except BgutilUnavailable em api/tasks.py ANTES de except RuntimeError
   # ---------------------------------------------------------------------------
   ```

3. Stub 1 — ConnectError levanta exceção com "bgutil" na mensagem:
   ```python
   def test_pipe06_bgutil_probe_connect_error_raises():
       """PIPE-06: ConnectError no probe → exceção com 'bgutil' na mensagem."""
       with patch("pipeline.httpx.get", side_effect=httpx.ConnectError("Connection refused")):
           with pytest.raises(Exception) as exc_info:
               pipeline.download_audio(
                   url="https://www.youtube.com/watch?v=FAKEID00001",
                   cookies_path="",
                   po_token="",
                   bgutil_base_url="http://bgutil.railway.internal:4416",
               )
           assert "bgutil" in str(exc_info.value).lower(), (
               f"PIPE-06 fix missing: mensagem de erro deve conter 'bgutil'. "
               f"Got: {exc_info.value!r}"
           )
   ```

4. Stub 2 — ConnectTimeout levanta exceção com "bgutil" na mensagem:
   ```python
   def test_pipe06_bgutil_probe_timeout_raises():
       """PIPE-06: ConnectTimeout no probe → exceção com 'bgutil' na mensagem."""
       with patch("pipeline.httpx.get", side_effect=httpx.ConnectTimeout("Timeout")):
           with pytest.raises(Exception) as exc_info:
               pipeline.download_audio(
                   url="https://www.youtube.com/watch?v=FAKEID00002",
                   cookies_path="",
                   po_token="",
                   bgutil_base_url="http://bgutil.railway.internal:4416",
               )
           assert "bgutil" in str(exc_info.value).lower(), (
               f"PIPE-06 fix missing: mensagem de erro deve conter 'bgutil'. "
               f"Got: {exc_info.value!r}"
           )
   ```

5. Stub 3 — sem bgutil_base_url, probe NÃO é chamado:
   ```python
   def test_pipe06_no_probe_when_bgutil_url_empty():
       """PIPE-06: bgutil_base_url='' → httpx.get NÃO deve ser chamado."""
       mock_get = MagicMock()
       with patch("pipeline.httpx.get", mock_get):
           try:
               pipeline.download_audio(
                   url="https://www.youtube.com/watch?v=FAKEID00003",
                   cookies_path="",
                   po_token="",
                   bgutil_base_url="",
               )
           except Exception:
               pass  # yt-dlp vai falhar sem cookies/URL real — irrelevante
           mock_get.assert_not_called()
   ```

6. Stub 4 — tasks.py: BgutilUnavailable → error_type="bgutil_unavailable":
   ```python
   def test_pipe06_tasks_bgutil_error_type():
       """PIPE-06: JobFailure gerada por exceção de bgutil deve ter error_type='bgutil_unavailable'."""
       from api.tasks import process_job, JobFailure

       # Simular exceção com mensagem de bgutil saindo de download_audio
       bgutil_error_msg = (
           "PO Token service unavailable (bgutil at http://bgutil.railway.internal:4416). "
           "Download requires bgutil to be running."
       )

       with patch("api.tasks.download_audio", side_effect=RuntimeError(bgutil_error_msg)), \
            patch("api.tasks.check_duration", return_value={"duration": 180, "title": "test"}):
           # Invocar a função Celery sem o decorator (apply() não está disponível sem broker)
           # Usar o método __wrapped__ ou chamar a função diretamente via request mock
           try:
               # process_job é um bound task; chamar sem broker levanta ComandError — não importa
               # O que verificamos é que a exceção lançada é JobFailure com o error_type correto
               import celery
               task_func = process_job
               # Criar um mock do request Celery para evitar AttributeError em self.request.id
               mock_request = MagicMock()
               mock_request.id = "test-job-id-pipe06"
               task_func.request_stack = MagicMock()
               task_func.request_stack.top = mock_request

               task_func(url="https://www.youtube.com/watch?v=FAKEID00004")
           except JobFailure as jf:
               assert jf.error_type == "bgutil_unavailable", (
                   f"PIPE-06 fix missing: error_type esperado 'bgutil_unavailable', "
                   f"obtido {jf.error_type!r}. "
                   f"api/tasks.py precisa de 'except BgutilUnavailable' ANTES de 'except RuntimeError'."
               )
           except Exception:
               pass  # outros erros de infra Celery são ignorados neste stub
   ```

IMPORTANTE: Após adicionar os 4 stubs, VERIFICAR que os 11 testes anteriores não foram
alterados executando: `pytest tests/test_pipeline_fixes.py -x -q --tb=no 2>&1 | head -30`

Os 4 novos stubs devem FALHAR (RED). Os 11 existentes devem PASSAR.
  </action>

  <verify>
    <automated>pytest tests/test_pipeline_fixes.py -q --tb=short 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_pipe06_" tests/test_pipeline_fixes.py` retorna `4`
    - `pytest tests/test_pipeline_fixes.py::test_pipe06_bgutil_probe_connect_error_raises -x` FALHA com AttributeError ou similar (pipeline.httpx não existe)
    - `pytest tests/test_pipeline_fixes.py::test_pipe06_bgutil_probe_timeout_raises -x` FALHA
    - `pytest tests/test_pipeline_fixes.py::test_pipe06_no_probe_when_bgutil_url_empty -x` FALHA
    - `pytest tests/test_pipeline_fixes.py::test_pipe06_tasks_bgutil_error_type -x` FALHA com AssertionError (error_type é "download_error", não "bgutil_unavailable")
    - `pytest tests/test_pipeline_fixes.py -k "not test_pipe06" -q` — todos os 11 testes anteriores PASSAM (zero falhas)
    - `grep "pipeline.httpx.get" tests/test_pipeline_fixes.py` encontra pelo menos 2 ocorrências
    - `grep "bgutil_unavailable" tests/test_pipeline_fixes.py` encontra pelo menos 1 ocorrência
  </acceptance_criteria>

  <done>4 stubs RED existem no arquivo; 11 testes anteriores continuam verdes; nenhum arquivo de produção foi tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| test → production code | Testes chamam pipeline.download_audio e api.tasks.process_job com inputs controlados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | tests/test_pipeline_fixes.py | accept | Arquivo de teste somente; sem lógica de produção; sem dados sensíveis |
| T-10-01-02 | Information Disclosure | Stubs com URLs fake | accept | URLs são intencionalmente inválidas (FAKEID); nenhum request real é feito nos stubs RED |

Nenhum controle do Security Gate do CLAUDE.md é acionado: nenhum novo endpoint HTTP,
nenhum arquivo em /tmp, nenhum script shell neste plano.
</threat_model>

<verification>
Verificação de fase completa para o plano 01:

```bash
# Confirmar 4 stubs RED existem
grep -c "def test_pipe06_" tests/test_pipeline_fixes.py
# Esperado: 4

# Confirmar stubs estão em RED (falham sem implementação)
pytest tests/test_pipeline_fixes.py -k "test_pipe06" --tb=line -q
# Esperado: 4 failed

# Confirmar regressão zero nos testes anteriores
pytest tests/test_pipeline_fixes.py -k "not test_pipe06" -q
# Esperado: 11 passed
```
</verification>

<success_criteria>
- 4 funções `test_pipe06_*` presentes em tests/test_pipeline_fixes.py
- Todos os 4 falham (RED) contra o código de produção atual
- Zero regressões: os 11 testes PIPE-01..05 e bgutil 0.8.x passam sem alteração
- Nenhum arquivo de produção (pipeline.py, api/tasks.py) foi modificado
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/10-failure-hardening-and-e2e-validation/10-01-SUMMARY.md`
com: stubs criados, contagem RED/GREEN por stub, estado dos testes anteriores.
</output>
