---
phase: 07-infrastructure-security
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_security.py
autonomous: true
requirements:
  - SEC-INFRA-01
  - SEC-INFRA-04
tags: [security, infrastructure, testing, tdd]

must_haves:
  truths:
    - "tests/conftest.py defines DEV_MODE=true via os.environ.setdefault before api.* imports"
    - "tests/test_security.py has 3 new RED stubs: test_redis_auth_required, test_redis_auth_bypass_dev_mode, test_hsts_header"
    - "Running the full test suite (pytest tests/ -v -m 'not e2e and not integration') stays GREEN — existing tests do not regress"
    - "The 3 new stubs FAIL when run individually (RED state confirmed) — they expect behavior that Plan 02 will implement"
  artifacts:
    - path: "tests/conftest.py"
      provides: "DEV_MODE=true setdefault for test environment"
      contains: 'os.environ.setdefault("DEV_MODE", "true")'
    - path: "tests/test_security.py"
      provides: "3 new RED stubs for SEC-INFRA-01 and SEC-INFRA-04"
      contains: "def test_redis_auth_required"
  key_links:
    - from: "tests/conftest.py"
      to: "api.config.Settings"
      via: "DEV_MODE env var read by Settings.dev_mode field (added in Plan 02)"
      pattern: 'DEV_MODE'
    - from: "tests/test_security.py::test_hsts_header"
      to: "api/main.py _security_headers middleware"
      via: "TestClient response headers"
      pattern: "Strict-Transport-Security"
---

<objective>
Wave 0: Criar testes RED para SEC-INFRA-01 (Redis auth enforcement) e SEC-INFRA-04 (HSTS header), e ajustar conftest.py para que os testes existentes continuem passando depois que Plan 02 substituir o `logger.warning` por `raise RuntimeError`.

Purpose: Nyquist protocol — toda mudanca de comportamento de seguranca tem teste falhando antes da implementacao. Sem isso, Plan 02 viraria implementacao sem cobertura, e o ajuste do conftest precisa ser feito ANTES da mudanca em api/main.py senao todos os testes existentes quebram simultaneamente.

Output:
- tests/conftest.py com `os.environ.setdefault("DEV_MODE", "true")` adicionado antes dos imports api.*
- 3 stubs RED em tests/test_security.py: test_redis_auth_required, test_redis_auth_bypass_dev_mode, test_hsts_header
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-infrastructure-security/07-CONTEXT.md
@.planning/phases/07-infrastructure-security/07-RESEARCH.md
@.planning/phases/07-infrastructure-security/07-VALIDATION.md
@tests/conftest.py
@tests/test_security.py
@api/main.py
@api/config.py

<interfaces>
<!-- Tipos e contratos que os testes vao consumir. Plan 02 adiciona dev_mode em Settings. -->

From api/config.py (existing fields):
```python
@dataclass(frozen=True)
class Settings:
    redis_url: str = field(default_factory=lambda: os.environ.get("REDIS_URL", "redis://localhost:6379/0"))
    # ... outros campos ...
    # Plan 02 vai adicionar:
    # dev_mode: bool = field(default_factory=lambda: os.environ.get("DEV_MODE", "false").lower() == "true")
```

From api/main.py (existing lifespan — Plan 02 vai modificar):
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    if "@" not in settings.redis_url:
        logger.warning(...)  # Plan 02 substitui por raise RuntimeError quando not settings.dev_mode
    sweeper = threading.Thread(...)
    sweeper.start()
    yield
```

From api/main.py (existing _security_headers — Plan 02 vai adicionar HSTS):
```python
@app.middleware("http")
async def _security_headers(request: Request, call_next):
    response = await call_next(request)
    response.headers["X-Frame-Options"] = "DENY"
    # ... outros headers ...
    # Plan 02 vai adicionar:
    # response.headers["Strict-Transport-Security"] = "max-age=31536000; includeSubDomains"
    return response
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar DEV_MODE=true em conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (estado atual — linha 13 ja tem `os.environ.setdefault("REDIS_URL", "redis://localhost:6380/0")`)
    - api/main.py (linhas 110-125 — ver o lifespan atual com logger.warning)
    - .planning/phases/07-infrastructure-security/07-RESEARCH.md (Pitfall 1 explica por que isso e necessario)
  </read_first>
  <behavior>
    - Antes desta mudanca: Pytest funciona porque api/main.py:lifespan apenas faz logger.warning quando REDIS_URL nao tem senha
    - Depois desta mudanca + Plan 02: Pytest continua funcionando porque conftest.py seta DEV_MODE=true antes dos imports de api.*, fazendo o lifespan bypassar o RuntimeError
    - Sem esta mudanca + Plan 02: TODOS os testes que usam api_client fixture quebrariam com RuntimeError simultaneamente
  </behavior>
  <action>
    Em tests/conftest.py, adicionar uma linha imediatamente apos a linha 13 (que tem `os.environ.setdefault("REDIS_URL", "redis://localhost:6380/0")`):

    ```python
    os.environ.setdefault("DEV_MODE", "true")  # SEC-INFRA-01: bypassa Redis auth check em testes (D-06)
    ```

    Esta linha DEVE ficar no mesmo bloco (linhas 11-13 atual) — antes de qualquer import de api.* ou de fixtures que importem api.*. Manter o comentario do bloco existente intacto.

    NAO modificar o `os.environ.setdefault("REDIS_URL", ...)` existente. NAO adicionar dev_mode em qualquer outro lugar do conftest.py.

    Por que `setdefault` e nao `os.environ["DEV_MODE"] = "true"`: setdefault preserva o valor existente se o desenvolvedor exportou DEV_MODE=false manualmente para testar caminho de producao localmente.
  </action>
  <verify>
    <automated>grep -n 'DEV_MODE' tests/conftest.py</automated>
    <automated>pytest tests/ -v -m "not e2e and not integration" -x --co | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'os.environ.setdefault("DEV_MODE", "true")' tests/conftest.py` retorna `1`
    - A linha DEV_MODE aparece ANTES de qualquer linha contendo `from api` ou `import api` no arquivo: `awk '/from api|import api/{print NR; exit} /DEV_MODE/{print NR; found=1} END{exit !found}' tests/conftest.py` deve mostrar a linha de DEV_MODE primeiro
    - `pytest tests/ -v -m "not e2e and not integration" --co -q 2>&1 | tail -3` nao mostra erro de coleta
    - `pytest tests/test_security.py -x -q` continua passando como estava antes (Phase 6 testes verdes)
  </acceptance_criteria>
  <done>conftest.py tem DEV_MODE=true via setdefault no bloco de setup de env vars; pytest collection passa; suite Phase 6 continua verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar 3 stubs RED em test_security.py</name>
  <files>tests/test_security.py</files>
  <read_first>
    - tests/test_security.py (estado atual — formato e padroes dos testes existentes da Phase 6)
    - api/main.py (linhas 110-125 — lifespan atual; linhas 161-174 — _security_headers atual)
    - api/config.py (estrutura do Settings dataclass)
    - .planning/phases/07-infrastructure-security/07-RESEARCH.md (secao Validation Architecture e Code Examples)
  </read_first>
  <behavior>
    - test_redis_auth_required: Cria um Settings mock com `redis_url="redis://localhost:6379/0"` (sem `@`) e `dev_mode=False`; espera que rodar a logica do lifespan levante RuntimeError com mensagem contendo "REDIS_URL" e "password" (case-insensitive)
    - test_redis_auth_bypass_dev_mode: Cria um Settings mock com `redis_url="redis://localhost:6379/0"` (sem `@`) e `dev_mode=True`; espera que NAO levante RuntimeError
    - test_hsts_header: GET / via TestClient deve retornar header `Strict-Transport-Security: max-age=31536000; includeSubDomains` na resposta
  </behavior>
  <action>
    Anexar (append) ao final de tests/test_security.py o bloco abaixo. Os tres testes sao RED — vao FALHAR ate Plan 02 implementar Settings.dev_mode, RuntimeError no lifespan, e HSTS no _security_headers.

    Estrutura: Os dois primeiros testes (Redis auth) devem testar uma funcao auxiliar `_check_redis_auth(redis_url, dev_mode)` que Plan 02 vai EXTRAIR do lifespan. Isso evita ter que executar o lifespan inteiro (com sweeper thread) so para validar a checagem de auth. Plan 02 deve criar essa funcao em api/main.py e o lifespan deve chama-la.

    Conteudo a anexar (preserve o conteudo existente do arquivo, so adicione no final):

    ```python


    # ---------------------------------------------------------------------------
    # SEC-INFRA-01: Redis auth enforcement (Phase 7)
    # ---------------------------------------------------------------------------

    def test_redis_auth_required():
        """SEC-INFRA-01: _check_redis_auth deve levantar RuntimeError se URL sem '@' e dev_mode=False.

        RED: api/main.py atual apenas loga WARNING. Plan 02 (Wave 1) adiciona a funcao
        `_check_redis_auth(redis_url: str, dev_mode: bool)` em api/main.py que levanta
        RuntimeError com mensagem clara quando a URL nao contem credenciais e nao esta
        em modo desenvolvimento.

        Mensagem deve mencionar 'REDIS_URL' e 'password' (case-insensitive) para que o
        operador entenda imediatamente o que esta faltando.
        """
        from api.main import _check_redis_auth

        with pytest.raises(RuntimeError) as excinfo:
            _check_redis_auth("redis://localhost:6379/0", dev_mode=False)

        msg = str(excinfo.value).lower()
        assert "redis_url" in msg, f"Mensagem deve mencionar REDIS_URL: {excinfo.value}"
        assert "password" in msg, f"Mensagem deve mencionar password: {excinfo.value}"


    def test_redis_auth_bypass_dev_mode():
        """SEC-INFRA-01 (D-06): _check_redis_auth NAO levanta quando dev_mode=True, mesmo sem '@' na URL.

        RED: a funcao nao existe ainda. Plan 02 (Wave 1) implementa.
        """
        from api.main import _check_redis_auth

        # Nao deve levantar — modo dev permite Redis sem senha
        _check_redis_auth("redis://localhost:6379/0", dev_mode=True)


    def test_redis_auth_passes_with_password():
        """SEC-INFRA-01: _check_redis_auth aceita URLs com credenciais (presenca de '@').

        Cobre o caminho positivo — formato Railway: redis://default:senha@host:6379
        """
        from api.main import _check_redis_auth

        # Nao deve levantar — URL tem credenciais
        _check_redis_auth("redis://default:abc123@redis.railway.internal:6379", dev_mode=False)


    # ---------------------------------------------------------------------------
    # SEC-INFRA-04: HSTS header
    # ---------------------------------------------------------------------------

    def test_hsts_header(api_client):
        """SEC-INFRA-04: header Strict-Transport-Security: max-age=31536000; includeSubDomains.

        RED: api/main.py:_security_headers atual nao injeta HSTS. Plan 02 (Wave 1) adiciona.

        Valor exato exigido pela especificacao SEC-INFRA-04 + research recomendacao:
          max-age=31536000; includeSubDomains
        """
        response = api_client.get("/")
        # GET / retorna index.html (Phase 4) — nao precisa estar 200; apenas precisa
        # passar pelo middleware _security_headers para checar o header injetado.
        hsts = response.headers.get("Strict-Transport-Security")
        assert hsts is not None, "Header Strict-Transport-Security ausente"
        assert "max-age=31536000" in hsts, f"max-age=31536000 ausente: {hsts}"
        assert "includeSubDomains" in hsts, f"includeSubDomains ausente: {hsts}"
    ```

    NAO modificar nenhum teste existente. NAO remover ou renomear stubs da Phase 6. Apenas adicionar o bloco acima ao final.
  </action>
  <verify>
    <automated>pytest tests/test_security.py::test_redis_auth_required tests/test_security.py::test_redis_auth_bypass_dev_mode tests/test_security.py::test_redis_auth_passes_with_password tests/test_security.py::test_hsts_header -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_redis_auth_required" tests/test_security.py` retorna `1`
    - `grep -c "def test_redis_auth_bypass_dev_mode" tests/test_security.py` retorna `1`
    - `grep -c "def test_redis_auth_passes_with_password" tests/test_security.py` retorna `1`
    - `grep -c "def test_hsts_header" tests/test_security.py` retorna `1`
    - Os 4 testes ESTAO em estado RED (falham): `pytest tests/test_security.py::test_redis_auth_required tests/test_security.py::test_hsts_header 2>&1 | grep -E "(FAILED|ERROR|ImportError)"` retorna pelo menos 2 linhas (test_redis_auth_required vai falhar com ImportError de `_check_redis_auth`; test_hsts_header vai falhar com `Header ausente`)
    - Suite Phase 6 continua verde: `pytest tests/test_security.py -x --deselect tests/test_security.py::test_redis_auth_required --deselect tests/test_security.py::test_redis_auth_bypass_dev_mode --deselect tests/test_security.py::test_redis_auth_passes_with_password --deselect tests/test_security.py::test_hsts_header -q 2>&1 | tail -3` mostra `passed` e `0 failed`
  </acceptance_criteria>
  <done>4 stubs RED adicionados (3 para SEC-INFRA-01, 1 para SEC-INFRA-04); todos falham; testes Phase 6 continuam passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest test runner -> api modules | Test environment isolation via env vars (DEV_MODE, REDIS_URL); a Wave 0 nao introduz boundaries novos, apenas prepara o conftest para a mudanca de comportamento de Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-W0-01 | Tampering | tests/conftest.py DEV_MODE setdefault | accept | Risco baixo: o conftest e codigo de teste; um atacante com acesso ao repo ja tem acesso direto. setdefault preserva valor exportado pelo dev se houver. |
| T-7-W0-02 | Information Disclosure | RED stubs vazam que existira `_check_redis_auth` em api/main.py | accept | Funcao publica do modulo apos Plan 02; nao ha segredo aqui — apenas valida formato de URL. |
</threat_model>

<verification>
Comandos a rodar manualmente apos completar todos os tasks:

```bash
# 1. Verificar que conftest.py tem DEV_MODE setdefault
grep -n 'DEV_MODE' tests/conftest.py

# 2. Verificar que os 4 stubs estao em RED state
pytest tests/test_security.py::test_redis_auth_required \
       tests/test_security.py::test_redis_auth_bypass_dev_mode \
       tests/test_security.py::test_redis_auth_passes_with_password \
       tests/test_security.py::test_hsts_header -x 2>&1 | tail -15

# 3. Verificar que suite Phase 6 nao regrediu
pytest tests/test_security.py -q \
  --deselect tests/test_security.py::test_redis_auth_required \
  --deselect tests/test_security.py::test_redis_auth_bypass_dev_mode \
  --deselect tests/test_security.py::test_redis_auth_passes_with_password \
  --deselect tests/test_security.py::test_hsts_header

# 4. Verificar que toda a suite continua valida
pytest tests/ -v -m "not e2e and not integration" --co | tail -5
```
</verification>

<success_criteria>
1. tests/conftest.py contem `os.environ.setdefault("DEV_MODE", "true")` no bloco de env vars antes dos imports api.*
2. tests/test_security.py contem os 4 stubs novos (test_redis_auth_required, test_redis_auth_bypass_dev_mode, test_redis_auth_passes_with_password, test_hsts_header)
3. Os 4 stubs FALHAM individualmente (RED state confirmado)
4. Suite Phase 6 (10 testes existentes em test_security.py) continua passando
5. Pytest collection e execucao da suite completa nao regridem
</success_criteria>

<output>
After completion, create `.planning/phases/07-infrastructure-security/07-01-SUMMARY.md`
</output>
