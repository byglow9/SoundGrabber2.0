---
phase: 14-pipeline-e2e-on-notebook
plan: 04
type: execute
wave: 2
depends_on:
  - 14-02
  - 14-03
files_modified:
  - .planning/phases/14-pipeline-e2e-on-notebook/14-DEPLOY-LOG.md
autonomous: false
requirements:
  - AUTH-04
  - PIPE-08
tags:
  - checkpoint
  - e2e
  - manual-verification
must_haves:
  truths:
    - "Diretório /data/yt-dlp-cache existe no notebook com chmod 700 e dono = usuário operador"
    - "cookies.txt fresco foi copiado para /data/yt-dlp-cache via scp via Tailscale"
    - "docker compose logs api NÃO mostra linha CRITICAL relacionada a cookies/cache no startup (AUTH-04)"
    - "3 URLs de beats reais retornam status=done com WAV não-vazio, BPM plausível e key em standard notation (PIPE-08)"
    - "Resultado E2E documentado em 14-DEPLOY-LOG.md incluindo se Plano B (BGUTIL_BASE_URL ativado) foi necessário"
  artifacts:
    - path: ".planning/phases/14-pipeline-e2e-on-notebook/14-DEPLOY-LOG.md"
      provides: "Audit trail do checkpoint humano AUTH-04 + PIPE-08 com outputs literais"
      min_lines: 40
  key_links:
    - from: "host /data/yt-dlp-cache/cookies.txt"
      to: "api/main.py _check_oauth_cache"
      via: "bind mount :ro do docker-compose.yml + variável YTDLP_CACHE_DIR do .env"
      pattern: "AUTH: cookies.txt encontrado"
    - from: "POST /jobs no notebook"
      to: "Celery worker → yt-dlp + Essentia"
      via: "Tailscale http://<IP>:8000"
      pattern: "status.*done"
---

<objective>
Checkpoint humano BLOQUEANTE para AUTH-04 e PIPE-08. O operador (Moisés) prepara host + cookies, invoca `scripts/deploy.sh` via SSH (exercendo AUTH-05), valida startup logs sem CRITICAL (D-13), submete 3 URLs de beats reais e registra o resultado em `14-DEPLOY-LOG.md`. Plano B (D-10) é documentado se `LOGIN_REQUIRED` aparecer mesmo em IP residencial com cookies frescos.

Purpose: AUTH-04 e PIPE-08 dependem de hardware real (notebook HP via Tailscale) e comportamento externo (YouTube anti-bot + cookies frescos). Não automatizáveis em CI.
Output: `14-DEPLOY-LOG.md` com outputs literais comprovando ambos os requisitos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md
@.planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md
@.planning/phases/14-pipeline-e2e-on-notebook/14-VALIDATION.md
@docker-compose.yml
@.env.example
@scripts/deploy.sh

<interfaces>
Pré-condições (devem estar verdadeiras antes deste plano):
- Plan 02 e Plan 03 mergeados; `pytest tests/test_deploy_sh.py -x -q` retorna `8 passed`.
- Phase 13 deployada no notebook: `sudo docker compose ps` no notebook mostra 4 serviços `running/healthy`.
- Tailscale ativo em ambas máquinas, IP do notebook conhecido (placeholder `<NOTEBOOK_TAILSCALE_IP>`).
- Operador tem acesso a uma conta Google autenticada no browser local para exportar cookies.

Pós-condições produzidas por este plano:
- `/data/yt-dlp-cache/cookies.txt` no notebook (dir 0700 / file 0600), dono = operador.
- `.env` no notebook contém `YTDLP_CACHE_DIR=/data/yt-dlp-cache` e `BGUTIL_BASE_URL=` (vazio inicial; se Plano B for ativado, `http://bgutil:4416`).
- `14-DEPLOY-LOG.md` registrado.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operador prepara host e copia cookies frescos para o notebook (D-03 + D-11 + D-12)</name>
  <files>/data/yt-dlp-cache/cookies.txt (no notebook, fora do repo)</files>
  <read_first>
    - .planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md (D-03, D-11, D-12)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md §"Pitfall 2: /data/yt-dlp-cache não existe no host" e §"Pitfall 3: scp para path errado"
  </read_first>
  <action>
    Esta task é manual e bloqueante — Claude apresenta as instruções abaixo e aguarda confirmação do operador antes de prosseguir. Operador executa três passos concretos:

    Passo A (no notebook, via SSH Tailscale, como usuário operador): criar `/data/yt-dlp-cache` com `sudo mkdir -p /data/yt-dlp-cache && sudo chown $USER: /data/yt-dlp-cache && chmod 700 /data/yt-dlp-cache`. Confirmar com `ls -ld /data/yt-dlp-cache` — esperado `drwx------ <user> <user>`.

    Passo B (na máquina local): abrir browser com a conta Google autenticada em youtube.com (assistir um vídeo qualquer para reativar a sessão), usar a extensão "Get cookies.txt LOCALLY" para exportar cookies de `youtube.com` em arquivo `cookies.txt`. Confirmar que contém o sentinela `__Secure-3PSID` com `grep -c "__Secure-3PSID" cookies.txt` retornando >= 1.

    Passo C (da máquina local para o notebook): verificar conectividade com `tailscale ping <NOTEBOOK_TAILSCALE_IP>`, depois `scp cookies.txt <user>@<NOTEBOOK_TAILSCALE_IP>:/data/yt-dlp-cache/cookies.txt`. No notebook: `chmod 600 /data/yt-dlp-cache/cookies.txt` e verificar `wc -c` retorna **> 2000 bytes** (Pitfall 3: ~1600 bytes indica corrupção; re-exportar) e `grep -c __Secure-3PSID` retorna >= 1.

    Passo D (garantir `.env` no notebook): em `~/soundgrabber/.env`, confirmar `YTDLP_CACHE_DIR=/data/yt-dlp-cache` e `BGUTIL_BASE_URL=` (vazio). Se `.env` não existir, copiar de `.env.example` (já atualizado pelo Plan 02) e ajustar `ADMIN_PASSWORD`/`ADMIN_SESSION_SECRET` para valores reais. NÃO usar `change-me-locally` em produção.
  </action>
  <verify>
    Manual, confirmado por outputs literais do operador no resume-signal: `ls -ld /data/yt-dlp-cache` mostra modo `drwx------`, `wc -c /data/yt-dlp-cache/cookies.txt` retorna > 2000, `grep -c __Secure-3PSID /data/yt-dlp-cache/cookies.txt` retorna >= 1, e `grep -E "^YTDLP_CACHE_DIR|^BGUTIL_BASE_URL" ~/soundgrabber/.env` mostra os dois valores corretos.
  </verify>
  <done>
    Operador respondeu "host preparado e cookies copiados" com os 4 outputs literais acima. `/data/yt-dlp-cache/cookies.txt` pronto para ser montado read-only nos containers via bind mount do Plan 02.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operador invoca scripts/deploy.sh via SSH e valida AUTH-04 (logs sem CRITICAL)</name>
  <files>scripts/deploy.sh (já criado pelo Plan 03, exercitado aqui via SSH)</files>
  <read_first>
    - scripts/deploy.sh (script criado no Plan 03 — confirmar conteúdo antes de executar)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md (D-04, D-05, D-13)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md §"Pattern 3: Checkpoint de Cookie via Logs"
  </read_first>
  <action>
    Operador executa três comandos da máquina local, todos via SSH não-interativo sobre Tailscale (exatamente a forma exigida por AUTH-05).

    Comando 1 (AUTH-05 — exercer o deploy.sh com um único comando SSH conforme D-04): `ssh <user>@<NOTEBOOK_TAILSCALE_IP> 'bash ~/soundgrabber/scripts/deploy.sh'`. Esperado exit code 0; output mostra `git pull` (Already up to date OU commits novos) seguido de `sudo docker compose up --build -d` reportando containers `Started`/`Recreated`. Tempo total típico 1-5 minutos dependendo do rebuild.

    Comando 2 (sanity check dos serviços): `ssh <user>@<NOTEBOOK_TAILSCALE_IP> 'cd ~/soundgrabber && sudo docker compose ps'`. Esperado 4 serviços (redis, bgutil, api, worker) com `State=Up`. bgutil deve estar Up mas inativo — `BGUTIL_BASE_URL=` vazio no .env (D-09).

    Comando 3 (AUTH-04 — checkpoint canônico definido por D-13 e Pattern 3 do RESEARCH.md): `ssh <user>@<NOTEBOOK_TAILSCALE_IP> 'cd ~/soundgrabber && sudo docker compose logs api --tail 200 | grep -E "CRITICAL|AUTH:|cookies"'`. Esperado: linhas afirmativas do `_check_oauth_cache()` em `api/main.py` (ex.: `AUTH: cookies.txt encontrado`), **zero ocorrências de CRITICAL**. Se aparecer `CRITICAL: cookies.txt nao encontrado`: rodar `ssh ... 'ls -la /data/yt-dlp-cache/'` no notebook e confirmar dono != root (Pitfall 2 — Docker criou dir como root); se for o caso, executar `sudo chown $USER: /data/yt-dlp-cache && sudo docker compose restart api` e repetir o checkpoint.

    Comando 4 (sanity check do bind mount dentro do container): `ssh <user>@<NOTEBOOK_TAILSCALE_IP> 'cd ~/soundgrabber && sudo docker compose exec api ls -la /data/yt-dlp-cache/cookies.txt'`. Esperado: arquivo listado com tamanho > 2000 bytes.
  </action>
  <verify>
    Manual, comprovado pelos outputs literais dos 4 comandos no resume-signal: (1) `ssh ... bash deploy.sh` exit 0; (2) `docker compose ps` lista 4 serviços Up; (3) `grep -c CRITICAL` na log do api retorna 0 (AUTH-04 satisfeito); (4) `docker compose exec api ls -la /data/yt-dlp-cache/cookies.txt` mostra arquivo > 2000 bytes acessível dentro do container.
  </verify>
  <done>
    AUTH-04 verificado: cookies presentes no startup, zero CRITICAL nos logs. AUTH-05 exercitado: comando único SSH deployou com sucesso. Pronto para PIPE-08 na Task 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operador valida PIPE-08 (3 beats E2E) e registra resultado em 14-DEPLOY-LOG.md</name>
  <files>.planning/phases/14-pipeline-e2e-on-notebook/14-DEPLOY-LOG.md</files>
  <read_first>
    - .planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md (D-09, D-10 — Plano B)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md §"Code Examples — Verificação E2E via curl"
    - .planning/ROADMAP.md §Phase 14 Success Criteria 3 (3 beats → status=done com WAV + BPM + key)
  </read_first>
  <action>
    Operador escolhe 3 URLs de beats distintos (recomendação: 1 trap, 1 lo-fi, 1 house — diversidade de gêneros aumenta confiança no detector Essentia) e submete cada uma ao `POST /jobs` no notebook via Tailscale.

    Para cada URL, executar da máquina local: `JOB=$(curl -s -X POST http://<NOTEBOOK_TAILSCALE_IP>:8000/jobs -H "Content-Type: application/json" -d '{"youtube_url": "<BEAT_URL>"}')`, capturar `JOB_ID` parsing o JSON com `python3 -c "import sys,json; print(json.load(sys.stdin)['job_id'])"`, depois poll a cada 3s até `status` ser `done` ou `error` (timeout máximo ~5 min via loop `for i in $(seq 1 100)`), e capturar o JSON final com `curl -s http://<IP>:8000/jobs/$JOB_ID | python3 -m json.tool`.

    Critérios PIPE-08 que cada um dos 3 jobs deve satisfazer: `status == "done"` (nunca `error`/`failed`/timeout); resposta inclui `bpm` numérico > 0 e plausível (60-200 para a maioria dos beats); resposta inclui `key` em standard notation (`"F# minor"`, `"C major"`, etc — não vazio, não null); resposta inclui URL de download apontando para `GET /files/{id}`. Validação extra opcional: `curl -o /tmp/beat.wav http://<IP>:8000/files/$JOB_ID && file /tmp/beat.wav` reporta `RIFF (little-endian) data, WAVE audio`.

    Plano B se algum job falhar com `LOGIN_REQUIRED` (D-10): inspecionar `ssh ... 'sudo docker compose logs worker --tail 100 | grep -iE "login|bot|sign in"'`. Se bot detection confirmado, editar `~/soundgrabber/.env` no notebook setando `BGUTIL_BASE_URL=http://bgutil:4416`, executar `ssh ... 'cd ~/soundgrabber && sudo docker compose up -d'` (sem `--build`, apenas restart com nova env), e repetir os 3 jobs. Documentar no log qual plano (A = sem bgutil OU B = com bgutil) entregou os 3 jobs done.

    Criar `.planning/phases/14-pipeline-e2e-on-notebook/14-DEPLOY-LOG.md` contendo: data/hora, IP Tailscale (mascarado se sensível), output literal de `sudo docker compose ps` (da Task 2), output literal do `grep -E "CRITICAL|AUTH:|cookies"` na log do api (Task 2), e para cada um dos 3 jobs: URL submetida, job_id, status final, bpm, key, tamanho do WAV. Indicar com nitidez: "Plano A (BGUTIL_BASE_URL vazio) — 3/3 passou" OU "Plano B (BGUTIL_BASE_URL=http://bgutil:4416) — 3/3 passou; Plano A falhou com <erro literal>". Incluir hash do commit dos Plans 02 e 03 no notebook via `ssh ... 'git -C ~/soundgrabber rev-parse HEAD'`.

    Após o log ser escrito, executar localmente `pytest tests/ -ra --strict-markers -q` na máquina de desenvolvimento (não no notebook) e anexar o resumo final ao log — confirma que nenhum teste regrediu após Plans 02/03.
  </action>
  <verify>
    Manual, comprovado pelo conteúdo de `14-DEPLOY-LOG.md`: 3 jobs distintos com `status=done` (3 blocos JSON visíveis no log), BPM > 0 plausível em todos, key em standard notation em todos, e indicação explícita de qual Plano (A ou B) foi suficiente. Adicionalmente: hash do commit no notebook coincide com HEAD do repo local; output de `pytest tests/ -ra --strict-markers -q` anexado mostra suite verde.
  </verify>
  <done>
    `14-DEPLOY-LOG.md` existe com audit trail completo de AUTH-04 e PIPE-08; 3/3 jobs done (Plano A OU B documentado); Phase 14 pode ser marcada como Done no ROADMAP e a próxima fase (15 - Cloudflare Tunnel) pode ser iniciada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (máquina local) → notebook (SSH/Tailscale) | Comandos SSH no checkpoint humano |
| host filesystem → containers (bind mount :ro) | Cookies expostos read-only |
| notebook → YouTube (egress via IP residencial) | yt-dlp autentica com cookies; PIPE-08 valida ausência de bot detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-deploy-09 | Spoofing | Cookies expirados causam LOGIN_REQUIRED | accept | Risco residual aceito — mitigação é Plano B (D-10) explícito; documentado no log; renovação manual periódica |
| T-deploy-10 | Information Disclosure | cookies.txt world-readable no host | mitigate | Task 1 enforce: dir 700, file 600, chown $USER (D-03); verificação `ls -ld` no resume-signal |
| T-deploy-11 | Denial of Service | bgutil ativo em produção mascarando falha de PIPE-08 "sem bgutil" | mitigate | Plano A é o default (BGUTIL_BASE_URL=); Plano B só ativa após falha documentada; log distingue qual plano passou |
</threat_model>

<verification>
- Task 1: diretório host criado com chmod 700, cookies copiados com > 2000 bytes e __Secure-3PSID presente
- Task 2: 4 containers Up; `grep CRITICAL` nos logs do api retorna 0 ocorrências
- Task 3: 3 jobs distintos retornam `status=done` com BPM/key/WAV válidos; resultado documentado em `14-DEPLOY-LOG.md`; suite pytest verde
</verification>

<success_criteria>
- AUTH-04: log do startup sem CRITICAL (D-13); cookies.txt acessível dentro do container api via bind mount :ro
- AUTH-05: comando único `ssh ... 'bash ~/soundgrabber/scripts/deploy.sh'` deploya com sucesso (validado na Task 2)
- PIPE-08: 3 beats distintos → status=done com WAV + BPM + key — sem bgutil (Plano A) ou com bgutil documentado (Plano B)
- `14-DEPLOY-LOG.md` é o audit trail aceito pelo `/gsd-verify-work`
</success_criteria>

<output>
Criar `.planning/phases/14-pipeline-e2e-on-notebook/14-04-SUMMARY.md` referenciando `14-DEPLOY-LOG.md`, incluindo:
- Confirmação AUTH-04 (output do grep CRITICAL)
- Confirmação AUTH-05 (ssh + deploy.sh exit 0)
- Confirmação PIPE-08 (3 jobs done com BPM/key)
- Qual plano (A/B) passou
- Próximos passos: marcar Phase 14 como Done em ROADMAP.md; preparar Phase 15 (Cloudflare Tunnel)
</output>
