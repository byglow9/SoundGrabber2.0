---
phase: 14-pipeline-e2e-on-notebook
plan: 03
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - scripts/deploy.sh
autonomous: true
requirements:
  - AUTH-05
tags:
  - deploy
  - shell
  - security-gate
must_haves:
  truths:
    - "scripts/deploy.sh existe, é executável (0o750), e pode ser invocado via SSH com bash"
    - "deploy.sh executa exatamente: cd ~/soundgrabber → git pull → sudo docker compose up --build -d"
    - "deploy.sh cumpre o Security Gate: set -e, chmod 750 auto-aplicado, sem eval"
    - "deploy.sh NÃO inclui migração/cópia de cookies (D-06 — separação de responsabilidades)"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "Deploy remoto SoundGrabber no notebook via SSH/Tailscale com um único comando"
      min_lines: 15
      contains: "sudo docker compose up --build -d"
  key_links:
    - from: "operador (máquina local)"
      to: "scripts/deploy.sh no notebook"
      via: "ssh moisés@100.x.x.x 'bash ~/soundgrabber/scripts/deploy.sh'"
      pattern: "ssh.*bash.*scripts/deploy\\.sh"
    - from: "scripts/deploy.sh"
      to: "docker compose"
      via: "sudo docker compose up --build -d"
      pattern: "sudo docker compose up --build -d"
---

<objective>
Implementar AUTH-05: criar `scripts/deploy.sh` que pode ser invocado pelo operador com um único comando SSH para fazer `git pull + docker compose up --build -d` no notebook, cumprindo integralmente o Security Gate do projeto (CLAUDE.md).

Purpose: Operador deploya código novo no notebook sem precisar SSH interativo. Script é commitado no repo (D-04), instalado via clone em `~/soundgrabber/scripts/deploy.sh`, e atualiza a si mesmo a cada `git pull` antes de rodar.
Output: 2 testes RED restantes do Plan 01 viram GREEN (testes 7-8); arquivo commitado e executável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md
@.planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md
@CLAUDE.md
@start-all.sh
@scripts/notebook-setup.sh
@tests/test_deploy_sh.py

<interfaces>
Padrão estabelecido de scripts shell no projeto (verificado via Read tool):

`start-all.sh` linha 17: `set -e` é a primeira linha não-comentário não-shebang.
`scripts/notebook-setup.sh` linhas 1-4: `#!/usr/bin/env bash` → `set -e` → comentário SEC-SCRIPT → `chmod 750 "$(realpath "$0")"`.

Security Gate (CLAUDE.md §"Controles obrigatorios em qualquer novo script shell"):
1. `set -e` na primeira linha após shebang
2. Auto-chmod restritivo: `chmod 750 "$(realpath "$0")"` para scripts de operação
3. Sem `eval` de input externo

Conteúdo exato definido por D-05: `cd ~/soundgrabber`, `git pull`, `sudo docker compose up --build -d`.
D-06 explicitamente exclui: cópia/migração de cookies (responsabilidade do operador via scp em AUTH-04).
Phase 12 D-06: `sudo docker` em vez de grupo docker — obrigatório usar `sudo` no script.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scripts/deploy.sh com Security Gate e 3 comandos canônicos</name>
  <files>scripts/deploy.sh</files>
  <read_first>
    - CLAUDE.md §"Security Gate" (controles obrigatórios para novos scripts shell)
    - start-all.sh (padrão de shebang + set -e do projeto)
    - scripts/notebook-setup.sh (padrão de chmod 750 auto-aplicado)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md (D-04, D-05, D-06, D-07)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md §"Pattern 2: Deploy Script com Security Gate" e §"Pitfall 4: docker compose vs sudo docker compose"
    - tests/test_deploy_sh.py (já criado no Plan 01 — duas asserções pendentes definem exatamente o conteúdo esperado)
  </read_first>
  <action>
    Criar `scripts/deploy.sh` com a seguinte estrutura (em prosa — escreva o arquivo seguindo esta especificação literal):

    1. Linha 1: shebang `#!/usr/bin/env bash` (não `#!/bin/bash` — alinhado com start-all.sh e notebook-setup.sh).
    2. Linhas 2-8 (comentários): bloco explicando propósito do script, invocação SSH exata (`ssh moisés@100.x.x.x 'bash ~/soundgrabber/scripts/deploy.sh'`), o que faz (git pull + rebuild + restart), o que NÃO faz (NÃO gerencia cookies — D-06), e referências (D-04..D-07 e Security Gate em CLAUDE.md).
    3. Primeira linha de código após o bloco de comentários: literalmente `set -e` (sem nada antes nela exceto comentários/shebang — Security Gate Item 1; também é a asserção literal do teste 7 do Plan 01).
    4. Próxima linha de código: comentário `# Security Gate: auto-aplica permissões restritivas a cada execução (CLAUDE.md §1.2)` seguido da linha literal `chmod 750 "$(realpath "$0")"` (Security Gate Item 2; asserção do teste 8).
    5. Próxima linha: literal `cd ~/soundgrabber` (D-05 — diretório de trabalho assumido pelo operador).
    6. Próxima linha (após linha em branco): literal `git pull` (D-05).
    7. Próxima linha (após linha em branco): literal `sudo docker compose up --build -d` (D-05 + Pitfall 4: `sudo` obrigatório por Phase 12 D-06).

    PROIBIDO: nenhuma ocorrência da string `eval` em qualquer linha do arquivo (Security Gate Item 3; asserção do teste 8). NÃO adicionar `set -u` ou `set -o pipefail` — D-05 especifica conteúdo exato e não inclui esses flags. NÃO adicionar comandos de cópia de cookies, scp, rsync, ou qualquer manipulação de credenciais (D-06). NÃO adicionar `docker compose pull` ou `docker compose down` antes do `up --build -d`. NÃO usar `sudo -E` ou exportar variáveis. NÃO adicionar logging colorido (ANSI) — script é executado via SSH não-interativo e mantém-se minimal.

    Após escrever o arquivo, executar `chmod 0o750 scripts/deploy.sh` no checkout local para que `git ls-files --stage scripts/deploy.sh` mostre modo `100750` (Git preserva o bit executável). Isso garante que o operador pode rodar `bash ~/soundgrabber/scripts/deploy.sh` (que ignora bit X, mas alguém pode também invocar direto) e que `chmod 750` é re-aplicado a cada execução para autocura.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/SoundGrabber2.0 &amp;&amp; pytest tests/test_deploy_sh.py::test_deploy_sh_exists_and_has_set_e tests/test_deploy_sh.py::test_deploy_sh_security_gate_and_commands -x -q 2>&amp;1 | tail -10 &amp;&amp; bash -n scripts/deploy.sh &amp;&amp; echo "shellcheck-syntax: OK"</automated>
    Esperado: 2 passed + `shellcheck-syntax: OK`. Adicionalmente: `pytest tests/test_deploy_sh.py -x -q 2>&1 | tail -3` deve agora reportar `8 passed` (todos os testes do Plan 01 são GREEN após Plans 02 e 03).
  </verify>
  <acceptance_criteria>
    - Arquivo `scripts/deploy.sh` existe
    - `bash -n scripts/deploy.sh` retorna exit code 0 (sintaxe bash válida)
    - Primeira linha de código (não-comentário, não-shebang) em `scripts/deploy.sh` é exatamente `set -e` — verificável com `grep -v '^#' scripts/deploy.sh | grep -v '^$' | head -1` retornando `set -e`
    - `grep -F 'chmod 750 "$(realpath "$0")"' scripts/deploy.sh | wc -l` retorna 1
    - `grep -F 'cd ~/soundgrabber' scripts/deploy.sh | wc -l` retorna 1
    - `grep -cE '^git pull$' scripts/deploy.sh` retorna 1
    - `grep -cE '^sudo docker compose up --build -d$' scripts/deploy.sh` retorna 1
    - `grep -c 'eval' scripts/deploy.sh` retorna 0 (Security Gate Item 3)
    - `grep -c 'scp\|rsync\|cookies' scripts/deploy.sh` retorna 0 (D-06 — sem cookie management)
    - `git ls-files --stage scripts/deploy.sh` mostra modo `100750`
    - Testes 7 e 8 do Plan 01 passam: `pytest tests/test_deploy_sh.py -k "deploy_sh_exists or deploy_sh_security_gate_and_commands" -x -q` reporta 2 passed
    - Suite completa do Plan 01 está GREEN: `pytest tests/test_deploy_sh.py -x -q` reporta `8 passed`
  </acceptance_criteria>
  <done>
    `scripts/deploy.sh` existe, é sintaticamente válido, contém exatamente os 3 comandos canônicos D-05, cumpre todos os 3 controles do Security Gate, não inclui gestão de cookies (D-06), está marcado executável no git (0o750), e a suíte completa `tests/test_deploy_sh.py` é GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (máquina local) → notebook (SSH/Tailscale) | Comando SSH invoca o script remoto; conteúdo do script é o único surface escutado pelo notebook |
| script bash → docker daemon (via sudo) | Script eleva privilégios para `sudo docker compose` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-deploy-05 | Elevation of Privilege | scripts/deploy.sh world-executable | mitigate | `chmod 750 "$(realpath "$0")"` auto-aplicado (D-07) — apenas owner + grupo executam; modo 100750 no git index reforça padrão |
| T-deploy-06 | Tampering | `eval` ou execução de input externo | mitigate | Proibido por Security Gate; acceptance_criteria verifica `grep -c eval == 0`; script não aceita argumentos posicionais nem stdin |
| T-deploy-07 | Information Disclosure | Cookies misturados com código | mitigate | D-06: deploy.sh NÃO manipula cookies; `grep -c 'scp\|rsync\|cookies' == 0` |
| T-deploy-08 | Denial of Service | `set -e` ausente → erros silenciosos em `git pull` deixam containers em estado inconsistente | mitigate | `set -e` é a primeira linha de código; falha de qualquer step para o deploy imediatamente, exit code não-zero propaga via SSH ao operador |
</threat_model>

<verification>
- 8/8 testes em `tests/test_deploy_sh.py` GREEN
- `bash -n scripts/deploy.sh` exit 0
- Padrões grep nos acceptance_criteria todos satisfeitos
- `git ls-files --stage scripts/deploy.sh` mostra `100750`
</verification>

<success_criteria>
- `scripts/deploy.sh` cumpre D-04..D-07 integralmente
- Security Gate aplicado (set -e, chmod 750, sem eval)
- Operador pode invocar via `ssh moisés@100.x.x.x 'bash ~/soundgrabber/scripts/deploy.sh'` — comando único (AUTH-05)
- Pronto para checkpoint humano E2E do Plan 04
</success_criteria>

<output>
Criar `.planning/phases/14-pipeline-e2e-on-notebook/14-03-SUMMARY.md` com:
- Conteúdo completo do `scripts/deploy.sh` criado
- Output de `bash -n scripts/deploy.sh` (deve ser vazio + exit 0)
- Output de `pytest tests/test_deploy_sh.py -x -q` (8 passed)
- Confirmação de que `git ls-files --stage scripts/deploy.sh` mostra modo `100750`
- Mapeamento linha-do-script → decisão D-XX correspondente
</output>
