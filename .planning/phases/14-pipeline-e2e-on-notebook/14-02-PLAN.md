---
phase: 14-pipeline-e2e-on-notebook
plan: 02
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - AUTH-04
tags:
  - docker-compose
  - cookies
  - bind-mount
must_haves:
  truths:
    - "Serviços api e worker do docker-compose.yml montam /data/yt-dlp-cache:/data/yt-dlp-cache:ro"
    - "Serviços api e worker preservam o volume sg_tmp:/tmp existente (Pitfall 1)"
    - ".env.example documenta YTDLP_CACHE_DIR=/data/yt-dlp-cache (D-02)"
    - ".env.example deixa BGUTIL_BASE_URL= vazio para o notebook (D-09 + Pitfall 5)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Definição dos 4 serviços com bind mount :ro adicionado em api e worker"
      contains: "/data/yt-dlp-cache:/data/yt-dlp-cache:ro"
    - path: ".env.example"
      provides: "Template de .env atualizado para o notebook"
      contains: "YTDLP_CACHE_DIR=/data/yt-dlp-cache"
  key_links:
    - from: "docker-compose.yml api.volumes"
      to: "host /data/yt-dlp-cache"
      via: "bind mount read-only"
      pattern: "/data/yt-dlp-cache:/data/yt-dlp-cache:ro"
    - from: "docker-compose.yml worker.volumes"
      to: "host /data/yt-dlp-cache"
      via: "bind mount read-only"
      pattern: "/data/yt-dlp-cache:/data/yt-dlp-cache:ro"
---

<objective>
Implementar AUTH-04 lado infra: adicionar o bind mount `/data/yt-dlp-cache:/data/yt-dlp-cache:ro` (D-01) nos serviços `api` e `worker` do `docker-compose.yml`, e atualizar `.env.example` para refletir os valores corretos do notebook (D-02 + D-09).

Purpose: Faz com que `_check_oauth_cache()` em `api/main.py` (já implementado em Phase 10.1) encontre `cookies.txt` no path correto no container, e que o template `.env.example` documente que o notebook usa cookies sem bgutil.
Output: 6 dos 8 testes RED do Plan 01 viram GREEN (testes 1-6 da lista).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md
@.planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md
@docker-compose.yml
@.env.example
@tests/test_deploy_sh.py

<interfaces>
Estado atual a modificar (verificado via Read tool):

docker-compose.yml — serviço `api` (linhas ~45-58 atualmente):
```
  api:
    image: soundgrabber:latest
    ...
    volumes:
      - sg_tmp:/tmp
    ...
```

docker-compose.yml — serviço `worker` (linhas ~60-72 atualmente):
```
  worker:
    image: soundgrabber:latest
    ...
    volumes:
      - sg_tmp:/tmp
    ...
```

.env.example (estado atual):
- Linha 11: `BGUTIL_BASE_URL=http://bgutil:4416`  → mudar para `BGUTIL_BASE_URL=` (vazio) com comentário explicando D-09
- Linha 14: `YTDLP_CACHE_DIR=`  → mudar para `YTDLP_CACHE_DIR=/data/yt-dlp-cache` com comentário explicando D-02

Demais serviços (`redis`, `bgutil`) — sem mudança. bgutil permanece no compose por D-08.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar bind mount /data/yt-dlp-cache:ro em api e worker</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (estado atual completo — confirmar que sg_tmp:/tmp está presente em ambos os serviços e NÃO removê-lo)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md (D-01)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md §"Pitfall 1: sg_tmp volume perdido ao adicionar bind mount"
  </read_first>
  <action>
    Editar `docker-compose.yml` adicionando uma segunda entrada na lista `volumes:` dos serviços `api` e `worker`. A entrada deve ser literalmente `- /data/yt-dlp-cache:/data/yt-dlp-cache:ro` (mesmos paths host e container, flag `:ro`). NÃO substituir nem remover a linha existente `- sg_tmp:/tmp` — ambas devem coexistir (Pitfall 1). NÃO adicionar o bind mount aos serviços `redis` ou `bgutil`. NÃO alterar `mem_limit`, `depends_on`, `networks`, `ports`, `env_file`, `command` ou qualquer outra propriedade. Manter ordenação atual dos serviços (redis, bgutil, api, worker). Adicionar comentário inline acima da nova linha em cada serviço: `# bind mount :ro — Phase 14 D-01; cookies.txt vem do host, yt-dlp não pode sobrescrever`.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/SoundGrabber2.0 &amp;&amp; pytest tests/test_deploy_sh.py::test_bind_mount_in_compose_api tests/test_deploy_sh.py::test_bind_mount_in_compose_worker tests/test_deploy_sh.py::test_compose_preserves_sg_tmp_in_api tests/test_deploy_sh.py::test_compose_preserves_sg_tmp_in_worker -x -q 2>&amp;1 | tail -10</automated>
    Esperado: 4 passed. Adicionalmente: `docker compose -f docker-compose.yml config 2>&1 | grep -c "/data/yt-dlp-cache"` deve retornar >= 2 (uma vez por serviço).
  </verify>
  <acceptance_criteria>
    - `grep -c "/data/yt-dlp-cache:/data/yt-dlp-cache:ro" docker-compose.yml` retorna exatamente 2
    - `grep -c "sg_tmp:/tmp" docker-compose.yml` retorna exatamente 2 (preservado em api e worker)
    - `docker compose -f docker-compose.yml config -q` (validação de sintaxe YAML) retorna exit code 0
    - 4 testes do Plan 01 (bind_mount_in_compose_api/worker, preserves_sg_tmp_in_api/worker) passam
    - Nenhum dos 4 serviços (redis, bgutil, api, worker) foi removido; `docker compose config --services` lista exatamente: bgutil, api, redis, worker (ordem irrelevante)
  </acceptance_criteria>
  <done>
    `docker-compose.yml` tem bind mount `:ro` em api e worker, sg_tmp:/tmp preservado em ambos, sintaxe YAML válida, e 4 testes do Plan 01 viraram GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar .env.example para valores do notebook (D-02 + D-09)</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (estado atual completo — confirmar linhas 11 e 14)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md (D-02, D-09)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md §"Pitfall 5: E2E com BGUTIL_BASE_URL ativo quando deveria estar vazio"
  </read_first>
  <action>
    Editar `.env.example`. Mudança 1 (D-09): substituir a linha `BGUTIL_BASE_URL=http://bgutil:4416` por `BGUTIL_BASE_URL=` (valor vazio à direita do `=`, sem espaços, sem comentário inline na mesma linha). Acima dessa linha, atualizar o comentário existente para: `# bgutil PO Token provider INATIVO no notebook (D-09) — IP residencial usa apenas cookies; setar http://bgutil:4416 apenas como Plano B se E2E falhar com LOGIN_REQUIRED (D-10)`. Mudança 2 (D-02): substituir a linha `YTDLP_CACHE_DIR=` por `YTDLP_CACHE_DIR=/data/yt-dlp-cache`. Acima dela, atualizar comentário para: `# yt-dlp cache + cookies — Phase 14 D-02; bind mount :ro no docker-compose.yml lê do host`. Não alterar outras variáveis (REDIS_URL, DEV_MODE, RATE_LIMIT_*, WAV_TTL_SECONDS, ADMIN_*, FEATURED_FALLBACK_PATH). Não introduzir trailing whitespace nem alterar a ordem das chaves.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/SoundGrabber2.0 &amp;&amp; pytest tests/test_deploy_sh.py::test_env_example_ytdlp_cache_dir tests/test_deploy_sh.py::test_env_example_bgutil_empty -x -q 2>&amp;1 | tail -10</automated>
    Esperado: 2 passed.
  </verify>
  <acceptance_criteria>
    - `grep -c "^YTDLP_CACHE_DIR=/data/yt-dlp-cache$" .env.example` retorna 1
    - `grep -c "^BGUTIL_BASE_URL=$" .env.example` retorna 1
    - `grep -c "^BGUTIL_BASE_URL=http" .env.example` retorna 0 (a linha antiga `http://bgutil:4416` foi removida)
    - `grep -c "^REDIS_URL=redis://redis:6379/0$" .env.example` retorna 1 (não foi alterado por engano)
    - `grep -c "^DEV_MODE=true$" .env.example` retorna 1 (não foi alterado)
    - 2 testes do Plan 01 (env_example_ytdlp_cache_dir, env_example_bgutil_empty) passam
  </acceptance_criteria>
  <done>
    `.env.example` documenta `YTDLP_CACHE_DIR=/data/yt-dlp-cache` e `BGUTIL_BASE_URL=` (vazio), com comentários referenciando D-02/D-09/D-10. 6/8 dos testes RED do Plan 01 são agora GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem → containers (bind mount) | Cookies em `/data/yt-dlp-cache` no host expostos read-only aos containers api/worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-deploy-02 | Tampering | yt-dlp sobrescrevendo cookies.txt corrompidos no container | mitigate | Bind mount com flag `:ro` (D-01) — yt-dlp não consegue escrever no path montado, mesmo bug do Railway (bytes 2987→1600) fica impossível |
| T-deploy-03 | Information Disclosure | YTDLP_CACHE_DIR não documentado em template | mitigate | `.env.example` agora documenta path canônico `/data/yt-dlp-cache` (D-02) — operadores não improvisam paths inseguros |
| T-deploy-04 | Tampering | bgutil acidentalmente ativo no notebook mascarando falha real de PIPE-08 | mitigate | `.env.example` deixa `BGUTIL_BASE_URL=` vazio (D-09) com comentário explicando Plano B (D-10) |
</threat_model>

<verification>
- 6 testes do Plan 01 viraram GREEN: `pytest tests/test_deploy_sh.py -k "bind_mount or sg_tmp or env_example" -x -q` reporta 6 passed
- `docker compose -f docker-compose.yml config -q` exit code 0
- `grep -c "/data/yt-dlp-cache:/data/yt-dlp-cache:ro" docker-compose.yml` retorna 2
</verification>

<success_criteria>
- Bind mount `:ro` presente em api e worker, sg_tmp preservado
- `.env.example` atualizado conforme D-02 e D-09
- 6/8 testes do Plan 01 GREEN; 2 restantes (deploy.sh) são responsabilidade do Plan 03
</success_criteria>

<output>
Criar `.planning/phases/14-pipeline-e2e-on-notebook/14-02-SUMMARY.md` com:
- Diff conceitual do `docker-compose.yml` (volumes adicionados em api e worker)
- Diff conceitual do `.env.example` (2 linhas alteradas)
- Output de `pytest tests/test_deploy_sh.py -x -q` (6 passed, 2 failed restantes — deploy.sh)
</output>
