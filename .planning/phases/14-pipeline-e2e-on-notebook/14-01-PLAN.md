---
phase: 14-pipeline-e2e-on-notebook
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_deploy_sh.py
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05
tags:
  - tdd
  - deploy
  - cookies
must_haves:
  truths:
    - "tests/test_deploy_sh.py existe e pytest coleta 8 testes RED"
    - "Cada teste é RED por uma razão concreta (arquivo ausente OU conteúdo ainda não modificado)"
    - "Suite completa permanece coletável (nenhum import quebra outros arquivos)"
  artifacts:
    - path: "tests/test_deploy_sh.py"
      provides: "8 stubs RED cobrindo bind mount no compose, .env.example, e scripts/deploy.sh"
      min_lines: 80
  key_links:
    - from: "tests/test_deploy_sh.py::test_bind_mount_in_compose"
      to: "docker-compose.yml"
      via: "YAML parse — busca volume '/data/yt-dlp-cache:/data/yt-dlp-cache:ro' nos serviços api e worker"
    - from: "tests/test_deploy_sh.py::test_deploy_sh_*"
      to: "scripts/deploy.sh"
      via: "leitura de texto do script"
---

<objective>
Wave 0 (TDD RED): criar `tests/test_deploy_sh.py` com 8 stubs RED que travam a baseline do que os Plans 02 e 03 devem produzir. Cada teste falha por motivo concreto antes da implementação, vira GREEN exatamente quando o artefato alvo é escrito corretamente.

Purpose: Garante que as decisões D-01, D-02, D-04..D-07 e D-09 sejam invioláveis — qualquer regressão futura no `docker-compose.yml`, `.env.example` ou `scripts/deploy.sh` quebra a suíte.
Output: `tests/test_deploy_sh.py` com 8 testes que importam sem erro e falham com mensagens acionáveis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-pipeline-e2e-on-notebook/14-CONTEXT.md
@.planning/phases/14-pipeline-e2e-on-notebook/14-RESEARCH.md
@.planning/phases/14-pipeline-e2e-on-notebook/14-VALIDATION.md
@docker-compose.yml
@.env.example
@pytest.ini
@tests/test_pipeline_docker.py

<interfaces>
Estado atual relevante (do repo):

- `docker-compose.yml` linhas 45-72: serviços `api` e `worker` têm apenas `volumes: [- sg_tmp:/tmp]`. NÃO há bind mount para `/data/yt-dlp-cache` ainda.
- `.env.example` linha 11: `BGUTIL_BASE_URL=http://bgutil:4416` (D-09 exige vazio no notebook).
- `.env.example` linha 14: `YTDLP_CACHE_DIR=` (D-02 exige `/data/yt-dlp-cache`).
- `scripts/deploy.sh`: NÃO existe ainda — `os.path.exists` retorna False.
- Padrão de testes do projeto: pytest com testpaths=tests, addopts=`-ra --strict-markers`.

Os 8 testes do plano devem usar apenas stdlib (pathlib, yaml — yaml já disponível por dependência indireta; se faltar, usar parser textual via `re` no docker-compose.yml).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar tests/test_deploy_sh.py com 8 stubs RED</name>
  <files>tests/test_deploy_sh.py</files>
  <read_first>
    - tests/test_pipeline_docker.py (padrão de testes file-content/YAML usado no projeto)
    - docker-compose.yml (estado atual dos volumes em api e worker)
    - .env.example (valores atuais de YTDLP_CACHE_DIR e BGUTIL_BASE_URL)
    - pytest.ini (markers e addopts em uso)
    - .planning/phases/14-pipeline-e2e-on-notebook/14-VALIDATION.md (Per-Task Verification Map define os 8 testes)
  </read_first>
  <behavior>
    Cada teste valida exatamente uma asserção concreta:

    AUTH-04 (compose + .env.example):
    1. test_bind_mount_in_compose_api — serviço `api` tem volume "/data/yt-dlp-cache:/data/yt-dlp-cache:ro"
    2. test_bind_mount_in_compose_worker — serviço `worker` tem volume "/data/yt-dlp-cache:/data/yt-dlp-cache:ro"
    3. test_compose_preserves_sg_tmp_in_api — serviço `api` mantém `sg_tmp:/tmp` (Pitfall 1 RESEARCH.md)
    4. test_compose_preserves_sg_tmp_in_worker — serviço `worker` mantém `sg_tmp:/tmp`
    5. test_env_example_ytdlp_cache_dir — `.env.example` contém literal `YTDLP_CACHE_DIR=/data/yt-dlp-cache`
    6. test_env_example_bgutil_empty — `.env.example` contém literal `BGUTIL_BASE_URL=` em linha cujo valor após `=` é vazio (sem `http://bgutil:4416`)

    AUTH-05 (deploy.sh):
    7. test_deploy_sh_exists_and_has_set_e — `scripts/deploy.sh` existe E primeira linha não-comentário/não-shebang é exatamente `set -e`
    8. test_deploy_sh_security_gate_and_commands — `scripts/deploy.sh` contém todas as substrings: `chmod 750 "$(realpath "$0")"`, `cd ~/soundgrabber`, `git pull`, `sudo docker compose up --build -d`, E NÃO contém a substring `eval`
  </behavior>
  <action>
    Criar `tests/test_deploy_sh.py` no padrão dos outros test_*.py do projeto (sem classe wrapper, funções soltas). Implementar os 8 testes listados em <behavior> usando: pathlib.Path("docker-compose.yml").read_text() + parser YAML (preferir `yaml.safe_load`; se indisponível no ambiente de teste, regex direto sobre linhas com `/data/yt-dlp-cache`); pathlib.Path(".env.example").read_text() para os dois testes de env; pathlib.Path("scripts/deploy.sh").exists() + read_text para os 2 testes de deploy.sh. Os testes 1-6 devem FALHAR contra o estado atual do repo porque o bind mount ainda não foi adicionado e `.env.example` ainda tem `BGUTIL_BASE_URL=http://bgutil:4416` e `YTDLP_CACHE_DIR=`. Os testes 7-8 devem FALHAR porque `scripts/deploy.sh` ainda não existe. Use mensagens de assert explícitas citando a string esperada (ex.: `assert "/data/yt-dlp-cache:/data/yt-dlp-cache:ro" in api_volumes, f"Expected bind mount in api service, got {api_volumes}"`). Não adicionar marcadores `@pytest.mark.integration` ou `@pytest.mark.e2e` — estes são testes de file-content puros, devem rodar no quick suite.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/SoundGrabber2.0 &amp;&amp; pytest tests/test_deploy_sh.py -x -q 2>&amp;1 | tail -20</automated>
    Esperado: 8 failed, 0 passed. Toda falha por motivo concreto (string ausente, arquivo ausente), nenhuma por ImportError/SyntaxError. Suite continua coletável: `pytest tests/ --collect-only -q | tail -3` mostra >= existing_count + 8 testes coletados.
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_deploy_sh.py --collect-only -q` lista exatamente 8 test items
    - `pytest tests/test_deploy_sh.py -x -q` retorna exit code != 0 com "8 failed" no relatório
    - Nenhum dos 8 testes falha com `ImportError` ou `SyntaxError` ou `FileNotFoundError` da fixture (apenas `AssertionError` ou `FileNotFoundError` explicitamente esperado nos testes 7-8 com mensagem "scripts/deploy.sh não existe")
    - `grep -c "^def test_" tests/test_deploy_sh.py` retorna 8
    - Cada `def test_*` cita uma das 8 verificações listadas em `<behavior>` (uma asserção por teste, sem mesclar)
    - `pytest tests/ --collect-only -q 2>&amp;1 | tail -3` ainda lista a suite completa coletada (nenhum outro arquivo quebra)
  </acceptance_criteria>
  <done>
    `tests/test_deploy_sh.py` existe com 8 testes RED, falham por razões concretas alinhadas com D-01/D-02/D-04..D-07/D-09, e a suite completa permanece coletável. Estes testes são os gates que Plans 02 e 03 devem virar GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → notebook (SSH) | Phase 14 opera neste boundary; testes desta fase verificam que o script invocado via SSH tem Security Gate aplicado |
| host filesystem → containers (bind mount) | Bind mount `:ro` impede containers de corromper cookies; testes verificam que `:ro` está presente no compose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-deploy-01 | Tampering | tests/test_deploy_sh.py ausente | mitigate | Este plano cria o arquivo com 8 testes RED — gate contra regressão silenciosa em compose/env/script |
</threat_model>

<verification>
- `pytest tests/test_deploy_sh.py --collect-only -q` lista 8 testes
- `pytest tests/test_deploy_sh.py -x -q` reporta `8 failed`
- `pytest tests/ -ra --strict-markers -q 2>&1 | tail -5` continua a executar a suite inteira (nenhum import side-effect quebra outros arquivos)
</verification>

<success_criteria>
- 8 stubs RED criados, todos falhando por razões concretas alinhadas com as decisões locked
- Suite completa permanece coletável
- Plan 02 (compose + .env.example) e Plan 03 (deploy.sh) terão os GREEN targets perfeitamente definidos
</success_criteria>

<output>
Criar `.planning/phases/14-pipeline-e2e-on-notebook/14-01-SUMMARY.md` com:
- Caminho do arquivo criado e número exato de testes
- Output literal de `pytest tests/test_deploy_sh.py -x -q` (deve ser 8 failed)
- Mapping linha por linha de cada teste → decisão D-XX que ele protege
</output>
