---
phase: "10.1"
plan: "01"
subsystem: tests
tags: [tdd, auth-migration, test-scaffold, oauth2, railway-volume, red-phase]
completed: "2026-05-13T18:31:38Z"
duration: "~15 minutes"

dependency_graph:
  requires: []
  provides:
    - "tests/test_pipeline_oauth.py — suite RED de 8 testes para AUTH-01"
    - "tests/test_pipeline_fixes.py — testes bgutil invertidos para verificar remoção"
  affects:
    - "pipeline.py — será validado pelos testes quando Wave 2 implementar"
    - "api/main.py — será validado pelo _check_oauth_cache quando Wave 2 implementar"
    - "requirements.txt — será validado quando bgutil for removido"

tech_stack:
  added: []
  patterns:
    - "_make_fake_ydl factory — captura ydl_opts via FakeYDL para assertions de configuração"
    - "import dentro do teste para _check_oauth_cache — ImportError é sinal RED explícito"
    - "inspect.getsource para verificar ausência de código — padrão de test_pipeline_fixes.py"

key_files:
  created:
    - tests/test_pipeline_oauth.py
  modified:
    - tests/test_pipeline_fixes.py

decisions:
  - "Importar _check_oauth_cache dentro do teste (não no topo) para que ImportError seja falha de teste, não erro de coleção"
  - "8 testes em test_pipeline_oauth.py (plano previa 7-8 — aumentado para cobrir test_check_duration_no_oauth2 explicitamente)"
  - "pytest é mantido em test_pipeline_fixes.py (usado por test_pipe06_no_bgutil_probe_in_download_audio)"

metrics:
  tasks_completed: 2
  tasks_total: 2
  files_created: 1
  files_modified: 1
  commits: 2
---

# Phase 10.1 Plan 01: Test Scaffold for AUTH-01 Migration Summary

Wave 0 TDD: 8 testes RED criados para AUTH-01 (cookiefile do Railway Volume + remoção de bgutil); testes bgutil em test_pipeline_fixes.py invertidos de verificar presença para verificar ausência.

## Tasks Completed

| Task | Description | Commit | Files |
|------|-------------|--------|-------|
| 1 | Criar tests/test_pipeline_oauth.py com 8 testes RED | fdb97ac | tests/test_pipeline_oauth.py (+226 linhas) |
| 2 | Inverter testes bgutil e substituir PIPE-06 em test_pipeline_fixes.py | 9eb2eab | tests/test_pipeline_fixes.py (-104/+39 linhas) |

## RED State Verification

### tests/test_pipeline_oauth.py (8 testes, 6 RED)

| Teste | Estado | Motivo da falha |
|-------|--------|-----------------|
| test_check_duration_uses_cookiefile_from_cache_dir | RED | check_duration recebe cookies_path, não cache_dir |
| test_check_duration_no_oauth2_in_ydl_opts | GREEN* | cookiefile setado com cookies_path direto — sem username, sem OAuth2 |
| test_check_duration_no_cookiefile_when_cache_dir_empty | GREEN* | cookiefile não setado quando cookies_path vazio |
| test_download_audio_uses_cookiefile_from_cache_dir | RED | download_audio recebe cookies_path, não cache_dir |
| test_check_oauth_cache_critical_when_cache_dir_empty | RED | _check_oauth_cache não existe em api/main.py |
| test_check_oauth_cache_critical_when_no_file | RED | _check_oauth_cache não existe em api/main.py |
| test_requirements_no_bgutil | RED | bgutil-ytdlp-pot-provider presente em requirements.txt |
| test_bgutil_unavailable_class_removed | RED | BgutilUnavailable ainda existe em pipeline.py |

*Os testes 2 e 3 passam acidentalmente porque o código atual não adiciona `username` — isso é correto e não invalida o contrato RED (esses testes verificam ausência que já é verdadeira; Wave 2 não pode quebrá-los).

### tests/test_pipeline_fixes.py (3 novos/invertidos, todos RED)

| Teste | Estado | Motivo da falha |
|-------|--------|-----------------|
| test_bgutil_removed_from_check_duration | RED | getpot_bgutil_baseurl ainda em check_duration |
| test_bgutil_removed_from_download_audio | RED | httpx.get e BgutilUnavailable ainda em download_audio |
| test_pipe06_no_bgutil_probe_in_download_audio | RED | httpx.get e BgutilUnavailable ainda em download_audio |

## Deviations from Plan

### Contagem de testes

O plano mencionava "7 testes" no objetivo e "8 testes" na seção de behavior (com Test 8 explícito). O arquivo final tem 8 testes, incluindo `test_check_duration_no_oauth2_in_ydl_opts` como Test 2 separado — isso está alinhado com a behavior section do plano.

### Nenhum desvio estrutural

Plano executado exatamente como descrito. Nenhuma Rule 1/2/3 ativada — este plano não modifica código de produção.

## Wave 2 Contract

Os testes criados neste plano definem o contrato que Wave 2 (Plan 03) deve satisfazer:

1. `check_duration(url, cache_dir)` — nova assinatura com cache_dir
2. `download_audio(url, cache_dir)` — nova assinatura com cache_dir
3. `_check_oauth_cache(cache_dir)` — nova função em api/main.py
4. `bgutil-ytdlp-pot-provider` removido de requirements.txt
5. `BgutilUnavailable` removido de pipeline.py
6. `httpx.get` probe removido de download_audio

## Known Stubs

Nenhum. Este plano cria apenas testes — sem código de produção.

## Threat Flags

Nenhuma nova superfície de segurança introduzida. Os testes usam `tmp_path` isolado do pytest e cookies fake (`__Secure-3PSID\tvalue\n`) — nenhum dado sensível real.

## Self-Check: PASSED

| Item | Status |
|------|--------|
| tests/test_pipeline_oauth.py | FOUND |
| tests/test_pipeline_fixes.py | FOUND |
| Commit fdb97ac (Task 1) | FOUND |
| Commit 9eb2eab (Task 2) | FOUND |
| SUMMARY.md | FOUND |
