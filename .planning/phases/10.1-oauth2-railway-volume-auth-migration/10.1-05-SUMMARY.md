---
plan: 10.1-05
phase: 10.1
status: blocked
completed: 2026-05-13
wave: 4
blocked_reason: "AUTH-02 falhou — YouTube retorna LOGIN_REQUIRED no IP datacenter do Railway mesmo com cookies autenticados válidos"
---

# Summary — 10.1-05: E2E Validation + bgutil Teardown (BLOCKED)

## Status: BLOCKED — Wave 4 não aprovada

Wave 4 não pode ser concluída. Task 1 (AUTH-02) falhou durante o checkpoint humano de E2E. Tasks 2 e 3 (AUTH-03 e teardown bgutil) NÃO foram executadas — bgutil permanece ativo como fallback de produção.

## Evidências do checkpoint humano (Task 1)

### O que funcionou (pilha completa verificada)

| Componente | Evidência |
|------------|-----------|
| `YTDLP_CACHE_DIR` no container | `AUTH_BOOTSTRAP: YTDLP_CACHE_DIR=present` nos logs |
| `cookies.txt` escrito no Volume | `bytes=2987, secure_3psid_lines=3` |
| yt-dlp recebe `cookiefile=` | log: `cookiefile=/data/yt-dlp-cache/cookies.txt` |
| yt-dlp reconhece cookies | log: `Found YouTube account cookies` |
| Node/JS runtime ativo | log: `JS runtimes: node-24.15.0` |

### O que falhou

- **Erro:** `LOGIN_REQUIRED` / `Sign in to confirm you're not a bot`
- **Diagnóstico:** YouTube rejeita cookies autenticados originados de IPs de datacenter cloud (Railway `us-west2`). Comportamento documentado no yt-dlp PO Token Guide.
- **Não é bug de código:** o pipeline está tecnicamente correto. O bloqueio é de rede/autenticação extra.

### O que NÃO foi executado (Wave 4 parada no Task 1)

- Task 2 (AUTH-03 — redeploy test): não executada
- Task 3 (teardown bgutil): **NÃO executada** — bgutil permanece ativo
- `BGUTIL_BASE_URL` e `YTDLP_COOKIES_B64` mantidas nas env vars

## Análise do gap

**Hipótese inicial (Phase 10.1):** cookies autenticados no Railway Volume seriam suficientes para contornar bot detection.

**Realidade verificada:** Railway datacenter IPs são classificados como bots por YouTube independentemente de cookies válidos. Para cloud IPs, YouTube exige autenticação adicional — PO Token ou equivalente.

**Referências:**
- https://github.com/yt-dlp/yt-dlp/wiki/PO-Token-Guide
- https://github.com/Brainicism/bgutil-ytdlp-pot-provider

## Próximos caminhos

1. **Reintroduzir bgutil/PO Token + cookies no Volume** — cookies como identidade, bgutil como prova de JS challenge. Arquitetura híbrida.
2. **Proxy residencial/VPS com IP não-datacenter** — move o download para um IP que o YouTube não classifica como bot.
3. **Manter bgutil sozinho** — reverter Wave 2 se o custo de manter bgutil for aceitável.

## Self-Check: BLOCKED

- ❌ AUTH-02 não validado (3 URLs testadas → todas falharam com LOGIN_REQUIRED)
- ❌ AUTH-03 não testado
- ❌ bgutil não teardown (correto — não pode deletar enquanto AUTH-02 falhou)
- ✅ diagnóstico completo e objetivo obtido
- ✅ infraestrutura (Volume + env var) permanece correta para próxima tentativa
