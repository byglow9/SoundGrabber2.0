---
phase: 10.1
plan: 04
type: execute
wave: 3
depends_on: [10.1-03]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-03]
tags: [railway, volume, infrastructure, checkpoint, cookies-upload]

must_haves:
  truths:
    - "Railway Volume montado em /data/yt-dlp-cache no serviço 248e8eaf-68e1-4fb1-beb0-448bd26f317a"
    - "Env var YTDLP_CACHE_DIR=/data/yt-dlp-cache configurada no serviço 248e8eaf"
    - "cookies.txt válido (contendo __Secure-3PSID) gravado em /data/yt-dlp-cache/cookies.txt com permissões 0o600"
    - "Redeploy executado após criação do Volume — startup logs mostram CRITICAL ausente OU mensagem específica sobre cookies"
    - "Container do serviço 248e8eaf consegue ler /data/yt-dlp-cache/cookies.txt no runtime"
  artifacts:
    - path: .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-04-SUMMARY.md
      provides: "Registro do estado do Volume e env vars no Railway"
      contains: "YTDLP_CACHE_DIR"
  key_links:
    - from: Railway service 248e8eaf
      to: Volume /data/yt-dlp-cache
      via: volume mount via Railway dashboard/MCP
      pattern: "mount path /data/yt-dlp-cache"
    - from: Railway service 248e8eaf
      to: env YTDLP_CACHE_DIR
      via: variável de ambiente
      pattern: "YTDLP_CACHE_DIR=/data/yt-dlp-cache"
    - from: cookies.txt no Volume
      to: pipeline.download_audio
      via: ydl_opts["cookiefile"]
      pattern: "/data/yt-dlp-cache/cookies.txt"
---

<objective>
Wave 3 da fase 10.1: provisionar a infraestrutura Railway que o código da Wave 2 espera. Criar o Volume, configurar a env var, e popular o cookies.txt via checkpoint humano. NÃO deletar o serviço bgutil ainda — isso é Wave 4, depois do E2E confirmar que o novo caminho funciona.

Purpose: Tornar o ambiente Railway compatível com o novo código. Garantir que o redeploy use a env var nova (YTDLP_CACHE_DIR) e que cookies estejam disponíveis para o yt-dlp ler do Volume.

Output:
- Volume criado e montado
- YTDLP_CACHE_DIR setada
- cookies.txt no Volume com 0o600
- Redeploy executado, startup logs verificados
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar Railway Volume e setar YTDLP_CACHE_DIR via railway MCP</name>
  <files></files>
  <read_first>
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md (seção canonical_refs: Project ID 1b0d39e7-bda8-4b44-80da-1751ba65c76f; Serviço 248e8eaf-68e1-4fb1-beb0-448bd26f317a; Environment a42aa82d-259e-4eb6-85a5-98355d69e27a)
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md (seção "Runtime State Inventory" — ordem segura de teardown)
  </read_first>
  <action>
    Usar a `railway-agent` MCP tool (ou tools individuais do railway MCP) para executar 3 operações no projeto Railway `1b0d39e7-bda8-4b44-80da-1751ba65c76f`, environment `a42aa82d-259e-4eb6-85a5-98355d69e27a`:
    
    (1) **Criar Railway Volume** no serviço `248e8eaf-68e1-4fb1-beb0-448bd26f317a` (Uvicorn Web + Celery Worker):
       - Mount path: `/data/yt-dlp-cache`
       - Tamanho: 1 GB (mínimo Railway aceita; cookies.txt é < 5 KB)
       - Nome sugerido: `yt-dlp-cache` ou `youtube-auth-cache`
       - Confirmar via `railway-agent` antes de criar — Railway MCP requer confirmação explícita para ações destrutivas/criativas. Se a tool oferecer dry-run, usar primeiro.
    
    (2) **Configurar variável de ambiente** no serviço `248e8eaf`:
       - `YTDLP_CACHE_DIR=/data/yt-dlp-cache`
       - NÃO remover ainda `BGUTIL_BASE_URL` nem `YTDLP_COOKIES_B64` — Wave 4 fará a remoção após E2E.
    
    (3) **Verificar via MCP** que o Volume foi criado e a variável está setada:
       - Listar volumes do serviço: deve aparecer `/data/yt-dlp-cache`
       - Listar variáveis do serviço: deve aparecer `YTDLP_CACHE_DIR=/data/yt-dlp-cache`
    
    Se a Railway MCP tools não estiverem disponíveis ou retornarem erro de autenticação:
       - Pausar e instruir o operador a fazer manualmente via Railway dashboard:
         * Service "Uvicorn Web + Celery Worker" → Settings → Volumes → New Volume → mount path `/data/yt-dlp-cache` → save
         * Service → Variables → New Variable → `YTDLP_CACHE_DIR` = `/data/yt-dlp-cache` → save
       - Aguardar confirmação humana antes de prosseguir.
    
    Não trigger redeploy ainda — Task 3 fará isso depois do checkpoint de cookies.
  </action>
  <verify>
    <automated>echo "Verification is via railway MCP introspection in next task; this task creates infra via MCP"</automated>
  </verify>
  <acceptance_criteria>
    - Via railway MCP `list-volumes` ou equivalente no serviço 248e8eaf: aparece Volume com mount path `/data/yt-dlp-cache`
    - Via railway MCP `list-variables` no serviço 248e8eaf: aparece `YTDLP_CACHE_DIR=/data/yt-dlp-cache`
    - `BGUTIL_BASE_URL` e `YTDLP_COOKIES_B64` ainda existem no serviço (NÃO removidas nesta wave)
    - Nenhum redeploy automático foi disparado por esta task (volume + var não trigger redeploy sozinhos em Railway; redeploy explícito vem em Task 3)
  </acceptance_criteria>
  <done>
    Volume `/data/yt-dlp-cache` existe no serviço 248e8eaf; YTDLP_CACHE_DIR setada. Confirmado via MCP query (não via assumption).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Checkpoint humano — exportar cookies do browser e copiar para o Volume</name>
  <files></files>
  <read_first>
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md (seção "Checkpoint humano — comando exato para popular o Volume")
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md (D-06, D-07 adaptados)
  </read_first>
  <what-built>
    Wave 3 Task 1 criou o Railway Volume montado em `/data/yt-dlp-cache` e setou `YTDLP_CACHE_DIR=/data/yt-dlp-cache` no serviço `248e8eaf`. O Volume está vazio. O código da Wave 2 espera ler `/data/yt-dlp-cache/cookies.txt` mas não pode gerar esse arquivo automaticamente — exportar cookies do YouTube requer browser autenticado do operador (não pode ser feito por Claude).
  </what-built>
  <how-to-verify>
    Esta task requer ação manual do operador. Não é uma verificação visual — é a única etapa truly não-automatizável da phase. Steps:
    
    **Passo 1 — Exportar cookies frescos do YouTube:**
    1. Abrir Chrome ou Firefox onde você está logado no YouTube (sua conta — preferir conta secundária para isolamento de risco)
    2. Instalar extensão "Get cookies.txt LOCALLY" (verificar autor confiável)
    3. Antes de exportar, visitar `https://www.youtube.com/robots.txt` (previne rotação de cookies durante o export — confirmado no RESEARCH.md)
    4. Clicar na extensão na barra de ferramentas → "Export" no domínio `.youtube.com` → salvar como `cookies.txt` localmente (ex: `~/Downloads/cookies.txt`)
    5. Abrir o arquivo em editor de texto e confirmar que existe a linha com `__Secure-3PSID` — sentinel é a única garantia de cookie autenticado válido
    
    **Passo 2 — Copiar para o Railway Volume via railway CLI:**
    
    Método A — copy via railway run + ssh-style (preferido se railway CLI suporta):
    ```bash
    # Substitua $LOCAL_COOKIES pelo caminho real, ex: ~/Downloads/cookies.txt
    railway link  # se ainda não linkou — selecionar projeto SoundGrabber e service "Uvicorn Web + Celery Worker"
    cat $LOCAL_COOKIES | railway run -- bash -c "cat > /data/yt-dlp-cache/cookies.txt && chmod 600 /data/yt-dlp-cache/cookies.txt && echo 'OK gravado'"
    ```
    
    Método B — fallback via base64 + railway run se o pipe stdin não funcionar:
    ```bash
    BASE64_COOKIES=$(base64 -w0 ~/Downloads/cookies.txt)
    railway run -- bash -c "echo '$BASE64_COOKIES' | base64 -d > /data/yt-dlp-cache/cookies.txt && chmod 600 /data/yt-dlp-cache/cookies.txt && echo 'OK gravado'"
    ```
    
    Método C — fallback se railway run -- não estiver disponível: usar `railway shell` ou Railway dashboard CLI Web tunnel para colar conteúdo.
    
    **Passo 3 — Validar gravação:**
    ```bash
    # Sentinel check
    railway run -- bash -c "grep -c '__Secure-3PSID' /data/yt-dlp-cache/cookies.txt && echo sentinel-OK"
    
    # Permissão check
    railway run -- ls -la /data/yt-dlp-cache/cookies.txt
    # Output esperado: -rw------- 1 root root <bytes> ... cookies.txt
    
    # yt-dlp smoke test (opcional mas recomendado — Rick Astley URL funcionou no E2E anterior)
    railway run -- yt-dlp --cookies /data/yt-dlp-cache/cookies.txt --skip-download "https://www.youtube.com/watch?v=dQw4w9WgXcQ" 2>&1 | head -20
    # Output esperado: NÃO contém "Sign in to confirm you're not a bot"
    ```
    
    **Passo 4 — Responder ao executor:**
    Reportar:
    - Caminho local do cookies.txt usado (apenas referência — não compartilhar conteúdo)
    - Output literal de `railway run -- ls -la /data/yt-dlp-cache/cookies.txt`
    - Resultado do sentinel check (`sentinel-OK` ou erro)
    - Resultado do yt-dlp smoke test (se realizado)
  </how-to-verify>
  <resume-signal>
    Responda com:
    - `aprovado` + outputs literais dos 3 checks (ls -la, sentinel, smoke test) — Task 3 prossegue com redeploy
    - `falhou: <razão>` + qual passo falhou — replanejar antes de continuar
    
    Não responder `aprovado` sem ter executado os 3 checks de validação. Falhas silenciosas aqui causam falha de E2E na Wave 4 com diagnóstico difícil.
  </resume-signal>
  <action>
    Apresentar ao operador as instruções dos Passos 1-4 no bloco `<how-to-verify>` acima e PAUSAR a execução. Esta task requer ação humana fora do alcance do executor — exportar cookies do browser autenticado do operador e copiá-los para o Railway Volume via `railway run`. Não modificar arquivos do projeto. Não disparar redeploy. Aguardar resposta `aprovado <outputs>` ou `falhou <razão>`.

    Após receber `aprovado` com outputs literais dos 3 checks (ls -la, sentinel, smoke test opcional), gravar no SUMMARY do plan 04 (parcial):
    - Mode mostrado pelo `ls -la` (esperado `-rw-------`)
    - Resultado do sentinel check
    - Resultado do smoke test (se realizado)
    - Confirmação de que cookies.txt NÃO foi colado no canal de comunicação

    Se a resposta for `falhou`, encerrar Wave 3 com status BLOCKED — Task 3 (redeploy) NÃO inicia. O operador deve revisar exportação de cookies, verificar acesso ao Volume, ou ajustar permissões.
  </action>
  <verify>
    <automated>grep -qE "mode.*-rw-------|sentinel-OK|ls -la /data/yt-dlp-cache/cookies.txt" .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-04-SUMMARY.md 2>/dev/null || echo "WAITING: operator response not yet recorded in SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - Operator response contains literal output of `railway run -- ls -la /data/yt-dlp-cache/cookies.txt`
    - The ls output shows mode `-rw-------` (octal 0600)
    - Operator response includes sentinel check result with `sentinel-OK` or equivalent confirmation that `__Secure-3PSID` is present
    - Operator did NOT paste the actual cookies.txt content (security — only metadata/checks)
    - If smoke test was run, output does NOT contain "Sign in to confirm you're not a bot"
  </acceptance_criteria>
  <done>
    cookies.txt válido (com __Secure-3PSID, permissões 0600) está gravado em /data/yt-dlp-cache/cookies.txt no Railway Volume do serviço 248e8eaf. Operador confirmou via outputs literais.
  </done>
</task>

<task type="auto">
  <name>Task 3: Trigger redeploy do serviço 248e8eaf e verificar startup logs</name>
  <files></files>
  <read_first>
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md (D-06, D-08 — comportamento esperado de _check_oauth_cache no startup)
    - api/main.py após Wave 2 (referência ao novo _check_oauth_cache)
  </read_first>
  <action>
    Disparar um redeploy do serviço `248e8eaf-68e1-4fb1-beb0-448bd26f317a` via railway MCP:
    
    (1) Usar `railway-agent` ou tool específica de deploy. Antes do trigger, confirmar com o operador (Railway MCP server instructions: "Confirm with the user before destructive actions e.g. accept-deploy"). Mostrar resumo:
        - Será um redeploy do branch atual / commit atual
        - Os arquivos da Wave 2 (pipeline.py, api/config.py, api/main.py, api/tasks.py, requirements.txt, start-all.sh) precisam estar commitados e pushed para o branch que o Railway monitora ANTES desta task. Se git status mostra mudanças locais não comitadas, instruir o operador a comitar e push primeiro.
    
    (2) Após confirmação, trigger deploy via MCP.
    
    (3) Aguardar build + startup terminar (tipicamente 2-5 minutos no Railway). Usar railway MCP para tail dos logs.
    
    (4) Verificar startup logs (filtrar por nível CRITICAL e INFO):
        - DEVE conter linha do `_check_redis_auth` passando (sem erro de "REDIS_URL does not contain a password")
        - DEVE conter linha do `wav-sweeper thread started`
        - DEVE NÃO conter linha CRITICAL de `_check_oauth_cache` mencionando "YTDLP_CACHE_DIR não configurado" (a env var está setada na Task 1)
        - DEVE NÃO conter linha CRITICAL de `_check_oauth_cache` mencionando "cookies.txt não encontrado" (o arquivo foi gravado na Task 2)
        - DEVE NÃO conter linha CRITICAL mencionando "__Secure-3PSID" ausente (cookies têm o sentinel, validado na Task 2)
        - Se aparecer linha INFO do Uvicorn "Application startup complete" ou equivalente: startup bem-sucedido
        - Se aparecer linha sobre `/data/yt-dlp-cache` do start-all.sh chmod: confirmar que não falhou (set -e + || true protege)
    
    Se aparecerem CRITICAL logs inesperados, NÃO prosseguir para Wave 4: registrar no SUMMARY e devolver controle ao operador para investigação.
    
    (5) Se startup OK: registrar no SUMMARY com:
        - Deploy ID/timestamp
        - Linhas-chave dos logs (sanitizadas — sem credenciais)
        - Confirmação de que próxima wave (E2E) pode iniciar
  </action>
  <verify>
    <automated>echo "Verification via railway MCP log tail — manual verification in task action above"</automated>
  </verify>
  <acceptance_criteria>
    - Operator confirmou que git status estava limpo antes do deploy (mudanças da Wave 2 commitadas e pushed)
    - Railway MCP retornou deploy success (status COMPLETED/SUCCESS, não FAILED nem CRASHED)
    - Tail dos logs do startup contém: "Application startup complete" OU "Uvicorn running on" OU "wav-sweeper thread started"
    - Tail dos logs NÃO contém: linha CRITICAL com "YTDLP_CACHE_DIR não configurado"
    - Tail dos logs NÃO contém: linha CRITICAL com "cookies.txt não encontrado"
    - Tail dos logs NÃO contém: linha CRITICAL com "__Secure-3PSID"
    - Tail dos logs NÃO contém: ImportError, ModuleNotFoundError, AttributeError relativo a settings/pipeline/BgutilUnavailable
    - SUMMARY do plan 04 inclui: deploy ID, lines literais (sanitizadas) dos logs relevantes, status final do redeploy
  </acceptance_criteria>
  <done>
    Redeploy do serviço 248e8eaf concluído com sucesso após Wave 2 + Task 1 (Volume + env var) + Task 2 (cookies). Startup logs limpos. Sistema pronto para Wave 4 (E2E + teardown bgutil).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → cookies.txt | cookies contêm sessão Google autenticada (credential equivalente) |
| Local filesystem → Railway Volume | cookies.txt em trânsito via railway CLI; railway usa TLS no transport |
| Railway Volume → container | mount com permissão padrão do container (root) — chmod 600 do arquivo restringe acesso |
| railway MCP → Railway control plane | actions destrutivas/criativas precisam confirmação por convenção MCP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-11 | Information Disclosure | cookies.txt loggado acidentalmente em CI ou histórico de shell | mitigate | Task 2 instruções explícitas: operador NÃO cola conteúdo de cookies.txt em qualquer canal; reporta apenas outputs literais de checks (ls -la, sentinel grep) que não vazam o cookie value |
| T-10.1-12 | Tampering | Volume montado mas arquivo ausente — yt-dlp falha silenciosamente sem cookiefile | mitigate | Task 3 lê startup logs e confirma ausência de CRITICAL de "cookies.txt não encontrado" antes de declarar success; sem essa confirmação, Wave 4 não inicia |
| T-10.1-13 | Elevation of Privilege | Railway MCP executa deploy sem confirmação humana | mitigate | Railway MCP server instructions exigem confirmação antes de ações destrutivas; Task 3 explicita o prompt de confirmação ao operador |
| T-10.1-14 | Repudiation | Redeploy executado sem registro de qual commit | mitigate | Task 3 registra deploy ID + timestamp + commit SHA (se acessível via MCP) no SUMMARY |
| T-10.1-15 | Denial of Service | cookies expirados causam download falhando em produção sem aviso | mitigate | _check_oauth_cache verifica sentinel __Secure-3PSID na startup E na Task 2 antes do redeploy; sentinel ausente bloqueia o checkpoint |
| T-10.1-16 | Information Disclosure | sentinel `__Secure-3PSID` em log CRITICAL revela estrutura do cookie | accept | Log apenas verifica existência da string "__Secure-3PSID", não imprime valor; mesmo padrão aceito em PIPE-05 |
</threat_model>

<verification>
- Railway Volume existe e está montado em /data/yt-dlp-cache (via MCP list-volumes)
- Env var YTDLP_CACHE_DIR=/data/yt-dlp-cache setada (via MCP list-variables)
- /data/yt-dlp-cache/cookies.txt existe com permissões 0600 e contém __Secure-3PSID (via railway run check)
- Redeploy concluído com sucesso (deploy status SUCCESS)
- Startup logs sem CRITICAL de _check_oauth_cache
</verification>

<success_criteria>
- Infraestrutura Railway preparada: Volume + env var + cookies populados
- Operador validou cada checkpoint com outputs literais
- Redeploy do código novo bem-sucedido com startup limpo
- Wave 4 (E2E + bgutil teardown) pode iniciar
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-04-SUMMARY.md` com:
- Volume ID e mount path confirmados via MCP
- Env vars antes/depois do deploy
- Deploy ID + timestamp do redeploy
- Linhas-chave dos startup logs (sanitizadas)
</output>
