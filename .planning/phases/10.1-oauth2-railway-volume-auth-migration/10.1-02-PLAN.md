---
phase: 10.1
plan: 02
type: execute
wave: 1
depends_on: [10.1-01]
files_modified: []
autonomous: false
requirements: [AUTH-01]
tags: [checkpoint, adaptation, decision, oauth2, railway-volume]

must_haves:
  truths:
    - "O usuário confirmou explicitamente a adaptação do D-03 (cookies no Volume em vez de OAuth2)"
    - "A decisão está registrada para auditoria futura (em STATE.md ou no SUMMARY do plano)"
    - "Wave 2 só pode iniciar APÓS este checkpoint ser aprovado"
  artifacts:
    - path: .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-02-SUMMARY.md
      provides: "Registro da decisão do usuário sobre D-03 adaptation"
      contains: "D-03 adaptation"
  key_links:
    - from: 10.1-02-SUMMARY.md
      to: STATE.md
      via: Key Decisions table (próxima atualização)
      pattern: "D-03 adaptation"
---

<objective>
Wave 1 da fase 10.1: pausar a execução para confirmação humana da adaptação do D-03. O CONTEXT.md original especificou `username=oauth2`/`cachedir`, mas o RESEARCH.md (Seção "DESCOBERTA CRÍTICA") verificou que OAuth2 foi removido do yt-dlp em 2024.11.18 e NÃO existe na versão pinada 2026.3.17. A adaptação proposta é usar `cookiefile=Path(cache_dir)/"cookies.txt"` — mesmo objetivo de persistência via Railway Volume, mecanismo viável tecnicamente.

Purpose: Honrar a fidelidade ao usuário (locked decisions são NON-NEGOTIABLE) parando ANTES de gravar código que contradiz o CONTEXT.md. O usuário precisa aprovar a desvio do D-03 antes de a Wave 2 modificar produção.

Output: 
- Confirmação registrada no SUMMARY
- Sinal verde (ou redirecionamento) para Wave 2
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Checkpoint humano — confirmar adaptação D-03 (cookies no Volume vs OAuth2)</name>
  <files></files>
  <read_first>
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md (Seção "DESCOBERTA CRÍTICA" — evidência da remoção do OAuth2; verificar a linha "Login with OAuth is no longer supported")
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md (D-03, D-07, D-08, D-09 originais que pressupunham OAuth2)
  </read_first>
  <action>
    Apresentar ao operador a tabela de decisão abaixo e PAUSAR a execução até receber resposta explícita. Não executar nenhuma ação além de exibir o conteúdo dos blocos `<decision>`, `<context>` e `<options>` para o operador. Não modificar arquivos. Não disparar Wave 2 antes da resposta.

    Após receber a resposta literal do operador (one of: `option-a`, `option-b`, `option-c`, ou texto descrevendo alternativa), gravar em `.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-02-SUMMARY.md`:
    - Decisão escolhida (literal do operador)
    - Timestamp ISO 8601
    - Próxima ação autorizada (Wave 2 inicia OU phase pausa)

    Se a resposta for `option-a`, sinalizar ao orquestrador que Wave 2 (Plan 03) está autorizada. Se for `option-b` ou `option-c`, encerrar Wave 1 com status BLOCKED — Wave 2 NÃO inicia sem nova autorização.
  </action>
  <decision>
    O CONTEXT.md D-03 especifica `username=oauth2`/`cachedir`. O RESEARCH.md confirmou que isso falha em runtime no yt-dlp 2026.3.17 (versão pinada do projeto) com erro `Login with OAuth is no longer supported`. A adaptação proposta — usar `cookiefile=Path(cache_dir)/"cookies.txt"` no Railway Volume — mantém o objetivo de persistência (D-06) e remove a dependência bgutil (D-05), mas substitui o mecanismo de autenticação.
    
    Confirme antes da Wave 2 (código) executar.
  </decision>
  <context>
    Evidência da invalidade do D-03 original (RESEARCH.md):
    
    1. Verificação direta via `python3 -c "import yt_dlp; ydl_opts={'username':'oauth2',...}"` retorna `ERROR: [youtube] dQw4w9WgXcQ: Login with OAuth is no longer supported.`
    2. Código fonte instalado em `/home/glow/.local/lib/python3.12/site-packages/yt_dlp/extractor/youtube.py` contém o hard reject explícito.
    3. Commit yt-dlp 52c0ffe "Remove broken OAuth support (#11558)" em 2024.11.18 confirma a remoção upstream.
    
    Impacto no plano original:
    - D-03 (`username=oauth2`, `cachedir`) — não funciona
    - D-07 (`railway run yt-dlp --username oauth2 ...`) — não funciona
    - D-08 (`_check_oauth_cache` verificando `youtube-oauth2.token_data`) — token nunca seria criado
    - D-09 (testes asserting `"username": "oauth2"`) — testaria comportamento que falha em runtime
    
    Impacto da adaptação:
    - D-01 (quebra limpa), D-02 (assinaturas `check_duration(url, cache_dir)`), D-04 (remover probe bgutil), D-05 (teardown bgutil), D-06 (Railway Volume) — TODOS preservados
    - D-03 — cookies no Volume (mesmo `cache_dir`, mesma assinatura)
    - D-07 — `railway run -- bash -c "cp /tmp/cookies.txt /data/yt-dlp-cache/cookies.txt && chmod 600 ..."` em vez de OAuth flow
    - D-08 — `_check_oauth_cache` verifica `cookies.txt` em vez de token OAuth2 (mesmo padrão de PIPE-05)
    - D-09 — asserts verificam `cookiefile` em vez de `username=oauth2`
  </context>
  <options>
    <option id="option-a">
      <name>Prosseguir com cookies no Railway Volume (RECOMENDADO)</name>
      <pros>
        - Tecnicamente viável (verificado contra yt-dlp 2026.3.17 instalado)
        - Mantém TODOS os objetivos de Phase 10.1: elimina bgutil, persiste auth no Volume, sem env var Base64
        - Reuso máximo dos padrões existentes (PIPE-05 cookies validation, `cookiefile` arg do yt-dlp)
        - Auth via cookies é o método PADRÃO atual do yt-dlp em 2026 (web_creator + mweb clients)
        - Sem dependência de PO Token (ios/mweb defaults não precisam)
      </pros>
      <cons>
        - Cookies expiram (semanas/meses) — operador precisa re-exportar manualmente
        - Mudança em relação ao CONTEXT.md original (registrar no SUMMARY como "D-03 adapted")
        - Continuação do problema de cookies de datacenter IP (mas é o mesmo problema do OAuth2 original)
      </cons>
    </option>
    <option id="option-b">
      <name>Pausar para re-discutir abordagem (executar /gsd-discuss-phase 10.1 novamente)</name>
      <pros>
        - Permite reconsiderar a estratégia completa (ex: Cloudflare WARP, mudança de host, accepting bot detection)
        - Não força mudança técnica que o usuário pode discordar
      </pros>
      <cons>
        - Atrasa a resolução do problema atual (bgutil + cookies B64 + nsig stale)
        - Requer nova rodada de research/context gathering
        - Os 3 ideias deferidas em CONTEXT.md (pool OAuth2, monitor de token, Cloudflare WARP) já foram avaliadas como fora de escopo
      </cons>
    </option>
    <option id="option-c">
      <name>Cancelar Phase 10.1 — manter bgutil + cookies B64</name>
      <pros>
        - Sistema atual está funcional (Phase 10 chegou a status=done com cookies)
        - Zero risco de regressão
      </pros>
      <cons>
        - Não resolve nenhum problema: bgutil ainda cobrando, cookies expiram via env var, redeploy necessário a cada rotação
        - Desperdiça o trabalho de research/context já feito
        - O bgutil deployment foi marcado como temporário pelo próprio operador (D-05 do CONTEXT.md original previa remoção)
      </cons>
    </option>
  </options>
  <resume-signal>
    Responda com: 
    - `option-a` (recomendado) — Wave 2 executa imediatamente
    - `option-b` — Wave 1 encerra como blocked; usuário roda `/gsd-discuss-phase 10.1` para revisar
    - `option-c` — Wave 1 encerra como blocked; usuário decide se cancela a phase
    Ou descreva uma quarta alternativa para discussão.
  </resume-signal>
  <verify>
    <automated>test -f .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-02-SUMMARY.md && grep -qE "option-[abc]|alternativa" .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - User explicit response is one of: `option-a`, `option-b`, `option-c`, ou texto livre indicando outra direção
    - If `option-a`: o SUMMARY do plan 02 registra "D-03 adapted: cookies no Railway Volume aprovado pelo operador em <data>"
    - If `option-b` ou `option-c`: o SUMMARY registra a decisão e a phase pausa
    - STATE.md poderá ser atualizado com a decisão na próxima execução de planejamento
    - `.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-02-SUMMARY.md` exists e contém a resposta literal do operador
  </acceptance_criteria>
  <done>
    Resposta do operador registrada no SUMMARY do plan 02. Para `option-a`, a Wave 2 está autorizada a iniciar. Para `option-b` ou `option-c`, a Wave 2 NÃO inicia até nova autorização.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan → User | Plano apresenta opções; usuário decide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-04 | Repudiation | Decisão tomada sem registro auditável | mitigate | SUMMARY do plan 02 grava resposta literal do operador + timestamp; STATE.md será atualizado com a Key Decision |
| T-10.1-05 | Elevation of Privilege | Executor procede com Wave 2 sem aprovação | mitigate | `autonomous: false` no frontmatter; `type="checkpoint:decision"` com `gate="blocking"` força pausa do orquestrador |
</threat_model>

<verification>
- O orquestrador NÃO inicia Wave 2 (Plan 03) até este checkpoint receber resposta
- SUMMARY do plan 02 existe e registra a opção escolhida
- Se opção != `option-a`, a Wave 2 não inicia
</verification>

<success_criteria>
- Operador respondeu com opção clara (option-a, option-b, option-c, ou alternativa)
- Decisão registrada para auditoria
- Próxima ação determinística baseada na resposta
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-02-SUMMARY.md` registering:
- Opção escolhida pelo operador (literal)
- Data e contexto
- Próxima ação autorizada
</output>
