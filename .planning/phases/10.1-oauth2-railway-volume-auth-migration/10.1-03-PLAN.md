---
phase: 10.1
plan: 03
type: execute
wave: 2
depends_on: [10.1-01, 10.1-02]
files_modified:
  - pipeline.py
  - api/config.py
  - api/main.py
  - api/tasks.py
  - requirements.txt
  - start-all.sh
autonomous: true
requirements: [AUTH-01]
tags: [refactor, pipeline, auth-migration, code-changes, bgutil-teardown]

must_haves:
  truths:
    - "pipeline.check_duration tem assinatura (url, cache_dir) e lê cookies.txt do Volume quando disponível"
    - "pipeline.download_audio tem assinatura (url, cache_dir) e lê cookies.txt do Volume quando disponível"
    - "pipeline.BgutilUnavailable, probe httpx.get(bgutil) e classe inteira foram removidos"
    - "api/config.py Settings tem campo cache_dir e NÃO tem campos cookies_path, po_token, bgutil_base_url"
    - "api/main.py tem função _check_oauth_cache substituindo _check_cookies e o lifespan a invoca"
    - "api/tasks.py NÃO importa BgutilUnavailable, NÃO tem except BgutilUnavailable, e chama check_duration/download_audio com a nova assinatura"
    - "requirements.txt NÃO contém bgutil-ytdlp-pot-provider"
    - "start-all.sh garante chmod 700 em /data/yt-dlp-cache no startup (não-bloqueante se diretório não existir)"
    - "Todos os testes em tests/test_pipeline_oauth.py passam (GREEN)"
    - "Todos os testes em tests/test_pipeline_fixes.py passam (GREEN — incluindo os invertidos)"
  artifacts:
    - path: pipeline.py
      provides: "check_duration + download_audio com cookies do Volume; sem bgutil"
      contains: "def check_duration(url: str, cache_dir: str)"
    - path: api/config.py
      provides: "Settings com cache_dir"
      contains: "cache_dir: str = field"
    - path: api/main.py
      provides: "_check_oauth_cache substituindo _check_cookies"
      contains: "def _check_oauth_cache"
    - path: api/tasks.py
      provides: "process_job sem BgutilUnavailable"
      contains: "check_duration(url, settings.cache_dir)"
    - path: requirements.txt
      provides: "Sem bgutil"
      contains: "yt-dlp==2026.3.17"
    - path: start-all.sh
      provides: "chmod do Volume no startup"
      contains: "/data/yt-dlp-cache"
  key_links:
    - from: pipeline.check_duration
      to: cookies.txt no Railway Volume
      via: ydl_opts["cookiefile"] = str(Path(cache_dir)/"cookies.txt")
      pattern: "Path\\(cache_dir\\) / \"cookies.txt\""
    - from: api/main.py lifespan
      to: _check_oauth_cache(settings.cache_dir)
      via: chamada no lifespan
      pattern: "_check_oauth_cache\\(settings\\.cache_dir\\)"
    - from: api/tasks.py process_job
      to: pipeline.check_duration / pipeline.download_audio
      via: chamada direta com settings.cache_dir
      pattern: "check_duration\\(url, settings\\.cache_dir\\)"
---

<objective>
Wave 2 da fase 10.1: refatorar o código de produção para usar cookies no Railway Volume e remover o bgutil PO Token service. Este plano é a "quebra limpa" (D-01) — não há suporte duplo, não há fallback.

Purpose: Tornar GREEN todos os testes RED criados em Wave 0 (plan 01). Eliminar dependência do bgutil. Habilitar persistência de autenticação no Volume para Wave 3.

Output:
- pipeline.py refatorado (check_duration, download_audio, __main__ block, BgutilUnavailable removida)
- api/config.py refatorado (cache_dir adicionado; cookies_path/po_token/bgutil_base_url removidos)
- api/main.py refatorado (_check_oauth_cache substitui _check_cookies)
- api/tasks.py refatorado (sem BgutilUnavailable)
- requirements.txt sem bgutil
- start-all.sh com chmod 700 do Volume
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-PATTERNS.md
@pipeline.py
@api/config.py
@api/main.py
@api/tasks.py
@requirements.txt
@start-all.sh
@tests/test_pipeline_oauth.py
@tests/test_pipeline_fixes.py

<interfaces>
<!-- Assinaturas e contratos a implementar. Extraídos de 10.1-CONTEXT.md D-02 e 10.1-PATTERNS.md. -->

```python
# pipeline.py — após o plano
def check_duration(url: str, cache_dir: str) -> dict[str, Any]: ...
def download_audio(url: str, cache_dir: str) -> Path: ...
# BgutilUnavailable: REMOVIDA
# import httpx: REMOVIDO

# api/config.py — após o plano
@dataclass(frozen=True)
class Settings:
    redis_url: str
    wav_ttl: int
    rate_limit_per_minute: int
    max_queue_depth: int
    job_poll_rate_limit_per_minute: int
    file_download_rate_limit_per_minute: int
    dev_mode: bool
    cache_dir: str  # NOVO: os.environ.get("YTDLP_CACHE_DIR", "")
    # REMOVIDOS: cookies_path, po_token, bgutil_base_url
# REMOVIDA: função _resolve_cookies_path
# REMOVIDO: import base64

# api/main.py — após o plano
def _check_oauth_cache(cache_dir: str) -> None:
    """Substitui _check_cookies. Valida cookies.txt no Railway Volume.
    Non-blocking, loga CRITICAL e retorna. Não levanta."""
# lifespan chama _check_oauth_cache(settings.cache_dir) em vez de _check_cookies(settings.cookies_path)

# api/tasks.py — após o plano
from pipeline import check_duration, download_audio, analyze_audio  # SEM BgutilUnavailable
# chamada: check_duration(url, settings.cache_dir)
# chamada: download_audio(url, settings.cache_dir)
# REMOVIDO: except BgutilUnavailable as e: ...

# start-all.sh — após o plano
# Antes de iniciar Celery e Uvicorn, garantir permissões do Volume:
#   if [ -d /data/yt-dlp-cache ]; then chmod 700 /data/yt-dlp-cache; fi
```

Comportamento esperado dos ydl_opts:
- `cookiefile`: presente quando `Path(cache_dir)/"cookies.txt"` existe; ausente caso contrário
- `username`, `password`, `cachedir`: NUNCA presentes (OAuth2 não funciona em runtime)
- `extractor_args` com `getpot_bgutil_baseurl` ou `player_client=android/web`: NUNCA presentes (ios/mweb defaults)
- `extractor_args` com `po_token`: NUNCA presente (ios/mweb não precisam)
- Restante de ydl_opts preservado: `no_cache_dir`, `retries`, `fragment_retries`, `ffmpeg_location`, `socket_timeout`, `postprocessors`, `outtmpl`, `http_chunk_size`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refatorar pipeline.py (check_duration, download_audio, __main__, BgutilUnavailable, imports)</name>
  <files>pipeline.py</files>
  <read_first>
    - pipeline.py (estado atual — linhas 1-50 para imports e BgutilUnavailable; linhas 82-133 para check_duration; linhas 137-249 para download_audio; linhas 561-621 para __main__)
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-PATTERNS.md (seções "check_duration", "download_audio", "__main__ block" — contém o código exato a usar)
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md (D-02, D-04, código_context — assets a remover/adicionar)
    - tests/test_pipeline_oauth.py (recém-criado — para entender exatamente quais opts os asserts esperam)
  </read_first>
  <action>
    Editar `pipeline.py` com 6 mudanças explícitas:
    
    (1) **Remover imports não usados após a migração:** remover `import httpx` da linha 31. Manter `import imageio_ffmpeg`, `import yt_dlp`, todos os outros imports.
    
    (2) **Remover classe `BgutilUnavailable`** (linhas 38-45 atualmente). Remover o docstring `"""Raised when..."""` também. Não deixar comentário órfão.
    
    (3) **Modificar `check_duration`** (linhas 82-133):
       - Trocar assinatura `def check_duration(url: str, cookies_path: str, bgutil_base_url: str = "") -> dict[str, Any]:` para `def check_duration(url: str, cache_dir: str) -> dict[str, Any]:`
       - Atualizar docstring Args: trocar `cookies_path: Path to Netscape-format cookies.txt (D-01).` por `cache_dir: Railway Volume path (de YTDLP_CACHE_DIR). Cookies serão lidos de Path(cache_dir)/"cookies.txt" se existir.`
       - Remover linhas que definem `player_client = "web" if bgutil_base_url else "android"`
       - Substituir bloco ydl_opts (linhas ~100-116) por dict SEM `extractor_args` — manter chaves: `"quiet"`, `"no_warnings"`, `"skip_download"`, `"socket_timeout"`, `"noplaylist"`, `"no_cache_dir"`, `"ffmpeg_location"`. NÃO incluir `"extractor_args"`. NÃO incluir `"username"`, `"password"`, `"cachedir"`.
       - Substituir bloco `if bgutil_base_url: ydl_opts["extractor_args"]["youtube"].append(...)` e `if cookies_path: ydl_opts["cookiefile"] = cookies_path` por:
         ```
         if cache_dir:
             cookies_file = Path(cache_dir) / "cookies.txt"
             if cookies_file.exists():
                 ydl_opts["cookiefile"] = str(cookies_file)
         ```
       - Preservar todo o resto da função (extract_info call, validação de duration, return info).
    
    (4) **Modificar `download_audio`** (linhas 137-249):
       - Trocar assinatura `def download_audio(url: str, cookies_path: str, po_token: str, bgutil_base_url: str = "") -> Path:` para `def download_audio(url: str, cache_dir: str) -> Path:`
       - Atualizar docstring Args: remover `cookies_path`, `po_token` da doc; adicionar `cache_dir: Railway Volume path. Cookies lidos de Path(cache_dir)/"cookies.txt".`
       - Remover linhas 164-176 (bloco `dl_player`, `extractor_args` setup, append `po_token`, append `getpot_bgutil_baseurl`)
       - Remover linhas 178-196 (probe `httpx.get(bgutil_base_url, timeout=2.0)` e o `raise BgutilUnavailable`)
       - No bloco `ydl_opts` (linhas 198-215): REMOVER a chave `"extractor_args": extractor_args,`. Preservar TODAS as outras chaves: `"format"`, `"outtmpl"`, `"quiet"`, `"no_warnings"`, `"socket_timeout"`, `"noplaylist"`, `"postprocessors"`, `"no_cache_dir": True`, `"retries": 3`, `"fragment_retries": 3`, `"http_chunk_size"`, `"ffmpeg_location"`.
       - Substituir bloco `if cookies_path: ydl_opts["cookiefile"] = cookies_path` (linhas 216-217) por:
         ```
         if cache_dir:
             cookies_file = Path(cache_dir) / "cookies.txt"
             if cookies_file.exists():
                 ydl_opts["cookiefile"] = str(cookies_file)
         ```
       - Preservar try/except yt_dlp.utils.YoutubeDLError, try/finally cleanup, `os.chmod(wav_path, 0o600)` (SEC-FILE-01), e o return.
    
    (5) **Modificar `__main__` block** (linhas 561-620):
       - Remover linhas que definem `cookies_path = os.environ.get("YTDLP_COOKIES_FILE", "")`, `po_token = os.environ.get("YTDLP_PO_TOKEN", "")`, `bgutil_base_url = os.environ.get("BGUTIL_BASE_URL", "")`. Adicionar `cache_dir = os.environ.get("YTDLP_CACHE_DIR", "")`.
       - Remover validações `if not cookies_path: _emit_error(...)`, `if not Path(cookies_path).exists(): _emit_error(...)`, e o warning sobre `YTDLP_PO_TOKEN`.
       - Substituir os calls: `check_duration(url, cookies_path, bgutil_base_url)` → `check_duration(url, cache_dir)`; `download_audio(url, cookies_path, po_token, bgutil_base_url)` → `download_audio(url, cache_dir)`.
       - Adicionar uma validação não-bloqueante: se `cache_dir` vazio, imprimir em stderr `WARNING: YTDLP_CACHE_DIR is empty. Downloads may fail with bot detection.` (não emitir error envelope; não sys.exit).
    
    (6) **Atualizar docstring do módulo** (linhas 1-17): trocar a seção "Authentication (D-01, D-02)" para refletir `YTDLP_CACHE_DIR — path do Railway Volume com cookies.txt`. Remover menções a `YTDLP_COOKIES_FILE` e `YTDLP_PO_TOKEN`. O texto "Contract per D-03" pode ser atualizado para "Contract per D-02 (.planning/phases/10.1-.../10.1-CONTEXT.md): check_duration(url, cache_dir) -> dict; download_audio(url, cache_dir) -> Path" ou deixado como referência histórica ao D-03 original — preferir a atualização para a Phase 10.1.
    
    Não introduzir nenhum suporte duplo (sem `if cookies_path else cache_dir` etc). Sem stubs nem placeholders.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_oauth.py::test_check_duration_uses_cookiefile_from_cache_dir tests/test_pipeline_oauth.py::test_check_duration_no_oauth2_in_ydl_opts tests/test_pipeline_oauth.py::test_check_duration_no_cookiefile_when_cache_dir_empty tests/test_pipeline_oauth.py::test_download_audio_uses_cookiefile_from_cache_dir tests/test_pipeline_oauth.py::test_bgutil_unavailable_class_removed -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class BgutilUnavailable" pipeline.py` returns 0
    - `grep -c "^import httpx" pipeline.py` returns 0
    - `grep -F "def check_duration(url: str, cache_dir: str)" pipeline.py` returns 1+ match
    - `grep -F "def download_audio(url: str, cache_dir: str)" pipeline.py` returns 1+ match
    - `grep -v "^#" pipeline.py | grep -c "getpot_bgutil_baseurl"` returns 0
    - `grep -v "^#" pipeline.py | grep -c "po_token"` returns 0 (variável e referências removidas)
    - `grep -v "^#" pipeline.py | grep -c "bgutil_base_url"` returns 0
    - `grep -F "Path(cache_dir) / \"cookies.txt\"" pipeline.py` returns 2+ matches (uma em check_duration, uma em download_audio)
    - `grep -F "ydl_opts[\"cookiefile\"] = str(cookies_file)" pipeline.py` returns 2+ matches
    - `python -c "import pipeline; assert not hasattr(pipeline, 'BgutilUnavailable'); import inspect; sig = inspect.signature(pipeline.check_duration); assert list(sig.parameters) == ['url', 'cache_dir'], list(sig.parameters); sig = inspect.signature(pipeline.download_audio); assert list(sig.parameters) == ['url', 'cache_dir'], list(sig.parameters); print('OK')"` exits 0 and prints "OK"
    - Tests pass: `pytest tests/test_pipeline_oauth.py::test_check_duration_uses_cookiefile_from_cache_dir -x` exits 0
    - Tests pass: `pytest tests/test_pipeline_oauth.py::test_bgutil_unavailable_class_removed -x` exits 0
    - pipeline.py preserves `os.chmod(wav_path, 0o600)` line for SEC-FILE-01: `grep -F "os.chmod(wav_path, 0o600)" pipeline.py` returns 1+ match
  </acceptance_criteria>
  <done>
    pipeline.py refatorado: BgutilUnavailable removida; check_duration e download_audio com nova assinatura `(url, cache_dir)`; cookies lidos do Volume; bloco __main__ atualizado. Testes Wave 0 relacionados a pipeline.py passam GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refatorar api/config.py, api/main.py, api/tasks.py, requirements.txt e start-all.sh</name>
  <files>api/config.py, api/main.py, api/tasks.py, requirements.txt, start-all.sh</files>
  <read_first>
    - api/config.py (atual — _resolve_cookies_path + dataclass Settings)
    - api/main.py (linhas 138-200 — _check_cookies e lifespan)
    - api/tasks.py (atual — import + process_job + except handlers)
    - requirements.txt (atual — 16 linhas, bgutil é linha 16)
    - start-all.sh (atual — set -e, celery, exec uvicorn)
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-PATTERNS.md (seções api/config.py, api/main.py, api/tasks.py, requirements.txt — código exato)
    - tests/test_pipeline_oauth.py (testes 5, 6, 7 esperam comportamento específico)
  </read_first>
  <action>
    Editar 5 arquivos:
    
    (1) **api/config.py:**
       - Remover `import base64` (linha 3) — só era usado por `_resolve_cookies_path`.
       - Remover função `_resolve_cookies_path` inteira (linhas 10-18).
       - Remover campo `cookies_path: str = field(default_factory=_resolve_cookies_path)` (linha 39).
       - Remover campo `po_token: str = field(default_factory=lambda: os.environ.get("YTDLP_PO_TOKEN", ""))` (linha 40).
       - Remover campo `bgutil_base_url: str = field(default_factory=lambda: os.environ.get("BGUTIL_BASE_URL", ""))` (linha 52).
       - Adicionar (na mesma região, mantendo a ordem de fields existentes) campo `cache_dir: str = field(default_factory=lambda: os.environ.get("YTDLP_CACHE_DIR", ""))`. Posicionar logo após o último campo de auth/runtime — sugestão: imediatamente antes de `dev_mode` ou no lugar do antigo `bgutil_base_url`.
       - Atualizar docstring do módulo se mencionar cookies_path/po_token/bgutil — substituir por `cache_dir`.
       - Remover `from pathlib import Path` se não houver outro uso (era usado por _resolve_cookies_path). Manter se outros campos usam.
    
    (2) **api/main.py:**
       - Substituir função `_check_cookies(cookies_path: str)` (linhas 138-184) por `_check_oauth_cache(cache_dir: str)` seguindo o template em 10.1-PATTERNS.md seção "api/main.py — substituir `_check_cookies()` por `_check_oauth_cache()`":
         * Se `cache_dir` vazio: logger.critical mencionando YTDLP_CACHE_DIR e a sugestão de `railway run -- cp ... /data/yt-dlp-cache/cookies.txt`; return.
         * Construir `cookies_file = Path(cache_dir) / "cookies.txt"`. Se não existir: logger.critical mencionando o path e "Copie cookies validos do YouTube para o Railway Volume"; return.
         * Tentar `content = cookies_file.read_text(encoding="utf-8", errors="ignore")` com `except OSError as e: logger.critical(...); return`.
         * Verificar sentinel `__Secure-3PSID`. Se ausente: logger.critical mencionando "cookie expirado ou invalido. Re-exporte do browser apos login no YouTube".
         * NUNCA levantar exceção (mesmo padrão de _check_cookies).
       - No `lifespan` (linha 192), substituir `_check_cookies(settings.cookies_path)` por `_check_oauth_cache(settings.cache_dir)`.
       - Preservar `_check_redis_auth(settings.redis_url, settings.dev_mode)` (linha 190) e a inicialização do sweeper (linhas 193-198) inalteradas.
       - Não tocar em nenhuma outra função (`submit_job`, `get_job`, `download_file`, `health_check`, `_security_headers`, `_limit_body_size`, `_rate_limit_handler`, `_validation_exception_handler`, etc.).
    
    (3) **api/tasks.py:**
       - Linha 9: trocar `from pipeline import check_duration, download_audio, analyze_audio, BgutilUnavailable` por `from pipeline import check_duration, download_audio, analyze_audio`.
       - Linha 57: trocar `info = check_duration(url, settings.cookies_path, settings.bgutil_base_url)` por `info = check_duration(url, settings.cache_dir)`.
       - Linha 61: trocar `wav_path = download_audio(url, settings.cookies_path, settings.po_token, settings.bgutil_base_url)` por `wav_path = download_audio(url, settings.cache_dir)`.
       - Remover bloco `except BgutilUnavailable as e:` (linhas 104-109) inteiro — incluindo o `logger.warning("Job %s bgutil_unavailable: ...")` e o `raise JobFailure(error=str(e), error_type="bgutil_unavailable")`.
       - Preservar ordem dos demais except: `ValueError`, `FileNotFoundError`, `RuntimeError`, `Exception` (catch-all).
       - Não tocar em `JobFailure`, `celery_app`, `process_job` headers, demais lógica.
    
    (4) **requirements.txt:**
       - Remover a linha 16 `bgutil-ytdlp-pot-provider==0.8.5`.
       - Manter `httpx>=0.27` (linha 13) — pode ser dependência transitiva; remover só justifica se um audit dedicado for feito (RESEARCH.md Pitfall 4).
       - Preservar ordem e versão de todas as outras linhas.
    
    (5) **start-all.sh:**
       - Adicionar entre `set -e` e `celery -A ...` (após linha do `set -e`) um bloco:
         ```
         # Phase 10.1 D-06: garantir permissoes do Railway Volume antes de qualquer worker iniciar.
         if [ -d /data/yt-dlp-cache ]; then
             chmod 700 /data/yt-dlp-cache || true
         fi
         ```
       - Justificativa: Volume montado em runtime (não em build); chmod precisa ser feito no startup. `|| true` evita que o `set -e` aborte se chmod falhar por permissão herdada do Railway. Pitfall 2 do RESEARCH.md cobre esta razão.
       - Não tocar nas linhas existentes do `celery` e `exec uvicorn`.
    
    Após todas as edições, o `pytest tests/test_pipeline_oauth.py -x -q` deve passar GREEN para os 8 testes, e `pytest tests/test_pipeline_fixes.py -x -q` deve passar GREEN para todos (incluindo os invertidos do Plan 01).
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_oauth.py tests/test_pipeline_fixes.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_resolve_cookies_path" api/config.py` returns 0
    - `grep -v "^#" api/config.py | grep -c "cookies_path"` returns 0
    - `grep -v "^#" api/config.py | grep -c "po_token"` returns 0
    - `grep -v "^#" api/config.py | grep -c "bgutil_base_url"` returns 0
    - `grep -F "cache_dir: str = field(default_factory=lambda: os.environ.get(\"YTDLP_CACHE_DIR\", \"\"))" api/config.py` returns 1+ match
    - `grep -c "^import base64" api/config.py` returns 0
    - `grep -F "def _check_oauth_cache(cache_dir: str)" api/main.py` returns 1+ match
    - `grep -c "def _check_cookies" api/main.py` returns 0
    - `grep -F "_check_oauth_cache(settings.cache_dir)" api/main.py` returns 1+ match
    - `grep -c "_check_cookies(settings.cookies_path)" api/main.py` returns 0
    - `grep -c "BgutilUnavailable" api/tasks.py` returns 0
    - `grep -c "bgutil_unavailable" api/tasks.py` returns 0
    - `grep -F "check_duration(url, settings.cache_dir)" api/tasks.py` returns 1+ match
    - `grep -F "download_audio(url, settings.cache_dir)" api/tasks.py` returns 1+ match
    - `grep -c "bgutil-ytdlp-pot-provider" requirements.txt` returns 0
    - `grep -c "yt-dlp==2026.3.17" requirements.txt` returns 1 (preservado)
    - `grep -c "httpx" requirements.txt` returns 1 (mantido per Pitfall 4)
    - `grep -F "/data/yt-dlp-cache" start-all.sh` returns 1+ match
    - `grep -F "chmod 700 /data/yt-dlp-cache" start-all.sh` returns 1+ match
    - All Wave 0 tests pass GREEN: `pytest tests/test_pipeline_oauth.py -x -q` exits 0
    - All test_pipeline_fixes tests pass GREEN: `pytest tests/test_pipeline_fixes.py -x -q` exits 0
    - `python -c "from api.config import settings; assert hasattr(settings, 'cache_dir'); assert not hasattr(settings, 'cookies_path'); assert not hasattr(settings, 'po_token'); assert not hasattr(settings, 'bgutil_base_url'); print('OK')"` exits 0 and prints "OK"
    - `python -c "from api.main import _check_oauth_cache; assert callable(_check_oauth_cache); print('OK')"` exits 0
    - `bash -n start-all.sh` exits 0 (sintaxe shell válida)
  </acceptance_criteria>
  <done>
    api/config.py, api/main.py, api/tasks.py, requirements.txt e start-all.sh refatorados conforme D-02/D-04/D-05/D-06/D-08. Toda a suite test_pipeline_oauth.py + test_pipeline_fixes.py passa GREEN. Wave 0 contratos satisfeitos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway Volume → app | cookies.txt é credencial sensível (sessão Google); volume é confiável mas conteúdo precisa proteção 0o600 |
| User input → cookiefile path | Path construído programaticamente como `Path(settings.cache_dir) / "cookies.txt"` — sem input externo |
| api/tasks.py → pipeline | Argumento `cache_dir` vem de `settings` (env var no startup); não é controlado por requisição HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-06 | Information Disclosure | cookies.txt no Volume legível por outros processos | mitigate | start-all.sh executa `chmod 700 /data/yt-dlp-cache` no startup; cookies.txt em si recebe `chmod 600` no checkpoint humano do Plan 04. Path construído programaticamente impede traversal. |
| T-10.1-07 | Tampering | Codigo de produção desviado para silent fallback (D-06) | mitigate | "Quebra limpa" (D-01) explícita: sem branches `if cookies_path else cache_dir`; ydl_opts construído deterministicamente; testes test_pipeline_oauth verificam ausência de `username=oauth2` e de extractor_args bgutil |
| T-10.1-08 | Denial of Service | cookies.txt corrompido derruba lifespan | mitigate | `_check_oauth_cache` segue padrão PIPE-05: try/except OSError + return; NUNCA levanta exceção; log CRITICAL é visível para o operador mas não bloqueia startup |
| T-10.1-09 | Information Disclosure | logs CRITICAL imprimem path do cache_dir | accept | Path é o mount point fixo (`/data/yt-dlp-cache`), não contém credenciais; é necessário no log para operador diagnosticar |
| T-10.1-10 | Repudiation | Operador faz `chmod 700` manual sem registro | mitigate | start-all.sh executa o chmod em todo startup; mesmo após rollback humano, o próximo deploy reforça as permissões |
</threat_model>

<verification>
- pytest tests/test_pipeline_oauth.py -x -q → exit 0 (todos os 8 testes GREEN)
- pytest tests/test_pipeline_fixes.py -x -q → exit 0 (testes invertidos GREEN, demais preservados)
- python -c "from api.config import settings; print(settings.cache_dir)" → não levanta AttributeError
- python -c "from api.main import _check_oauth_cache" → não levanta ImportError
- bash -n start-all.sh → exit 0
- grep -c "bgutil" requirements.txt → 0
- pytest tests/ -x -q (suite completa) — esperado GREEN, exceto se Redis não rodar localmente
</verification>

<success_criteria>
- check_duration e download_audio têm assinatura `(url, cache_dir)` e leem cookies do Volume
- BgutilUnavailable, probe httpx, campos bgutil_base_url/po_token/cookies_path removidos
- _check_oauth_cache substitui _check_cookies no lifespan
- requirements.txt sem bgutil-ytdlp-pot-provider
- start-all.sh aplica chmod 700 no Volume no startup
- Toda a suite de testes Wave 0 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-03-SUMMARY.md`
</output>
