---
phase: 10.1
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_pipeline_oauth.py
  - tests/test_pipeline_fixes.py
autonomous: true
requirements: [AUTH-01]
tags: [tdd, auth-migration, test-scaffold, oauth2, railway-volume]

must_haves:
  truths:
    - "tests/test_pipeline_oauth.py existe com 7 testes RED cobrindo AUTH-01 (ydl_opts, startup validation, ausência de bgutil)"
    - "tests/test_pipeline_fixes.py::test_bgutil_08x_extractor_key_check_duration invertido para verificar AUSÊNCIA de getpot_bgutil_baseurl"
    - "tests/test_pipeline_fixes.py::test_bgutil_08x_extractor_key_download_audio invertido para verificar AUSÊNCIA de getpot_bgutil_baseurl"
    - "Os 4 testes PIPE-06 (test_pipe06_*) em tests/test_pipeline_fixes.py removidos ou substituídos por testes que verificam ausência do probe bgutil"
    - "Todos os novos testes em test_pipeline_oauth.py estão RED contra o código atual (verificável por pytest -x retornando exit != 0 com falhas explícitas)"
  artifacts:
    - path: tests/test_pipeline_oauth.py
      provides: "Suite de testes AUTH-01 para Phase 10.1"
      contains: "def test_check_duration_uses_cookiefile_from_cache_dir"
    - path: tests/test_pipeline_fixes.py
      provides: "Testes bgutil invertidos para verificar remoção"
      contains: "def test_bgutil_removed_from_check_duration"
  key_links:
    - from: tests/test_pipeline_oauth.py
      to: pipeline.py
      via: import pipeline + patch pipeline.yt_dlp.YoutubeDL
      pattern: "patch\\(\"pipeline\\.yt_dlp\\.YoutubeDL\""
    - from: tests/test_pipeline_oauth.py
      to: api/main.py
      via: from api.main import _check_oauth_cache
      pattern: "from api.main import _check_oauth_cache"
    - from: tests/test_pipeline_oauth.py
      to: requirements.txt
      via: Path read e assert "bgutil" not in content
      pattern: "requirements.txt"
---

<objective>
Wave 0 da fase 10.1: criar a infraestrutura de testes RED para a migração de autenticação. Este plano NÃO modifica código de produção. Define o contrato verificável que as Waves 2+ devem satisfazer.

Purpose: Garantir que toda alteração de produção feita nas próximas waves tenha um teste automatizado que passou de RED para GREEN, atendendo ao Nyquist Rule e ao TDD detection (workflow.tdd_mode).

Output: 
- `tests/test_pipeline_oauth.py` novo, com 7 testes RED
- `tests/test_pipeline_fixes.py` modificado: testes bgutil invertidos + testes PIPE-06 substituídos
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-PATTERNS.md
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-VALIDATION.md
@tests/test_pipeline_fixes.py
@tests/conftest.py
@pipeline.py

<interfaces>
<!-- Contratos que serão testados. Não existem ainda no código (Wave 2 implementará). -->

Após a Wave 2 (Plan 03), o código de produção terá estas assinaturas:

```python
# pipeline.py
def check_duration(url: str, cache_dir: str) -> dict[str, Any]: ...
def download_audio(url: str, cache_dir: str) -> Path: ...
# Classe BgutilUnavailable NÃO deve existir mais

# api/main.py
def _check_oauth_cache(cache_dir: str) -> None: ...

# api/config.py
@dataclass(frozen=True)
class Settings:
    ...
    cache_dir: str  # field(default_factory=lambda: os.environ.get("YTDLP_CACHE_DIR", ""))
    # cookies_path / po_token / bgutil_base_url removidos
```

Comportamento esperado dos ydl_opts após Wave 2:
- `cookiefile = str(Path(cache_dir) / "cookies.txt")` quando o arquivo existir
- `cookiefile` NÃO setado quando cache_dir vazio OU cookies.txt ausente
- `username` NÃO presente em ydl_opts (OAuth2 removido do yt-dlp 2026.3.17)
- `extractor_args` com `getpot_bgutil_baseurl` NÃO presente
- `extractor_args` com `player_client=android` ou `player_client=web` NÃO presente (default ios/mweb)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar tests/test_pipeline_oauth.py com 7 testes RED</name>
  <files>tests/test_pipeline_oauth.py</files>
  <read_first>
    - tests/test_pipeline_fixes.py (analog principal — copiar estrutura de header, padrão de patch+MagicMock, padrão de caplog para CRITICAL logs)
    - tests/conftest.py (entender DEV_MODE=true e fixtures disponíveis)
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-PATTERNS.md (seção "tests/test_pipeline_oauth.py (NOVO arquivo — AUTH-01)" — código completo a copiar)
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md (Seção "Code Examples > Testes adaptados (D-09 com abordagem de cookies no Volume)")
    - pipeline.py (linhas 1-100 — entender imports atuais e BgutilUnavailable que será removida)
  </read_first>
  <behavior>
    Criar arquivo novo `tests/test_pipeline_oauth.py` com cabeçalho docstring explicando que cobre AUTH-01 da Phase 10.1.
    
    Imports obrigatórios: `from __future__ import annotations`, `inspect`, `logging`, `from pathlib import Path`, `from unittest.mock import MagicMock, patch`, `import pytest`, `import pipeline`.
    
    Helper factory `_make_fake_ydl(captured_opts: dict, info: dict) -> type` que retorna uma classe `FakeYDL` com:
    - `__init__(self, opts)` que faz `captured_opts.update(opts)`
    - `__enter__/__exit__` para uso como context manager
    - `extract_info(self, url, download=False)` que retorna `info`
    - `download(self, urls)` que escreve um WAV mínimo em `Path(f"{captured_opts['outtmpl']}.wav")` com bytes `b"RIFF\\x00\\x00\\x00\\x00WAVEfmt "` (esse padrão é usado pelo download_audio test)
    
    Os 7 testes RED:
    
    - Test 1: `test_check_duration_uses_cookiefile_from_cache_dir(tmp_path)` — escreve `cookies.txt` válido (com sentinel `__Secure-3PSID`) em `tmp_path`; chama `pipeline.check_duration("https://www.youtube.com/watch?v=test", str(tmp_path))` com `patch("pipeline.yt_dlp.YoutubeDL", FakeYDL)`. Asserts: `captured_opts["cookiefile"] == str(tmp_path / "cookies.txt")`; `"username" not in captured_opts`; `"getpot_bgutil_baseurl" not in str(captured_opts.get("extractor_args", ""))`.
    
    - Test 2: `test_check_duration_no_oauth2_in_ydl_opts(tmp_path)` — mesmo setup mas asserts focados em OAuth2 ausente: `"username" not in captured_opts`; `"password" not in captured_opts` (nunca usar OAuth2 — yt-dlp 2026.3.17 rejeita em runtime); `captured_opts.get("cachedir") is None or captured_opts.get("cachedir") != str(tmp_path)` (cachedir não deve ser misturado com cookiefile path).
    
    - Test 3: `test_check_duration_no_cookiefile_when_cache_dir_empty()` — chama `check_duration("https://www.youtube.com/watch?v=test", "")`. Asserts: `"cookiefile" not in captured_opts`; `"username" not in captured_opts`.
    
    - Test 4: `test_download_audio_uses_cookiefile_from_cache_dir(tmp_path)` — escreve cookies.txt válido em tmp_path; chama `pipeline.download_audio("https://www.youtube.com/watch?v=test", str(tmp_path))` com `patch("pipeline.yt_dlp.YoutubeDL", FakeYDL)`. Asserts: `captured_opts["cookiefile"] == str(tmp_path / "cookies.txt")`; ydl_opts contém `"retries": 3` e `"fragment_retries": 3` preservados; `"extractor_args" not in captured_opts` OU `"getpot_bgutil_baseurl" not in str(captured_opts["extractor_args"])`.
    
    - Test 5: `test_check_oauth_cache_critical_when_cache_dir_empty(caplog)` — usa `from api.main import _check_oauth_cache` (import dentro do teste, mesmo padrão de PIPE-05); com `caplog.at_level(logging.CRITICAL, logger="api.main")`; chama `_check_oauth_cache("")`; assert existe ao menos um record com `levelno >= logging.CRITICAL` cuja mensagem contenha "YTDLP_CACHE_DIR" ou "cache_dir" (case-insensitive).
    
    - Test 6: `test_check_oauth_cache_critical_when_no_file(caplog, tmp_path)` — chama `_check_oauth_cache(str(tmp_path))` (diretório existe mas sem cookies.txt); assert ao menos um CRITICAL log mencionando "cookies.txt" ou "não encontrado"/"not found".
    
    - Test 7: `test_requirements_no_bgutil()` — lê `Path(__file__).parent.parent / "requirements.txt"`; assert `"bgutil-ytdlp-pot-provider" not in content` e `"bgutil" not in content.lower()` (mensagem de falha referenciando D-05).
    
    - Test 8: `test_bgutil_unavailable_class_removed()` — assert `not hasattr(pipeline, "BgutilUnavailable")` (mensagem mencionando D-04).
    
    Cada teste deve falhar AGORA (Wave 0 RED) com mensagens explícitas sobre o que falta. Os testes 5-8 testam comportamentos que ainda não existem (`_check_oauth_cache` não importa, requirements.txt ainda tem bgutil, BgutilUnavailable ainda existe).
    
    Verificar empiricamente que pytest exit code != 0 com pelo menos 6 falhas (uma para cada comportamento ausente) — o teste 7 pode passar acidentalmente se requirements.txt já não tiver bgutil, mas com requirements.txt atual deve falhar.
  </behavior>
  <action>
    Criar `tests/test_pipeline_oauth.py` novo seguindo o padrão de `tests/test_pipeline_fixes.py` (header docstring + imports + função helper + testes individuais). Não usar fixture `api_client` (puxa Redis); usar `tmp_path` + `caplog` apenas. Os 8 testes acima devem estar todos no arquivo. Não inventar comportamento novo — copiar exatamente o padrão de mock YoutubeDL de `test_pipeline.py` linhas 57-77 (FakeYDL inline com captured_opts.update). Para os testes 5 e 6, fazer `from api.main import _check_oauth_cache` DENTRO do teste para que o ImportError seja capturado como falha de teste explícita (não falha de coleção). Se o import falhar, o teste falha com mensagem "cannot import name '_check_oauth_cache' from 'api.main'" — esse é o sinal RED correto.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_oauth.py -x -q 2>&1 | tail -30; test $? -ne 0 || (echo "FAIL: expected RED tests" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_pipeline_oauth.py` exists
    - `grep -c "^def test_" tests/test_pipeline_oauth.py` returns >= 7
    - `grep -F "test_check_duration_uses_cookiefile_from_cache_dir" tests/test_pipeline_oauth.py` returns 1+ match
    - `grep -F "test_check_duration_no_oauth2_in_ydl_opts" tests/test_pipeline_oauth.py` returns 1+ match
    - `grep -F "test_check_duration_no_cookiefile_when_cache_dir_empty" tests/test_pipeline_oauth.py` returns 1+ match
    - `grep -F "test_download_audio_uses_cookiefile_from_cache_dir" tests/test_pipeline_oauth.py` returns 1+ match
    - `grep -F "test_check_oauth_cache_critical_when_cache_dir_empty" tests/test_pipeline_oauth.py` returns 1+ match
    - `grep -F "test_check_oauth_cache_critical_when_no_file" tests/test_pipeline_oauth.py` returns 1+ match
    - `grep -F "test_requirements_no_bgutil" tests/test_pipeline_oauth.py` returns 1+ match
    - `grep -F "test_bgutil_unavailable_class_removed" tests/test_pipeline_oauth.py` returns 1+ match
    - `pytest tests/test_pipeline_oauth.py -x -q` exits with non-zero (RED state — failures expected)
    - `pytest tests/test_pipeline_oauth.py --collect-only -q 2>&1 | grep -c "test_"` returns >= 7 (testes coletáveis, não erro de import)
    - File does NOT use fixture `api_client` (verified: `grep -c "api_client" tests/test_pipeline_oauth.py` returns 0)
  </acceptance_criteria>
  <done>
    Arquivo `tests/test_pipeline_oauth.py` existe com 7+ testes definidos. pytest coleta todos os testes (sem erro de sintaxe/collection). Pelo menos 6 dos 7+ testes falham (RED) contra o código atual. As falhas são explícitas (assertion error com mensagens claras, ou ImportError para `_check_oauth_cache`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inverter testes bgutil e substituir testes PIPE-06 em test_pipeline_fixes.py</name>
  <files>tests/test_pipeline_fixes.py</files>
  <read_first>
    - tests/test_pipeline_fixes.py (linhas 130-152: testes test_bgutil_08x_*; linhas 155-252: testes PIPE-06 a remover)
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-PATTERNS.md (seção "tests/test_pipeline_fixes.py (inverter asserts bgutil)" — código exato a colocar no lugar)
    - pipeline.py (entender que após Wave 2, BgutilUnavailable e probe httpx serão removidos)
  </read_first>
  <behavior>
    Modificar `tests/test_pipeline_fixes.py` para inverter os asserts dos 2 testes bgutil e substituir os 4 testes PIPE-06.
    
    A. Testes bgutil 0.8.x (linhas 130-152) — INVERTER asserts:
    
    - Renomear `test_bgutil_08x_extractor_key_check_duration` para `test_bgutil_removed_from_check_duration`. Substituir corpo: ler `inspect.getsource(pipeline.check_duration)`; assert `"getpot_bgutil_baseurl" not in src` (mensagem mencionando D-03/D-04: "bgutil não foi removido de check_duration após migração"); assert `"bgutil" not in src.lower()` (case-insensitive — pega referências em comentários também).
    
    - Renomear `test_bgutil_08x_extractor_key_download_audio` para `test_bgutil_removed_from_download_audio`. Substituir corpo: ler `inspect.getsource(pipeline.download_audio)`; assert `"getpot_bgutil_baseurl" not in src`; assert `"BgutilUnavailable" not in src`; assert `"httpx.get" not in src` (probe removido); mensagem mencionando D-04.
    
    B. Testes PIPE-06 (linhas 155-252) — REMOVER OS 4 TESTES:
    
    Remover completamente: `test_pipe06_bgutil_probe_connect_error_raises`, `test_pipe06_bgutil_probe_timeout_raises`, `test_pipe06_no_probe_when_bgutil_url_empty`, `test_pipe06_tasks_bgutil_error_type`.
    
    Remover também os imports usados só por esses testes (linhas 163-165): `import httpx`, `import pytest`, `from unittest.mock import patch, MagicMock` — VERIFICAR se outros testes no arquivo dependem deles antes de remover (pytest pode ser usado por outros testes — preservar nesse caso).
    
    Substituir o bloco PIPE-06 (todo de linha 155 até linha 252) por um único teste novo:
    
    - `test_pipe06_no_bgutil_probe_in_download_audio()` — verifica que o probe foi removido: `src = inspect.getsource(pipeline.download_audio)`; assert `"httpx.get" not in src` (mensagem: "probe bgutil ainda presente em download_audio — remover conforme D-04"); assert `"BgutilUnavailable" not in src`.
    
    Manter inalterados todos os outros testes do arquivo (test_pipe01_*, test_pipe02_*, test_pipe03_*, test_pipe04_*, test_pipe05_*, test_deploy01_*).
    
    Após Wave 2 (Plan 03), todos os testes em test_pipeline_fixes.py devem voltar a passar (GREEN). Antes da Wave 2, os 2 testes invertidos + o test_pipe06_no_bgutil_probe_in_download_audio devem estar RED (porque o código atual AINDA tem bgutil).
  </behavior>
  <action>
    Editar `tests/test_pipeline_fixes.py`. Renomear os dois `test_bgutil_08x_*` e inverter asserts (de `in src` para `not in src` com mensagens explicativas). Remover os 4 testes `test_pipe06_*` (linhas 155-252) e o bloco de imports/separador acima deles (linhas 155-165). Adicionar um único teste `test_pipe06_no_bgutil_probe_in_download_audio` no lugar. Garantir que `import inspect` no topo do arquivo permanece (usado pelos testes mantidos). Não remover `import pytest` se outro teste do arquivo usa. Verificar com grep depois da edição que `"getpot_bgutil_baseurl"` aparece APENAS dentro de strings `not in src` e mensagens de assert (verificar com `grep -c "getpot_bgutil_baseurl" tests/test_pipeline_fixes.py`).
  </action>
  <verify>
    <automated>grep -c "^def test_" tests/test_pipeline_fixes.py | awk '$1>=8 {exit 0} {exit 1}' && grep -F "test_bgutil_removed_from_check_duration" tests/test_pipeline_fixes.py && grep -F "test_bgutil_removed_from_download_audio" tests/test_pipeline_fixes.py && ! grep -q "test_pipe06_bgutil_probe_connect_error_raises" tests/test_pipeline_fixes.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_pipe06_bgutil_probe" tests/test_pipeline_fixes.py` returns 0 (4 testes PIPE-06 originais removidos)
    - `grep -c "test_pipe06_tasks_bgutil_error_type" tests/test_pipeline_fixes.py` returns 0
    - `grep -c "test_bgutil_08x_extractor_key" tests/test_pipeline_fixes.py` returns 0 (renomeados)
    - `grep -F "def test_bgutil_removed_from_check_duration" tests/test_pipeline_fixes.py` returns 1+ match
    - `grep -F "def test_bgutil_removed_from_download_audio" tests/test_pipeline_fixes.py` returns 1+ match
    - `grep -F "def test_pipe06_no_bgutil_probe_in_download_audio" tests/test_pipeline_fixes.py` returns 1+ match
    - `grep -v "^#" tests/test_pipeline_fixes.py | grep -c '"getpot_bgutil_baseurl"'` returns 2 OR 3 (apenas em asserts `not in src`)
    - All test_pipe01_*, test_pipe02_*, test_pipe03_*, test_pipe04_*, test_pipe05_*, test_deploy01_* preservados (verificar: `grep -c "def test_pipe0[1-5]\\|def test_deploy01" tests/test_pipeline_fixes.py` >= 8)
    - pytest pode coletar o arquivo sem erro: `pytest tests/test_pipeline_fixes.py --collect-only -q` exits 0
    - Os 3 testes novos/invertidos estão RED contra código atual: `pytest tests/test_pipeline_fixes.py::test_bgutil_removed_from_check_duration tests/test_pipeline_fixes.py::test_bgutil_removed_from_download_audio tests/test_pipeline_fixes.py::test_pipe06_no_bgutil_probe_in_download_audio` exits non-zero
  </acceptance_criteria>
  <done>
    Arquivo `tests/test_pipeline_fixes.py` editado: 2 testes bgutil invertidos com novos nomes (`test_bgutil_removed_from_*`), 4 testes PIPE-06 removidos, 1 teste novo `test_pipe06_no_bgutil_probe_in_download_audio` adicionado. pytest coleta o arquivo sem erro de sintaxe/import. Testes invertidos estão RED contra código atual (bgutil ainda presente).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → pipeline.py | tests carregam código de produção via import; código de produção não é alterado nesta wave |
| tests → file system | tmp_path fixture isolado; nenhum acesso a /tmp do sistema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-01 | Information Disclosure | tests/test_pipeline_oauth.py escreve cookies.txt fake | accept | Cookies fake em tmp_path (pytest gerencia cleanup); valor é literal `"__Secure-3PSID\tvalue\n"` — não é credencial real |
| T-10.1-02 | Tampering | Modificação de testes existentes em test_pipeline_fixes.py | mitigate | Manter testes test_pipe01..05 e test_deploy01 inalterados; remover apenas testes obsoletos PIPE-06; renomear (não deletar) testes bgutil para preservar histórico de cobertura |
| T-10.1-03 | Repudiation | Testes RED sem mensagens claras geram diagnóstico fraco | mitigate | Toda assertion em test_pipeline_oauth.py inclui mensagem explicativa referenciando D-XX do CONTEXT.md ou AUTH-01 |
</threat_model>

<verification>
- `pytest tests/test_pipeline_oauth.py --collect-only -q` exit 0 (coleta funciona)
- `pytest tests/test_pipeline_fixes.py --collect-only -q` exit 0 (coleta funciona)
- `pytest tests/test_pipeline_oauth.py -x -q` exit != 0 (RED expected — toda Wave 0 de TDD começa RED)
- `grep -c "^def test_" tests/test_pipeline_oauth.py` >= 7
- Nenhum teste novo usa `api_client` fixture (sem dependência de Redis live)
- Sintaxe Python válida nos dois arquivos (pytest collect funciona)
</verification>

<success_criteria>
- tests/test_pipeline_oauth.py criado com 7+ testes RED
- tests/test_pipeline_fixes.py: 2 testes bgutil invertidos, 4 testes PIPE-06 removidos, 1 teste novo de "bgutil não está presente" adicionado
- Todos os outros testes em test_pipeline_fixes.py preservados (test_pipe01..05, test_deploy01)
- Suite de testes RED estabelecida — Wave 2 (Plan 03) terá um contrato verificável para tornar GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-01-SUMMARY.md`
</output>
