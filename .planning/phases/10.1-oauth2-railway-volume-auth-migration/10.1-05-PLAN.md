---
phase: 10.1
plan: 05
type: execute
wave: 4
depends_on: [10.1-04]
files_modified: []
autonomous: false
requirements: [AUTH-02, AUTH-03]
tags: [e2e, validation, bgutil-teardown, railway, redeploy-test]

must_haves:
  truths:
    - "3 URLs de beats reais submetidas via POST /jobs no Railway atingem status=done com WAV baixável e BPM/key plausíveis"
    - "Cada job atinge status=done sem a env var YTDLP_COOKIES_B64 ativa e sem o serviço bgutil rodando"
    - "Um redeploy forçado do serviço 248e8eaf — sem alterar Volume — produz container que processa novo job com sucesso (cookies do Volume persistem)"
    - "Serviço bgutil (2fc3a8a5-b50b-48e2-b408-87167f5ac28a) deletado via Railway MCP após E2E success"
    - "Env vars BGUTIL_BASE_URL e YTDLP_COOKIES_B64 removidas do serviço 248e8eaf após E2E success"
  artifacts:
    - path: .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-05-SUMMARY.md
      provides: "Evidência de E2E success e teardown bgutil"
      contains: "AUTH-02"
  key_links:
    - from: 3 URLs de beats
      to: Railway production API
      via: POST /jobs polling
      pattern: "status=done"
    - from: redeploy forçado
      to: AUTH-03 satisfied
      via: cookies persistem no Volume
      pattern: "redeploy success"
    - from: bgutil service 2fc3a8a5
      to: deleted
      via: Railway MCP
      pattern: "service deleted"
---

<objective>
Wave 4 da fase 10.1: validar end-to-end que o novo caminho de autenticação funciona em produção (AUTH-02), que sobrevive a redeploy (AUTH-03), e então fazer o teardown do bgutil e env vars antigas. Esta é a "limpeza segura" — após confirmação de que o novo caminho funciona, removemos os fallbacks antigos.

Purpose: Concluir Phase 10.1 com:
- AUTH-02 validado por 3 URLs de beats reais
- AUTH-03 validado por redeploy forçado
- bgutil service deletado (D-05) — para de cobrar
- Env vars BGUTIL_BASE_URL e YTDLP_COOKIES_B64 removidas

Output:
- 3 logs de E2E success com BPM/key/download_url para cada URL
- Confirmação de redeploy + job success após redeploy
- bgutil service não existe mais no Railway
- Env vars limpas
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md
@.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: E2E manual — submeter 3 URLs de beats via API Railway e verificar status=done (AUTH-02)</name>
  <files></files>
  <read_first>
    - .planning/phases/10-failure-hardening-and-e2e-validation/10-CONTEXT.md (referência de URLs de beats usadas em Phase 10)
    - tests/conftest.py (fixture youtube_test_urls — sugestões de URLs por gênero)
  </read_first>
  <what-built>
    Após Wave 2 (código) + Wave 3 (Railway Volume + env vars + cookies + redeploy), a infraestrutura está pronta. yt-dlp lê cookies de `/data/yt-dlp-cache/cookies.txt`, sem bgutil envolvido, sem env var YTDLP_COOKIES_B64 sendo usada pelo código novo. AUTH-02 exige confirmar que isso funciona end-to-end com 3 URLs reais.
  </what-built>
  <how-to-verify>
    O operador precisa do URL público do serviço Railway (Uvicorn web). Caso não saiba, recuperar via Railway dashboard ou MCP: o domain do serviço 248e8eaf.
    
    **Passo 1 — Escolher 3 URLs de beats curtos (< 5 min idealmente, sempre < 15 min CORE-05):**
    Sugestão de set diverso (preferir essas):
    - URL 1: trap beat (ex: do canal de um produtor underground)
    - URL 2: lo-fi beat
    - URL 3: instrumental rap / boom bap
    
    Fixtures conhecidos em `tests/conftest.py::youtube_test_urls`:
    - rock_lofi: https://www.youtube.com/watch?v=b1f6o0GMT8c
    - trap: https://www.youtube.com/watch?v=npoTcSToYTc
    - lofi_house: https://www.youtube.com/watch?v=jfKfPfyJRdk
    
    **Passo 2 — Para cada URL, submeter e polar:**
    ```bash
    # Substitua $RAILWAY_URL pelo domain real (ex: https://soundgrabber-production.up.railway.app)
    # Substitua $URL pela URL do beat
    
    JOB_RESP=$(curl -sS -X POST "$RAILWAY_URL/jobs" \
      -H "Content-Type: application/json" \
      -d "{\"youtube_url\": \"$URL\"}")
    echo "$JOB_RESP"
    # Esperado: {"job_id":"<uuid>"}
    
    JOB_ID=$(echo "$JOB_RESP" | python3 -c "import sys,json; print(json.load(sys.stdin)['job_id'])")
    
    # Pollar até status final (done ou failed) ou timeout 5 min
    for i in $(seq 1 60); do
      STATUS=$(curl -sS "$RAILWAY_URL/jobs/$JOB_ID")
      echo "[$i] $STATUS"
      if echo "$STATUS" | grep -q '"status":"done"'; then break; fi
      if echo "$STATUS" | grep -q '"status":"failed"'; then break; fi
      sleep 5
    done
    
    # Final status
    echo "FINAL: $STATUS"
    
    # Se done, testar download
    if echo "$STATUS" | grep -q '"status":"done"'; then
      curl -sS -I "$RAILWAY_URL/files/$JOB_ID" | head -10
      # Esperado: HTTP/1.1 200 OK + Content-Type: audio/wav
    fi
    ```
    
    **Passo 3 — Coletar evidência para o SUMMARY:**
    Para cada uma das 3 URLs, registrar:
    - URL completa
    - job_id retornado
    - status final (deve ser `done`)
    - bpm + bpm_half + bpm_double do response final (devem ser numéricos plausíveis: 60-200 para a maioria dos beats)
    - key (ex: "C minor", "F# major") + camelot (ex: "8A", "11B")
    - duration_sec (deve ser < 900)
    - download_url
    - HEAD do download retornou 200 + audio/wav
    
    **Passo 4 — Verificar logs do Celery Worker (mesmo container):**
    ```bash
    railway logs --service "Uvicorn Web + Celery Worker" --tail 200 | grep -E "(ERROR|CRITICAL|bgutil|Sign in to confirm)"
    ```
    - Esperado: ausência de "Sign in to confirm you're not a bot"
    - Esperado: ausência de erros relacionados a bgutil (já não chamamos bgutil)
    - Esperado: ausência de tracebacks
    
    Se TODAS as 3 URLs atingiram status=done com BPM/key plausíveis e download de 200 OK: AUTH-02 satisfeito → responder `aprovado`.
    
    Se 1 ou mais falharam: registrar exatamente qual falhou com qual erro, e responder `falhou: <detalhes>`. Não prosseguir para Task 2/3 — debug primeiro.
  </how-to-verify>
  <resume-signal>
    Responda com:
    - `aprovado` + tabela de 3 URLs (URL, job_id, status, bpm, key) + confirmação de logs limpos — Task 2 (AUTH-03) prossegue
    - `falhou: <descrição>` — Wave 4 pausa, debug antes de continuar
  </resume-signal>
  <action>
    Apresentar ao operador as instruções dos Passos 1-4 do bloco `<how-to-verify>` e PAUSAR. Este checkpoint exige que o operador rode os comandos `curl` reais contra a URL pública do serviço Railway 248e8eaf — Claude não tem acesso a um beat URL válido nem ao domínio público sem o operador fornecê-los.

    Após o operador responder com `aprovado` + tabela das 3 URLs (URL, job_id, status, bpm, key, duration_sec, HTTP status do /files), gravar no SUMMARY do plan 05:
    - Tabela completa fornecida pelo operador
    - Confirmação de que logs do Celery worker NÃO contém "Sign in to confirm" nem referências a bgutil
    - Sinal verde para Task 2 (AUTH-03 redeploy test)

    Se a resposta for `falhou`, encerrar Wave 4 com status BLOCKED. NÃO disparar Task 2 nem Task 3 — bgutil é o fallback de produção atual e não pode ser deletado enquanto AUTH-02 não estiver verde.
  </action>
  <verify>
    <automated>grep -qE "bpm|status.*done|AUTH-02" .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-05-SUMMARY.md 2>/dev/null || echo "WAITING: operator response not yet recorded in SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - 3 URLs distintas testadas (não repetir a mesma URL)
    - Todas 3 URLs atingiram `"status":"done"` no polling final
    - Cada URL retornou bpm (numérico, plausível 60-200), key (string não-vazia), camelot (string não-vazia)
    - Cada URL retornou download_url e HEAD do download retornou HTTP 200 + Content-Type contendo "audio/wav"
    - Logs do Celery worker NÃO contém "Sign in to confirm you're not a bot"
    - Logs do Celery worker NÃO contém referência a bgutil (probe, BgutilUnavailable, ou similar)
    - SUMMARY do plan 05 inclui tabela completa: URL | job_id | status | bpm | key | duration_sec | download HTTP status
  </acceptance_criteria>
  <done>
    AUTH-02 validado: 3 URLs de beats reais processadas com sucesso no Railway usando cookies do Volume, sem bgutil. Evidência registrada.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E redeploy — disparar redeploy forçado e validar AUTH-03 (cookies persistem no Volume)</name>
  <files></files>
  <read_first>
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md (AUTH-03 — cookies sobrevivem redeploy)
  </read_first>
  <what-built>
    Wave 4 Task 1 confirmou que jobs funcionam com cookies do Volume. AUTH-03 exige confirmar especificamente que após um redeploy do container, o Volume persiste e o próximo job não precisa re-popular cookies.
  </what-built>
  <how-to-verify>
    **Passo 1 — Forçar redeploy do serviço 248e8eaf sem alterar nada:**
    - Via Railway dashboard: Service "Uvicorn Web + Celery Worker" → Deployments → "Redeploy" no deploy mais recente
    - OU via Railway MCP: trigger redeploy (com confirmação per MCP instructions)
    - OU via railway CLI: `railway redeploy` no serviço linkado
    
    Não fazer git push, não alterar env vars, não alterar Volume. O objetivo é forçar um restart do container sem mudar nada.
    
    **Passo 2 — Aguardar startup completo (2-5 min).**
    
    **Passo 3 — Verificar startup logs (mesmo padrão de Wave 3 Task 3):**
    - NÃO deve aparecer CRITICAL de _check_oauth_cache (cookies.txt ainda está no Volume)
    - Application startup complete OU equivalente
    
    **Passo 4 — Submeter UMA URL nova de beat (escolher uma que não foi usada na Task 1):**
    ```bash
    JOB_RESP=$(curl -sS -X POST "$RAILWAY_URL/jobs" \
      -H "Content-Type: application/json" \
      -d "{\"youtube_url\": \"<nova URL de beat>\"}")
    JOB_ID=$(echo "$JOB_RESP" | python3 -c "import sys,json; print(json.load(sys.stdin)['job_id'])")
    
    # Pollar até 5 min
    for i in $(seq 1 60); do
      STATUS=$(curl -sS "$RAILWAY_URL/jobs/$JOB_ID")
      if echo "$STATUS" | grep -q '"status":"done"\|"status":"failed"'; then break; fi
      sleep 5
    done
    echo "POST-REDEPLOY FINAL: $STATUS"
    ```
    
    **Passo 5 — Coletar evidência:**
    Registrar:
    - Deploy ID/timestamp do redeploy forçado
    - URL submetida, job_id, status final
    - Confirmação de que NÃO houve necessidade de re-popular cookies entre o redeploy e o job
    
    Se status final = `done`: AUTH-03 satisfeito.
    Se status final = `failed` com erro relacionado a cookies/auth: AUTH-03 falhou — cookies NÃO persistiram. Não prosseguir para teardown.
  </how-to-verify>
  <resume-signal>
    Responda com:
    - `aprovado` + deploy ID + status final do job pós-redeploy — Task 3 (teardown bgutil) prossegue
    - `falhou: <razão>` — Wave 4 pausa para investigar persistência do Volume
  </resume-signal>
  <action>
    Apresentar ao operador as instruções dos Passos 1-5 do bloco `<how-to-verify>` e PAUSAR. O executor PODE acionar o redeploy forçado via railway MCP (com confirmação humana), mas a submissão do job e a verificação do status final dependem do operador rodar os comandos curl contra a URL pública.

    Caso o operador autorize o executor a disparar o redeploy via railway MCP, o executor:
    1. Confirma com o operador (Railway MCP server instructions exigem confirmação para ações destrutivas/disruptivas — redeploy reinicia o container).
    2. Aciona redeploy do serviço `248e8eaf-68e1-4fb1-beb0-448bd26f317a` sem alterar nada (mesmo commit, mesmas env vars, mesmo Volume).
    3. Tail logs via MCP até `Application startup complete` ou equivalente.
    4. Reporta deploy ID + linhas-chave dos logs ao operador.
    5. Aguarda operador submeter o job de teste e responder com job_id + status final.

    Após `aprovado` do operador, gravar no SUMMARY (parcial):
    - Deploy ID do redeploy
    - Linhas-chave dos startup logs (sanitizadas, sem credenciais)
    - job_id e status final do job pós-redeploy
    - Confirmação literal de que cookies.txt NÃO foi re-populado entre redeploy e job

    Se `falhou`, encerrar Wave 4 BLOCKED. NÃO disparar Task 3 (teardown) — cookies não persistiram, bgutil ainda é necessário como fallback.
  </action>
  <verify>
    <automated>grep -qE "AUTH-03|post-redeploy|cookies persisten" .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-05-SUMMARY.md 2>/dev/null || echo "WAITING: operator response not yet recorded in SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - Redeploy forçado do serviço 248e8eaf concluído com sucesso (Railway status SUCCESS/COMPLETED)
    - Operador NÃO repopulou cookies.txt entre redeploy e novo job
    - Startup logs do redeploy NÃO contém CRITICAL de _check_oauth_cache
    - Job submetido pós-redeploy atingiu `"status":"done"` em até 5 minutos
    - Response do job pós-redeploy contém bpm + key + download_url válidos
    - SUMMARY do plan 05 inclui: deploy ID do redeploy + job_id + status final + evidência de cookies não-recopiados
  </acceptance_criteria>
  <done>
    AUTH-03 validado: cookies sobreviveram redeploy forçado, próximo job processou imediatamente sem re-auth. Evidência registrada.
  </done>
</task>

<task type="auto">
  <name>Task 3: Teardown bgutil via railway MCP (delete service + remove env vars)</name>
  <files></files>
  <read_first>
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-CONTEXT.md (D-05 — teardown completo; canonical_refs com IDs Railway)
    - .planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-RESEARCH.md (Runtime State Inventory — ordem segura de teardown)
  </read_first>
  <action>
    Esta task SÓ executa após Task 1 e Task 2 confirmados (`aprovado`). Se qualquer uma falhou, NÃO executar — bgutil é o fallback que ainda existe na infra.
    
    Usar railway MCP no projeto `1b0d39e7-bda8-4b44-80da-1751ba65c76f`, environment `a42aa82d-259e-4eb6-85a5-98355d69e27a`:
    
    (1) **Confirmar com operador antes de cada ação destrutiva** (Railway MCP server instructions exigem):
        - "Vou DELETAR o serviço bgutil (ID 2fc3a8a5-b50b-48e2-b408-87167f5ac28a). Esta ação é irreversível e o serviço para de cobrar. Confirma? (sim/não)"
        - "Vou REMOVER as env vars BGUTIL_BASE_URL e YTDLP_COOKIES_B64 do serviço 248e8eaf. Confirma? (sim/não)"
    
    (2) **Após confirmação, deletar o serviço bgutil:**
        - Service ID: `2fc3a8a5-b50b-48e2-b408-87167f5ac28a`
        - Via railway MCP delete-service ou `railway-agent` com instrução explícita
        - Verificar via MCP list-services que o ID não aparece mais
    
    (3) **Remover env var `BGUTIL_BASE_URL` do serviço 248e8eaf:**
        - Via railway MCP delete-variable
        - Verificar via list-variables que não aparece mais
    
    (4) **Remover env var `YTDLP_COOKIES_B64` do serviço 248e8eaf:**
        - Via railway MCP delete-variable
        - Verificar via list-variables que não aparece mais
    
    (5) **Remover env var `YTDLP_PO_TOKEN` do serviço 248e8eaf** (se existir — pode não estar setada):
        - Tentar delete; se não existe, ok
    
    (6) **Trigger último redeploy** sem essas env vars para garantir estado limpo:
        - Confirmar com operador antes
        - Tail dos logs por 2 min: NÃO deve aparecer nenhum traceback novo (não há código que referencia essas vars depois da Wave 2, mas verificar para segurança)
    
    (7) **Atualizar SUMMARY** do plan 05 com:
        - Confirmação de que serviço bgutil deletado (ID + timestamp)
        - Confirmação de env vars removidas (listar quais)
        - Deploy ID do redeploy final pós-teardown
        - Confirmação de que startup logs ainda estão limpos
        - Custo Railway estimado economizado (se MCP expõe — opcional)
  </action>
  <verify>
    <automated>echo "Verification via railway MCP introspection — see manual checks in action steps"</automated>
  </verify>
  <acceptance_criteria>
    - Via railway MCP `list-services`: serviço com ID `2fc3a8a5-b50b-48e2-b408-87167f5ac28a` NÃO aparece (deletado)
    - Via railway MCP `list-variables` no serviço 248e8eaf: variável `BGUTIL_BASE_URL` NÃO aparece
    - Via railway MCP `list-variables` no serviço 248e8eaf: variável `YTDLP_COOKIES_B64` NÃO aparece
    - Via railway MCP `list-variables` no serviço 248e8eaf: variável `YTDLP_CACHE_DIR` AINDA aparece com valor `/data/yt-dlp-cache`
    - Via railway MCP: Volume `/data/yt-dlp-cache` AINDA está montado no serviço 248e8eaf (NÃO deletado)
    - Redeploy pós-teardown concluído com sucesso (status SUCCESS)
    - Startup logs do redeploy pós-teardown NÃO contém CRITICAL relacionado a auth
    - SUMMARY do plan 05 registra todas as ações e evidências
  </acceptance_criteria>
  <done>
    bgutil service deletado, env vars antigas removidas, sistema rodando exclusivamente com cookies do Railway Volume. Phase 10.1 completa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Railway API (via MCP) | ações destrutivas (delete service, remove env var) precisam confirmação |
| Production users → API | endpoints existentes, não há novos endpoints; rate limits e validações inalterados |
| API → yt-dlp + cookies.txt | path validation programático; cookies em Volume com 0600 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-17 | Denial of Service | Teardown executado antes de E2E success deixa sistema sem auth | mitigate | Task 3 depende explicitamente do `aprovado` de Task 1 e Task 2; ordem de teardown segue RESEARCH.md Runtime State Inventory |
| T-10.1-18 | Repudiation | Delete de service sem registro audit | mitigate | Task 3 registra IDs, timestamps, deploy IDs, e confirma via MCP queries após cada delete |
| T-10.1-19 | Elevation of Privilege | Railway MCP executa delete sem confirmação humana | mitigate | Task 3 instrui executor a confirmar cada ação destrutiva com operador antes de invocar MCP delete |
| T-10.1-20 | Information Disclosure | Logs de E2E contêm URLs ou cookies | mitigate | Task 1 e 2 instruem operador a sanitizar outputs — apenas metadata (job_id, status, bpm, key) no SUMMARY; não cookies, não tokens, não URLs completas se sensíveis |
| T-10.1-21 | Tampering | Bgutil deletado mas código de produção ainda faz import | mitigate | Wave 2 (Plan 03) já removeu `from pipeline import ... BgutilUnavailable`, `httpx.get(bgutil_base_url)`, e `bgutil_base_url` de settings — testes Wave 0 verificaram. Task 3 não introduz nova dependência |
| T-10.1-22 | Spoofing | Operador entra `aprovado` sem ter testado de fato | accept | Padrão GSD: confiança no operador para checkpoints; mitigado parcialmente por requerir outputs literais nos acceptance_criteria das Tasks 1 e 2 |
</threat_model>

<verification>
- AUTH-02: 3 URLs de beats reais → status=done com WAV baixável (Task 1 confirmação)
- AUTH-03: redeploy forçado preserva cookies → próximo job success (Task 2 confirmação)
- bgutil service não existe mais no Railway (Task 3 via MCP)
- Env vars BGUTIL_BASE_URL e YTDLP_COOKIES_B64 removidas (Task 3 via MCP)
- Redeploy final pós-teardown com startup limpo
</verification>

<success_criteria>
- AUTH-02 satisfeito (3 URLs reais → done)
- AUTH-03 satisfeito (redeploy preserva cookies)
- bgutil completamente teardown (service + env vars)
- Sistema rodando exclusivamente com cookies no Railway Volume
- Phase 10.1 completa, pronto para /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-oauth2-railway-volume-auth-migration/10.1-05-SUMMARY.md` com:
- Tabela das 3 URLs (URL, job_id, bpm, key, status final)
- Evidência do redeploy AUTH-03 (deploy ID, job_id pós-redeploy, status)
- Confirmação de bgutil service deletion (timestamp + MCP query result)
- Lista final de env vars do serviço 248e8eaf (deve conter YTDLP_CACHE_DIR, REDIS_URL, demais; NÃO conter BGUTIL_BASE_URL, YTDLP_COOKIES_B64, YTDLP_PO_TOKEN)
- Notas para Phase 11 ou backlog: política de rotação de cookies (quando re-exportar)
</output>
