---
phase: 10.1
plan: 02
status: complete
completed: 2026-05-13
decision: option-a
---

# Summary — Plan 10.1-02: Checkpoint D-03 Adaptation

## Decision

**Operador escolheu:** `option-a` — Prosseguir com cookies no Railway Volume

**Timestamp:** 2026-05-13T18:35:00Z

## Context

O CONTEXT.md D-03 original especificava `username=oauth2`/`cachedir`, mas o RESEARCH.md confirmou que OAuth2 foi removido do yt-dlp em 2024.11.18 (commit `52c0ffe`, yt-dlp 2026.3.17 instalado retorna `Login with OAuth is no longer supported`).

**D-03 adapted:** Em vez de OAuth2, usar `cookiefile=Path(cache_dir)/"cookies.txt"` no Railway Volume — mesmo `cache_dir`, mesma assinatura de função, objetivo de persistência mantido.

## Decisions Preserved

| Decisão | Status |
|---------|--------|
| D-01 (quebra limpa, sem fallback) | Preservado |
| D-02 (assinaturas `check_duration(url, cache_dir)`) | Preservado |
| D-03 (mecanismo de auth) | **Adaptado: OAuth2 → cookies no Volume** |
| D-04 (remover probe bgutil) | Preservado |
| D-05 (teardown bgutil) | Preservado |
| D-06 (Railway Volume `/data/yt-dlp-cache`) | Preservado |
| D-07 (one-time auth via `railway run`) | Adaptado: `cp cookies.txt` em vez de OAuth flow |
| D-08 (startup validation verifica cookies.txt) | Preservado (mesmo padrão PIPE-05) |
| D-09 (testes assertam cookiefile em vez de username=oauth2) | Preservado |

## Next Action

Wave 2 (Plan 10.1-03) está **autorizada** a iniciar. Refatoração do código de produção pode executar.
