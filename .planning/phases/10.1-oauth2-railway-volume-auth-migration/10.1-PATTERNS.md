# Phase 10.1: OAuth2 + Railway Volume Auth Migration — Pattern Map

**Mapped:** 2026-05-12
**Files analyzed:** 6 (5 modified + 1 new)
**Analogs found:** 6 / 6

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `pipeline.py` | service | request-response (yt-dlp) | `pipeline.py` itself (self-modification) | exact — modify existing functions |
| `api/config.py` | config | — | `api/config.py` itself (self-modification) | exact — add/remove dataclass fields |
| `api/main.py` | middleware/startup | request-response | `api/main.py::_check_cookies()` (lines 138–184) | exact — function replacement, same structure |
| `api/tasks.py` | service/task | event-driven (Celery) | `api/tasks.py` itself (self-modification) | exact — remove import + except branch |
| `requirements.txt` | config | — | `requirements.txt` itself | exact — remove one line |
| `tests/test_pipeline_oauth.py` | test | — | `tests/test_pipeline_fixes.py` (PIPE-06 block, lines 163–252) | exact — same mock strategy, same assertion style |

---

## Pattern Assignments

### `pipeline.py` — `check_duration` (modificar assinatura + ydl_opts)

**Analog:** `pipeline.py` linhas 82–133 (função `check_duration` atual)

**Assinatura atual** (linha 82):
```python
def check_duration(url: str, cookies_path: str, bgutil_base_url: str = "") -> dict[str, Any]:
```

**Assinatura após migração** (D-02):
```python
def check_duration(url: str, cache_dir: str) -> dict[str, Any]:
```

**ydl_opts atual** (linhas 100–116) — bloco a substituir:
```python
player_client = "web" if bgutil_base_url else "android"
ydl_opts: dict[str, Any] = {
    "quiet": True,
    "no_warnings": True,
    "skip_download": True,
    "socket_timeout": 30,
    "noplaylist": True,
    "no_cache_dir": True,
    "ffmpeg_location": _YTDLP_FFMPEG_LOCATION,
    "extractor_args": {"youtube": [f"player_client={player_client}"]},
}
if bgutil_base_url:
    ydl_opts["extractor_args"]["youtube"].append(f"getpot_bgutil_baseurl={bgutil_base_url}")
if cookies_path:
    ydl_opts["cookiefile"] = cookies_path
```

**ydl_opts após migração** (D-03 adaptado — cookies do Volume):
```python
ydl_opts: dict[str, Any] = {
    "quiet": True,
    "no_warnings": True,
    "skip_download": True,
    "socket_timeout": 30,
    "noplaylist": True,
    "no_cache_dir": True,
    "ffmpeg_location": _YTDLP_FFMPEG_LOCATION,
    # Sem extractor_args — ios/mweb default nao precisam de player_client manual
    # Sem po_token — ios/mweb nao requerem PO Token [VERIFIED: INNERTUBE_CLIENTS]
}
# Cookies do Railway Volume — se existirem, yt-dlp usa autenticado (web_creator+mweb)
if cache_dir:
    cookies_file = Path(cache_dir) / "cookies.txt"
    if cookies_file.exists():
        ydl_opts["cookiefile"] = str(cookies_file)
```

**Padrão de erro preservado** (linhas 120–133) — sem mudança:
```python
if info is None:
    raise ValueError("yt-dlp returned no metadata for the URL")
duration = info.get("duration")
if duration is None:
    raise ValueError("Could not determine video duration from YouTube metadata")
if duration > MAX_DURATION_SEC:
    raise ValueError(
        f"Video too long: {duration}s exceeds the 15-minute limit ..."
    )
return info
```

---

### `pipeline.py` — `download_audio` (modificar assinatura + ydl_opts + remover probe)

**Analog:** `pipeline.py` linhas 137–249 (função `download_audio` atual)

**Assinatura atual** (linha 137):
```python
def download_audio(url: str, cookies_path: str, po_token: str, bgutil_base_url: str = "") -> Path:
```

**Assinatura após migração** (D-02):
```python
def download_audio(url: str, cache_dir: str) -> Path:
```

**Bloco a remover — probe PIPE-06** (linhas 164–196):
```python
dl_player = "web" if bgutil_base_url else "android"
extractor_args: dict[str, list[str]] = {"youtube": [f"player_client={dl_player}"]}
if po_token:
    extractor_args["youtube"].append(f"po_token=web.gvs+{po_token}")
if bgutil_base_url:
    extractor_args["youtube"].append(f"getpot_bgutil_baseurl={bgutil_base_url}")

# PIPE-06: probe de disponibilidade do bgutil ...
if bgutil_base_url:
    logger.info("Probing bgutil availability at %s", bgutil_base_url)
    try:
        httpx.get(f"{bgutil_base_url}/", timeout=2.0)
        ...
    except httpx.RequestError as exc:
        raise BgutilUnavailable(...) from exc
```

**ydl_opts após migração** — substituir `extractor_args` por `cookiefile` do Volume:
```python
# Cookies do Railway Volume
cookies_file_path: str | None = None
if cache_dir:
    cookies_file = Path(cache_dir) / "cookies.txt"
    if cookies_file.exists():
        cookies_file_path = str(cookies_file)

ydl_opts: dict[str, Any] = {
    "format": "bestaudio/best",
    "outtmpl": outtmpl_base,
    "quiet": True,
    "no_warnings": True,
    "socket_timeout": 30,
    "noplaylist": True,
    # Sem extractor_args — ios/mweb default nao precisam de player_client manual
    "postprocessors": [{
        "key": "FFmpegExtractAudio",
        "preferredcodec": "wav",
    }],
    "no_cache_dir": True,
    "retries": 3,
    "fragment_retries": 3,
    "http_chunk_size": 10485760,
    "ffmpeg_location": _YTDLP_FFMPEG_LOCATION,
}
if cookies_file_path:
    ydl_opts["cookiefile"] = cookies_file_path
```

**Padrão de try/finally preservado** (linhas 219–249) — sem mudança:
```python
try:
    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
        ydl.download([url])
except yt_dlp.utils.YoutubeDLError as e:
    for f in WAV_TMP_DIR.glob(f"{TMP_PREFIX}{wav_id}*"):
        try:
            f.unlink()
        except OSError:
            pass
    raise RuntimeError(f"yt-dlp failed: {e}") from e
finally:
    # D-09: remove non-.wav intermediates that survived
    for f in WAV_TMP_DIR.glob(f"{TMP_PREFIX}{wav_id}*"):
        if f.suffix != ".wav":
            ...
```

**Classe a remover** (linhas 38–45):
```python
class BgutilUnavailable(RuntimeError):
    """Raised when the bgutil PO Token service is unreachable."""
    pass
```

**Import a remover** (linha 32):
```python
import httpx
```

---

### `pipeline.py` — `__main__` block (atualizar call sites)

**Analog:** `pipeline.py` linhas 562–620 (bloco `if __name__ == "__main__"`)

**Call sites atuais** (linhas 598–600):
```python
info = check_duration(url, cookies_path, bgutil_base_url)
wav_path = download_audio(url, cookies_path, po_token, bgutil_base_url)
```

**Call sites após migração**:
```python
cache_dir = os.environ.get("YTDLP_CACHE_DIR", "")
info = check_duration(url, cache_dir)
wav_path = download_audio(url, cache_dir)
```

**Variáveis a remover** (linhas 576–578):
```python
cookies_path = os.environ.get("YTDLP_COOKIES_FILE", "")
po_token = os.environ.get("YTDLP_PO_TOKEN", "")
bgutil_base_url = os.environ.get("BGUTIL_BASE_URL", "")
```

**Blocos a remover** (linhas 581–593) — validação de cookies no `__main__`:
```python
if not cookies_path:
    _emit_error("config_error", "YTDLP_COOKIES_FILE env var is not set. See .env.example.")
    sys.exit(1)
if not Path(cookies_path).exists():
    _emit_error("config_error", f"YTDLP_COOKIES_FILE does not exist: {cookies_path}")
    sys.exit(1)
if not po_token:
    print("WARNING: YTDLP_PO_TOKEN is empty. ...", file=sys.stderr)
```

---

### `api/config.py` (remover campos bgutil/cookies, adicionar cache_dir)

**Analog:** `api/config.py` linhas 36–55 (dataclass `Settings`)

**Padrão de campo existente** (campo simples via `os.environ.get`):
```python
# Padrao para campo novo — copiar exatamente este estilo (linha 52):
bgutil_base_url: str = field(default_factory=lambda: os.environ.get("BGUTIL_BASE_URL", ""))
```

**Campos a remover** (linhas 39–40, 52):
```python
cookies_path: str = field(default_factory=_resolve_cookies_path)  # linha 39
po_token: str = field(default_factory=lambda: os.environ.get("YTDLP_PO_TOKEN", ""))  # linha 40
bgutil_base_url: str = field(default_factory=lambda: os.environ.get("BGUTIL_BASE_URL", ""))  # linha 52
```

**Função a remover** (linhas 10–18):
```python
def _resolve_cookies_path() -> str:
    """Decode YTDLP_COOKIES_B64 env var to /tmp/sg_cookies.txt if set, else fall back to YTDLP_COOKIES_FILE."""
    b64 = os.environ.get("YTDLP_COOKIES_B64", "")
    if b64:
        dest = Path("/tmp/sg_cookies.txt")
        dest.write_bytes(base64.b64decode(b64))
        dest.chmod(0o600)
        return str(dest)
    return os.environ.get("YTDLP_COOKIES_FILE", "")
```

**Import a remover** (linha 3 — `base64` só era usado por `_resolve_cookies_path`):
```python
import base64
```

**Campo a adicionar** — inserir na posição de `bgutil_base_url` (D-02):
```python
cache_dir: str = field(default_factory=lambda: os.environ.get("YTDLP_CACHE_DIR", ""))
```

---

### `api/main.py` — substituir `_check_cookies()` por `_check_oauth_cache()`

**Analog perfeito:** `api/main.py` linhas 138–184 (`_check_cookies()`)

**Função existente completa** (linhas 138–184) — template para copiar e adaptar:
```python
def _check_cookies(cookies_path: str) -> None:
    """PIPE-05: Validate cookies.txt contains __Secure-3PSID sentinel.

    Non-blocking — logs CRITICAL and returns. Does NOT raise. Does NOT prevent startup.
    """
    if not cookies_path:
        logger.critical(
            "PIPE-05: cookies_path is not configured (YTDLP_COOKIES_FILE or "
            "YTDLP_COOKIES_B64 not set). YouTube downloads will likely fail with "
            "bot detection. Set cookies in Railway environment variables."
        )
        return

    cookies_file = Path(cookies_path)
    if not cookies_file.exists():
        logger.critical(
            "PIPE-05: cookies file not found at %s. YouTube downloads will likely "
            "fail with bot detection. Verify YTDLP_COOKIES_FILE or YTDLP_COOKIES_B64.",
            cookies_path,
        )
        return

    try:
        content = cookies_file.read_text(encoding="utf-8", errors="ignore")
    except OSError as e:
        logger.critical(
            "PIPE-05: could not read cookies file %s: %s. "
            "YouTube downloads may fail.",
            cookies_path, e,
        )
        return

    if "__Secure-3PSID" not in content:
        logger.critical(
            "PIPE-05: cookies file %s does not contain '__Secure-3PSID'. "
            "The cookie may be expired or from a non-authenticated export. "
            "Re-export cookies after logging into YouTube.",
            cookies_path,
        )
```

**Função nova** (D-08) — mesma estrutura, argumento `cache_dir`, path construído programaticamente:
```python
def _check_oauth_cache(cache_dir: str) -> None:
    """AUTH: Valida cookies.txt no Railway Volume — substitui _check_cookies() (PIPE-05).

    Non-blocking — logs CRITICAL e retorna. Nao levanta excecao. Nao bloqueia startup.
    Mesmo padrao de PIPE-05: aviso visivel nos Railway logs antes do primeiro job.
    """
    if not cache_dir:
        logger.critical(
            "AUTH: YTDLP_CACHE_DIR nao configurado. Downloads podem falhar com bot detection. "
            "Monte um Railway Volume em /data/yt-dlp-cache e configure YTDLP_CACHE_DIR. "
            "Para popular o Volume: railway run -- cp /local/cookies.txt /data/yt-dlp-cache/cookies.txt"
        )
        return
    cookies_file = Path(cache_dir) / "cookies.txt"
    if not cookies_file.exists():
        logger.critical(
            "AUTH: cookies.txt nao encontrado em %s. "
            "Copie cookies validos do YouTube para o Railway Volume.",
            cookies_file,
        )
        return
    try:
        content = cookies_file.read_text(encoding="utf-8", errors="ignore")
    except OSError as e:
        logger.critical("AUTH: Erro ao ler %s: %s", cookies_file, e)
        return
    if "__Secure-3PSID" not in content:
        logger.critical(
            "AUTH: cookies.txt em %s sem '__Secure-3PSID' — cookie expirado ou invalido. "
            "Re-exporte do browser apos login no YouTube.",
            cookies_file,
        )
```

**Lifespan — linha de chamada a substituir** (linha 192):
```python
# Antes:
_check_cookies(settings.cookies_path)

# Depois:
_check_oauth_cache(settings.cache_dir)
```

---

### `api/tasks.py` (remover import BgutilUnavailable + except handler)

**Analog:** `api/tasks.py` linhas 1–125 (arquivo completo)

**Import a remover** (linha 9):
```python
from pipeline import check_duration, download_audio, analyze_audio, BgutilUnavailable
# Depois:
from pipeline import check_duration, download_audio, analyze_audio
```

**Call sites a atualizar** (linhas 57–61):
```python
# Antes:
info = check_duration(url, settings.cookies_path, settings.bgutil_base_url)
wav_path = download_audio(url, settings.cookies_path, settings.po_token, settings.bgutil_base_url)

# Depois:
info = check_duration(url, settings.cache_dir)
wav_path = download_audio(url, settings.cache_dir)
```

**Bloco except a remover** (linhas 104–109):
```python
except BgutilUnavailable as e:
    logger.warning("Job %s bgutil_unavailable: %s", self.request.id, e)
    raise JobFailure(
        error=str(e),
        error_type="bgutil_unavailable",
    ) from e
```

**Padrão de exception chain preservado** — demais `except` ficam inalterados. A ordem correta sem bgutil:
```python
except ValueError as e:    # check_duration / validate_wav
    ...
except FileNotFoundError as e:    # WAV nao produzido
    ...
except RuntimeError as e:    # yt-dlp DownloadError wrappado
    ...
except Exception as e:    # catch-all sanitizer
    ...
```

---

### `requirements.txt` (remover linha bgutil)

**Analog:** `requirements.txt` linhas 1–16

**Linha a remover** (linha 16):
```
bgutil-ytdlp-pot-provider==0.8.5
```

**httpx** (linha 13) — **manter**. `httpx>=0.27` permanece porque pode ser dependência transitiva de fastapi/httpx-cliente; remover o probe não justifica remover a lib do requirements (D-05 e Pitfall 4 da RESEARCH.md).

---

### `tests/test_pipeline_fixes.py` (inverter asserts bgutil)

**Analog:** `tests/test_pipeline_fixes.py` linhas 130–152 (testes `test_bgutil_08x_*`) e linhas 163–252 (testes PIPE-06)

**Testes a inverter** (linhas 130–152) — de verificar presença para verificar ausência:
```python
# Antes (assert presença):
def test_bgutil_08x_extractor_key_check_duration():
    src = inspect.getsource(pipeline.check_duration)
    assert "getpot_bgutil_baseurl" in src, "..."
    assert "youtubepot-bgutilhttp" not in src, "..."

def test_bgutil_08x_extractor_key_download_audio():
    src = inspect.getsource(pipeline.download_audio)
    assert "getpot_bgutil_baseurl" in src, "..."

# Depois (assert ausencia):
def test_bgutil_removed_from_check_duration():
    """AUTH: check_duration nao deve conter nenhuma referencia a bgutil apos a migracao."""
    src = inspect.getsource(pipeline.check_duration)
    assert "getpot_bgutil_baseurl" not in src, (
        "bgutil nao foi removido de check_duration. "
        "Remover extractor_args de bgutil conforme D-03/D-04."
    )
    assert "bgutil" not in src, (
        "check_duration ainda contem referencia a bgutil."
    )

def test_bgutil_removed_from_download_audio():
    """AUTH: download_audio nao deve conter nenhuma referencia a bgutil apos a migracao."""
    src = inspect.getsource(pipeline.download_audio)
    assert "getpot_bgutil_baseurl" not in src, (
        "bgutil nao foi removido de download_audio."
    )
    assert "BgutilUnavailable" not in src, (
        "BgutilUnavailable ainda referenciado em download_audio."
    )
```

**Testes PIPE-06 a substituir** (linhas 163–252) — remover os 4 testes de probe; substituir por testes que verificam ausência:
```python
# Remover: test_pipe06_bgutil_probe_connect_error_raises
# Remover: test_pipe06_bgutil_probe_timeout_raises
# Remover: test_pipe06_no_probe_when_bgutil_url_empty
# Remover: test_pipe06_tasks_bgutil_error_type

# Substituir por (em test_pipeline_oauth.py — ver secao abaixo):
# - test_no_bgutil_probe_in_download_audio (verifica ausencia via inspect)
# - test_bgutil_unavailable_class_removed (verifica que a classe nao existe mais)
```

---

### `tests/test_pipeline_oauth.py` (NOVO arquivo — AUTH-01)

**Analog:** `tests/test_pipeline_fixes.py` linhas 163–252 (bloco PIPE-06 com `patch` + `MagicMock`)
**Analog secundario:** `tests/test_pipeline.py` linhas 57–77 (padrao `FakeYDL` + `captured_opts`)

**Estrutura do arquivo** — copiar cabeçalho de `test_pipeline_fixes.py`:
```python
"""Tests for Phase 10.1 auth migration (AUTH-01).

Verifica que apos a migracao:
- ydl_opts usa cookiefile do Railway Volume (cache_dir/cookies.txt) quando existir
- ydl_opts NAO contem username=oauth2, extractor_args de bgutil, cookiefile hardcoded
- _check_oauth_cache loga CRITICAL nos casos esperados
- bgutil-ytdlp-pot-provider nao esta em requirements.txt
- BgutilUnavailable nao existe mais em pipeline.py
"""
from __future__ import annotations

import inspect
import logging
from pathlib import Path
from unittest.mock import MagicMock, patch

import pytest
import pipeline
```

**Padrao de mock YoutubeDL** (copiar de `test_pipeline.py` linhas 66–78, adaptar para capturar opts):
```python
def _make_fake_ydl(captured_opts: dict, info: dict):
    """Factory: retorna classe FakeYDL que captura ydl_opts e retorna info mockado."""
    class FakeYDL:
        def __init__(self, opts):
            captured_opts.update(opts)
        def __enter__(self):
            return self
        def __exit__(self, *args):
            return False
        def extract_info(self, url, download=False):
            return info
        def download(self, urls):
            outtmpl = captured_opts["outtmpl"]
            Path(f"{outtmpl}.wav").write_bytes(b"RIFF\x00\x00\x00\x00WAVEfmt ")
            return 0
    return FakeYDL
```

**Teste principal AUTH-01** (D-09 adaptado para cookies no Volume):
```python
def test_check_duration_uses_cookiefile_from_cache_dir(tmp_path):
    """AUTH-01: check_duration deve usar cookiefile=cache_dir/cookies.txt quando existir."""
    cookies = tmp_path / "cookies.txt"
    cookies.write_text("# Netscape HTTP Cookie File\n__Secure-3PSID\tvalue\n")

    captured_opts: dict = {}
    FakeYDL = _make_fake_ydl(captured_opts, {"duration": 180, "title": "test"})

    with patch("pipeline.yt_dlp.YoutubeDL", FakeYDL):
        pipeline.check_duration("https://www.youtube.com/watch?v=test", str(tmp_path))

    assert captured_opts.get("cookiefile") == str(cookies), (
        f"cookiefile deveria ser {cookies!s}, obtido {captured_opts.get('cookiefile')!r}"
    )
    assert "username" not in captured_opts, "username=oauth2 nao deve estar presente (OAuth2 removido do yt-dlp)"
    assert "getpot_bgutil_baseurl" not in str(captured_opts.get("extractor_args", "")), (
        "extractor_args de bgutil nao deve estar presente"
    )
```

**Teste sem cookies** (cache_dir vazio):
```python
def test_check_duration_no_cookiefile_when_cache_dir_empty():
    """AUTH-01: check_duration sem cache_dir nao deve tentar cookiefile."""
    captured_opts: dict = {}
    FakeYDL = _make_fake_ydl(captured_opts, {"duration": 180})

    with patch("pipeline.yt_dlp.YoutubeDL", FakeYDL):
        pipeline.check_duration("https://www.youtube.com/watch?v=test", "")

    assert "cookiefile" not in captured_opts, "cookiefile nao deve ser setado quando cache_dir esta vazio"
    assert "username" not in captured_opts
```

**Teste startup validation** (copiar padrao de PIPE-05 em `test_pipeline_fixes.py` linhas 95–113):
```python
def test_check_oauth_cache_critical_when_missing(caplog, tmp_path):
    """AUTH-01: _check_oauth_cache deve logar CRITICAL quando cookies.txt nao existe."""
    from api.main import _check_oauth_cache
    with caplog.at_level(logging.CRITICAL, logger="api.main"):
        _check_oauth_cache(str(tmp_path))  # diretorio existe mas sem cookies.txt

    critical_msgs = [r for r in caplog.records if r.levelno >= logging.CRITICAL]
    assert len(critical_msgs) >= 1, (
        "_check_oauth_cache deve logar CRITICAL quando cookies.txt nao encontrado."
    )


def test_check_oauth_cache_critical_when_cache_dir_empty(caplog):
    """AUTH-01: _check_oauth_cache deve logar CRITICAL quando YTDLP_CACHE_DIR esta vazio."""
    from api.main import _check_oauth_cache
    with caplog.at_level(logging.CRITICAL, logger="api.main"):
        _check_oauth_cache("")

    critical_msgs = [r for r in caplog.records if r.levelno >= logging.CRITICAL]
    assert len(critical_msgs) >= 1, (
        "_check_oauth_cache deve logar CRITICAL quando cache_dir esta vazio."
    )
```

**Teste de requirements** (verificar ausencia de bgutil):
```python
def test_requirements_no_bgutil():
    """AUTH-01: bgutil-ytdlp-pot-provider nao deve estar em requirements.txt."""
    req_path = Path(__file__).parent.parent / "requirements.txt"
    content = req_path.read_text()
    assert "bgutil" not in content, (
        "bgutil-ytdlp-pot-provider ainda presente em requirements.txt. "
        "Remover a linha conforme D-05."
    )
```

**Teste de ausencia da classe** (verificar que BgutilUnavailable foi removida):
```python
def test_bgutil_unavailable_class_removed():
    """AUTH-01: BgutilUnavailable nao deve existir em pipeline.py apos a migracao."""
    assert not hasattr(pipeline, "BgutilUnavailable"), (
        "BgutilUnavailable ainda existe em pipeline.py. Remover a classe conforme D-04."
    )
```

---

## Shared Patterns

### Padrao de startup validation (non-blocking CRITICAL log)

**Source:** `api/main.py` linhas 138–184 (`_check_cookies`)
**Apply to:** `_check_oauth_cache` em `api/main.py`

Estrutura canonica:
1. `if not arg: logger.critical(...); return`
2. `if not path.exists(): logger.critical(...); return`
3. `try: content = path.read_text(...) except OSError: logger.critical(...); return`
4. `if sentinel not in content: logger.critical(...)`
5. Sem `raise` em nenhum ponto — sempre retorna silenciosamente apos o log

### Padrao de campo Settings (dataclass frozen)

**Source:** `api/config.py` linhas 36–55
**Apply to:** Novo campo `cache_dir`

```python
# Padrao: lambda com os.environ.get e default vazio
campo: str = field(default_factory=lambda: os.environ.get("ENV_VAR_NAME", ""))
```

### Padrao de mock yt_dlp.YoutubeDL em testes

**Source:** `tests/test_pipeline.py` linhas 57–77 + `tests/test_pipeline_fixes.py` linhas 199–213
**Apply to:** Todos os testes em `test_pipeline_oauth.py`

Dois estilos existentes:
- `FakeYDL` inline com `captured_opts.update(opts)` — preferido para verificar opts
- `patch("pipeline.httpx.get", side_effect=...)` — para verificar comportamento de erros

Para testes AUTH-01: usar o primeiro estilo (`FakeYDL` + `captured_opts`).

### Padrao de inspect.getsource para verificar ausencia

**Source:** `tests/test_pipeline_fixes.py` linhas 130–152
**Apply to:** Testes de ausencia de bgutil em `test_pipeline_fixes.py` (testes invertidos)

```python
src = inspect.getsource(pipeline.check_duration)
assert "termo_proibido" not in src, "mensagem de falha explicativa"
```

### Padrao de exception chain em tasks.py

**Source:** `api/tasks.py` linhas 85–124
**Apply to:** `api/tasks.py` — após remover BgutilUnavailable, verificar que RuntimeError ainda e o ultimo handler especifico antes do catch-all

Ordem obrigatoria (sem BgutilUnavailable):
```python
except ValueError as e:        # validacao de duracao
except FileNotFoundError as e: # WAV nao produzido
except RuntimeError as e:      # yt-dlp DownloadError wrappado
except Exception as e:         # catch-all sanitizer
```

---

## No Analog Found

Nenhum arquivo desta fase esta sem analog — todos tem correspondente direto no codebase.

---

## Metadata

**Analog search scope:** `/home/glow/Documentos/SoundGrabber2.0/` — `pipeline.py`, `api/`, `tests/`
**Files scanned:** 7 (`pipeline.py`, `api/config.py`, `api/main.py`, `api/tasks.py`, `requirements.txt`, `tests/test_pipeline_fixes.py`, `tests/conftest.py`)
**Pattern extraction date:** 2026-05-12
