---
phase: 06-precision-analysis-engine
plan: 02
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - pipeline.py
autonomous: true
requirements:
  - PREC-01
  - TUNING-01
  - TUNING-02

must_haves:
  truths:
    - "pipeline.py contém detect_tuning() que usa librosa HPSS e retorna float ou None"
    - "detect_tuning() retorna None quando razão de energia harmônica < 0.2 (sinal percussivo)"
    - "detect_tuning() retorna float Hz plausível para sinal harmônico (440Hz fixture → ~440)"
    - "detect_bpm() usa Essentia RhythmExtractor2013(method='multifeature') com sampleRate=44100"
    - "detect_bpm() retorna float Python nativo (não numpy.float32)"
    - "test_detect_tuning_harmonic e test_detect_tuning_percussive passam (GREEN)"
    - "test_bpm_accuracy passa (GREEN)"
  artifacts:
    - path: "pipeline.py"
      provides: "detect_tuning() e detect_bpm() Essentia"
      contains: "def detect_tuning("
    - path: "pipeline.py"
      provides: "import essentia.standard"
      contains: "import essentia.standard as es"
  key_links:
    - from: "pipeline.detect_tuning"
      to: "librosa.effects.hpss"
      via: "librosa.load(str(wav_path), sr=None, mono=True)"
      pattern: "librosa\\.effects\\.hpss"
    - from: "pipeline.detect_bpm"
      to: "essentia.standard.RhythmExtractor2013"
      via: "es.MonoLoader(filename=str(wav_path), sampleRate=44100)()"
      pattern: "sampleRate=44100"
---

<objective>
Implementar detect_tuning() (HPSS gate + librosa.estimate_tuning) e substituir detect_bpm() pelo algoritmo Essentia RhythmExtractor2013(method="multifeature"). Fazer os stubs RED do Plan 01 ficarem GREEN.

Purpose: detect_tuning() produz o tuning_hz que será passado como tuningFrequency ao KeyExtractor no Plan 03 — ordem de execução crítica. detect_bpm() Essentia é o mesmo algoritmo do Tunebat, eliminando a divergência que erode confiança do produtor.
Output: pipeline.py com detect_tuning() nova e detect_bpm() reescrita; testes TUNING-01, TUNING-02, PREC-01 verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-precision-analysis-engine/06-RESEARCH.md
@.planning/phases/06-precision-analysis-engine/06-01-SUMMARY.md

<interfaces>
<!-- Assinaturas existentes em pipeline.py que devem ser preservadas ou substituídas -->

<!-- MANTER (não alterar) -->
def check_duration(url: str, cookies_path: str) -> dict[str, Any]: ...
def download_audio(url: str, cookies_path: str, po_token: str) -> Path: ...
def convert_to_wav(audio_path: Path) -> Path: ...
def validate_wav(wav_path: Path) -> float: ...
def key_to_camelot(key: str) -> str: ...

<!-- SUBSTITUIR — assinatura atual (librosa-based): -->
def detect_bpm(wav_path: Path, total_duration: float) -> float:
    # usa librosa.feature.tempo + scipy.stats.uniform + _pick_best_tempo

<!-- NOVA assinatura após substituição: -->
def detect_bpm(wav_path: Path) -> float:
    # usa es.MonoLoader(sampleRate=44100) + es.RhythmExtractor2013(method="multifeature")
    # remove o argumento total_duration — Essentia não precisa de offset
    # retorna float(bpm)

<!-- NOVA função (não existe ainda): -->
def detect_tuning(wav_path: Path) -> float | None:
    # usa librosa.load(str(wav_path), sr=None, mono=True)
    # calcula ratio = harm_energy / (total_energy + 1e-10)
    # se ratio < 0.2: retorna None
    # senão: retorna float(librosa.tuning_to_A4(librosa.estimate_tuning(y=y_harmonic, sr=sr, resolution=0.01)))

<!-- INALTERADA por enquanto — será atualizada no Plan 03: -->
def detect_key(wav_path: Path) -> tuple[str, float]: ...
def analyze_audio(wav_path: Path) -> dict[str, Any]: ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar detect_tuning() com HPSS gate em pipeline.py</name>

  <read_first>
    - pipeline.py — ler COMPLETO antes de qualquer edição; ver imports existentes (librosa, numpy), posição das funções, estilo de docstrings
    - .planning/phases/06-precision-analysis-engine/06-RESEARCH.md — seção "Pattern 2: detect_tuning() com HPSS gate" e "Exemplo completo: detect_tuning()"
    - tests/test_pipeline.py — ler os stubs test_detect_tuning_harmonic e test_detect_tuning_percussive para entender o contrato exato esperado
  </read_first>

  <files>pipeline.py</files>

  <behavior>
    - detect_tuning(sample_wav_path) onde sample.wav é tom 440Hz → retorna float entre 400.0 e 480.0, não None
    - detect_tuning(percussive_wav) onde percussive_wav é ruído branco puro → retorna None (ratio harmônico ~0 < 0.2)
    - Retorno é sempre `float` nativo Python (via `float()`) ou `None` — nunca numpy.float64 diretamente
  </behavior>

  <action>
Adicionar `import essentia.standard as es` ao bloco de imports no topo de pipeline.py (após `import librosa`, antes do bloco de constantes). Manter todos os imports existentes.

Adicionar a função `detect_tuning()` a pipeline.py ANTES de `detect_bpm()`. Usar exatamente esta implementação:

```python
# Stage 3a: Tuning detection (TUNING-01, TUNING-02)
def detect_tuning(wav_path: Path) -> float | None:
    """Detecta frequência de referência (concert pitch) do beat via HPSS + librosa.estimate_tuning.

    Aplica Harmonic-Percussive Source Separation (HPSS) para isolar o componente harmônico.
    Se a razão de energia harmônica for < 0.2 (beat puramente percussivo), retorna None —
    o resultado seria ruído, não um concert pitch mensurável.

    Args:
        wav_path: Path para o arquivo WAV.

    Returns:
        Frequência de afinação em Hz (float), ou None se o beat for essencialmente percussivo.
        Exemplo: 440.0 para A=440 padrão, 432.0 para A=432, None para trap sem melodia.
    """
    y, sr = librosa.load(str(wav_path), sr=None, mono=True)
    y_harmonic, _ = librosa.effects.hpss(y)

    total_energy = float(np.sum(y**2))
    harm_energy = float(np.sum(y_harmonic**2))
    ratio = harm_energy / (total_energy + 1e-10)  # 1e-10: evita divisão por zero em silêncio puro

    if ratio < 0.2:
        return None  # beat puramente percussivo — tuning seria ruído

    raw_tuning = librosa.estimate_tuning(y=y_harmonic, sr=sr, resolution=0.01)
    return float(librosa.tuning_to_A4(raw_tuning))
```

Após adicionar, rodar os dois testes de tuning para confirmar GREEN:

```bash
.venv/bin/python3 -m pytest tests/test_pipeline.py::test_detect_tuning_harmonic tests/test_pipeline.py::test_detect_tuning_percussive -x -v 2>&1 | tail -20
```

Ambos devem passar (PASSED). Se test_detect_tuning_harmonic falhar com "400.0 < result < 480.0", revisar se `sr=None` está sendo usado (preservar taxa de amostragem original).
  </action>

  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_pipeline.py::test_detect_tuning_harmonic tests/test_pipeline.py::test_detect_tuning_percussive -x -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - pipeline.py contém `def detect_tuning(wav_path: Path) -> float | None:`
    - pipeline.py contém `import essentia.standard as es`
    - pipeline.py contém `librosa.effects.hpss(y)`
    - pipeline.py contém `ratio < 0.2`
    - pipeline.py contém `librosa.tuning_to_A4(`
    - pipeline.py contém `return float(librosa.tuning_to_A4(`
    - `pytest tests/test_pipeline.py::test_detect_tuning_harmonic -x -q` sai com 0 (PASSED)
    - `pytest tests/test_pipeline.py::test_detect_tuning_percussive -x -q` sai com 0 (PASSED)
  </acceptance_criteria>

  <done>detect_tuning() implementada; test_detect_tuning_harmonic e test_detect_tuning_percussive GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Substituir detect_bpm() por Essentia RhythmExtractor2013 em pipeline.py</name>

  <read_first>
    - pipeline.py — ler a função detect_bpm() atual e _pick_best_tempo() para entender o que será removido; ver analyze_audio() para entender como detect_bpm é chamada (atualmente com argumento total_duration)
    - tests/test_pipeline.py — ler test_bpm_accuracy (linha 121-130) e test_bpm_half_double_calculation (linha 155-168) para entender as assinaturas esperadas pelos testes existentes
    - .planning/phases/06-precision-analysis-engine/06-RESEARCH.md — seção "Pattern 3: detect_bpm() com Essentia RhythmExtractor2013" e "Pitfall 1: sampleRate incorreto"
  </read_first>

  <files>pipeline.py</files>

  <behavior>
    - detect_bpm(sample_wav_path) retorna float > 30.0 e < 300.0 (range plausível para qualquer beat)
    - detect_bpm retorna Python float nativo (json.dumps({'bpm': detect_bpm(p)}) não levanta TypeError)
    - Assinatura nova: detect_bpm(wav_path: Path) -> float (sem argumento total_duration)
  </behavior>

  <action>
Substituir COMPLETAMENTE a função `detect_bpm()` e o helper `_pick_best_tempo()` em pipeline.py. Manter as constantes `_MAJOR_PROFILE`, `_MINOR_PROFILE`, `_NOTES` no arquivo (são usadas pelo detect_key() legado que ainda existe — será substituído no Plan 03).

REMOVER:
- Função `_pick_best_tempo(onset_env, sr, tempo_base)` inteira
- Função `detect_bpm(wav_path, total_duration)` inteira

ADICIONAR no lugar (mesma posição no arquivo, após detect_tuning()):

```python
# Stage 3b: BPM detection (PREC-01)
def detect_bpm(wav_path: Path) -> float:
    """BPM via Essentia RhythmExtractor2013 multifeature — mesmo algoritmo do Tunebat.

    Resistente a erros de octave (half-tempo trap): o modo multifeature funde múltiplas
    funções de onset e é built-in octave-resistant.

    CRÍTICO: sampleRate=44100 é obrigatório para RhythmExtractor2013. Valores incorretos
    produzem BPM ~50% ou ~200% do real, sem Python exception.

    Args:
        wav_path: Path para o arquivo WAV.

    Returns:
        BPM como Python float. Nunca numpy.float32 — float() defensivo aplicado.
    """
    audio = es.MonoLoader(filename=str(wav_path), sampleRate=44100)()
    bpm, _, _, _, _ = es.RhythmExtractor2013(method="multifeature")(audio)
    return float(bpm)  # float() defensivo: binding cp312 já retorna float, mas garante robustez futura
```

TAMBÉM atualizar a chamada em `analyze_audio()`. A linha atual é:
```python
bpm = detect_bpm(wav_path, total_duration=duration_sec)
```
Substituir por:
```python
bpm = detect_bpm(wav_path)
```

TAMBÉM atualizar o mock em `test_bpm_half_double_calculation` em tests/test_pipeline.py. A linha atual é:
```python
with patch.object(pipeline, "detect_bpm", return_value=140.0), \
     patch.object(pipeline, "detect_key", return_value=("C major", 1.0)), \
```
O mock já retorna 140.0 diretamente (não chama detect_bpm de verdade), então a assinatura da função não afeta esse teste. Verificar que o teste ainda passa.

Após as alterações, rodar:

```bash
.venv/bin/python3 -m pytest tests/test_pipeline.py::test_bpm_accuracy -m integration -x -v 2>&1 | tail -20
.venv/bin/python3 -m pytest tests/test_pipeline.py -m "not integration and not e2e" -q 2>&1 | tail -10
```

O test_bpm_accuracy deve PASSED. Os testes unit devem permanecer todos PASSED.

PITFALL a evitar: NÃO remover `import scipy.stats` do topo do arquivo neste plan — pode quebrar outros módulos importados. A limpeza de imports obsoletos (scipy.stats, _MAJOR_PROFILE, etc.) ocorre no Plan 03 junto com a substituição de detect_key().
  </action>

  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_pipeline.py::test_bpm_accuracy -m integration -x -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - pipeline.py contém `def detect_bpm(wav_path: Path) -> float:` (sem argumento total_duration)
    - pipeline.py contém `es.RhythmExtractor2013(method="multifeature")`
    - pipeline.py contém `es.MonoLoader(filename=str(wav_path), sampleRate=44100)`
    - pipeline.py NÃO contém `def _pick_best_tempo(` (função legada removida)
    - pipeline.py NÃO contém `scipy.stats.uniform` dentro de detect_bpm (librosa prior removido)
    - `pytest tests/test_pipeline.py::test_bpm_accuracy -m integration -x -q` sai com 0 (PASSED)
    - `pytest tests/test_pipeline.py -m "not integration and not e2e" -q` sai com 0 falhas
    - `grep "detect_bpm(wav_path, total_duration" pipeline.py` retorna 0 linhas (assinatura antiga removida)
  </acceptance_criteria>

  <done>detect_bpm() reescrita com Essentia; test_bpm_accuracy GREEN; suite unit intacta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wav_path → detect_tuning/detect_bpm | Path vindo de analyze_audio(); já sanitizado por validate_wav() em Stage 2 antes de chegar aqui |
| Essentia MonoLoader → filesystem | Lê o arquivo WAV em disco; caminho validado upstream por validate_wav() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | detect_bpm numpy output | mitigate | `float()` defensivo aplicado em todos os retornos Essentia — numpy.float32 no dict quebraria o Celery JSON serializer silenciosamente |
| T-06-02-02 | Information Disclosure | detect_tuning WAV path | accept | wav_path já foi validado por validate_wav() (ffprobe check) antes de chegar a detect_tuning; path traversal já mitigado upstream |
| T-06-02-03 | Denial of Service | librosa.load sr=None em WAV grande | accept | O pipeline já impõe limite de 15 min (900s) via check_duration — WAVs maiores que o limite nunca chegam a este ponto |
| T-06-02-04 | Elevation of Privilege | essentia.standard subprocess | accept | Essentia roda in-process como biblioteca Python pura — não executa subprocessos nem tem acesso especial ao sistema |
</threat_model>

<verification>
Após este plan, a suite de integração parcial deve estar verde:

```bash
.venv/bin/python3 -m pytest tests/test_pipeline.py -m "not e2e" -q
```

Esperado: test_detect_tuning_harmonic PASSED, test_detect_tuning_percussive PASSED, test_bpm_accuracy PASSED, testes unit PASSED. test_detect_key_uses_tuning_hz ainda RED (implementado no Plan 03).
</verification>

<success_criteria>
- `grep "def detect_tuning" pipeline.py` retorna match
- `grep "RhythmExtractor2013" pipeline.py` retorna match
- `grep "sampleRate=44100" pipeline.py` retorna match
- `pytest tests/test_pipeline.py::test_detect_tuning_harmonic tests/test_pipeline.py::test_detect_tuning_percussive tests/test_pipeline.py::test_bpm_accuracy -m integration -q` — 3 passed
- `pytest tests/test_pipeline.py -m "not integration and not e2e" -q` — 0 falhas
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-precision-analysis-engine/06-02-SUMMARY.md`
</output>
