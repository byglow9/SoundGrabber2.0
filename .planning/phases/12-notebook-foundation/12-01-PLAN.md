---
phase: 12-notebook-foundation
plan: 01
type: create
wave: 1
depends_on: []
files_modified:
  - scripts/notebook-setup.sh
autonomous: true
requirements:
  - SVR-04

must_haves:
  truths:
    - "scripts/notebook-setup.sh existe e passa em bash -n (sintaxe válida)"
    - "Script começa com set -e imediatamente após o shebang"
    - "Script aplica chmod 750 em si mesmo"
    - "Script cobre 7 seções: preflight, lid-close, Docker, swap, cgroups v2, watchdog, verificação final"
    - "Seção Docker usa apt repo oficial da Docker (não convenience script, não snap)"
    - "Swap de 4GB com fallocate e fallback dd, idempotente"
    - "Drop-ins de sleep/lid em /etc/systemd/logind.conf.d/ e /etc/systemd/sleep.conf.d/"
    - "Watchdog via /etc/systemd/system.conf.d/10-watchdog.conf com RuntimeWatchdogSec=15"
    - "Seção de verificação final imprime status observável de cada componente"
  artifacts:
    - path: "scripts/notebook-setup.sh"
      provides: "Script reproduzível para configurar o notebook HP do zero (SVR-04)"
      contains: "Docker install, swap 4GB, cgroups v2 check, watchdog systemd, lid-close prevention"
  key_links:
    - from: "scripts/notebook-setup.sh"
      to: "SVR-04"
      via: "script commitado e reproduzível — satisfaz requisito de script documentado"
      pattern: "notebook-setup.sh"
---

<objective>
Criar `scripts/notebook-setup.sh` — script bash reproduzível que configura o notebook HP do Moisés
para rodar o SoundGrabber de forma headless e autônoma com Ubuntu Server 24.04 LTS.

Purpose: O Moisés deve poder reconfigurar o notebook do zero com um único comando `sudo bash notebook-setup.sh`.
O script é a evidência auditável de SVR-04 e o pré-requisito para os demais SVR-01..03 verificados no Plan 02.

Output: scripts/notebook-setup.sh commitado ao repositório, executável, com sintaxe válida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-notebook-foundation/12-CONTEXT.md
@.planning/ROADMAP.md

<!-- Phase 12 success criteria (4 critérios observáveis do ROADMAP):
  1. uname -m via SSH retorna x86_64 e lsb_release -rs retorna 24.04 (SVR-01)
  2. docker info mostra Cgroup Version: 2 + swapon --show mostra /swapfile Size ≥ 4G (SVR-02)
  3. HandleLidSwitch=ignore confirmado + RuntimeWatchdogSec=15 em 10-watchdog.conf (SVR-03)
  4. bash notebook-setup.sh reproduz o ambiente sem passos manuais (SVR-04 — satisfeito por este plan) -->
</context>

<tasks>

<task type="create_file">
  <name>Tarefa 1: Criar scripts/notebook-setup.sh</name>
  <files>scripts/notebook-setup.sh</files>
  <read_first>
    - .planning/phases/12-notebook-foundation/12-CONTEXT.md — decisões D-01..D-12
    - CLAUDE.md §Security Gate §Controles obrigatórios em qualquer novo script shell
    - start.sh (linhas 1-20) — padrão de script bash do projeto
  </read_first>
  <what-to-build>
Script bash com as seguintes seções em ordem:

**Seção 0 — Header e segurança**
- `#!/usr/bin/env bash`
- `set -e` imediatamente após o shebang
- `chmod 750 "$(realpath "$0")"` — auto-chmod obrigatório (Security Gate)
- Verificar que está rodando como root (`[ "$(id -u)" -eq 0 ]`)
- Capturar `TARGET_USER="${SUDO_USER:-}"` — usuário operador (não hardcode)

**Seção 1 — Preflight (read-only, não modifica nada)**
- `uname -m` — confirmar x86_64 (imprimir valor; se não for x86_64, avisar mas não bloquear — Ubuntu Server 24.04 só instala em amd64 então isso é apenas sanity check)
- `lsb_release -rs` — confirmar 24.04
- `df -h /` — espaço disponível (aviso se < 10GB)
- `free -h` — RAM disponível
- `swapon --show` — swap atual

**Seção 2 — Prevenção de sleep/hibernate (crítico para notebook servidor)**
- Criar `/etc/systemd/logind.conf.d/nosleep.conf` de forma idempotente:
  ```ini
  [Login]
  HandleLidSwitch=ignore
  HandleLidSwitchExternalPower=ignore
  HandleSuspendKey=ignore
  HandleHibernateKey=ignore
  IdleAction=ignore
  IdleActionSec=0
  ```
- Criar `/etc/systemd/sleep.conf.d/nosleep.conf` de forma idempotente:
  ```ini
  [Sleep]
  AllowSuspend=no
  AllowHibernation=no
  AllowSuspendThenHibernate=no
  AllowHybridSleep=no
  ```
- `systemctl daemon-reload`
- `systemctl restart systemd-logind`

**Seção 3 — Docker via apt repo oficial**
- Instalar dependências: `apt-get update && apt-get install -y ca-certificates curl gnupg`
- Criar `/etc/apt/keyrings/docker.asc` com a chave GPG oficial da Docker
  URL: `https://download.docker.com/linux/ubuntu/gpg`
  Idempotente: verificar se arquivo existe antes de baixar
- Adicionar repo: `deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu noble stable`
  Idempotente: verificar se `/etc/apt/sources.list.d/docker.list` existe antes de criar
- `apt-get update && apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin`
- `systemctl enable --now docker`
- Se `TARGET_USER` não vazio: `usermod -aG docker "$TARGET_USER"` com aviso explícito que grupo docker equivale a root

**Seção 4 — Swap (4GB)**
- Idempotente: verificar `[ -f /swapfile ]` antes de criar
- `fallocate -l 4G /swapfile` com fallback: `dd if=/dev/zero of=/swapfile bs=1M count=4096 status=progress`
- `chmod 600 /swapfile`
- `mkswap /swapfile`
- `swapon /swapfile`
- Persistência em `/etc/fstab` — verificar se já existe entrada antes de adicionar:
  `/swapfile none swap sw 0 0`
- `sysctl vm.swappiness=10` + persistir em `/etc/sysctl.d/99-swappiness.conf`

**Seção 5 — Cgroups v2 (verificação)**
- Ubuntu Server 24.04 usa cgroups v2 por padrão — não precisa modificar nada
- Verificar: `cat /sys/fs/cgroup/cgroup.controllers | grep memory`
- Se não contiver `memory`, imprimir aviso claro (não abortar — raro no Ubuntu 24.04)

**Seção 6 — Watchdog systemd**
- Criar `/etc/systemd/system.conf.d/10-watchdog.conf` de forma idempotente:
  ```ini
  [Manager]
  RuntimeWatchdogSec=15
  ShutdownWatchdogSec=2min
  ```
- `systemctl daemon-reload`
- Nota: o kernel expõe `/dev/watchdog` via módulo iTCO_wdt (Intel) ou sp5100_tco (AMD)
  systemd faz feed automático via /dev/watchdog se disponível; não exigir o módulo aqui

**Seção 7 — Verificação final**
Imprimir uma tabela de status com todos os componentes:

```
========================================
  NOTEBOOK SETUP — VERIFICAÇÃO FINAL
========================================
Arquitetura:    [output de uname -m]
OS:             [output de lsb_release -ds]
Docker:         [output de docker --version ou ERRO]
Swap:           [output de swapon --show --noheadings ou NENHUM]
Cgroup Version: [output de docker info grep "Cgroup Version" ou ERRO]
Lid Switch:     [output de systemctl show logind grep HandleLidSwitch]
Watchdog conf:  [cat 10-watchdog.conf RuntimeWatchdogSec line ou AUSENTE]
========================================
PRÓXIMO PASSO: sudo reboot
Após reboot, conectar via Tailscale SSH e reportar outputs ao Claude.
========================================
```

**Estrutura geral do script:**
- Funções auxiliares: `log_info()`, `log_ok()`, `log_warn()`, `log_error()` com cores ANSI simples
- Cada seção imprime seu próprio banner: `echo "--- [N] Nome da Seção ---"`
- Funções de verificação idempotente reutilizadas: `file_contains()`, `ensure_dir()`
  </what-to-build>
  <acceptance_criteria>
    - `bash -n scripts/notebook-setup.sh` retorna 0 (sem erros de sintaxe)
    - Script começa com `#!/usr/bin/env bash` + `set -e` nas primeiras 3 linhas
    - `chmod 750 "$(realpath "$0")"` presente no início
    - Seção Docker usa `download.docker.com/linux/ubuntu` — sem `get.docker.com`
    - Swap de 4GB com `fallocate` + fallback `dd` + idempotência
    - Dois drop-ins de sleep criados de forma idempotente
    - Drop-in de watchdog `10-watchdog.conf` criado de forma idempotente
    - Seção final imprime status observável de cada componente
    - Script termina instruindo `sudo reboot`
    - Nenhum `eval` de input externo
  </acceptance_criteria>
  <done>
    scripts/notebook-setup.sh existe com sintaxe válida, estrutura completa de 7 seções,
    e passa em `bash -n`. Pronto para o Moisés executar no notebook.
  </done>
</task>

<task type="shell">
  <name>Tarefa 2: Verificar sintaxe e commitar</name>
  <files>scripts/notebook-setup.sh</files>
  <commands>
    bash -n scripts/notebook-setup.sh
    git add scripts/notebook-setup.sh
    git commit -m "feat(12): add notebook-setup.sh — Ubuntu Server 24.04 host foundation script"
  </commands>
  <acceptance_criteria>
    - `bash -n` retorna 0
    - Commit criado com o script
  </acceptance_criteria>
  <done>
    Script commitado ao repositório. SVR-04 satisfeito.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Operador → apt repo Docker | Script baixa chave GPG e pacotes do repositório oficial Docker — HTTPS + verificação de assinatura |
| Script → /etc/systemd/ | Escreve drop-ins como root — idempotente, sem sobrescrever configs existentes |
| Script → /swapfile | Cria arquivo como root com chmod 600 — acesso restrito |
| Moisés → notebook (sudo bash) | Operador executa o script como root — confia no conteúdo commitado no repositório |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Supply Chain | Docker GPG key download via HTTPS | accept | HTTPS + apt verifica assinatura antes de instalar; risco residual pequeno vs convenience script |
| T-12-02 | Privilege Escalation | usermod -aG docker TARGET_USER | accept | Aviso explícito no script; grupo docker equivale a root — escolha consciente do operador |
| T-12-03 | Tampering | /etc/fstab modificado pelo script | mitigate | Verificar existência da entrada antes de adicionar; `set -e` aborta em caso de falha |
| T-12-04 | DoS | swap insuficiente se HDD falhar durante criação | mitigate | Fallback dd com status progress; falha aborta script via set -e |
</threat_model>

<verification>
```bash
# SVR-04: script existe e tem sintaxe válida
bash -n scripts/notebook-setup.sh && echo "SVR-04: sintaxe OK"

# Security Gate: set -e presente
head -3 scripts/notebook-setup.sh | grep -q "set -e" && echo "Security Gate set -e: OK"

# Security Gate: sem eval de input externo
grep -n "eval" scripts/notebook-setup.sh || echo "Sem eval: OK"

# Docker via apt repo oficial (não convenience script)
grep -q "download.docker.com/linux/ubuntu" scripts/notebook-setup.sh && echo "Docker apt repo: OK"
! grep -q "get.docker.com" scripts/notebook-setup.sh && echo "Sem convenience script: OK"

# Swap 4GB
grep -q "4G\|4096" scripts/notebook-setup.sh && echo "Swap 4GB: OK"

# Drop-ins de sleep presentes
grep -q "nosleep.conf" scripts/notebook-setup.sh && echo "Sleep prevention: OK"

# Watchdog systemd
grep -q "10-watchdog.conf" scripts/notebook-setup.sh && echo "Watchdog conf: OK"
grep -q "RuntimeWatchdogSec=15" scripts/notebook-setup.sh && echo "RuntimeWatchdogSec: OK"
```
</verification>

<success_criteria>
- scripts/notebook-setup.sh existe, tem sintaxe válida (`bash -n` passa), e está commitado
- Script cobre as 7 seções: preflight, lid-close, Docker, swap, cgroups v2, watchdog, verificação final
- Docker instalado via apt repo oficial (sem get.docker.com)
- Swap 4GB idempotente com fallback dd
- Drop-ins de sleep e watchdog criados de forma idempotente
- SVR-04 satisfeito: script documentado e commitado ao repositório
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-notebook-foundation/12-01-SUMMARY.md`
</output>
