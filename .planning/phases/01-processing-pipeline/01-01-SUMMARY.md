---
phase: 01-processing-pipeline
plan: 01
subsystem: test-scaffolding
tags: [setup, testing, pipeline, python, wave-0]
completed: 2026-04-30
duration: ~5 minutes
tasks_completed: 3/3
files_created: 8
files_modified: 0

dependency_graph:
  requires: []
  provides:
    - requirements.txt (pinned deps for all Phase 1 plans)
    - tests/test_pipeline.py (13 verification stubs for Plans 02-04)
    - tests/fixtures/sample.wav (offline analysis fixture)
    - pytest.ini (integration + e2e markers)
  affects:
    - Plans 02, 03, 04 (implement pipeline.py to turn stubs green)

tech_stack:
  added:
    - yt-dlp==2026.3.17
    - librosa==0.11.0
    - soundfile==0.13.1
    - numpy>=2.0,<3.0
    - scipy>=1.10
    - pytest==9.0.3
    - pytest-subprocess>=1.5
  patterns:
    - pytest.importorskip for pipeline-absent test collection
    - Fixture WAV generated by committed reproducible script

key_files:
  created:
    - requirements.txt
    - .env.example
    - .gitignore
    - pytest.ini
    - scripts/generate_sample_wav.py
    - tests/__init__.py
    - tests/conftest.py
    - tests/fixtures/sample.wav
    - tests/test_pipeline.py
  modified: []

decisions:
  - "Used pytest.importorskip at test function scope (not module-level) so each stub can carry its own reason string"
  - "WAV fixture committed to repo (220KB) — deterministic, reproducible via generate_sample_wav.py, verified by soundfile"
  - "4 integration markers (not 3 minimum) — test_ffprobe_validates_wav added as integration alongside the 3 analysis tests"

key_decisions:
  - "pytest.importorskip per-function (not module) for granular skip reasons"
  - "sample.wav committed: deterministic 220KB fixture, reproducible from committed script"
---

# Phase 01 Plan 01: Project Skeleton and Test Scaffolding Summary

Project skeleton and Wave 0 test infrastructure established: 9 files created including pinned requirements, environment documentation, security-aware gitignore, pytest configuration with markers, a reproducible fixture WAV generator, shared conftest fixtures, and 13 test stubs covering all Phase 1 pipeline behaviors.

## Files Created

| File | Role |
|------|------|
| `requirements.txt` | Pinned Python deps — yt-dlp, librosa, soundfile, pytest and supporting libs |
| `.env.example` | Documents `YTDLP_COOKIES_FILE` and `YTDLP_PO_TOKEN` (D-01/D-02) with usage notes |
| `.gitignore` | Excludes `cookies.txt`, `.env`, `/tmp/sg_*`, Python artifacts, editor files |
| `pytest.ini` | Configures `integration` and `e2e` markers; sets `testpaths = tests`; `--strict-markers` |
| `scripts/generate_sample_wav.py` | Reproducible generator for the fixture WAV (5s, 440Hz, 22050Hz, PCM16) |
| `tests/__init__.py` | Empty init to make tests a Python package |
| `tests/conftest.py` | Shared fixtures: `sample_wav_path`, `mock_yt_info_short`, `mock_yt_info_long`, `youtube_test_urls` |
| `tests/fixtures/sample.wav` | 5-second 440Hz mono WAV (216KB) for offline analysis tests |
| `tests/test_pipeline.py` | 13 test stubs for all Phase 1 verification behaviors (251 lines) |

## Exact Pinned Dependency Versions

| Package | Version | Purpose |
|---------|---------|---------|
| yt-dlp | 2026.3.17 | YouTube audio download |
| librosa | 0.11.0 | BPM + key detection |
| soundfile | 0.13.1 | WAV I/O backend for librosa |
| numpy | >=2.0,<3.0 | Array ops (librosa 0.11.0 supports numpy 2.x) |
| scipy | >=1.10 | Signal processing deps for librosa |
| pytest | 9.0.3 | Test framework |
| pytest-subprocess | >=1.5 | Mock subprocess for ffprobe tests |

## Test Stub List — Which Plan Turns Each Green

| Test Name | Requirement | Plan | Wave | Marker |
|-----------|-------------|------|------|--------|
| `test_duration_check_rejects_long_video` | CORE-05 | 02 | 1 | unit |
| `test_duration_check_accepts_short_video` | CORE-05 | 02 | 1 | unit |
| `test_download_opts_include_auth` | CORE-03 | 02 | 1 | unit |
| `test_wav_file_created` | CORE-04 | 02 | 1 | integration |
| `test_ffprobe_validates_wav` | CORE-04 | 02 | 1 | integration |
| `test_bpm_accuracy` | ANALYSIS-01 | 03 | 2 | integration |
| `test_key_detection` | ANALYSIS-02 | 03 | 2 | integration |
| `test_bpm_half_double_calculation` | ANALYSIS-03 | 03 | 2 | unit |
| `test_camelot_mapping` | ANALYSIS-04 | 03 | 2 | unit |
| `test_json_output_shape` | D-05 | 03/04 | 3 | unit |
| `test_e2e_rock` | D-07 | 04 | 3 | e2e |
| `test_e2e_trap` | D-07 | 04 | 3 | e2e |
| `test_e2e_house` | D-07 | 04 | 3 | e2e |

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| Task 1 | c64d428 | `chore(01-01): create project skeleton` |
| Task 2 | cfdf22b | `feat(01-01): add fixture WAV generator and pytest conftest` |
| Task 3 | f3a5cdf | `test(01-01): add 13 pipeline test stubs` |

## Deviations from Plan

None — plan executed exactly as written.

The only minor note: the plan specified `grep -c "@pytest.mark.integration" tests/test_pipeline.py` returns "at least 3". Actual count is 4 (`test_wav_file_created`, `test_ffprobe_validates_wav`, `test_bpm_accuracy`, `test_key_detection`) — this exceeds the minimum and is correct per 01-VALIDATION.md row 1-01-04.

## Threat Mitigations Applied

Per plan's `<threat_model>`:

| Threat | Mitigation Verified |
|--------|---------------------|
| T-01-01-01: cookies.txt accidentally committed | `.gitignore` has `cookies.txt` on its own line; verified by acceptance criteria grep |
| T-01-01-02: PO Token committed in code | `.env.example` shows empty value; `.env` is in `.gitignore` |
| T-01-01-03: sample.wav tampered | Synthetic tone generated by committed `scripts/generate_sample_wav.py`; reviewers can re-run to verify |

## Known Stubs

None that block the plan's goal. All 13 test stubs skip cleanly via `pytest.importorskip("pipeline", ...)` — this is intentional Wave 0 behavior. Plans 02-04 will implement `pipeline.py` and turn each stub green individually.

## Self-Check: PASSED

All files verified:
- `requirements.txt` exists and contains `yt-dlp==2026.3.17` ✓
- `.env.example` exists and contains `YTDLP_COOKIES_FILE` and `YTDLP_PO_TOKEN` ✓
- `.gitignore` contains `cookies.txt`, `.env`, `/tmp/sg_*` ✓
- `pytest.ini` contains `integration:` and `e2e:` markers ✓
- `scripts/generate_sample_wav.py` exists with `SAMPLE_RATE = 22050`, `DURATION_SEC = 5.0`, `FREQUENCY_HZ = 440.0` ✓
- `tests/fixtures/sample.wav` is 216KB (within 200-250KB range), validated at 5.00s @ 22050Hz ✓
- `tests/__init__.py` exists ✓
- `tests/conftest.py` has all 4 fixtures ✓
- `tests/test_pipeline.py` is 251 lines, 13 tests collected, 3 e2e markers, 4 integration markers ✓
- All commits exist: c64d428, cfdf22b, f3a5cdf ✓
