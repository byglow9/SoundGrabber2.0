---
phase: 11-som-da-semana-curated-sidebar-panel-featuring-underground-mu
plan: 04
type: execute
wave: 3
depends_on:
  - 11-02
  - 11-03
files_modified:
  - .planning/SECURITY-CHECKLIST.md
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
must_haves:
  truths:
    - "The project security checklist records the new Phase 11 operator auth, endpoint rate limits, validation, signed cookie, Redis fallback, and XSS-safe rendering controls."
    - "A human can visit /yonkou directly and verify the operator login/edit surface exists while the public page exposes no route affordance."
    - "Full security plus frontend test gates pass after backend and frontend implementation."
  artifacts:
    - path: ".planning/SECURITY-CHECKLIST.md"
      provides: "Documented Phase 11 security controls"
      contains: "Som da Semana"
  key_links:
    - from: ".planning/SECURITY-CHECKLIST.md"
      to: "api/main.py"
      via: "Checklist verification commands for /featured and /yonkou controls"
      pattern: "test_.*featured|test_.*yonkou"
    - from: ".planning/SECURITY-CHECKLIST.md"
      to: "static/app.js"
      via: "Checklist XSS-safe rendering assertion"
      pattern: "textContent|noopener"
---

<objective>
Close Phase 11 with security documentation, full automated gates, and direct hidden-route verification.

Purpose: The project Security Gate requires checklist updates for new controls, and the validation contract requires a manual browser check that `/yonkou` works only by direct navigation.
Output: Updated security checklist and a human-verified operator route.
</objective>

<execution_context>
@/home/glow/Documentos/projetos/SoundGrabber2.0/.codex/get-shit-done/workflows/execute-plan.md
@/home/glow/Documentos/projetos/SoundGrabber2.0/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SECURITY-CHECKLIST.md
@.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-CONTEXT.md
@.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-RESEARCH.md
@.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-PATTERNS.md
@.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-UI-SPEC.md
@.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-VALIDATION.md
@api/main.py
@static/index.html
@static/app.js
@static/style.css
@tests/test_security.py
@tests/test_frontend.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update security checklist for Phase 11 controls</name>
  <files>.planning/SECURITY-CHECKLIST.md</files>
  <read_first>
    @.planning/SECURITY-CHECKLIST.md
    @api/main.py
    @static/app.js
    @tests/test_security.py
    @tests/test_frontend.py
    @.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-CONTEXT.md
    @.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-VALIDATION.md
  </read_first>
  <action>Add a Phase 11 section to `.planning/SECURITY-CHECKLIST.md` covering `GET /featured` 60/min, `GET /yonkou` 60/min, `POST /yonkou/login` 5/min, `POST /featured` 10/min, `ADMIN_PASSWORD`, signed HttpOnly SameSite session cookie, Pydantic validation for D-03 fields, max three http/https links, Redis `featured:current` plus JSON fallback, no public `/yonkou` affordance, and XSS-safe `textContent` rendering with `rel="noopener"`. Include exact pytest commands for the new Phase 11 tests. Do not add new requirement IDs; Phase 11 uses D-01 through D-06 as mandatory requirements per planning context.</action>
  <acceptance_criteria>
    - `.planning/SECURITY-CHECKLIST.md` contains a Phase 11 or Som da Semana section.
    - Checklist mentions `/featured`, `/yonkou/login`, `POST /featured`, `ADMIN_PASSWORD`, signed cookie, `featured:current`, JSON fallback, `textContent`, and `noopener`.
    - Checklist includes exact commands for `pytest tests/test_security.py -x -q` and `pytest tests/test_frontend.py -x -q`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_security.py tests/test_frontend.py -q</automated>
  </verify>
  <done>Security documentation reflects the implemented Phase 11 controls and the combined security/frontend test gate is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify direct /yonkou route and no public affordance</name>
  <files>api/main.py, static/index.html, static/app.js, static/style.css</files>
  <read_first>
    @api/main.py
    @static/index.html
    @static/app.js
    @static/style.css
    @.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-CONTEXT.md
    @.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-UI-SPEC.md
    @.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-VALIDATION.md
  </read_first>
  <what-built>Plans 02 and 03 built the authenticated `/yonkou` operator panel, protected `/featured` update endpoint, public `GET /featured`, and public sidebar rendering.</what-built>
  <action>Before pausing for user approval, run the automated full non-e2e test gate and start the local FastAPI app only if needed for browser verification. Present the exact local URL for `/` and `/yonkou`, plus the configured temporary `ADMIN_PASSWORD` value if the executor created one for local verification. Do not add any public link or copy exposing `/yonkou`; this task verifies the route by direct navigation only.</action>
  <how-to-verify>
    1. Start the app locally with `ADMIN_PASSWORD` and `ADMIN_SESSION_SECRET` set, using the repo's normal FastAPI start command.
    2. Visit `/` and confirm there is no visible button, link, menu item, or copy revealing `/yonkou`.
    3. Visit `/yonkou` directly and confirm the logged-out panel renders with the CTA `Entrar no painel`.
    4. Log in with the configured `ADMIN_PASSWORD`, fill artista, titulo, genero, descricao, and one or more link rows, then click `Salvar Som`.
    5. Return to `/` and confirm the Som da Semana sidebar appears on the right with `:: SOM DA SEMANA ::`, `----`, DD/MM/YYYY date text, and external link buttons.
    6. Confirm link buttons open in a new tab and the downloader form still works visually.
  </how-to-verify>
  <acceptance_criteria>
    - Public `/` exposes no `/yonkou` affordance.
    - Direct `/yonkou` displays the operator login panel with `Entrar no painel`.
    - Saving a featured release updates the public sidebar.
    - Sidebar styling matches black/orange Y2K table aesthetic with no images, artwork, embeds, or platform icons.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -m "not e2e" -q</automated>
  </verify>
  <resume-signal>Type "approved" if the direct route and public sidebar behavior match the checklist, or describe the issue.</resume-signal>
  <done>Human verification confirms the intentionally hidden operator route and public sidebar behavior satisfy D-01 through D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Implementation -> project policy | New controls must be documented for future deploy and review. |
| Human browser -> `/yonkou` | Direct route is discoverable only by knowing the path, then protected by auth. |
| Public browser -> `/` | Public page must not leak the operator path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-12 | Repudiation | Security checklist | mitigate | Document exact controls and test commands for operator auth, rate limits, validation, fallback, and safe rendering. |
| T-11-13 | Information Disclosure | Public page | mitigate | Human verification confirms no visible `/yonkou` affordance after implementation. |
| T-11-14 | Elevation of Privilege | `/yonkou` | mitigate | Human verification confirms direct route starts at login and requires configured password before edit form. |
| T-11-15 | Tampering | Public sidebar | mitigate | Full automated tests plus human verification confirm saved content renders as text and link buttons only. |
</threat_model>

<verification>
Run `python -m pytest tests/test_security.py tests/test_frontend.py -q` after checklist update and `python -m pytest tests/ -m "not e2e" -q` before the human verification checkpoint.
</verification>

<success_criteria>
Phase 11 is complete when the checklist is updated, full non-e2e tests pass, and the user approves direct `/yonkou` plus public sidebar behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/11-som-da-semana-curated-sidebar-panel-featuring-underground-mu/11-04-SUMMARY.md`
</output>
