---
phase: 13-docker-compose
plan: 04
type: execute
wave: 3
depends_on:
  - "13-03"
files_modified:
  - docker-compose.yml
  - .env.example
autonomous: false
requirements:
  - DEPLOY-05
  - DEPLOY-06
user_setup: []

must_haves:
  truths:
    - "`docker compose up -d` sobe 4 serviços: api, worker, redis, bgutil — todos `restart: unless-stopped` (DEPLOY-05 / D-08 / D-15)"
    - "redis e bgutil NÃO têm `ports:` mapeadas para o host — apenas rede interna `soundgrabber_net` (D-10 / D-14)"
    - "api expõe porta 8000 no host (único ponto de entrada externo)"
    - "worker roda `celery -A api.tasks worker --concurrency=1 --max-tasks-per-child=10` (DEPLOY-05 D-08)"
    - "Volume named `sg_tmp` com `driver: local` + `driver_opts: type=tmpfs, device=tmpfs, o=size=512m,mode=1777` montado em `/tmp` nos serviços api e worker — compartilhamento de arquivos WAV (DEPLOY-06 / D-12 / Pitfall 1 do RESEARCH.md)"
    - "Arquivo escrito por `docker exec worker touch /tmp/sg_test.txt` é imediatamente legível por `docker exec api cat /tmp/sg_test.txt` (DEPLOY-06 verificação manual)"
    - "BGUTIL_BASE_URL=http://bgutil:4416 setado em api e worker (D-09)"
    - "DEV_MODE=true setado em api e worker para bypass do Redis auth check (Pitfall 4 do RESEARCH.md)"
    - ".env.example documenta todas as variáveis necessárias (REDIS_URL, DEV_MODE, BGUTIL_BASE_URL, YTDLP_CACHE_DIR, rate limits, WAV_TTL, admin)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Stack de 4 serviços + rede bridge + volume tmpfs compartilhado"
      contains: "sg_tmp"
      min_lines: 50
    - path: ".env.example"
      provides: "Template de variáveis de ambiente para `cp .env.example .env`"
      contains: "DEV_MODE=true"
      min_lines: 15
  key_links:
    - from: "docker-compose.yml worker"
      to: "docker-compose.yml api"
      via: "Volume sg_tmp montado em /tmp nos dois serviços"
      pattern: "sg_tmp:/tmp"
    - from: "docker-compose.yml api/worker"
      to: "docker-compose.yml redis"
      via: "REDIS_URL=redis://redis:6379/0 (hostname=nome do serviço)"
      pattern: "redis://redis:6379"
    - from: "docker-compose.yml api/worker"
      to: "docker-compose.yml bgutil"
      via: "BGUTIL_BASE_URL=http://bgutil:4416"
      pattern: "http://bgutil:4416"
    - from: ".env.example"
      to: "api/config.py"
      via: "os.environ.get() para REDIS_URL, DEV_MODE, BGUTIL_BASE_URL, YTDLP_CACHE_DIR"
      pattern: "os\\.environ\\.get"
---

<objective>
Criar `docker-compose.yml` (4 serviços + rede bridge interna + volume tmpfs compartilhado) e `.env.example` (template das variáveis lidas por api/config.py). O compose orquestra a imagem `soundgrabber:latest` construída no Plan 03 e satisfaz DEPLOY-05 (restart policy) e DEPLOY-06 (volume compartilhado entre api e worker). Após o `docker compose up -d`, dois gates manuais verificam: (a) `docker compose config | grep unless-stopped` retorna 4 ocorrências; (b) `docker exec worker touch /tmp/sg_test.txt && docker exec api cat /tmp/sg_test.txt` confirma compartilhamento do tmpfs.

Purpose: Última etapa da Phase 13. Stack pronto para Phase 14 (cookies + deploy.sh + E2E).
Output: docker-compose.yml + .env.example + stack up + 2 gates manuais verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-docker-compose/13-CONTEXT.md
@.planning/phases/13-docker-compose/13-RESEARCH.md
@.planning/phases/13-docker-compose/13-PATTERNS.md
@.planning/phases/13-docker-compose/13-VALIDATION.md
@.planning/phases/13-03-PLAN.md
@api/config.py
@.env.example

<interfaces>
<!-- Variáveis lidas por api/config.py (extraído via grep "os.environ.get") -->
api/config.py expõe estas env vars com defaults:
- REDIS_URL (default: redis://localhost:6379/0)
- DEV_MODE (default: false)
- BGUTIL_BASE_URL (default: vazio)
- YTDLP_CACHE_DIR (default: vazio)
- RATE_LIMIT_PER_MINUTE
- JOB_POLL_RATE_LIMIT_PER_MINUTE
- FILE_DOWNLOAD_RATE_LIMIT_PER_MINUTE
- WAV_TTL_SECONDS
- ADMIN_PASSWORD
- ADMIN_SESSION_SECRET
- FEATURED_FALLBACK_PATH

<!-- Pattern de docker-compose verificado em RESEARCH.md Pattern 2 (named tmpfs volume) -->
volumes:
  sg_tmp:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "size=512m,mode=1777"

<!-- Pattern de healthcheck Redis (RESEARCH.md Pattern 3) -->
redis:
  image: redis:7-alpine
  healthcheck:
    test: ["CMD", "redis-cli", "ping"]
    interval: 5s
    timeout: 3s
    retries: 5

<!-- Comando Celery canônico (start-all.sh linha 63 + DEPLOY-05) -->
celery -A api.tasks worker --loglevel=info --concurrency=1 --max-tasks-per-child=10

<!-- Pitfall 1: tmpfs por serviço NÃO é compartilhado — DEVE usar named volume -->
<!-- Pitfall 4: Redis sem senha → DEV_MODE=true para bypassar _check_redis_auth -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar .env.example documentando variáveis do compose</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (versão atual — 18 linhas com YTDLP_COOKIES_FILE, YTDLP_PO_TOKEN, REDIS_URL local, ADMIN_*)
    - api/config.py (Settings dataclass — todas as env vars que o app lê via os.environ.get)
    - .planning/phases/13-docker-compose/13-RESEARCH.md (seção `.env.example` — template completo para o compose)
    - .planning/phases/13-docker-compose/13-PATTERNS.md (seção `.env.example` — diferenças entre versão local existente e versão para compose: hostname `redis` em vez de `localhost`, hostname `bgutil` em vez de URL Railway)
    - .planning/phases/13-docker-compose/13-CONTEXT.md (Claude's Discretion — Claude decide a estrutura env_file vs environment block)
  </read_first>
  <action>
    Substituir `.env.example` por um template adequado ao compose local do notebook. Conteúdo (uma seção por agrupamento, com comentários explicativos curtos, sem fenced code blocks no arquivo):

    Seção `# Redis (Phase 2 + DEV_MODE bypass)`:
    - `REDIS_URL=redis://redis:6379/0` (hostname = nome do serviço no compose)
    - `DEV_MODE=true` (bypass `_check_redis_auth` em api/main.py — notebook é ambiente privado, Redis não exposto ao host; Pitfall 4 do RESEARCH.md)

    Seção `# bgutil PO Token provider (Phase 10.1)`:
    - `BGUTIL_BASE_URL=http://bgutil:4416` (hostname = nome do serviço; porta interna)

    Seção `# yt-dlp cache + cookies (Phase 14 popula este path via bind mount)`:
    - `YTDLP_CACHE_DIR=` (vazio em Phase 13 — Phase 14 configura bind mount para `/data/yt-dlp-cache` com cookies.txt)

    Seção `# Rate limiting (defaults conservadores)`:
    - `RATE_LIMIT_PER_MINUTE=3`
    - `JOB_POLL_RATE_LIMIT_PER_MINUTE=60`
    - `FILE_DOWNLOAD_RATE_LIMIT_PER_MINUTE=10`

    Seção `# WAV file lifecycle`:
    - `WAV_TTL_SECONDS=900` (15 minutos — alinhado a CORE-05)

    Seção `# Som da Semana operator panel (Phase 11)`:
    - `ADMIN_PASSWORD=change-me-locally` (placeholder; operador troca em produção)
    - `ADMIN_SESSION_SECRET=change-me-to-a-long-random-secret`
    - `FEATURED_FALLBACK_PATH=.data/featured-current.json`

    Cabeçalho do arquivo: `# SoundGrabber — .env template for docker-compose local deploy (Phase 13).\n# Copy this to .env (which is .dockerignore + .gitignore'd) and adjust values.\n# Notebook é ambiente privado: Redis e bgutil não são expostos ao host.`

    Não incluir mais `YTDLP_COOKIES_FILE` nem `YTDLP_PO_TOKEN` (legado pré-10.1).
    Não incluir variáveis que não são lidas por api/config.py.
  </action>
  <verify>
    <automated>grep -c "^REDIS_URL=redis://redis:6379/0" .env.example && grep -c "^DEV_MODE=true" .env.example && grep -c "^BGUTIL_BASE_URL=http://bgutil:4416" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` existe na raiz
    - `grep -c "^REDIS_URL=redis://redis:6379/0" .env.example` retorna 1
    - `grep -c "^DEV_MODE=true" .env.example` retorna 1
    - `grep -c "^BGUTIL_BASE_URL=http://bgutil:4416" .env.example` retorna 1
    - `grep -c "^YTDLP_CACHE_DIR=" .env.example` retorna 1
    - `grep -c "^RATE_LIMIT_PER_MINUTE=" .env.example` retorna 1
    - `grep -c "^WAV_TTL_SECONDS=" .env.example` retorna 1
    - `grep -c "^ADMIN_PASSWORD=" .env.example` retorna 1
    - `grep -c "^ADMIN_SESSION_SECRET=" .env.example` retorna 1
    - `grep -c "^FEATURED_FALLBACK_PATH=" .env.example` retorna 1
    - `grep -c "YTDLP_COOKIES_FILE\\|YTDLP_PO_TOKEN" .env.example` retorna 0 (legado removido)
    - `wc -l < .env.example` >= 15
  </acceptance_criteria>
  <done>.env.example atualizado e committed; documenta todas as variáveis para o compose.</done>
</task>

<task type="auto">
  <name>Task 2: Criar docker-compose.yml com 4 serviços (api, worker, redis, bgutil), rede soundgrabber_net e volume tmpfs sg_tmp compartilhado</name>
  <files>docker-compose.yml</files>
  <read_first>
    - .planning/phases/13-docker-compose/13-CONTEXT.md (D-08..D-15 — todas as decisões locked do compose)
    - .planning/phases/13-docker-compose/13-RESEARCH.md (seção Code Examples — docker-compose.yml completo verificado; Pattern 2 named tmpfs volume; Pattern 3 healthcheck Redis)
    - .planning/phases/13-docker-compose/13-RESEARCH.md (Pitfall 1 CRÍTICO — tmpfs por serviço NÃO é compartilhado; usar named volume com driver_opts)
    - .planning/phases/13-docker-compose/13-RESEARCH.md (Pitfall 4 — DEV_MODE=true para bypass Redis auth)
    - .planning/phases/13-docker-compose/13-PATTERNS.md (analogs railway.toml + start-all.sh — celery command canônico)
    - .planning/phases/13-docker-compose/13-VALIDATION.md (Manual-Only Verifications — comandos exatos dos gates DEPLOY-05/DEPLOY-06)
    - api/config.py (variáveis lidas via os.environ.get — confirmar nomes)
    - Dockerfile (Plan 03 — confirmar EXPOSE 8000 e CMD uvicorn padrão; worker substitui CMD via `command:` no compose)
  </read_first>
  <action>
    Criar `docker-compose.yml` na raiz com a estrutura completa abaixo (sem fenced code blocks aqui — escrever YAML direto no arquivo).

    **Top-level (sem `version:` — Docker Compose v2 dispensa este campo):**

    Bloco `networks:` com `soundgrabber_net` driver `bridge` (D-10).

    Bloco `volumes:` com `sg_tmp` driver `local` e `driver_opts`:
    - `type: tmpfs`
    - `device: tmpfs`
    - `o: "size=512m,mode=1777"`
    (D-11/D-12; Pitfall 1 — esta é a configuração crítica para compartilhamento entre containers)

    **Bloco `services:` com 4 serviços, EXATAMENTE nesta ordem:**

    1. `redis`:
       - `image: redis:7-alpine` (D-08)
       - `restart: unless-stopped` (D-15)
       - `networks: [soundgrabber_net]` (D-10)
       - Sem `ports:` mapeado (D-14, D-10 — apenas rede interna)
       - `healthcheck:` com `test: ["CMD", "redis-cli", "ping"]`, `interval: 5s`, `timeout: 3s`, `retries: 5` (RESEARCH Pattern 3)
       - `mem_limit: 128m` (discretion — Redis é leve)

    2. `bgutil`:
       - `image: jim60105/bgutil-pot` (D-08)
       - `restart: unless-stopped` (D-15)
       - `networks: [soundgrabber_net]` (D-10)
       - Sem `ports:` mapeado (D-09 — porta 4416 só na rede interna)
       - `mem_limit: 128m` (discretion)

    3. `api`:
       - `image: soundgrabber:latest` (construída no Plan 03)
       - `restart: unless-stopped` (D-15)
       - `ports: ["8000:8000"]` (único serviço com porta exposta — entrada externa)
       - `networks: [soundgrabber_net]` (D-10)
       - `volumes: ["sg_tmp:/tmp"]` (D-12 — tmpfs compartilhado)
       - `depends_on: { redis: { condition: service_healthy } }` (RESEARCH Pattern 3)
       - `env_file: .env` (Claude's Discretion — env_file consolidado)
       - `mem_limit: 512m` (discretion — i5-3210M/4GB; A4 do RESEARCH)
       - Sem `command:` (usa CMD do Dockerfile = uvicorn)

    4. `worker`:
       - `image: soundgrabber:latest` (mesma imagem que api)
       - `restart: unless-stopped` (D-15)
       - `command: ["celery", "-A", "api.tasks", "worker", "--loglevel=info", "--concurrency=1", "--max-tasks-per-child=10"]` (D-08, DEPLOY-05 — substitui CMD do Dockerfile)
       - `networks: [soundgrabber_net]` (D-10)
       - `volumes: ["sg_tmp:/tmp"]` (D-12 — mesmo volume da api → arquivos sg_*.wav compartilhados)
       - `depends_on: { redis: { condition: service_healthy } }`
       - `env_file: .env`
       - `mem_limit: 1g` (discretion — Essentia + ffmpeg + Python tem footprint maior)
       - Sem `ports:` (worker não expõe nada)

    Comentários no arquivo:
    - No topo: referência a D-08..D-15 + Phase 13 + nota crítica sobre volume sg_tmp (Pitfall 1)
    - Próximo a `sg_tmp` no bloco volumes: `# Named volume com driver=local+tmpfs é o ÚNICO modo de compartilhar tmpfs entre containers (Pitfall 1 RESEARCH.md). tmpfs: /tmp em cada serviço NÃO funciona.`
    - Próximo ao bloco redis ports: `# Sem ports: — Redis apenas na rede interna (D-10, D-14)`
    - Próximo ao DEV_MODE: `# DEV_MODE=true vem do .env (Pitfall 4 — Redis auth bypass para deploy local privado)`

    NÃO usar `privileged: true` em nenhum serviço (D-13).
    NÃO usar `network_mode: host` em nenhum serviço (D-13).
    NÃO mapear porta do redis nem do bgutil ao host (D-10, D-14).
    NÃO usar `tmpfs: /tmp` em cada serviço (Pitfall 1).
  </action>
  <verify>
    <automated>docker compose config > /dev/null && echo "OK: compose válido" && docker compose config | grep -c "unless-stopped"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` existe na raiz
    - `docker compose config` retorna exit 0 (YAML válido + interpolação de env válida)
    - `docker compose config | grep -c "unless-stopped"` retorna 4 (DEPLOY-05 — todos os 4 serviços)
    - `grep -c "image: redis:7-alpine" docker-compose.yml` retorna 1
    - `grep -c "image: jim60105/bgutil-pot" docker-compose.yml` retorna 1
    - `grep -c "image: soundgrabber:latest" docker-compose.yml` retorna 2 (api + worker)
    - `grep -c "sg_tmp:/tmp" docker-compose.yml` retorna 2 (montado em api e worker)
    - `grep -c "driver: local" docker-compose.yml` >= 1 (volume sg_tmp)
    - `grep -c "type: tmpfs" docker-compose.yml` >= 1
    - `grep -c "mode=1777" docker-compose.yml` >= 1
    - `grep -c "concurrency=1" docker-compose.yml` retorna 1 (worker)
    - `grep -c "max-tasks-per-child=10" docker-compose.yml` retorna 1
    - `grep -c "privileged" docker-compose.yml` retorna 0 (D-13)
    - `grep -c "network_mode: host" docker-compose.yml` retorna 0 (D-13)
    - `grep -c "soundgrabber_net" docker-compose.yml` >= 5 (rede + 4 referências de serviço)
    - Bloco `ports:` aparece somente uma vez no arquivo (apenas para api 8000:8000); confirmar com `grep -c "^    ports:" docker-compose.yml` retorna 1
  </acceptance_criteria>
  <done>docker-compose.yml committed; `docker compose config` valida; estrutura cumpre D-08..D-15 e Pitfall 1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: docker compose up + Gates manuais DEPLOY-05 (restart policy) e DEPLOY-06 (volume tmpfs compartilhado)</name>
  <files>(none — execução de comandos Docker; resultado em SUMMARY)</files>
  <read_first>
    - docker-compose.yml (recém-criado em Task 2)
    - .env.example (recém-criado em Task 1)
    - .planning/phases/13-docker-compose/13-VALIDATION.md (Manual-Only Verifications — DEPLOY-05 espera 4 ocorrências de "unless-stopped"; DEPLOY-06 espera ls/cat funcionar entre containers)
    - .planning/phases/13-docker-compose/13-CONTEXT.md (D-15 — restart policy; D-12 — volume compartilhado)
    - .planning/phases/13-docker-compose/13-RESEARCH.md (Pitfall 6 — se volume sg_tmp foi criado com config errada, precisa `docker compose down -v`)
  </read_first>
  <what-built>
    Plan 04 Tasks 1-2 criaram .env.example e docker-compose.yml. Esta task: copiar .env.example → .env, subir a stack com `docker compose up -d`, e executar os 2 gates manuais que validam DEPLOY-05 e DEPLOY-06.
  </what-built>
  <how-to-verify>
    Execute na raiz do projeto:

    1. **Preparar .env:**
       `[ -f .env ] || cp .env.example .env`
       (Se .env já existir, não sobrescrever — pode conter segredos. Validar manualmente que tem REDIS_URL, DEV_MODE=true, BGUTIL_BASE_URL.)

    2. **Subir a stack:**
       `docker compose up -d`
       Esperado: 4 containers em estado "Started" ou "Healthy". Verificar com `docker compose ps`. Saída esperada mostra: api (porta 8000:8000), worker, redis (Healthy), bgutil.
       Se algum serviço entrar em loop de restart, ler `docker compose logs <serviço>` antes de prosseguir. Causas comuns: imagem soundgrabber:latest ausente (Plan 03 não rodou), `_check_redis_auth` bloqueando (DEV_MODE não foi setado), volume sg_tmp criado com config antiga (rodar `docker compose down -v && docker compose up -d`).

    3. **Gate DEPLOY-05 (manual #1):**
       `docker compose config | grep -c "unless-stopped"`
       Esperado: saída `4`. Se for diferente de 4, revisar docker-compose.yml.

    4. **Gate DEPLOY-06 (manual #2 — CRÍTICO):**
       `docker compose exec worker touch /tmp/sg_test.txt`
       Em seguida: `docker compose exec api ls -la /tmp/sg_test.txt`
       Esperado: o segundo comando lista o arquivo (mostra permissões, tamanho, data). Se mostrar `No such file or directory`, o volume tmpfs NÃO está compartilhado — Pitfall 1 do RESEARCH.md. Nesse caso, revisar docker-compose.yml para confirmar que `sg_tmp:/tmp` está em AMBOS os serviços e que `volumes:` top-level tem `driver: local` com `driver_opts: type: tmpfs`.
       Confirmação adicional: `docker compose exec api cat /tmp/sg_test.txt` retorna conteúdo vazio (arquivo de 0 bytes), exit 0.
       Limpeza: `docker compose exec worker rm -f /tmp/sg_test.txt`.

    5. **Verificações complementares:**
       - Redis sem porta exposta: `docker compose port redis 6379 2>&1 || echo "EXPECTED: sem porta"` — deve retornar erro ou nada (D-14).
       - bgutil sem porta exposta: `docker compose port bgutil 4416 2>&1 || echo "EXPECTED: sem porta"` — mesmo.
       - api respondendo: `curl -s -o /dev/null -w "%{http_code}\\n" http://localhost:8000/` — esperado `200` (ou 404/redirect se não houver rota / mas servidor responde).
       - api consegue ver Redis: `docker compose exec api python -c "import redis; r = redis.Redis(host='redis'); print(r.ping())"` — esperado `True`.
       - Verificar restart policy de runtime: `docker inspect $(docker compose ps -q api) --format '{{.HostConfig.RestartPolicy.Name}}'` — esperado `unless-stopped`.

    6. **Documentar tudo em** `.planning/phases/13-docker-compose/13-04-SUMMARY.md`:
       - Output de `docker compose ps`
       - Output exato do gate DEPLOY-05 (`docker compose config | grep -c "unless-stopped"` = 4)
       - Output exato do gate DEPLOY-06 (`docker compose exec worker touch ...` + `docker compose exec api ls ...`)
       - Output das verificações complementares
       - Quaisquer warnings nos logs (`docker compose logs --tail=20`)

    **Critério de aprovação:**
    - DEPLOY-05 gate retorna `4` exato
    - DEPLOY-06 gate: worker cria arquivo em /tmp e api lê o mesmo arquivo no /tmp
    - Nenhum serviço em loop de restart
    - api responde HTTP na porta 8000

    Se algum gate falhar, NÃO marcar a Phase 13 como done. Iterar no docker-compose.yml até resolver.
  </how-to-verify>
  <action>
    Subir a stack docker compose conforme detalhado em how-to-verify e executar os 2 gates manuais. Operador humano: (1) garantir `.env` existe (cp do `.env.example` se necessário); (2) rodar `docker compose up -d`; (3) executar Gate DEPLOY-05 com `docker compose config | grep -c "unless-stopped"` — exigir saída exata `4`; (4) executar Gate DEPLOY-06 com `docker compose exec worker touch /tmp/sg_test.txt && docker compose exec api ls -la /tmp/sg_test.txt` — exigir que api liste o arquivo; (5) limpar com `docker compose exec worker rm -f /tmp/sg_test.txt`; (6) rodar verificações complementares (portas internas, curl :8000, redis ping); (7) registrar todos os outputs em 13-04-SUMMARY.md. Se DEPLOY-06 falhar (volume não compartilhado), rodar `docker compose down -v && docker compose up -d` para recriar o volume (Pitfall 6 RESEARCH.md).
  </action>
  <verify>
    <automated>docker compose config > /dev/null && [ "$(docker compose config | grep -c "unless-stopped")" = "4" ] && docker compose up -d && sleep 5 && docker compose exec -T worker touch /tmp/sg_test.txt && docker compose exec -T api ls /tmp/sg_test.txt && docker compose exec -T worker rm -f /tmp/sg_test.txt</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config | grep -c "unless-stopped"` retorna exatamente `4` (DEPLOY-05)
    - `docker compose up -d` exit 0 e `docker compose ps` lista 4 serviços (api, worker, redis, bgutil) sem estado "Restarting"
    - `docker compose exec -T worker touch /tmp/sg_test.txt` exit 0
    - `docker compose exec -T api ls /tmp/sg_test.txt` exit 0 (arquivo listado — DEPLOY-06 GREEN)
    - `docker compose port redis 6379` retorna vazio ou erro (D-14 — Redis sem ports no host)
    - `docker compose port bgutil 4416` retorna vazio ou erro (D-10)
    - `docker inspect $(docker compose ps -q api) --format '{{.HostConfig.RestartPolicy.Name}}'` retorna `unless-stopped`
    - 13-04-SUMMARY.md existe e contém: output de `docker compose ps`, output do gate DEPLOY-05 (=4), output do gate DEPLOY-06 (arquivo listado em api/tmp)
  </acceptance_criteria>
  <done>Stack docker compose up com 4 serviços; DEPLOY-05 e DEPLOY-06 gates verdes; SUMMARY documentado; Phase 13 pronta para /gsd-verify-work.</done>
  <resume-signal>Digite "approved" se ambos os gates DEPLOY-05 e DEPLOY-06 passaram; descreva problema persistente se houver</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente HTTP externo → api (porta 8000) | Único ponto de entrada — rate limits e validation já implementados em api/main.py (Phase 3 + Phase 6) |
| api ↔ worker (via Redis + volume sg_tmp) | Comunicação interna na rede `soundgrabber_net`; Redis sem porta exposta ao host |
| api/worker → bgutil (porta 4416 interna) | HTTP cleartext em rede bridge isolada — aceitável (deploy interno do notebook) |
| api/worker → redis (porta 6379 interna) | TCP cleartext em rede bridge isolada; DEV_MODE=true porque Redis não está exposto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Information Disclosure | Redis exposto ao host permitiria leitura de jobs/keys | mitigate | docker-compose.yml omite `ports:` em redis (D-10, D-14); Task 2 acceptance_criteria valida |
| T-13-12 | Information Disclosure | bgutil PO Tokens vazariam se porta 4416 exposta | mitigate | docker-compose.yml omite `ports:` em bgutil (D-10) |
| T-13-13 | Tampering | tmpfs sg_tmp acessível por qualquer container que entre na rede | accept | Apenas api e worker montam o volume; mode=1777 (sticky bit) protege contra remoção cross-user; WAVs com chmod 0o600 (já feito em pipeline.download_audio) |
| T-13-14 | Elevation of Privilege | privileged: true ou host network permitiriam bypass | mitigate | acceptance_criteria valida `grep -c "privileged" = 0` e `grep -c "network_mode: host" = 0` (D-13) |
| T-13-15 | Denial of Service | mem_limit ausente → container OOM kill o host | mitigate | mem_limit definido em todos os 4 serviços (128m/128m/512m/1g) — i5-3210M com 4GB RAM tem buffer |
| T-13-16 | Denial of Service | Redis sem auth + porta exposta = abuso público | mitigate | Sem porta exposta (D-14); DEV_MODE=true aceitável apenas porque a porta é interna |
| T-13-17 | Information Disclosure | .env com segredos copiado para imagem | mitigate | `.dockerignore` (Plan 03 Task 1) exclui `.env`; env vars chegam ao container via env_file no compose, NÃO via COPY |
| T-13-18 | Repudiation | Falta de logs para investigar incidente | accept | Docker mantém logs por padrão (`docker compose logs`); log rotation deferido para v2 (RESEARCH Out of Scope) |
</threat_model>

<verification>
- `docker compose config` exit 0 (YAML válido)
- `docker compose config | grep -c "unless-stopped"` retorna `4`
- `docker compose up -d` sobe 4 serviços; `docker compose ps` mostra todos running/healthy
- `docker compose exec worker touch /tmp/sg_test.txt && docker compose exec api ls /tmp/sg_test.txt` lista o arquivo (DEPLOY-06)
- `docker compose port redis 6379` retorna vazio/erro (D-14)
- `docker compose port bgutil 4416` retorna vazio/erro (D-10)
- `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/` retorna código HTTP (200/404 dependendo de rota)
- `pytest tests/test_pipeline_docker.py -x -q` (continua GREEN — não regredindo Plan 02)
</verification>

<success_criteria>
- [ ] .env.example documenta todas as 10 variáveis (REDIS_URL, DEV_MODE, BGUTIL_BASE_URL, YTDLP_CACHE_DIR, 3 rate limits, WAV_TTL_SECONDS, 3 admin)
- [ ] docker-compose.yml define 4 serviços com restart: unless-stopped
- [ ] Volume sg_tmp com driver_opts type=tmpfs montado em /tmp nos serviços api e worker
- [ ] Rede soundgrabber_net bridge configurada
- [ ] api é o único serviço com ports: 8000:8000 mapeado
- [ ] redis e bgutil sem ports: mapeado ao host
- [ ] worker command com --concurrency=1 --max-tasks-per-child=10
- [ ] DEPLOY-05 gate manual: `docker compose config | grep -c unless-stopped` retorna 4
- [ ] DEPLOY-06 gate manual: worker escreve em /tmp, api lê do mesmo /tmp
- [ ] Sem `privileged: true` ou `network_mode: host`
- [ ] Phase 13 completa — pronto para `/gsd-verify-work 13`
</success_criteria>

<output>
After completion, create `.planning/phases/13-docker-compose/13-04-SUMMARY.md` documentando:
- Output completo de `docker compose ps` (estado dos 4 serviços)
- Output exato dos gates DEPLOY-05 e DEPLOY-06 (com comandos)
- Output das verificações complementares (portas internas, redis ping, restart policy inspect)
- Resumo da Phase 13: quais REQs estão cobertos (DEPLOY-04 via Plans 02-03, DEPLOY-05 + DEPLOY-06 via Plan 04)
- Próximo passo: `/gsd-verify-work 13` ou `/gsd-plan-phase 14` (cookies + deploy.sh + E2E)
</output>
