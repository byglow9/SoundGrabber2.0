---
phase: 13-docker-compose
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_pipeline_docker.py
autonomous: true
requirements:
  - DEPLOY-04
user_setup: []

must_haves:
  truths:
    - "tests/test_pipeline_docker.py existe e expõe 3 testes RED para o refactor da Wave 1"
    - "Os 3 testes falham (RED) antes da refatoração — `test_no_imageio_ffmpeg_import` e `test_no_librosa_import` falham porque pipeline.py atual importa ambos"
    - "Os 3 testes passam (GREEN) após Plan 02 remover imageio_ffmpeg e librosa de pipeline.py"
  artifacts:
    - path: "tests/test_pipeline_docker.py"
      provides: "RED stubs para Wave 1 (test_no_imageio_ffmpeg_import, test_no_librosa_import, test_detect_tuning_essentia)"
      contains: "def test_no_imageio_ffmpeg_import"
      min_lines: 60
  key_links:
    - from: "tests/test_pipeline_docker.py"
      to: "pipeline.py"
      via: "ast.parse + import inspection"
      pattern: "ast\\.parse|importlib"
    - from: "tests/test_pipeline_docker.py"
      to: "pipeline.detect_tuning"
      via: "direct call with sample WAV from scripts/generate_sample_wav.py"
      pattern: "from pipeline import detect_tuning|pipeline\\.detect_tuning"
---

<objective>
Criar `tests/test_pipeline_docker.py` (Wave 0 RED stubs) — arquivo de testes que cobre DEPLOY-04 / D-02 / D-03: pipeline.py não importa `imageio_ffmpeg`, pipeline.py não importa `librosa`, e `detect_tuning()` funciona via Essentia (não-librosa). Este Plan estabelece a sampling rate de feedback (`pytest tests/test_pipeline_docker.py -x -q` em ~15s) que Plans 02..04 vão consumir.

Purpose: Garantir Nyquist compliance — Wave 0 cria os testes ANTES da refatoração para que Plan 02 ganhe imediatamente um gate verde após terminar.
Output: 1 arquivo de testes pytest com 3 stubs (`test_no_imageio_ffmpeg_import`, `test_no_librosa_import`, `test_detect_tuning_essentia`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-docker-compose/13-CONTEXT.md
@.planning/phases/13-docker-compose/13-RESEARCH.md
@.planning/phases/13-docker-compose/13-PATTERNS.md
@.planning/phases/13-docker-compose/13-VALIDATION.md
@pytest.ini
@tests/test_security.py
@scripts/generate_sample_wav.py

<interfaces>
<!-- pipeline.py exports relevantes para os testes (extraídos via grep) -->
<!-- Estes são os símbolos que test_pipeline_docker.py irá inspecionar/chamar. -->

From pipeline.py:
```python
def check_duration(url: str, cache_dir: str) -> dict[str, Any]: ...
def download_audio(url: str, cache_dir: str) -> Path: ...
def convert_to_wav(audio_path: Path) -> Path: ...
def validate_wav(wav_path: Path) -> float: ...
def detect_tuning(wav_path: Path) -> float | None: ...
def detect_bpm(wav_path: Path) -> float: ...
def detect_key(wav_path: Path, tuning_hz: float | None) -> tuple[str, float]: ...
def key_to_camelot(key: str) -> str: ...
def analyze_audio(wav_path: Path) -> dict[str, Any]: ...

CAMELOT: dict[str, str]  # 24-entry static table
MAX_DURATION_SEC = 900
TMP_PREFIX = "sg_"
WAV_TMP_DIR = Path("/tmp")
```

From tests/conftest.py (existente):
- DEV_MODE=true setado para os.environ — necessário para evitar Redis auth check no startup do api.main.

Pattern de inspeção de imports (analog: nenhum direto; estilo segue tests/test_security.py header):
```python
from __future__ import annotations
import ast
from pathlib import Path

def test_no_imageio_ffmpeg_import():
    source = Path("pipeline.py").read_text()
    tree = ast.parse(source)
    # ... walk e coletar nomes de imports
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar tests/test_pipeline_docker.py com 3 stubs RED para DEPLOY-04 / D-02 / D-03</name>
  <files>tests/test_pipeline_docker.py</files>
  <read_first>
    - pipeline.py (linhas 25-70 — imports atuais incluindo `import imageio_ffmpeg`, `import librosa`, `import numpy as np`)
    - pipeline.py (linhas 411-440 — função `detect_tuning` atual baseada em librosa.load + librosa.effects.hpss + librosa.estimate_tuning)
    - tests/test_security.py (linhas 1-80 — pattern de header, imports `from __future__`, docstring de teste, uso de pytest)
    - .planning/phases/13-docker-compose/13-PATTERNS.md (seção `tests/test_pipeline_docker.py` — pattern de verificação de import com ast.parse)
    - .planning/phases/13-docker-compose/13-VALIDATION.md (Per-Task Verification Map — comandos exatos `pytest tests/test_pipeline_docker.py::test_no_imageio_ffmpeg_import -x -q`)
    - scripts/generate_sample_wav.py (helper para gerar WAVs sintéticos em testes, se existir; se não existir, gerar WAV via soundfile.write em tmp_path)
    - pytest.ini (marker `integration` registrado ou não; testes que precisarem do marker devem usar `@pytest.mark.integration` somente se já registrado)
  </read_first>
  <action>
    Criar o arquivo tests/test_pipeline_docker.py com header `"""SoundGrabber Docker Phase 13 — Wave 0 RED stubs.\n\nCobertura:\n  DEPLOY-04 / D-02 -> test_no_imageio_ffmpeg_import\n  DEPLOY-04 / D-03 -> test_no_librosa_import\n  D-03 (detect_tuning Essentia) -> test_detect_tuning_essentia\n\nRun: pytest tests/test_pipeline_docker.py -x -q\n"""` e `from __future__ import annotations` no topo. Importar `ast`, `pathlib.Path`, `pytest`.

    Implementar três funções de teste:

    1. `test_no_imageio_ffmpeg_import()` — abrir `Path("pipeline.py").read_text()`, parsear com `ast.parse`, percorrer com `ast.walk` coletando todos os nomes de módulos importados via `ast.Import` (`node.names[i].name`) e `ast.ImportFrom` (`node.module`). Asserir que a string `"imageio_ffmpeg"` NÃO está presente em nenhum import name coletado. Mensagem de erro: `"D-02: imageio_ffmpeg ainda importado em pipeline.py"`. Este teste será RED enquanto a linha 38 `import imageio_ffmpeg` existir.

    2. `test_no_librosa_import()` — mesma mecânica de ast.parse. Asserir que `"librosa"` NÃO está presente em nenhum import name. Mensagem de erro: `"D-03: librosa ainda importado em pipeline.py"`. RED enquanto linha 39 `import librosa` existir.

    3. `test_detect_tuning_essentia(tmp_path)` — gerar um WAV sintético de 2 segundos a 440Hz (senoide pura) usando `soundfile.write(tmp_path / "sample.wav", numpy_signal, 44100)` (o teste pode importar `numpy` e `soundfile` localmente — ambos permanecem em requirements.txt). Chamar `from pipeline import detect_tuning; result = detect_tuning(tmp_path / "sample.wav")`. Asserir `result is None or isinstance(result, float)` — Plan 02 reescreve `detect_tuning` para usar Essentia (SpectralPeaks + TuningFrequency). RED enquanto pipeline.py importar librosa (o módulo nem carrega); GREEN após Plan 02 substituir a função.

    Não usar `@pytest.mark.integration` a menos que o marker já esteja registrado em `pytest.ini` (verificar com `grep markers pytest.ini`). Se não estiver, deixar todos os 3 testes sem marker — eles rodam em `pytest tests/test_pipeline_docker.py -x -q` sem flag adicional.

    Os 3 testes DEVEM falhar com a versão atual de pipeline.py (RED). O Plan 02 vai torná-los verdes.
  </action>
  <behavior>
    - test_no_imageio_ffmpeg_import: RED agora (pipeline.py linha 38 importa imageio_ffmpeg) — vira GREEN após Plan 02
    - test_no_librosa_import: RED agora (pipeline.py linha 39 importa librosa) — vira GREEN após Plan 02
    - test_detect_tuning_essentia: RED agora (módulo pipeline falha ao importar porque librosa será desinstalada ou pipeline.py crasha por estar inconsistente após Plan 02 Tasks 1+2 antes da Task 3) — vira GREEN após Plan 02 Task 3 reescrever detect_tuning
  </behavior>
  <verify>
    <automated>pytest tests/test_pipeline_docker.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def test_" tests/test_pipeline_docker.py` retorna exatamente `3`
    - `grep -c "test_no_imageio_ffmpeg_import" tests/test_pipeline_docker.py` >= 1
    - `grep -c "test_no_librosa_import" tests/test_pipeline_docker.py` >= 1
    - `grep -c "test_detect_tuning_essentia" tests/test_pipeline_docker.py` >= 1
    - `grep -c "import ast" tests/test_pipeline_docker.py` >= 1 (verificação por AST)
    - `pytest tests/test_pipeline_docker.py::test_no_imageio_ffmpeg_import -x -q` retorna exit code 1 (RED esperado neste estágio) — registrar saída no SUMMARY
    - `pytest tests/test_pipeline_docker.py::test_no_librosa_import -x -q` retorna exit code 1 (RED esperado)
    - `grep -v '^#' tests/test_pipeline_docker.py | grep -c "imageio_ffmpeg"` >= 1 (a string aparece dentro das assertions, fora de comentários)
    - Header do arquivo contém a string `Phase 13` e `DEPLOY-04`
  </acceptance_criteria>
  <done>
    Arquivo tests/test_pipeline_docker.py committed; 3 testes definidos; comando `pytest tests/test_pipeline_docker.py -x -q` falha com pelo menos 2 testes em RED (test_no_imageio_ffmpeg_import e test_no_librosa_import); test_detect_tuning_essentia pode falhar com ImportError (se librosa ainda presente, módulo carrega) ou AssertionError.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desenvolvedor → repositório | Código de teste lê arquivos do projeto (pipeline.py) via Path() relativo — sem input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | tests/test_pipeline_docker.py lê pipeline.py via Path().read_text() | accept | Arquivo pipeline.py é código-fonte público do projeto; sem segredos; leitura via Path() padrão não introduz path traversal porque o path é literal "pipeline.py" |
| T-13-02 | Tampering | Sample WAV gerado em tmp_path (pytest fixture) | accept | tmp_path é isolado por teste pelo pytest; sem persistência; sem cross-test pollution |
</threat_model>

<verification>
Após Plan 02 completar a refatoração de pipeline.py, executar `pytest tests/test_pipeline_docker.py -x -q` e confirmar 3 testes GREEN. Antes disso (após este Plan 01), 2/3 testes estarão RED como esperado.
</verification>

<success_criteria>
- [ ] tests/test_pipeline_docker.py existe e é executável via pytest
- [ ] 3 funções de teste declaradas com nomes exatos (test_no_imageio_ffmpeg_import, test_no_librosa_import, test_detect_tuning_essentia)
- [ ] Pelo menos 2 testes (no_imageio, no_librosa) estão RED com pipeline.py atual
- [ ] Header docstring referencia Phase 13 e cobre os requisitos DEPLOY-04 / D-02 / D-03
- [ ] Commit message segue convenção do projeto (test(13): ...)
</success_criteria>

<output>
After completion, create `.planning/phases/13-docker-compose/13-01-SUMMARY.md` documentando:
- Status RED dos 3 testes (qual rodou, qual falhou, com saída de erro)
- Confirmação de que pytest tests/test_pipeline_docker.py -x -q termina em ~15s
- Próximo passo: Plan 02 vai tornar todos os 3 GREEN
</output>
</content>
</invoke>