---
phase: 13-docker-compose
plan: 02
type: execute
wave: 1
depends_on:
  - "13-01"
files_modified:
  - requirements.txt
  - pipeline.py
autonomous: true
requirements:
  - DEPLOY-04
user_setup: []

must_haves:
  truths:
    - "requirements.txt não contém mais `imageio-ffmpeg` nem `librosa`"
    - "pipeline.py não importa mais `imageio_ffmpeg` nem `librosa`"
    - "pipeline.py.detect_tuning() funciona via Essentia (SpectralPeaks + TuningFrequency) e retorna float ou None"
    - "Pipeline preserva o contrato público: check_duration, download_audio, convert_to_wav, validate_wav, detect_tuning, detect_bpm, detect_key, key_to_camelot, analyze_audio, CAMELOT, MAX_DURATION_SEC"
    - "pytest tests/test_pipeline_docker.py -x -q passa com 3 testes GREEN após esta refatoração"
  artifacts:
    - path: "requirements.txt"
      provides: "Lista de dependências Python sem imageio-ffmpeg e librosa"
      contains: "essentia"
    - path: "pipeline.py"
      provides: "Pipeline atualizado: shutil.which para ffmpeg/ffprobe (fail-fast), detect_tuning via Essentia"
      contains: "es.SpectralPeaks"
  key_links:
    - from: "pipeline.py"
      to: "shutil.which('ffmpeg')"
      via: "_system_ffmpeg / _YTDLP_FFMPEG_LOCATION"
      pattern: "shutil\\.which\\(.ffmpeg.\\)"
    - from: "pipeline.py.detect_tuning"
      to: "essentia.standard.TuningFrequency"
      via: "Essentia algoritmo nativo"
      pattern: "TuningFrequency"
---

<objective>
Refatorar `requirements.txt` e `pipeline.py` para remover completamente `imageio-ffmpeg` (D-01, D-02) e `librosa` (D-03). Substituir o bloco de resolução de binários por `shutil.which` com fail-fast (sem fallback silencioso). Reescrever `detect_tuning()` usando Essentia `SpectralPeaks + TuningFrequency` (Pitfall 2 do RESEARCH.md — librosa não pode ser removida sem reescrever esta função).

Purpose: DEPLOY-04 exige imagem Docker sem imageio-ffmpeg; MEMORY.md proíbe librosa em todas as plataformas. Este Plan deixa o pipeline.py "Docker-ready" — pronto para o Dockerfile (Plan 03) que instala ffmpeg via apt.
Output: requirements.txt enxuto, pipeline.py refatorado, 3 testes do Plan 01 verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-docker-compose/13-CONTEXT.md
@.planning/phases/13-docker-compose/13-RESEARCH.md
@.planning/phases/13-docker-compose/13-PATTERNS.md
@.planning/phases/13-01-PLAN.md
@pipeline.py
@requirements.txt
@tests/test_pipeline_docker.py
@tests/test_pipeline.py
@scripts/generate_sample_wav.py

<interfaces>
<!-- Contratos de Essentia que detect_tuning vai consumir -->
<!-- Source: essentia.upf.edu/reference/std_TuningFrequency.html, std_SpectralPeaks.html -->

```python
import essentia.standard as es

# Já usado em pipeline.py (detect_bpm, detect_key)
loader = es.MonoLoader(filename=str(path), sampleRate=44100)
audio = loader()  # retorna numpy array float32

# Novo em detect_tuning:
windowed = es.Windowing(type="blackmanharris62")(frame)  # frame=2048 samples
spectrum = es.Spectrum()(windowed)  # retorna magnitudes float32

freqs, mags = es.SpectralPeaks(
    sampleRate=44100,
    maxPeaks=10000,
    magnitudeThreshold=0.00001,
    maxFrequency=5000,
    minFrequency=20,
    orderBy="frequency",
)(spectrum)
# freqs: array de Hz; mags: array de magnitudes

tuning_hz, tuning_cents = es.TuningFrequency()(freqs, mags)
# tuning_hz: float (Hz, ex: 440.0 a 444.0); tuning_cents: float (desvio em cents)
```

<!-- Contrato público de pipeline.py que NÃO pode mudar -->
def check_duration(url: str, cache_dir: str) -> dict[str, Any]: ...
def download_audio(url: str, cache_dir: str) -> Path: ...
def convert_to_wav(audio_path: Path) -> Path: ...
def validate_wav(wav_path: Path) -> float: ...
def detect_tuning(wav_path: Path) -> float | None: ...  # assinatura preservada
def detect_bpm(wav_path: Path) -> float: ...
def detect_key(wav_path: Path, tuning_hz: float | None) -> tuple[str, float]: ...
def key_to_camelot(key: str) -> str: ...
def analyze_audio(wav_path: Path) -> dict[str, Any]: ...
CAMELOT: dict[str, str]
MAX_DURATION_SEC = 900
TMP_PREFIX = "sg_"
WAV_TMP_DIR = Path("/tmp")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remover imageio-ffmpeg e librosa de requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (versão atual — 17 linhas; alvos a remover: linha 2 `librosa==0.11.0` e linha 16 `imageio-ffmpeg>=0.5.1`)
    - .planning/phases/13-docker-compose/13-CONTEXT.md (D-01, D-03 — remoção explícita)
    - .planning/phases/13-docker-compose/13-RESEARCH.md (seção State of the Art — confirmar que `soundfile==0.13.1` NÃO deve ser removido; `numpy>=2.0,<3.0` e `scipy>=1.10` MANTIDOS porque usados em tests e potencialmente como dep de essentia)
  </read_first>
  <behavior>
    - Após edição: `grep -c "^librosa" requirements.txt` retorna 0
    - Após edição: `grep -c "^imageio-ffmpeg" requirements.txt` retorna 0
    - Após edição: `grep -c "^soundfile" requirements.txt` retorna 1 (preservado)
    - Após edição: `grep -c "^essentia" requirements.txt` retorna 1 (preservado)
    - Após edição: `grep -c "^numpy" requirements.txt` retorna 1 (preservado — usado em tests e scripts/generate_sample_wav.py)
    - Após edição: `grep -c "^scipy" requirements.txt` retorna 1 (preservado)
  </behavior>
  <action>
    Editar requirements.txt para remover EXATAMENTE duas linhas:
    1. A linha `librosa==0.11.0` (D-03)
    2. A linha `imageio-ffmpeg>=0.5.1` (D-01)

    Manter todas as outras 15 linhas exatamente como estão — incluindo `numpy>=2.0,<3.0`, `scipy>=1.10`, `soundfile==0.13.1`, `essentia==2.1b6.dev1389`, `bgutil-ytdlp-pot-provider==0.8.1`, `yt-dlp==2026.3.17`, `fastapi==0.136.1`, `slowapi==0.1.9`, `uvicorn==0.46.0`, `celery[redis]==5.6.3`, `redis==6.4.0`, `itsdangerous==2.2.0`, `httpx>=0.27`, `pytest==9.0.3`, `pytest-subprocess>=1.5`.

    Não reordenar, não adicionar comentários, não tocar em pinos de versão.
  </action>
  <verify>
    <automated>diff <(grep -E "^librosa|^imageio-ffmpeg" requirements.txt) /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^librosa" requirements.txt` retorna 0
    - `grep -c "^imageio-ffmpeg" requirements.txt` retorna 0
    - `grep -c "^essentia==2.1b6.dev1389" requirements.txt` retorna 1
    - `grep -c "^soundfile==0.13.1" requirements.txt` retorna 1
    - `grep -c "^yt-dlp==2026.3.17" requirements.txt` retorna 1
    - `wc -l < requirements.txt` retorna 15 (17 originais menos 2 removidas)
  </acceptance_criteria>
  <done>requirements.txt enxuto sem imageio-ffmpeg e sem librosa; demais pinos preservados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refatorar pipeline.py — remover imageio_ffmpeg e librosa imports, substituir bloco de resolução de binários por shutil.which fail-fast</name>
  <files>pipeline.py</files>
  <read_first>
    - pipeline.py (linhas 25-70 — bloco de imports + `_FFMPEG_PATH = imageio_ffmpeg.get_ffmpeg_exe()` + cálculo de `_FFPROBE_PATH` e `_YTDLP_FFMPEG_LOCATION`)
    - pipeline.py (linhas 316-408 — função `validate_wav` que usa `_FFPROBE_PATH` e `_YTDLP_FFMPEG_LOCATION` no fallback ffmpeg)
    - .planning/phases/13-docker-compose/13-CONTEXT.md (D-02 — `shutil.which` apenas; fail-fast quando None)
    - .planning/phases/13-docker-compose/13-PATTERNS.md (seção `pipeline.py` — modificação; "Substituição — fail-fast sem fallback")
    - .planning/phases/13-docker-compose/13-RESEARCH.md (seção Refatoração de pipeline.py — pattern de fail-fast com `raise RuntimeError`)
    - tests/test_pipeline_docker.py (já criado em Plan 01 — testes que ficarão GREEN)
    - tests/test_pipeline.py (verificar testes existentes que importam pipeline — não devem quebrar)
  </read_first>
  <behavior>
    - Após edição: `import pipeline` carrega sem ModuleNotFoundError nem ImportError
    - Após edição: `grep -c "import imageio_ffmpeg" pipeline.py` retorna 0
    - Após edição: `grep -c "imageio_ffmpeg\\." pipeline.py` retorna 0 (zero usos)
    - Após edição: `grep -c "_FFMPEG_PATH" pipeline.py` retorna 0 (constante removida)
    - Após edição: `grep -c "_FFMPEG_DIR" pipeline.py` retorna 0 (constante removida)
    - Após edição: `_system_ffprobe = shutil.which("ffprobe")` + `raise RuntimeError` se None (fail-fast)
    - Após edição: `_system_ffmpeg = shutil.which("ffmpeg")` + `raise RuntimeError` se None (fail-fast)
    - Após edição: `_FFPROBE_PATH = _system_ffprobe` e `_YTDLP_FFMPEG_LOCATION = _system_ffmpeg` — sem condicionais de fallback
    - validate_wav continua funcional: como _system_ffprobe agora sempre existe (fail-fast no module load), o ramo `_use_ffmpeg_fallback` torna-se inalcançável em runtime mas pode permanecer no código (defensive); alternativa: remover o ramo fallback inteiro
    - test_no_imageio_ffmpeg_import passa (GREEN)
  </behavior>
  <action>
    Editar pipeline.py com as seguintes mudanças cirúrgicas:

    1. **Remover linha 38**: `import imageio_ffmpeg`.

    2. **Substituir linhas 46-68** (bloco inteiro de resolução de binários) por um novo bloco curto:
       - Remover `_FFMPEG_PATH = imageio_ffmpeg.get_ffmpeg_exe()` e a constante `_FFMPEG_DIR`.
       - Manter `_system_ffprobe = shutil.which("ffprobe")` mas trocar o `logger.warning` por `raise RuntimeError("ffprobe not found in PATH. Install ffmpeg system package: apt-get install ffmpeg")` quando `_system_ffprobe is None`.
       - Definir `_FFPROBE_PATH = _system_ffprobe` (sem `or fallback`).
       - Manter `_system_ffmpeg = shutil.which("ffmpeg")` e adicionar `raise RuntimeError("ffmpeg not found in PATH. Install ffmpeg system package: apt-get install ffmpeg")` quando `_system_ffmpeg is None`.
       - Definir `_YTDLP_FFMPEG_LOCATION = _system_ffmpeg` (sem condicional).
       - Preservar `_NODE_PATH = shutil.which("node")` (linha 69) como está.

    3. **Atualizar docstring/comentário** nas linhas 55-65 (que referenciam imageio-ffmpeg): substituir explicação pelo seguinte texto enxuto: `# DEPLOY-04 fix: ffmpeg/ffprobe resolvidos via PATH do sistema (apt no Dockerfile). Sem fallback — startup falha rápido se ffmpeg não estiver instalado (D-02).` Sem fenced code blocks.

    4. **validate_wav (linhas 339-380)**: como `_FFPROBE_PATH` agora SEMPRE aponta para um binário existente, o ramo `_use_ffmpeg_fallback = not Path(_FFPROBE_PATH).exists()` torna-se sempre False. Pode-se deixar o código como está (defensivo) OU simplificar removendo o ramo `_use_ffmpeg_fallback`. Discretion: manter o código defensivo para minimizar churn — apenas garantir que `_FFPROBE_PATH` e `_YTDLP_FFMPEG_LOCATION` existem como antes. Não introduzir regressão em validate_wav.

    NÃO tocar em `detect_tuning` nesta task — isso é Task 3.
    NÃO remover `import librosa` ainda — isso é Task 3 (para evitar ModuleNotFoundError intermediário entre tasks).
    NÃO tocar em `detect_bpm`, `detect_key`, `key_to_camelot`, `analyze_audio`, CAMELOT — preservados intactos.
  </action>
  <verify>
    <automated>python -c "import pipeline; print('OK')" && pytest tests/test_pipeline_docker.py::test_no_imageio_ffmpeg_import -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import imageio_ffmpeg" pipeline.py` retorna 0
    - `grep -c "imageio_ffmpeg" pipeline.py` retorna 0 (zero menções, incluindo em comentários antigos)
    - `grep -c "_FFMPEG_DIR" pipeline.py` retorna 0
    - `grep -c "shutil.which(.ffprobe.)" pipeline.py` >= 1
    - `grep -c "shutil.which(.ffmpeg.)" pipeline.py` >= 1
    - `grep -c "raise RuntimeError" pipeline.py` >= 2 (uma para ffprobe, uma para ffmpeg)
    - `python -c "import pipeline"` retorna exit code 0 (módulo carrega — librosa ainda importado mas instalado)
    - `pytest tests/test_pipeline_docker.py::test_no_imageio_ffmpeg_import -x -q` retorna exit code 0 (GREEN)
    - `pytest tests/test_pipeline_docker.py::test_no_librosa_import -x -q` retorna exit code 1 (ainda RED — librosa ainda importado; Task 3 corrige)
  </acceptance_criteria>
  <done>pipeline.py não menciona mais imageio_ffmpeg em lugar nenhum; binários resolvidos exclusivamente via shutil.which com fail-fast; módulo continua carregando; test_no_imageio_ffmpeg_import GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reescrever detect_tuning com Essentia (SpectralPeaks + TuningFrequency) e remover import librosa + uso de numpy interno</name>
  <files>pipeline.py</files>
  <read_first>
    - pipeline.py (linha 39 — `import librosa`; linha 40 — `import numpy as np`; linhas 411-440 — função `detect_tuning` baseada em `librosa.load`, `librosa.effects.hpss`, `librosa.estimate_tuning`, `librosa.tuning_to_A4`)
    - pipeline.py (linhas 442-461 — função `detect_bpm` como exemplo do pattern Essentia `es.MonoLoader` + algoritmo + `return float(...)`)
    - pipeline.py (linhas 463-487 — função `detect_key` como segundo exemplo do mesmo pattern)
    - .planning/phases/13-docker-compose/13-RESEARCH.md (seção Pattern 4 — Substituição de detect_tuning librosa → Essentia; nota sobre tuning_cents < 5.0 como aproximação para o gate de "beat percussivo")
    - .planning/phases/13-docker-compose/13-RESEARCH.md (seção Open Questions — recomendação: reescrever com Essentia SpectralPeaks + TuningFrequency, fallback retornar None se freqs vazio)
    - .planning/phases/13-docker-compose/13-PATTERNS.md (seção "Substituição — detect_tuning com Essentia")
    - .planning/phases/13-docker-compose/13-CONTEXT.md (D-03 — librosa proibida; MEMORY.md também)
    - tests/test_pipeline_docker.py (test_detect_tuning_essentia — espera retorno float ou None)
    - tests/test_pipeline.py (procurar test_detect_tuning_* — confirmar quais testes existentes da librosa-version vão precisar passar com a Essentia-version OU serem ajustados)
  </read_first>
  <behavior>
    - Após edição: `import pipeline` carrega sem ModuleNotFoundError mesmo se librosa for desinstalado
    - Após edição: `grep -c "import librosa" pipeline.py` retorna 0
    - Após edição: `grep -c "librosa" pipeline.py` retorna 0 (zero menções)
    - `detect_tuning(wav_path)` retorna `float` quando há conteúdo harmônico detectável com desvio de tuning, ou `None` quando o sinal não tem peaks espectrais ou o desvio é insignificante
    - `from pipeline import detect_tuning` funciona
    - test_no_librosa_import passa (GREEN)
    - test_detect_tuning_essentia passa (GREEN) — entrada é WAV senoidal a 440Hz, saída é None (desvio ~0) ou float próximo de 440.0
    - Assinatura preservada: `def detect_tuning(wav_path: Path) -> float | None`
  </behavior>
  <action>
    Editar pipeline.py com mudanças cirúrgicas:

    1. **Remover linha 39**: `import librosa`.

    2. **Remover linha 40**: `import numpy as np` — APENAS SE não houver mais nenhum uso de `np.` no arquivo após a reescrita de `detect_tuning`. Verificar com `grep -n "np\\." pipeline.py` após editar. Se o único uso era em `detect_tuning` (linhas 431-433), remover o import; caso contrário, manter.

    3. **Substituir a função `detect_tuning` inteira (linhas 411-440)** por uma nova implementação Essentia. Estrutura esperada:
       - Docstring atualizada: `"""Detecta frequência de referência (concert pitch) via Essentia SpectralPeaks + TuningFrequency.\n\nSubstitui a implementação baseada em librosa.estimate_tuning + HPSS (D-03 Phase 13).\nO gate 'beat puramente percussivo' do librosa.HPSS é aproximado por: se SpectralPeaks\nretornar zero peaks OU se TuningFrequency retornar desvio (em cents) < 5.0, retornar None.\n\nArgs:\n    wav_path: Path para o WAV.\n\nReturns:\n    Frequência em Hz (float) ou None.\n"""`
       - Corpo:
         a) `audio = es.MonoLoader(filename=str(wav_path), sampleRate=44100)()` — mesmo pattern de detect_bpm.
         b) Aplicar `es.Windowing(type="blackmanharris62")` ao primeiro frame de 2048 samples: `windowed = es.Windowing(type="blackmanharris62")(audio[:2048])`. Se `len(audio) < 2048`, retornar None (sinal curto demais).
         c) Aplicar `es.Spectrum()`: `spectrum = es.Spectrum()(windowed)`.
         d) Aplicar `es.SpectralPeaks(sampleRate=44100, maxPeaks=10000, magnitudeThreshold=0.00001, maxFrequency=5000, minFrequency=20, orderBy="frequency")`: `freqs, mags = es.SpectralPeaks(...)(spectrum)`.
         e) Se `len(freqs) == 0`, retornar None (sem conteúdo harmônico).
         f) `tuning_hz, tuning_cents = es.TuningFrequency()(freqs, mags)`.
         g) Se `abs(tuning_cents) < 5.0`, retornar None (sem desvio significativo — A=440 padrão).
         h) Retornar `float(tuning_hz)`.

    4. **Verificar uso de numpy após reescrita**: rodar mental `grep -c "np\\." pipeline.py` no resultado. Se for 0, remover `import numpy as np`. Se > 0 (improvável, mas Camelot ou outro lugar pode usar), manter.

    5. **NÃO tocar** em `detect_bpm`, `detect_key`, `key_to_camelot`, `analyze_audio`, CAMELOT, `convert_to_wav`, `validate_wav`, `download_audio`, `check_duration`. Preservar intactos.

    Após esta task, o estado de pipeline.py é Docker-ready: sem imageio-ffmpeg, sem librosa, sem numpy (se aplicável), usando apenas Essentia + yt-dlp + ffmpeg via shutil.which.

    Se `tests/test_pipeline.py` tiver testes que usam o `detect_tuning` legado e quebrarem com a nova implementação Essentia, marcar com `@pytest.mark.skip(reason="Wave 1 Phase 13 — detect_tuning reescrita; teste legado a ser revisado em Phase 14")` SOMENTE para os testes que não puderem ser facilmente adaptados. Não criar testes novos nesta task — Plan 01 já fez isso.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_docker.py -x -q && python -c "import pipeline; print(pipeline.detect_tuning)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import librosa" pipeline.py` retorna 0
    - `grep -c "librosa" pipeline.py` retorna 0 (sem menções em código nem comentários)
    - `grep -c "es.SpectralPeaks" pipeline.py` >= 1
    - `grep -c "es.TuningFrequency" pipeline.py` >= 1
    - `grep -c "es.Windowing" pipeline.py` >= 1
    - `grep -c "def detect_tuning" pipeline.py` retorna 1 (assinatura preservada, função reescrita)
    - `python -c "import pipeline; assert callable(pipeline.detect_tuning)"` exit 0
    - `pytest tests/test_pipeline_docker.py::test_no_librosa_import -x -q` exit 0 (GREEN)
    - `pytest tests/test_pipeline_docker.py::test_detect_tuning_essentia -x -q` exit 0 (GREEN)
    - `pytest tests/test_pipeline_docker.py -x -q` exit 0 (3 testes GREEN)
    - `grep -v '^#' pipeline.py | grep -c "imageio_ffmpeg\\|librosa"` retorna 0
  </acceptance_criteria>
  <done>pipeline.py 100% sem imageio-ffmpeg e sem librosa; detect_tuning reescrita com Essentia; 3 testes do Plan 01 verdes; módulo importa sem erro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container → ffmpeg binary | pipeline.py invoca ffmpeg/ffprobe via subprocess; path resolvido por shutil.which (PATH controlado pelo Dockerfile) |
| pipeline.py → essentia.standard | Algoritmo nativo C++ executado in-process; sem rede; sem input externo direto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | shutil.which("ffmpeg") pode retornar binário malicioso se PATH for adulterado | mitigate | No container, PATH é controlado pelo Dockerfile (apt instala em /usr/bin); fail-fast com `raise RuntimeError` se ausente — sem fallback silencioso (D-02) |
| T-13-04 | Denial of Service | detect_tuning com WAV adversarial pode consumir CPU/RAM em es.SpectralPeaks com maxPeaks alto | accept | maxPeaks=10000 e maxFrequency=5000 limitam o trabalho; sinal já validado por validate_wav (duração ≤ 15min) antes desta função |
| T-13-05 | Information Disclosure | Mensagens de erro "ffmpeg not found in PATH" não vazam segredos | accept | Mensagens são strings literais sem variáveis de ambiente |
</threat_model>

<verification>
- `pytest tests/test_pipeline_docker.py -x -q` deve mostrar 3 testes GREEN (no_imageio, no_librosa, detect_tuning_essentia).
- `python -c "import pipeline; print(pipeline.detect_tuning(Path('scripts/sample.wav')))"` deve retornar None ou float sem exceção (se script tiver WAV de exemplo).
- `pytest tests/ -x -q` (full suite) deve continuar passando — testes que dependiam de comportamento exato librosa-based em detect_tuning podem precisar de skip/ajuste documentado no SUMMARY.
- `grep -rn "imageio_ffmpeg\\|librosa" pipeline.py requirements.txt` deve retornar zero linhas (excluindo comentários removidos).
</verification>

<success_criteria>
- [ ] requirements.txt sem imageio-ffmpeg e sem librosa (15 linhas no total)
- [ ] pipeline.py sem `import imageio_ffmpeg` e sem `import librosa`
- [ ] Bloco de resolução de binários usa `shutil.which` com `raise RuntimeError` fail-fast
- [ ] detect_tuning reescrita com Essentia SpectralPeaks + TuningFrequency
- [ ] 3 testes em tests/test_pipeline_docker.py GREEN
- [ ] `python -c "import pipeline"` exit 0
- [ ] Contrato público preservado (todas as funções e constantes exportadas)
- [ ] Commit message segue convenção `refactor(13): ...`
</success_criteria>

<output>
After completion, create `.planning/phases/13-docker-compose/13-02-SUMMARY.md` documentando:
- Linhas exatas removidas/alteradas em pipeline.py
- Decisão sobre numpy (mantido ou removido)
- Status dos 3 testes do Plan 01 (todos GREEN)
- Status da full suite (pytest tests/ -x -q) — se algum teste legado precisou skip, listar
- Tamanho final de pipeline.py (linhas antes/depois)
</output>
</content>
</invoke>