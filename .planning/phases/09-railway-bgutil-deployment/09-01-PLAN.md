---
phase: 09-railway-bgutil-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md
autonomous: false
requirements:
  - DEPLOY-02
  - DEPLOY-03
tags:
  - railway
  - deployment
  - bgutil
  - verification
  - smoke-test

user_setup: []

must_haves:
  truths:
    - "O deployment do Celery Worker (10ec98b3) atingiu status SUCCESS no Railway"
    - "O deployment do Uvicorn (02cda13b) está em status SUCCESS no Railway"
    - "Logs de startup do Uvicorn não contêm 'BGUTIL_BASE_URL not set', 'connection refused' nem nenhum traceback de exceção relacionado ao bgutil"
    - "Logs de startup do Celery Worker não contêm 'BGUTIL_BASE_URL not set', 'connection refused' nem nenhum traceback de exceção relacionado ao bgutil"
    - "A URL pública do serviço Uvicorn é conhecida e responde a requisições HTTP"
    - "Submeter um beat URL real do YouTube (<15 min) via POST /jobs ao Uvicorn público resulta em job que atinge status='done' com wav_url presente em até 5 minutos"
    - "BGUTIL_BASE_URL=http://bgutil.railway.internal:4416 está configurado em ambos os serviços (Uvicorn 248e8eaf-68e1-4fb1-beb0-448bd26f317a e Celery Worker 145135bf-201e-4fd7-8ea2-ed790d334dbf)"
  artifacts:
    - path: ".planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md"
      provides: "Log estruturado dos resultados de verificação dos 4 tasks (status de deployments, trechos de log relevantes, job_id e wav_url do smoke test)"
      contains: "deployment_status: SUCCESS"
      min_lines: 40
  key_links:
    - from: "Uvicorn service (248e8eaf-68e1-4fb1-beb0-448bd26f317a)"
      to: "bgutil service (2fc3a8a5-b50b-48e2-b408-87167f5ac28a)"
      via: "BGUTIL_BASE_URL env var http://bgutil.railway.internal:4416"
      pattern: "Railway private networking — bgutil.railway.internal:4416"
    - from: "Celery Worker service (145135bf-201e-4fd7-8ea2-ed790d334dbf)"
      to: "bgutil service (2fc3a8a5-b50b-48e2-b408-87167f5ac28a)"
      via: "BGUTIL_BASE_URL env var http://bgutil.railway.internal:4416"
      pattern: "Railway private networking — bgutil.railway.internal:4416"
    - from: "POST /jobs no Uvicorn público"
      to: "GET /jobs/{id} polling até status=done"
      via: "Celery worker consome fila Redis, executa pipeline (download via bgutil → ffmpeg WAV → librosa)"
      pattern: "POST /jobs returns 202 with job_id; GET /jobs/{job_id} eventually returns status=done with wav_url"
---

<objective>
Verificar que a configuração já aplicada para a integração bgutil ↔ Uvicorn ↔ Celery Worker no Railway está funcional ponta-a-ponta. Esta fase é de **verificação**, não de configuração — toda mudança de env var (BGUTIL_BASE_URL) e o deploy do serviço bgutil já foram aplicados via MCP Railway durante /gsd-discuss-phase.

Purpose: Fechar a Phase 9 confirmando que (1) ambos os deployments redisparados no fim da discuss-phase atingiram SUCCESS, (2) os logs de startup não revelam erros de conexão com o bgutil, e (3) o pipeline completo funciona com um beat real, demonstrando que DEPLOY-02 (bgutil rodando) e DEPLOY-03 (BGUTIL_BASE_URL configurado em ambos os serviços) estão satisfeitos.

Decisions from 09-CONTEXT.md implemented/verified by this plan:
- D-01: bgutil porta 4416 por padrão (sem PORT env var) — confirmado por logs de startup do bgutil
- D-02: URL interna http://bgutil.railway.internal:4416 — verificada como BGUTIL_BASE_URL em ambos os serviços
- D-03: bgutil service ID 2fc3a8a5 deployment 1a83308d SUCCESS — usado nas chamadas MCP
- D-04: YTDLP_PO_TOKEN permanece vazio; bgutil é a única fonte de tokens — verificado por pipeline funcional sem token estático
- D-05: código usa web client quando bgutil_base_url está configurado — validado pelo smoke test E2E
- D-07: redeploy dos serviços disparado em 2026-05-11 — verificação de SUCCESS é Task 1 e Task 2
- D-08: sequência de verificação MCP (deployment status → logs Uvicorn → logs Celery → beat URL) — implementada pelas Tasks 1, 2, 3, 4
- D-09: toda inspeção via MCP Railway, sem dashboard — todas as tasks usam mcp__railway__* exclusivamente

Output: 09-01-SMOKE-TEST.md com status de deployments, trechos de log relevantes, job_id e wav_url do smoke test bem-sucedido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-railway-bgutil-deployment/09-CONTEXT.md
@api/config.py
@api/tasks.py
@pipeline.py

<interfaces>
<!-- IDs Railway exatos extraídos de 09-CONTEXT.md (D-03, D-06, D-07). -->
<!-- O executor DEVE usar estes IDs literais nas chamadas MCP — não inferir. -->

Project ID: 1b0d39e7-bda8-4b44-80da-1751ba65c76f
Environment ID: a42aa82d-259e-4eb6-85a5-98355d69e27a

Services:
- bgutil:        2fc3a8a5-b50b-48e2-b408-87167f5ac28a (jim60105/bgutil-pot v0.8.1, port 4416)
- Uvicorn:       248e8eaf-68e1-4fb1-beb0-448bd26f317a (web service)
- Celery Worker: 145135bf-201e-4fd7-8ea2-ed790d334dbf

Deployments to verify (disparados em /gsd-discuss-phase com BGUTIL_BASE_URL setado):
- Uvicorn:       02cda13b (status na criação do contexto: SUCCESS)
- Celery Worker: 10ec98b3 (status na criação do contexto: BUILDING)
- bgutil:        1a83308d (status confirmado: SUCCESS — POT server v0.8.1 listening on 0.0.0.0:4416)

Env var aplicada em Uvicorn e Celery Worker:
  BGUTIL_BASE_URL=http://bgutil.railway.internal:4416

Endpoints relevantes do pipeline (api/main.py):
- POST /jobs               — body {"url": "<youtube_url>"} → 202 {"job_id": "..."}
- GET  /jobs/{job_id}      — → {"status": "queued|downloading|converting|analyzing|done|failed", "wav_url": "...", "bpm": ..., "key": "...", "error": "..."}
- GET  /files/{job_id}     — streams WAV file
- GET  /health             — Redis ping check

Strings que NÃO podem aparecer nos logs (gates negativos):
- "BGUTIL_BASE_URL not set"
- "connection refused"
- "Connection refused"
- "ConnectionRefusedError"
- "Failed to connect to bgutil"
- "bgutil.railway.internal" combinado com "Name or service not known"

Strings positivas esperadas em Uvicorn:
- "Application startup complete"
- "Uvicorn running on"

Strings positivas esperadas em Celery Worker:
- "celery@" e "ready."
- "[tasks]" listando api.tasks.process_job
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar status final do Celery Worker deployment 10ec98b3</name>
  <files>.planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md</files>
  <read_first>
    - .planning/phases/09-railway-bgutil-deployment/09-CONTEXT.md (D-06, D-07: deployment 10ec98b3 estava em BUILDING ao criar o contexto)
    - Service ID Celery Worker: 145135bf-201e-4fd7-8ea2-ed790d334dbf
    - Project ID: 1b0d39e7-bda8-4b44-80da-1751ba65c76f
    - Environment ID: a42aa82d-259e-4eb6-85a5-98355d69e27a
  </read_first>
  <action>
    Usar mcp__railway__list-deployments com projectId=1b0d39e7-bda8-4b44-80da-1751ba65c76f, environmentId=a42aa82d-259e-4eb6-85a5-98355d69e27a, serviceId=145135bf-201e-4fd7-8ea2-ed790d334dbf, limit=5 para localizar o deployment 10ec98b3 e ler seu status. Se status=BUILDING ou DEPLOYING, repetir a chamada após 30 segundos (até no máximo 5 tentativas, 2.5 minutos). Se status=FAILED ou CRASHED, abortar plano e usar mcp__railway__railway-agent (prompt curto descrevendo: "Celery Worker deployment 10ec98b3 falhou após setar BGUTIL_BASE_URL — diagnosticar"). Quando atingir SUCCESS, chamar mcp__railway__get-logs com deploymentId=10ec98b3, type=DEPLOY, limit=200 e capturar trecho dos últimos ~50 linhas. Verificar que logs contêm 'celery@' + 'ready.' e que NÃO contêm nenhuma das strings proibidas listadas em <interfaces>. Criar arquivo .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md com seção '## Task 1 — Celery Worker Deployment 10ec98b3' contendo: deployment status final, timestamp, número de tentativas até SUCCESS, trecho dos últimos 30 logs (formatado em bloco de código), confirmação explícita de que strings proibidas não apareceram. Se qualquer gate falhar, registrar 'GATE FAILED' com a string exata encontrada e retornar erro ao orquestrador.
  </action>
  <verify>
    <automated>grep -E 'deployment_status:\s*SUCCESS' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md && grep -v '^#' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md | grep -cE '(connection refused|BGUTIL_BASE_URL not set|ConnectionRefusedError)' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Deployment 10ec98b3 está em status SUCCESS confirmado via mcp__railway__list-deployments
    - Logs do deployment contêm 'celery@' seguido de 'ready.' (worker startup completo)
    - Logs do deployment contêm '[tasks]' listando 'api.tasks.process_job' (tarefas registradas)
    - Logs do deployment NÃO contêm nenhuma das strings proibidas: 'BGUTIL_BASE_URL not set', 'connection refused', 'ConnectionRefusedError', 'Failed to connect to bgutil'
    - 09-01-SMOKE-TEST.md criado com seção 'Task 1' contendo os trechos de log capturados
  </acceptance_criteria>
  <done>
    Celery Worker confirmado healthy com BGUTIL_BASE_URL injetada; nenhum erro de conexão com bgutil nos logs de startup; resultado documentado no arquivo de smoke test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar logs de startup do Uvicorn deployment 02cda13b</name>
  <files>.planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md</files>
  <read_first>
    - .planning/phases/09-railway-bgutil-deployment/09-CONTEXT.md (D-06: Uvicorn deployment 02cda13b estava em SUCCESS)
    - Service ID Uvicorn: 248e8eaf-68e1-4fb1-beb0-448bd26f317a
    - Deployment ID: 02cda13b
    - api/main.py — entender que startup chama lifespan que valida cookies e Redis (não bgutil diretamente)
  </read_first>
  <action>
    Confirmar primeiro que o deployment 02cda13b ainda está em status SUCCESS via mcp__railway__list-deployments com projectId=1b0d39e7-bda8-4b44-80da-1751ba65c76f, environmentId=a42aa82d-259e-4eb6-85a5-98355d69e27a, serviceId=248e8eaf-68e1-4fb1-beb0-448bd26f317a, limit=5. Em seguida chamar mcp__railway__get-logs com deploymentId=02cda13b, type=DEPLOY, limit=300 e capturar logs de startup. Verificar gates positivos: a string 'Application startup complete' DEVE aparecer; a string 'Uvicorn running on' DEVE aparecer. Verificar gates negativos: NENHUMA das strings em <interfaces> ('BGUTIL_BASE_URL not set', 'connection refused', etc.) pode aparecer. Anexar ao arquivo 09-01-SMOKE-TEST.md uma seção '## Task 2 — Uvicorn Deployment 02cda13b' contendo: status atual do deployment, trecho de logs com as linhas onde 'Application startup complete' e 'Uvicorn running on' aparecem (com 5 linhas de contexto antes e depois), confirmação explícita de que cada string proibida foi verificada e não está presente. Se qualquer gate falhar, registrar 'GATE FAILED' com detalhes e parar o plano.
  </action>
  <verify>
    <automated>grep -A2 'Task 2 — Uvicorn' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md | grep -q 'SUCCESS' && grep -v '^#' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md | grep -q 'Application startup complete' && grep -v '^#' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md | grep -cE '(BGUTIL_BASE_URL not set|connection refused|ConnectionRefusedError)' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Deployment 02cda13b confirmado em status SUCCESS
    - Logs contêm 'Application startup complete' (FastAPI lifespan terminou sem exceção)
    - Logs contêm 'Uvicorn running on' (servidor escutando na porta)
    - Logs NÃO contêm: 'BGUTIL_BASE_URL not set', 'connection refused', 'ConnectionRefusedError', 'Failed to connect to bgutil'
    - 09-01-SMOKE-TEST.md atualizado com seção 'Task 2' completa (deployment status + trechos de log + confirmação de gates negativos)
  </acceptance_criteria>
  <done>
    Uvicorn confirmado em produção com startup limpo e sem erros relacionados ao bgutil; trechos de log documentados.
  </done>
</task>

<task type="auto">
  <name>Task 3: Obter URL pública do serviço Uvicorn</name>
  <files>.planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md</files>
  <read_first>
    - Service ID Uvicorn: 248e8eaf-68e1-4fb1-beb0-448bd26f317a
    - Project ID: 1b0d39e7-bda8-4b44-80da-1751ba65c76f
    - Environment ID: a42aa82d-259e-4eb6-85a5-98355d69e27a
  </read_first>
  <action>
    Tentar primeiro mcp__railway__service-info com serviceId=248e8eaf-68e1-4fb1-beb0-448bd26f317a, projectId=1b0d39e7-bda8-4b44-80da-1751ba65c76f, environmentId=a42aa82d-259e-4eb6-85a5-98355d69e27a para extrair o domínio público (campo serviceDomains ou customDomains). Se a ferramenta service-info não estiver disponível, usar mcp__railway__domain-list com mesmos IDs. Se nenhuma das duas funcionar, fallback final: mcp__railway__railway-agent com prompt: "Liste o domínio público do service 248e8eaf-68e1-4fb1-beb0-448bd26f317a no project 1b0d39e7-bda8-4b44-80da-1751ba65c76f environment a42aa82d-259e-4eb6-85a5-98355d69e27a — apenas retorne o(s) hostname(s) configurado(s)". Capturar a URL no formato 'https://<hostname>'. Validar com curl -sS -o /dev/null -w '%{http_code}' https://<hostname>/health — código esperado: 200. Se 200, anexar ao 09-01-SMOKE-TEST.md uma seção '## Task 3 — Uvicorn Public URL' contendo: hostname descoberto, ferramenta MCP usada, output de curl /health (status code + body se possível). Se curl falhar (timeout, código != 200), registrar e parar.
  </action>
  <verify>
    <automated>grep -A5 'Task 3 — Uvicorn Public URL' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md | grep -qE 'https://[a-zA-Z0-9.-]+' && grep -A10 'Task 3 — Uvicorn Public URL' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md | grep -q 'health.*200'</automated>
  </verify>
  <acceptance_criteria>
    - URL pública do Uvicorn descoberta no formato 'https://<hostname>'
    - GET /health na URL pública retorna HTTP 200
    - 09-01-SMOKE-TEST.md atualizado com hostname e prova do curl /health=200
  </acceptance_criteria>
  <done>
    URL pública confirmada respondendo; hostname registrado para uso na Task 4.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke test do pipeline completo (POST /jobs → status=done)</name>
  <files>.planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md</files>
  <read_first>
    - Hostname público do Uvicorn registrado na Task 3
    - REQUIREMENTS.md: CORE-05 (limite de 15 min), DEPLOY-02, DEPLOY-03
    - 09-CONTEXT.md decision D-08: sequência de verificação termina com submissão de beat URL real
    - api/main.py — endpoints POST /jobs e GET /jobs/{id}
  </read_first>
  <what-built>
    Tasks 1-3 confirmaram que ambos os deployments estão SUCCESS, logs de startup estão limpos (sem erros de bgutil), e a URL pública do Uvicorn responde HTTP 200 em /health. Tudo pronto para o teste end-to-end.
  </what-built>
  <action>
    Solicitar ao usuário uma URL real do YouTube de um beat instrumental com menos de 15 minutos (preferencialmente 3-5 min para feedback rápido). Após receber a URL, executar o smoke test sequencial:
    
    1. POST /jobs: `curl -sS -X POST -H 'Content-Type: application/json' -d '{"url":"<USER_URL>"}' https://<HOSTNAME>/jobs` — esperar HTTP 202 com JSON contendo job_id. Capturar job_id literal.
    2. Polling: a cada 5 segundos, executar `curl -sS https://<HOSTNAME>/jobs/<JOB_ID>` e parsear o campo status. Continuar até status='done' OU status='failed' OU 60 iterações (5 min máximo).
    3. Se status='done': verificar que a resposta contém wav_url, bpm (numérico), key (string). Validar download com `curl -sS -o /tmp/sg_smoke_test.wav -w '%{http_code}' https://<HOSTNAME><wav_url>` — esperar 200 e file size > 100KB.
    4. Se status='failed': capturar campo 'error' completo. Se mencionar bgutil/connection refused, abortar fase e usar mcp__railway__railway-agent para investigar (prompt: "POST /jobs ao Uvicorn 248e8eaf-68e1-4fb1-beb0-448bd26f317a falhou com '<error>' — investigar logs RUN do Celery Worker 145135bf-201e-4fd7-8ea2-ed790d334dbf nos últimos 5 min e diagnosticar conexão bgutil").
    5. Capturar logs RUN dos últimos 5 minutos de ambos os serviços via mcp__railway__get-logs (type=RUN) para anexar ao smoke test.
    
    Anexar ao 09-01-SMOKE-TEST.md uma seção '## Task 4 — End-to-End Smoke Test' contendo: URL submetida, job_id, número de polls, status final (done/failed), wav_url se done, bpm + key se done, file size do WAV baixado, trecho relevante dos logs RUN do Celery Worker durante o job.
  </action>
  <verify>
    <automated>grep -A20 'Task 4 — End-to-End' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md | grep -qE 'status:\s*done' && grep -A20 'Task 4 — End-to-End' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md | grep -qE 'wav_url:' && grep -A20 'Task 4 — End-to-End' .planning/phases/09-railway-bgutil-deployment/09-01-SMOKE-TEST.md | grep -qE 'file_size_bytes:\s*[1-9][0-9]{5,}'</automated>
  </verify>
  <how-to-verify>
    1. Forneça uma URL de beat real do YouTube (instrumental, menos de 15 min)
    2. Acompanhe o output: você deve ver job_id, polls com transições de status (queued → downloading → converting → analyzing → done)
    3. Status final deve ser 'done' com bpm e key plausíveis
    4. WAV baixado deve ter mais de 100KB e abrir num player de áudio
    5. Confirmar com 'approved' se tudo OK; descrever falha se algo der errado
  </how-to-verify>
  <acceptance_criteria>
    - POST /jobs retornou HTTP 202 com job_id
    - Polling resultou em status='done' (não 'failed') em até 5 minutos
    - Resposta com status='done' contém wav_url, bpm (numérico), key (string)
    - GET no wav_url retornou HTTP 200 e arquivo com size > 100KB
    - Logs RUN do Celery Worker durante o job NÃO contêm 'connection refused' ou 'BGUTIL_BASE_URL not set'
    - 09-01-SMOKE-TEST.md tem seção 'Task 4' completa com job_id, status final, wav_url, bpm, key, file_size_bytes
  </acceptance_criteria>
  <resume-signal>Digite 'approved' se o smoke test foi bem-sucedido, ou descreva o problema observado.</resume-signal>
  <done>
    Pipeline end-to-end validado com beat real: bgutil gerou PO Token, yt-dlp baixou áudio, ffmpeg converteu para WAV, librosa analisou BPM/key. Phase 9 (DEPLOY-02 + DEPLOY-03) está provada funcional.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Railway edge → Uvicorn | Smoke test envia POST /jobs com URL controlada pelo operador via TLS Railway |
| Uvicorn (web) ↔ Celery Worker | Comunicação via Redis interno Railway (private networking) — payload do job (URL YouTube) trafega aqui |
| Celery Worker ↔ bgutil | Worker envia requisições HTTP (sem TLS) ao bgutil via bgutil.railway.internal:4416 — Railway private network |
| Celery Worker → YouTube | yt-dlp faz download externo HTTPS, autenticado por cookies + PO Token gerado pelo bgutil |
| Ferramenta MCP Railway → API Railway | Operador invoca mcp__railway__* tools com tokens MCP — não há credencial nova introduzida nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | bgutil endpoint resolvido via Railway DNS interno | accept | Railway private networking confina o hostname `bgutil.railway.internal` ao tenant; nenhum DNS externo pode ser spoofed. Sem exposição pública na porta 4416 (D-01/D-02). |
| T-09-02 | Tampering | BGUTIL_BASE_URL injetado em runtime | accept | Variável é setada via Railway control plane (autenticado por MCP), não modificável em runtime pelo container; mudança requer redeploy. Sem ação adicional necessária nesta fase. |
| T-09-03 | Information Disclosure | Logs MCP get-logs podem incluir cookies, tokens ou URLs sensíveis | mitigate | Ao anexar trechos de log ao 09-01-SMOKE-TEST.md, executor DEVE redatar qualquer string que case com `__Secure-3PSID=*`, `Bearer *`, `eyJ*` (JWT-like), ou query strings YouTube com `pot=*` antes de gravar. Substituir por '<REDACTED>'. |
| T-09-04 | Denial of Service | Smoke test pode disparar rate limit `/jobs` (10/min) por IP do operador | accept | Plano executa apenas 1 POST /jobs no Task 4; 10/min é limite confortável. Se Task 4 precisar repetir o smoke test (ex: erro intermitente), aguardar 60s entre tentativas. |
| T-09-05 | Information Disclosure | URL pública Uvicorn descoberta na Task 3 + job_id na Task 4 ficam gravados em smoke-test.md commitado em git | accept | Hostname Railway é semi-público (qualquer scan TLS revela); job_ids são UUIDs efêmeros (TTL Redis curto) sem valor após a fase. README/PROJECT.md já documentam que SoundGrabber é uma ferramenta pública sem dados de usuário. |
| T-09-06 | Repudiation | Sem audit log estruturado de quem disparou o smoke test no Railway | accept | Operador único (solo dev); MCP Railway já registra ações no painel Railway. Audit local fora de escopo da fase. |
| T-09-07 | Elevation of Privilege | mcp__railway__railway-agent com prompt amplo poderia executar ações destrutivas | mitigate | Plano restringe railway-agent a prompts somente-leitura ('investigar', 'liste', 'diagnostique'). Operações destrutivas (accept-deploy, delete-service) NÃO podem ser invocadas neste plano — gate humano da Task 4 confirma cada passo antes de prosseguir. |
| T-09-08 | Spoofing | YouTube URL submetida no smoke test poderia apontar para conteúdo malicioso | accept | Pipeline já tem validação CORE-02 (allowlist YouTube) + CORE-05 (limite 15 min) + sanitização ffmpeg. Operador escolhe a URL; sem input externo nesta fase. |
</threat_model>

<verification>
Phase 9 verification:
- Ambos os deployments (02cda13b Uvicorn, 10ec98b3 Celery Worker) confirmados SUCCESS via MCP Railway
- Logs de startup de ambos sem erros de conexão ao bgutil (gates negativos passam)
- URL pública Uvicorn descoberta e /health responde 200
- Smoke test end-to-end: POST /jobs com beat real → polling → status=done com WAV válido (>100KB) baixável
- 09-01-SMOKE-TEST.md gerado com todas as 4 seções de task, contendo evidências verificáveis (status, trechos de log, job_id, wav_url, file size)
- Sem exposição de cookies/tokens nos logs anexados (T-09-03 mitigado)
</verification>

<success_criteria>
- DEPLOY-02 satisfeito: bgutil rodando na porta 4416 e acessível via private network (validado pelo smoke test bem-sucedido — yt-dlp+bgutil baixou o áudio)
- DEPLOY-03 satisfeito: BGUTIL_BASE_URL configurado em ambos os serviços, confirmado por logs limpos + pipeline funcional
- Ambos os 3 success criteria do roadmap Phase 9 verificados:
  1. ✅ Serviço bgutil healthy na porta 4416 interna (provado indiretamente pelo download bem-sucedido)
  2. ✅ BGUTIL_BASE_URL setado em ambos os serviços (já aplicado em discuss-phase, confirmado pelos deployments SUCCESS com a env var)
  3. ✅ Logs sem 'BGUTIL_BASE_URL not set' nem connection-refused em ambos os serviços
- Arquivo de evidência 09-01-SMOKE-TEST.md commitado em git
</success_criteria>

<output>
After completion, create `.planning/phases/09-railway-bgutil-deployment/09-01-SUMMARY.md` referenciando 09-01-SMOKE-TEST.md como evidência primária.
</output>
