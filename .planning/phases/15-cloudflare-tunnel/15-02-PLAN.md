---
phase: 15-cloudflare-tunnel
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified: []
autonomous: false
requirements:
  - TUNNEL-01
  - TUNNEL-02
---

## must_haves

- D-01: usar a URL `https://*.trycloudflare.com` copiada no Plan 01 nos gates de validação.
- D-02: Phase 15 pode fechar tecnicamente com `.trycloudflare.com` se `/health` público e os 3 E2E pelo frontend passarem; domínio customizado não bloqueia esta fase.
- D-04: os 3 E2E públicos usam o frontend no navegador, não apenas API direta; API/curl fica apenas para diagnóstico.
- D-09: `/yonkou` permanece acessível pela URL pública durante a janela temporária aceita para teste.
- D-11: o plano não encerra automaticamente o tunnel após os testes; operador decide quando parar.
- D-12: ao final, avisar que a URL pública e `/yonkou` continuam ativos enquanto `cloudflared` rodar e mostrar como encerrar o processo.

<objective>
Validar a URL pública `.trycloudflare.com` gerada no Plan 01: confirmar `/health` público via HTTPS, executar 3 downloads E2E pelo frontend público e encerrar a fase com aviso explícito de que o tunnel e `/yonkou` continuam públicos enquanto `cloudflared` estiver ativo.

Purpose: TUNNEL-02 exige que o SoundGrabber esteja acessível por URL HTTPS pública e que 3 downloads E2E funcionem por essa URL. Esta onda prova o fluxo real do usuário pelo frontend, não só por API.
Output: 3 E2E públicos aprovados pelo operador, com WAV baixável, BPM e tonalidade; aviso final de exposição ativa e instrução de parada do tunnel.
</objective>

<must_haves>
  <truths>
    - "D-01: a URL `https://*.trycloudflare.com` copiada do terminal no Plan 01 é usada nos gates de validação"
    - "D-02: Phase 15 pode fechar tecnicamente com `.trycloudflare.com` se `/health` público e os 3 E2E pelo frontend passarem; domínio customizado não bloqueia esta fase"
    - "D-04: os 3 E2E públicos usam o frontend no navegador; API/curl é permitido apenas para diagnóstico"
    - "D-09: `/yonkou` permanece acessível pela URL pública durante a janela temporária aceita para teste"
    - "D-11: o plano não encerra automaticamente o tunnel; operador decide quando parar"
    - "D-12: resumo final avisa que a URL pública e `/yonkou` continuam ativos enquanto `cloudflared` rodar e mostra como encerrar o processo"
  </truths>
  <artifacts>
    - path: "browser validation notes"
      provides: "Resultado dos 3 E2E públicos pelo frontend"
      contains: "3 successful downloads"
  </artifacts>
  <key_links>
    - from: "15-CONTEXT.md D-04"
      to: "frontend public E2E"
      via: "browser validation"
      pattern: "frontend|browser|3 downloads"
    - from: "15-CONTEXT.md D-11/D-12"
      to: "final exposure warning"
      via: "SUMMARY close-out"
      pattern: "tunnel remains active|yonkou"
  </key_links>
</must_haves>

<context>
@.planning/phases/15-cloudflare-tunnel/15-CONTEXT.md
@.planning/phases/15-cloudflare-tunnel/15-RESEARCH.md
@.planning/phases/15-cloudflare-tunnel/15-PATTERNS.md
@.planning/phases/15-cloudflare-tunnel/15-VALIDATION.md
@api/main.py
@docker-compose.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Validar `/health` pela URL pública HTTPS</name>
  <files>none</files>
  <read_first>
    - .planning/phases/15-cloudflare-tunnel/15-CONTEXT.md
    - .planning/phases/15-cloudflare-tunnel/15-RESEARCH.md
    - api/main.py
  </read_first>
  <action>
    Usar a URL `https://*.trycloudflare.com` copiada no Plan 01 e executar `curl -i https://<trycloudflare-url>/health` a partir de uma rede que não dependa da Tailscale quando possível. Confirmar HTTP 200 e corpo `{"status":"ok"}`. Se local `/health` passa mas público falha, diagnosticar `cloudflared`/URL antes de prosseguir.
  </action>
  <how-to-verify>
    Executar:
    `curl -i https://<trycloudflare-url>/health`

    Responder `approved` quando retornar HTTP 200 + `{"status":"ok"}`.
  </how-to-verify>
  <verify>
    <manual>`curl -i https://<trycloudflare-url>/health` retorna 200.</manual>
  </verify>
  <acceptance_criteria>
    - URL usada começa com `https://`
    - URL usada contém `trycloudflare.com`
    - `curl -i https://<trycloudflare-url>/health` retorna status HTTP 200
    - Corpo contém `"status":"ok"` ou `"status": "ok"`
  </acceptance_criteria>
  <done>Tunnel público roteia corretamente para a API local.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Executar 3 downloads E2E pelo frontend público</name>
  <files>none</files>
  <read_first>
    - .planning/phases/15-cloudflare-tunnel/15-CONTEXT.md
    - .planning/phases/15-cloudflare-tunnel/15-VALIDATION.md
    - api/main.py
  </read_first>
  <action>
    Abrir `https://<trycloudflare-url>/` no navegador. Para 3 links reais de beats, colar o link no frontend, iniciar processamento, aguardar status concluído, baixar o WAV e confirmar que BPM e tonalidade aparecem na interface. Registrar apenas o resultado de cada caso (pass/fail e sintomas); não registrar cookies, credenciais, URL secreta de admin, nem dados sensíveis.
  </action>
  <how-to-verify>
    Para cada um dos 3 links:
    1. Abrir o frontend público.
    2. Colar o link do YouTube.
    3. Iniciar job.
    4. Aguardar conclusão.
    5. Baixar WAV.
    6. Confirmar BPM e tonalidade exibidos.

    Responder `approved` quando os 3 passarem, ou descrever qual link/etapa falhou.
  </how-to-verify>
  <verify>
    <manual>Operador aprova 3 E2E pelo frontend público.</manual>
  </verify>
  <acceptance_criteria>
    - Frontend público carrega em `https://<trycloudflare-url>/`
    - E2E 1 conclui com WAV baixável
    - E2E 1 exibe BPM
    - E2E 1 exibe tonalidade/key
    - E2E 2 conclui com WAV baixável
    - E2E 2 exibe BPM
    - E2E 2 exibe tonalidade/key
    - E2E 3 conclui com WAV baixável
    - E2E 3 exibe BPM
    - E2E 3 exibe tonalidade/key
  </acceptance_criteria>
  <done>3 downloads E2E públicos validados pelo fluxo real do usuário.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar aviso final de exposição ativa e parada manual</name>
  <files>none</files>
  <read_first>
    - .planning/phases/15-cloudflare-tunnel/15-CONTEXT.md
    - .planning/phases/15-cloudflare-tunnel/15-RESEARCH.md
  </read_first>
  <action>
    Antes de fechar a fase, apresentar ao operador um aviso explícito: o tunnel permanece ativo até ele parar o processo; a URL pública continua acessível; `/yonkou` também continua acessível pela URL pública enquanto `cloudflared` estiver rodando; para parar, usar `Ctrl+C` no terminal onde `cloudflared tunnel --url http://localhost:8000` está rodando, ou encerrar o processo `cloudflared` se estiver detached. O plano não deve parar automaticamente o tunnel.
  </action>
  <how-to-verify>
    Operador confirma que entendeu que o tunnel continuará ativo e sabe como parar.
  </how-to-verify>
  <verify>
    <manual>Operador confirma o aviso final.</manual>
  </verify>
  <acceptance_criteria>
    - Aviso menciona que a URL pública continua ativa enquanto `cloudflared` rodar
    - Aviso menciona que `/yonkou` permanece acessível pela URL pública
    - Aviso inclui `Ctrl+C` no terminal do `cloudflared` como forma de parada
    - Tunnel não é parado automaticamente pela execução da fase
  </acceptance_criteria>
  <done>Operador ciente da exposição ativa e do procedimento de parada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet pública → frontend SoundGrabber | Usuários externos acessam a aplicação via Cloudflare |
| Public URL → `/yonkou` | Painel operador fica alcançável enquanto tunnel está ativo |
| Browser do operador → live YouTube pipeline | E2E público usa links reais e cookies existentes no notebook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Information Disclosure | Tunnel continua ativo após testes | mitigate | Task 3 exige aviso final e procedimento de parada manual |
| T-15-06 | Elevation of Privilege | `/yonkou` público durante teste | mitigate | Plan 01 gate de admin credentials fortes; Phase 15 aceita exposição temporária |
| T-15-07 | Denial of Service | Tráfego público durante E2E | accept | Quick tunnel temporário; sem divulgação pública ampla; rate limits existentes continuam ativos |
| T-15-08 | Repudiation | E2E manual sem registro | mitigate | SUMMARY.md deve registrar pass/fail dos 3 E2E sem segredos |
</threat_model>

<verification>
- `curl -i https://<trycloudflare-url>/health` retorna HTTP 200.
- Browser carrega `https://<trycloudflare-url>/`.
- 3 E2E pelo frontend público aprovados pelo operador.
- Operador confirma aviso final de exposição ativa e parada manual.
</verification>

<success_criteria>
- [ ] `/health` público retorna 200
- [ ] Frontend público carrega
- [ ] 3 links de beats processam pelo frontend público
- [ ] 3 WAVs são baixáveis
- [ ] BPM e tonalidade aparecem para os 3 casos
- [ ] Operador recebeu aviso de que tunnel e `/yonkou` permanecem públicos até parada manual
- [ ] TUNNEL-01 e TUNNEL-02 satisfeitos com URL `.trycloudflare.com`
</success_criteria>

<output>
After completion, create `.planning/phases/15-cloudflare-tunnel/15-02-SUMMARY.md` documenting:
- Public health check result
- Public frontend URL shape (`https://*.trycloudflare.com`, redact random subdomain if desired)
- 3 E2E pass/fail rows
- Any diagnostic API/curl commands used
- Final exposure warning acknowledged
- Deferred items: custom domain, systemd service, `/yonkou` hardening for permanent exposure
</output>
