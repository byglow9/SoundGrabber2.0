---
phase: 05-visual-identity
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_frontend.py
autonomous: true
requirements:
  - VISUAL-01
  - VISUAL-02
  - VISUAL-03
  - VISUAL-04
  - VISUAL-05

must_haves:
  truths:
    - "5 novos testes em tests/test_frontend.py que falham RED antes de qualquer implementação"
    - "test_html_required_ids_present expandido de 16 para 27 IDs verificados"
    - "pytest tests/test_frontend.py retorna 4 FAILED (novos) + 4 PASSED (existentes) = 8 total"
  artifacts:
    - path: "tests/test_frontend.py"
      provides: "Stubs RED para VISUAL-01..VISUAL-04; expansão do existing test para 27 IDs"
      contains: "test_style_css_served"
  key_links:
    - from: "tests/test_frontend.py::test_style_css_served"
      to: "static/style.css"
      via: "api_client.get('/static/style.css')"
      pattern: "api_client.get.*static/style.css"
    - from: "tests/test_frontend.py::test_fonts_selfhosted"
      to: "static/fonts/DelaGothicOne-Regular.woff2"
      via: "api_client.get('/static/fonts/...')"
      pattern: "static/fonts/DelaGothicOne"
---

<objective>
Wave 0 — TDD RED stubs: adicionar 5 novos testes a tests/test_frontend.py que devem falhar ANTES de qualquer implementação CSS/HTML.

Purpose: Nyquist protocol — cada comportamento visual da Fase 5 tem um teste vermelho aguardando. A execução dos planos 02-04 vai fazê-los ficar verdes na ordem correta.

Output: tests/test_frontend.py com 9 funções de teste (4 existentes + 4 novas falhas RED + 1 expansão); pytest retorna exatamente 4 falhas nos novos testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-identity/05-RESEARCH.md
@.planning/phases/05-visual-identity/05-UI-SPEC.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Adicionar stubs RED ao tests/test_frontend.py</name>
  <read_first>
    - tests/test_frontend.py (arquivo COMPLETO — ver estado atual dos 4 testes existentes, fixture api_client, imports)
  </read_first>
  <files>tests/test_frontend.py</files>

  <behavior>
    RED (antes de qualquer implementação):
    - test_style_css_served: FAIL — /static/style.css retorna 404 (arquivo não existe)
    - test_css_no_modern_properties: SKIP (arquivo não existe) — contará como skipped, não failed; aceitável
    - test_fonts_selfhosted: FAIL — 404 para ambas as fontes (diretório static/fonts/ não existe)
    - test_html_table_layout: FAIL — GET / retorna HTML sem <table (div-based layout Phase 4)
    - test_html_required_ids_present (expansão): FAIL — 11 IDs adicionais ausentes (app, header, site-title, site-tagline, form-area, duration-hint, input-group, result-bpm, result-key, download-area, result-size não passam no formato id="X")

    GREEN (depois dos planos 02-04):
    - test_style_css_served: PASS — 200, text/css, contém #000000 e #ff8800, sem var(
    - test_css_no_modern_properties: PASS — static/style.css existe e não contém flex/grid/var(--/border-radius/box-shadow/transition:/animation:/transform:
    - test_fonts_selfhosted: PASS — ambas retornam 200
    - test_html_table_layout: PASS — HTML contém <table, sem display: flex, sem display: grid
    - test_html_required_ids_present: PASS — todos os 27 IDs presentes
  </behavior>

  <action>
ATENÇÃO: Leia tests/test_frontend.py ANTES de editar. O arquivo tem 4 testes existentes que devem ser preservados intactos.

Faça DUAS modificações no arquivo:

**Modificação 1 — Expandir test_html_required_ids_present (existente):**

Substituir a lista `required_ids` atual (16 itens) pela lista completa de 27 IDs:

```python
    required_ids = [
        # IDs originais Phase 4 (16)
        "url-input",
        "submit-btn",
        "progress-area",
        "progress-label",
        "result-card",
        "bpm-value",
        "bpm-half-value",
        "bpm-double-value",
        "key-value",
        "camelot-value",
        "size-value",
        "download-link",
        "error-area",
        "error-message",
        "retry-btn",
        "validation-error",
        # IDs adicionais Phase 5 (11) — ausentes no HTML atual, RED até Plan 05-04
        "app",
        "header",
        "site-title",
        "site-tagline",
        "form-area",
        "duration-hint",
        "input-group",
        "result-bpm",
        "result-key",
        "result-size",
        "download-area",
    ]
```

Atualizar também a mensagem de assert para referenciar Phase 5:
```python
    assert not missing, (
        f"IDs ausentes no HTML: {missing}. "
        "Certifique-se de que static/index.html contém todos os 27 IDs do UI-SPEC (Plans 02 e 05-04)."
    )
```

**Modificação 2 — Adicionar 4 novas funções no fim do arquivo (após test_wav_size_formula):**

```python
def test_style_css_served(api_client):
    """VISUAL-02: GET /static/style.css retorna 200, Content-Type CSS.
    Contém paleta hex (#000000, #ff8800). Não contém CSS custom properties.
    RED antes do Plan 05-03. GREEN depois de static/style.css criado.
    """
    r = api_client.get("/static/style.css")
    assert r.status_code == 200, (
        f"GET /static/style.css esperava 200, recebeu {r.status_code}. "
        "Crie static/style.css (Plan 05-03)."
    )
    assert "text/css" in r.headers.get("content-type", ""), (
        f"Content-Type esperado text/css, recebeu '{r.headers.get('content-type')}'"
    )
    assert "#000000" in r.text, "style.css deve conter a cor de background #000000"
    assert "#ff8800" in r.text, "style.css deve conter a cor de acento #ff8800"
    assert "var(" not in r.text, (
        "style.css não deve conter CSS custom properties (var(--...)). "
        "Use hex brutas conforme CONTEXT.md §1."
    )


def test_css_no_modern_properties(api_client):
    """VISUAL-04: style.css não contém propriedades CSS modernas.
    Verifica ausência de: flex, grid, var(--, border-radius, box-shadow,
    transition:, animation:, transform:.
    RED antes do Plan 05-03. GREEN depois de static/style.css criado sem essas propriedades.
    """
    import os
    css_path = os.path.join(
        os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
        "static", "style.css"
    )
    if not os.path.exists(css_path):
        pytest.skip("static/style.css não criado ainda — aguardando Plan 05-03")
    with open(css_path, encoding="utf-8") as f:
        css = f.read()
    forbidden = [
        "flex",
        "grid",
        "var(--",
        "border-radius",
        "box-shadow",
        "transition:",
        "animation:",
        "transform:",
    ]
    found = [prop for prop in forbidden if prop in css]
    assert not found, (
        f"Propriedades CSS modernas encontradas em style.css: {found}. "
        "Este projeto usa CSS Level 2 apenas (Y2K authenticity — CONTEXT.md §6)."
    )


def test_fonts_selfhosted(api_client):
    """VISUAL-03: Ambos os arquivos woff2 de fonte são servidos de /static/fonts/.
    RED antes do Plan 05-02. GREEN depois de static/fonts/*.woff2 existirem.
    """
    r1 = api_client.get("/static/fonts/DelaGothicOne-Regular.woff2")
    assert r1.status_code == 200, (
        f"GET /static/fonts/DelaGothicOne-Regular.woff2 esperava 200, recebeu {r1.status_code}. "
        "Execute o download da fonte (Plan 05-02)."
    )
    r2 = api_client.get("/static/fonts/Sligoil-Micro.woff2")
    assert r2.status_code == 200, (
        f"GET /static/fonts/Sligoil-Micro.woff2 esperava 200, recebeu {r2.status_code}. "
        "Execute o download da fonte (Plan 05-02)."
    )


def test_html_table_layout(api_client):
    """VISUAL-01 / VISUAL-04: HTML usa table layout.
    Contém <table. Não contém display: flex nem display: grid inline.
    RED antes do Plan 05-04. GREEN depois de static/index.html convertido.
    """
    r = api_client.get("/")
    assert r.status_code == 200, (
        f"GET / esperava 200, recebeu {r.status_code}."
    )
    html = r.text
    assert "<table" in html, (
        "HTML deve conter <table para layout Y2K autêntico (Plan 05-04). "
        "Atualmente usa div-based layout da Phase 4."
    )
    assert "display: flex" not in html, (
        "HTML não deve conter 'display: flex' inline — viola autenticidade Y2K (CONTEXT.md §3)."
    )
    assert "display: grid" not in html, (
        "HTML não deve conter 'display: grid' inline — viola autenticidade Y2K (CONTEXT.md §3)."
    )
```

**Verificação de RED:** Após adicionar os testes, execute:
```bash
.venv/bin/pytest tests/test_frontend.py -v 2>&1 | tail -20
```

Resultado esperado:
- test_html_required_ids_present: FAILED (11 IDs adicionais ausentes no HTML Phase 4)
- test_style_css_served: FAILED (404 — style.css não existe)
- test_css_no_modern_properties: SKIPPED (arquivo não existe)
- test_fonts_selfhosted: FAILED (404 — fontes não existem)
- test_html_table_layout: FAILED (sem <table no HTML Phase 4)
- test_index_html_served: PASSED
- test_app_js_served: PASSED
- test_wav_size_formula: PASSED

Se test_css_no_modern_properties retornar PASSED em vez de SKIPPED, isso é aceitável — o pytest.skip() é chamado quando o arquivo não existe, o que é um SKIP, não um PASS.

Se QUALQUER dos testes existentes (test_index_html_served, test_app_js_served, test_wav_size_formula) falhar após a modificação, a modificação quebrou algo — reverta e corrija.
  </action>

  <verify>
    <automated>.venv/bin/pytest tests/test_frontend.py -v --tb=short 2>&1 | grep -E "PASSED|FAILED|SKIPPED|ERROR"</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_frontend.py contém a string "def test_style_css_served"
    - tests/test_frontend.py contém a string "def test_css_no_modern_properties"
    - tests/test_frontend.py contém a string "def test_fonts_selfhosted"
    - tests/test_frontend.py contém a string "def test_html_table_layout"
    - tests/test_frontend.py contém "download-area" na lista required_ids (expansão dos 27 IDs)
    - tests/test_frontend.py contém "result-bpm" na lista required_ids
    - .venv/bin/pytest tests/test_frontend.py::test_index_html_served retorna exit code 0 (não quebrado)
    - .venv/bin/pytest tests/test_frontend.py::test_app_js_served retorna exit code 0 (não quebrado)
    - .venv/bin/pytest tests/test_frontend.py::test_wav_size_formula retorna exit code 0 (não quebrado)
    - .venv/bin/pytest tests/test_frontend.py::test_style_css_served retorna exit code 1 (RED — style.css não existe)
    - .venv/bin/pytest tests/test_frontend.py::test_fonts_selfhosted retorna exit code 1 (RED — fontes não existem)
    - .venv/bin/pytest tests/test_frontend.py::test_html_table_layout retorna exit code 1 (RED — sem <table no HTML atual)
    - .venv/bin/pytest tests/test_frontend.py::test_html_required_ids_present retorna exit code 1 (RED — 11 IDs adicionais ausentes)
    - grep 'flex' tests/test_frontend.py retorna resultados apenas nas strings de assert (não como import ou dependência nova)
  </acceptance_criteria>

  <done>
    tests/test_frontend.py tem 9 funções de teste. Os 3 testes originais passam. Os 4 novos testes falham ou skippam (RED). O teste de IDs expandido falha. Nenhum import novo adicionado além de "import os" e "import pytest" (pytest já estava importado).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → filesystem | test_css_no_modern_properties lê static/style.css diretamente via open() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | test_css_no_modern_properties (open() de arquivo) | accept | Arquivo é código-fonte estático sem dados sensíveis; leitura local em ambiente de teste apenas |

Esta fase é CSS/HTML estático. Superfície de ataque: zero. Único acesso a filesystem é de testes lendo arquivos de código-fonte.
</threat_model>

<verification>
Executar após a task:

```bash
# Verificar que testes existentes não quebraram
.venv/bin/pytest tests/test_frontend.py::test_index_html_served tests/test_frontend.py::test_app_js_served tests/test_frontend.py::test_wav_size_formula -v

# Verificar que novos testes estão RED
.venv/bin/pytest tests/test_frontend.py::test_style_css_served tests/test_frontend.py::test_fonts_selfhosted tests/test_frontend.py::test_html_table_layout tests/test_frontend.py::test_html_required_ids_present -v 2>&1 | grep -E "FAILED|SKIPPED"

# Contar total de funções de teste
grep -c "^def test_" tests/test_frontend.py
# Esperado: 9
```
</verification>

<success_criteria>
- 4 testes Phase 4 sobrevivem intactos (3 PASSED + 1 agora FAILED por expansão dos IDs)
- 4 novos testes adicionados e todos falham RED (ou SKIP para test_css_no_modern_properties)
- grep -c "^def test_" tests/test_frontend.py retorna 9 (ou 8 se test_css_no_modern_properties for contado separado)
- Nenhum syntax error no arquivo (pytest coleta sem ImportError)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-visual-identity/05-01-SUMMARY.md`
</output>
