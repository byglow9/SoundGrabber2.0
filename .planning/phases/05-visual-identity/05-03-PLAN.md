---
phase: 05-visual-identity
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - static/style.css
autonomous: true
requirements:
  - VISUAL-02
  - VISUAL-03
  - VISUAL-04

must_haves:
  truths:
    - "static/style.css é servido pelo FastAPI em /static/style.css com status 200"
    - "Paleta monocromática #000000/#ff8800 expressa inteiramente em hex brutos — zero rgba(), zero hsl(), zero var(--)"
    - "Ambas as fontes declaradas com @font-face apontando para /static/fonts/ com path absoluto"
    - "font-smoothing desabilitado globalmente no body"
    - "Classe .sg-url-input--error definida — app.js usa classList.add() desta classe em estados de validação"
    - "Zero propriedades CSS3 proibidas: flex, grid, border-radius, box-shadow, transition, animation, transform"
    - "Elementos controlados por JS via .hidden não têm display: declarado no CSS"
  artifacts:
    - path: "static/style.css"
      provides: "Stylesheet completo — @font-face, reset, tabela, input, botões, estados interativos, classe de erro JS"
      contains: "@font-face"
  key_links:
    - from: "static/style.css"
      to: "static/fonts/DelaGothicOne-Regular.woff2"
      via: "url('/static/fonts/DelaGothicOne-Regular.woff2') format('woff2')"
      pattern: "/static/fonts/DelaGothicOne"
    - from: "static/style.css (.sg-url-input--error)"
      to: "static/app.js (classList.add)"
      via: "classList.add('sg-url-input--error') no showErrorValidation()"
      pattern: "sg-url-input--error"
---

<objective>
Wave 2 — Criação do stylesheet completo: criar static/style.css com @font-face, reset do body, estilos de tabela, input, botões, estados de hover/focus e a classe .sg-url-input--error que o app.js exige.

Purpose: CSS deve existir antes de index.html poder referenciá-lo (Pitfall 2 do RESEARCH.md). Os testes test_style_css_served e test_css_no_modern_properties (RED no Wave 0) ficam GREEN após este plano.

Output: static/style.css com exatamente as regras especificadas no UI-SPEC.md, sem nenhuma propriedade CSS3 proibida, servido em /static/style.css com status 200.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-visual-identity/05-CONTEXT.md
@.planning/phases/05-visual-identity/05-UI-SPEC.md
@.planning/phases/05-visual-identity/05-RESEARCH.md

@.planning/phases/05-visual-identity/05-02-SUMMARY.md

<interfaces>
<!-- Classe que app.js exige — CRÍTICO não esquecer -->
<!-- De static/app.js linhas 172, 184, 236, 282: -->
<!-- $('url-input').classList.add('sg-url-input--error')   — showErrorValidation() -->
<!-- $('url-input').classList.remove('sg-url-input--error') — showIdle(), showSubmitting(), showErrorJob() -->

<!-- IDs que o CSS estiliza diretamente: -->
<!-- #url-input, #submit-btn, #download-link, #retry-btn, #site-title, #site-tagline -->
<!-- #bpm-value, #key-value, #duration-hint, #validation-error, #progress-label, #error-message, #size-value -->

<!-- Elementos controlados por JS via .hidden = true/false (NÃO adicionar display: neles): -->
<!-- #progress-area, #result-card, #error-area, #validation-error, #submit-btn (em ERROR_JOB), #retry-btn -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar static/style.css completo</name>
  <read_first>
    - .planning/phases/05-visual-identity/05-UI-SPEC.md (seções Typography, Color, Interactive States — valores exatos de px, hex e font-family)
    - .planning/phases/05-visual-identity/05-CONTEXT.md (§1 paleta, §2 tipografia, §5 estados interativos, §6 regras adicionais)
    - static/app.js (linhas 170-295 — quais IDs são controlados via .hidden para NÃO adicionar display:)
  </read_first>
  <files>static/style.css</files>
  <action>
Criar o arquivo /home/glow/Documentos/SoundGrabber2.0/static/style.css com o conteúdo EXATO abaixo.

REGRAS CRÍTICAS antes de escrever qualquer linha:
1. ZERO uso de `flex` ou `grid` em qualquer valor de propriedade
2. ZERO `var(--` em qualquer lugar
3. ZERO `border-radius`, `box-shadow`, `transition:`, `animation:`, `transform:`
4. ZERO `rgba()` ou `hsl()` — apenas hex brutos
5. NÃO declarar `display:` em #progress-area, #result-card, #error-area, #validation-error, #submit-btn, #retry-btn (controlados pelo app.js via .hidden)
6. Os paths @font-face DEVEM ser absolutos: `/static/fonts/` (não `../fonts/` nem `fonts/`)

Conteúdo do arquivo:

```css
/* SoundGrabber — Visual Identity
   Y2K / NFO scene aesthetic
   CSS Level 2 only — no CSS3 properties
   Palette: #000000 background / #ff8800 accent (monochromatic)
   Fonts: Dela Gothic One (headers) + Sligoil (body)
   Reference: NFO BeatMasters International 2000
*/

/* ============================================================
   @font-face declarations
   Both fonts self-hosted — no CDN calls
   font-display: block — text stays invisible until font loads (no FOUT)
   ============================================================ */

@font-face {
  font-family: 'Dela Gothic One';
  src: url('/static/fonts/DelaGothicOne-Regular.woff2') format('woff2');
  font-weight: 400;
  font-style: normal;
  font-display: block;
}

@font-face {
  font-family: 'Sligoil';
  src: url('/static/fonts/Sligoil-Micro.woff2') format('woff2');
  font-weight: 400;
  font-style: normal;
  font-display: block;
}

/* ============================================================
   Reset / body
   font-smoothing disabled globally — bitmap rendering
   ============================================================ */

body {
  background-color: #000000;
  color: #ff8800;
  font-family: 'Sligoil', 'Courier New', monospace;
  font-size: 13px;
  margin: 0;
  padding: 32px 0;
  -webkit-font-smoothing: none;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeSpeed;
}

/* ============================================================
   Site title and tagline
   ============================================================ */

#site-title {
  font-family: 'Dela Gothic One', sans-serif;
  font-size: 48px;
  line-height: 1.1;
  color: #ff8800;
}

#site-tagline {
  font-size: 13px;
  color: #ff8800;
  text-transform: uppercase;
}

/* ============================================================
   Tables — border-collapse for authentic 1px borders
   ============================================================ */

table {
  border-collapse: collapse;
}

td {
  color: #ff8800;
  padding: 8px;
  border: 1px solid #ff8800;
}

/* ============================================================
   URL input field
   ============================================================ */

#url-input {
  background-color: #000000;
  color: #ff8800;
  border: 1px solid #ff8800;
  font-family: 'Sligoil', 'Courier New', monospace;
  font-size: 13px;
  padding: 8px;
  width: 100%;
  cursor: text;
  outline: none;
}

#url-input:focus {
  border-color: #ff6600;
}

::-webkit-input-placeholder {
  color: #804400;
}

::placeholder {
  color: #804400;
}

/* Error state on input — used by app.js classList.add('sg-url-input--error')
   CRITICAL: do not remove this class — app.js depends on it */
.sg-url-input--error {
  border-color: #ff3300 !important;
}

/* ============================================================
   Buttons — submit and download (primary actions)
   Inverted: orange background, black text
   ============================================================ */

#submit-btn,
#download-link {
  background-color: #ff8800;
  color: #000000;
  border: 1px solid #ff8800;
  font-family: 'Dela Gothic One', sans-serif;
  font-size: 16px;
  line-height: 1.0;
  padding: 8px 16px;
  cursor: pointer;
}

#submit-btn:hover,
#download-link:hover {
  background-color: #ff6600;
  border-color: #ff6600;
  color: #000000;
}

/* ============================================================
   Retry button (secondary action)
   Default: black background, orange border + text
   Hover: inverted (orange background, black text)
   ============================================================ */

#retry-btn {
  background-color: #000000;
  color: #ff8800;
  border: 1px solid #ff8800;
  font-family: 'Sligoil', 'Courier New', monospace;
  font-size: 13px;
  padding: 8px 16px;
  cursor: pointer;
}

#retry-btn:hover {
  background-color: #ff8800;
  color: #000000;
  border-color: #ff8800;
}

/* ============================================================
   Result values — BPM and Key (large display)
   ============================================================ */

#bpm-value,
#key-value {
  font-size: 32px;
  line-height: 1.2;
  color: #ff8800;
}

/* ============================================================
   Labels — uppercase small text
   ============================================================ */

.label {
  font-size: 13px;
  text-transform: uppercase;
  color: #ff8800;
}

/* ============================================================
   Informational text — hints, progress, errors
   All use Sligoil monospace, 13px, orange
   ============================================================ */

#duration-hint,
#validation-error,
#progress-label,
#error-message,
#size-value {
  font-family: 'Sligoil', 'Courier New', monospace;
  font-size: 13px;
  color: #ff8800;
}
```

**Verificação pós-criação — executar imediatamente:**

```bash
# Verificar ausência de propriedades proibidas
for prop in flex grid "var(--" "border-radius" "box-shadow" "transition:" "animation:" "transform:" "rgba(" "hsl("; do
  if grep -q "$prop" /home/glow/Documentos/SoundGrabber2.0/static/style.css; then
    echo "ERRO: '$prop' encontrado em style.css"
  fi
done

# Verificar presença de elementos obrigatórios
for item in "@font-face" "#000000" "#ff8800" "sg-url-input--error" "font-display: block" "-webkit-font-smoothing: none" "Dela Gothic One" "Sligoil"; do
  if grep -q "$item" /home/glow/Documentos/SoundGrabber2.0/static/style.css; then
    echo "OK: '$item' presente"
  else
    echo "FALTANDO: '$item' ausente — adicionar antes de prosseguir"
  fi
done
```
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_frontend.py::test_style_css_served tests/test_frontend.py::test_css_no_modern_properties -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - static/style.css existe (ls retorna o arquivo)
    - grep '@font-face' static/style.css retorna pelo menos 2 linhas (@font-face para Dela Gothic One e para Sligoil)
    - grep 'DelaGothicOne-Regular.woff2' static/style.css retorna resultado com '/static/fonts/' no path
    - grep 'Sligoil-Micro.woff2' static/style.css retorna resultado com '/static/fonts/' no path
    - grep '#000000' static/style.css retorna pelo menos 1 resultado
    - grep '#ff8800' static/style.css retorna pelo menos 5 resultados
    - grep 'sg-url-input--error' static/style.css retorna exatamente 1 linha com border-color: #ff3300
    - grep 'font-display: block' static/style.css retorna pelo menos 2 resultados (um por @font-face)
    - grep '-webkit-font-smoothing: none' static/style.css retorna 1 resultado
    - grep 'flex' static/style.css retorna vazio (zero resultados)
    - grep 'grid' static/style.css retorna vazio OU retorna apenas nos comentários (não como propriedade CSS)
    - grep 'var(--' static/style.css retorna vazio
    - grep 'border-radius' static/style.css retorna vazio
    - grep 'box-shadow' static/style.css retorna vazio
    - grep 'transition:' static/style.css retorna vazio
    - grep 'animation:' static/style.css retorna vazio
    - grep 'transform:' static/style.css retorna vazio
    - grep 'rgba(' static/style.css retorna vazio
    - grep 'display:' static/style.css retorna vazio (nenhum display: em nenhum seletor — elementos de visibilidade são controlados pelo HTML hidden attribute via JS)
    - .venv/bin/pytest tests/test_frontend.py::test_style_css_served -v retorna PASSED
    - .venv/bin/pytest tests/test_frontend.py::test_css_no_modern_properties -v retorna PASSED
  </acceptance_criteria>
  <done>
    static/style.css criado com todas as regras especificadas. Dois testes ficam GREEN: test_style_css_served e test_css_no_modern_properties. Nenhuma propriedade CSS3 proibida presente. Classe .sg-url-input--error definida para app.js.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /static/style.css | CSS servido como asset estático pelo FastAPI StaticFiles |
| style.css → /static/fonts/*.woff2 | Referências de @font-face para arquivos binários locais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | CSS injection via style.css | accept | style.css é arquivo estático read-only servido pelo FastAPI; nenhum conteúdo de usuário é interpolado no CSS |
| T-05-05 | Information Disclosure | font-face paths em style.css | accept | Paths são públicos (/static/fonts/); as fontes têm licença SIL OFL — nenhum dado sensível exposto |

Esta fase é CSS estático puro. O único vetor teórico seria injeção de conteúdo malicioso no arquivo static/style.css em tempo de deploy, o que está fora do escopo desta threat model (controle de acesso ao servidor).
</threat_model>

<verification>
```bash
# Testes devem ficar GREEN após este plano
.venv/bin/pytest tests/test_frontend.py::test_style_css_served tests/test_frontend.py::test_css_no_modern_properties -v

# Verificação manual de qualidade do CSS (nenhuma propriedade proibida)
echo "=== Propriedades proibidas (deve estar vazio) ==="
grep -n "flex\|grid\|var(--\|border-radius\|box-shadow\|transition:\|animation:\|transform:\|rgba(\|hsl(\|display:" static/style.css

echo "=== Elementos obrigatórios (deve ter resultados) ==="
grep -n "@font-face\|#000000\|#ff8800\|sg-url-input--error\|font-display" static/style.css

# Suíte completa — nenhuma regressão
.venv/bin/pytest tests/test_frontend.py -v
```
</verification>

<success_criteria>
- static/style.css criado com conteúdo correto
- test_style_css_served e test_css_no_modern_properties passam GREEN
- Todos os 3 testes Phase 4 continuam PASSED (sem regressão)
- test_fonts_selfhosted continua PASSED (fontes do Wave 1 não foram tocadas)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-visual-identity/05-03-SUMMARY.md`
</output>
