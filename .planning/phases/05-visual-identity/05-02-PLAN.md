---
phase: 05-visual-identity
plan: "02"
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - static/fonts/DelaGothicOne-Regular.woff2
  - static/fonts/Sligoil-Micro.woff2
autonomous: true
requirements:
  - VISUAL-03

must_haves:
  truths:
    - "Dela Gothic One woff2 existe em static/fonts/ e é servida pela FastAPI em 200"
    - "Sligoil-Micro woff2 existe em static/fonts/ e é servida pela FastAPI em 200"
    - "Nenhuma chamada a CDN externo (fonts.googleapis.com) é necessária em runtime"
  artifacts:
    - path: "static/fonts/DelaGothicOne-Regular.woff2"
      provides: "Fonte display ultra-heavy para títulos e botões"
      min_lines: 0
    - path: "static/fonts/Sligoil-Micro.woff2"
      provides: "Fonte monospace inktraps para body, labels, valores numéricos"
      min_lines: 0
  key_links:
    - from: "static/style.css (@font-face)"
      to: "static/fonts/DelaGothicOne-Regular.woff2"
      via: "url('/static/fonts/DelaGothicOne-Regular.woff2') format('woff2')"
      pattern: "DelaGothicOne-Regular.woff2"
    - from: "static/style.css (@font-face)"
      to: "static/fonts/Sligoil-Micro.woff2"
      via: "url('/static/fonts/Sligoil-Micro.woff2') format('woff2')"
      pattern: "Sligoil-Micro.woff2"
---

<objective>
Wave 1 — Download das fontes self-hosted: baixar DelaGothicOne-Regular.woff2 e Sligoil-Micro.woff2 para static/fonts/.

Purpose: As fontes devem existir no disco antes de style.css poder referenciá-las com paths absolutos. O teste test_fonts_selfhosted (RED no Wave 0) ficará GREEN após este plano.

Output: Dois arquivos woff2 em static/fonts/ servidos pelo StaticFiles mount do FastAPI via /static/fonts/*.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-visual-identity/05-RESEARCH.md

@.planning/phases/05-visual-identity/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baixar DelaGothicOne-Regular.woff2 do Google Fonts</name>
  <read_first>
    - .planning/phases/05-visual-identity/05-RESEARCH.md (seção "Download Procedures — Fontes", subseção "Dela Gothic One") — contém a URL verificada e o procedimento exato
  </read_first>
  <files>static/fonts/DelaGothicOne-Regular.woff2</files>
  <action>
Executar em sequência:

**Passo 1 — Criar diretório:**
```bash
mkdir -p /home/glow/Documentos/SoundGrabber2.0/static/fonts
```

**Passo 2 — Descobrir URL atual do woff2 Latin (a URL inclui versão que pode mudar):**
```bash
curl -s "https://fonts.googleapis.com/css2?family=Dela+Gothic+One&display=block" \
  -H "User-Agent: Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
  | grep -A5 "/* latin */"
```

A saída conterá a linha `src: url(https://fonts.gstatic.com/s/delagothicone/vXX/....woff2)`.

URL verificada em 2026-05-08: `https://fonts.gstatic.com/s/delagothicone/v19/hESp6XxvMDRA-2eD0lXpDa6QkBA2QkEI.woff2`

Se o Passo 2 falhar (sem saída ou diferente), usar a URL verificada acima como fallback.

**Passo 3 — Baixar o woff2:**
```bash
curl -L "https://fonts.gstatic.com/s/delagothicone/v19/hESp6XxvMDRA-2eD0lXpDa6QkBA2QkEI.woff2" \
  -o /home/glow/Documentos/SoundGrabber2.0/static/fonts/DelaGothicOne-Regular.woff2
```

Se a URL do Passo 2 for diferente da v19, use a URL extraída do Passo 2 no lugar.

**Passo 4 — Verificar:**
```bash
ls -lh /home/glow/Documentos/SoundGrabber2.0/static/fonts/DelaGothicOne-Regular.woff2
file /home/glow/Documentos/SoundGrabber2.0/static/fonts/DelaGothicOne-Regular.woff2
```

Esperado: arquivo ~20-25KB, tipo "Web Open Font Format 2".

**Se curl falhar (sem rede ou bloqueio):**
Tentar com wget como fallback:
```bash
wget -q "https://fonts.gstatic.com/s/delagothicone/v19/hESp6XxvMDRA-2eD0lXpDa6QkBA2QkEI.woff2" \
  -O /home/glow/Documentos/SoundGrabber2.0/static/fonts/DelaGothicOne-Regular.woff2
```
  </action>
  <verify>
    <automated>ls -lh /home/glow/Documentos/SoundGrabber2.0/static/fonts/DelaGothicOne-Regular.woff2 && file /home/glow/Documentos/SoundGrabber2.0/static/fonts/DelaGothicOne-Regular.woff2</automated>
  </verify>
  <acceptance_criteria>
    - static/fonts/DelaGothicOne-Regular.woff2 existe (ls retorna o arquivo sem erro)
    - Tamanho do arquivo é maior que 10000 bytes (ls -lh mostra no mínimo 10K)
    - file retorna "Web Open Font Format 2" ou "data" (woff2 é binário; alguns sistemas não identificam o tipo exato mas o arquivo existe e tem tamanho correto)
    - Arquivo NÃO é HTML de erro (head -c 20 não começa com "<!DOCTYPE" ou "<html")
  </acceptance_criteria>
  <done>
    static/fonts/DelaGothicOne-Regular.woff2 existe, tamanho >= 10KB, é um binário woff2 válido (não uma página de erro HTML).
  </done>
</task>

<task type="auto">
  <name>Task 2: Baixar Sligoil-Micro.woff2 do GitLab Velvetyne</name>
  <read_first>
    - .planning/phases/05-visual-identity/05-RESEARCH.md (seção "Download Procedures — Fontes", subseção "Sligoil-Micro") — contém estrutura verificada do zip e caminho exato do arquivo
  </read_first>
  <files>static/fonts/Sligoil-Micro.woff2</files>
  <action>
Executar em sequência:

**Passo 1 — Baixar o zip do repositório main:**
```bash
curl -L "https://gitlab.com/velvetyne/sligoil/-/archive/main/sligoil-main.zip" \
  -o /tmp/sligoil-main.zip
```

Esperado: ~4MB. Se demorar mais de 60s, o GitLab pode estar lento — tentar novamente.

**Passo 2 — Verificar que o zip baixou corretamente:**
```bash
ls -lh /tmp/sligoil-main.zip
unzip -l /tmp/sligoil-main.zip | grep "Sligoil-Micro.woff2"
```

Esperado: `sligoil-main/fonts/web/Sligoil-Micro.woff2` com ~41944 bytes.

**Passo 3 — Extrair apenas o woff2 necessário:**
```bash
unzip -p /tmp/sligoil-main.zip \
  "sligoil-main/fonts/web/Sligoil-Micro.woff2" \
  > /home/glow/Documentos/SoundGrabber2.0/static/fonts/Sligoil-Micro.woff2
```

**Passo 4 — Verificar:**
```bash
ls -lh /home/glow/Documentos/SoundGrabber2.0/static/fonts/Sligoil-Micro.woff2
```

Esperado: ~41KB (41944 bytes verificados em 2026-05-08).

**Passo 5 — Limpar arquivo temporário:**
```bash
rm -f /tmp/sligoil-main.zip
```

**Se o path dentro do zip for diferente (o nome do branch pode mudar):**
```bash
# Listar todos os woff2 dentro do zip
unzip -l /tmp/sligoil-main.zip | grep ".woff2"
# Usar o caminho correto do Sligoil-Micro.woff2 que aparecer
```

**Se unzip falhar:**
Alternativa com Python:
```bash
python3 -c "
import zipfile, shutil
with zipfile.ZipFile('/tmp/sligoil-main.zip') as z:
    names = [n for n in z.namelist() if 'Sligoil-Micro.woff2' in n and 'web' in n]
    print('Found:', names)
    if names:
        with z.open(names[0]) as src, open('static/fonts/Sligoil-Micro.woff2', 'wb') as dst:
            shutil.copyfileobj(src, dst)
"
```
  </action>
  <verify>
    <automated>ls -lh /home/glow/Documentos/SoundGrabber2.0/static/fonts/Sligoil-Micro.woff2 && ls -lh /home/glow/Documentos/SoundGrabber2.0/static/fonts/</automated>
  </verify>
  <acceptance_criteria>
    - static/fonts/Sligoil-Micro.woff2 existe
    - Tamanho é entre 35000 e 50000 bytes (ls mostra ~41K ou similar)
    - static/fonts/ contém exatamente dois arquivos: DelaGothicOne-Regular.woff2 e Sligoil-Micro.woff2 (não mais que isso)
    - Arquivo NÃO é HTML de erro (verificar com: head -c 5 static/fonts/Sligoil-Micro.woff2 | xxd | head -1 — bytes iniciais woff2 são "77 4f 46 32" = "wOF2")
    - .venv/bin/pytest tests/test_frontend.py::test_fonts_selfhosted -v retorna PASSED
  </acceptance_criteria>
  <done>
    static/fonts/ contém os dois arquivos woff2. test_fonts_selfhosted passa GREEN. Arquivo temporário /tmp/sligoil-main.zip removido.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → static/fonts/ | Arquivos woff2 baixados de fonts.gstatic.com e gitlab.com/velvetyne |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | Download de woff2 (curl sem verificação de hash) | accept | Ambas as fontes têm licença SIL OFL verificada; woff2 é formato binário não executável — não pode executar código no servidor; risco de conteúdo malicioso em arquivo de fonte é irrelevante para este contexto |
| T-05-03 | Information Disclosure | /tmp/sligoil-main.zip | accept | Arquivo temporário removido após extração; conteúdo é código-fonte público (GitLab) sem dados sensíveis |

Esta fase é static asset download. Superfície de ataque: zero. Fontes são arquivos binários não executáveis servidos como assets estáticos.
</threat_model>

<verification>
```bash
# Verificar ambas as fontes existem
ls -lh /home/glow/Documentos/SoundGrabber2.0/static/fonts/

# Verificar magic bytes woff2 (deve começar com "wOF2")
python3 -c "
with open('static/fonts/DelaGothicOne-Regular.woff2', 'rb') as f:
    print('Dela:', f.read(4))
with open('static/fonts/Sligoil-Micro.woff2', 'rb') as f:
    print('Sligoil:', f.read(4))
"
# Esperado: b'wOF2' para ambos

# Teste GREEN
.venv/bin/pytest tests/test_frontend.py::test_fonts_selfhosted -v
```
</verification>

<success_criteria>
- static/fonts/DelaGothicOne-Regular.woff2 existe, tamanho >= 10KB, magic bytes "wOF2"
- static/fonts/Sligoil-Micro.woff2 existe, tamanho ~41KB, magic bytes "wOF2"
- .venv/bin/pytest tests/test_frontend.py::test_fonts_selfhosted passa GREEN
- .venv/bin/pytest tests/test_frontend.py -v passa os mesmos testes que antes (sem regressões nos 3 testes Phase 4)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-visual-identity/05-02-SUMMARY.md`
</output>
