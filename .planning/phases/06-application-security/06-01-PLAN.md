---
phase: 06-application-security
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_security.py
autonomous: true
requirements:
  - SEC-FILE-01
  - SEC-FILE-02
  - SEC-API-01
  - SEC-API-02
  - SEC-API-03
  - SEC-TEST-01
  - SEC-TEST-02
  - SEC-TEST-03
  - SEC-TEST-04
  - SEC-TEST-05

must_haves:
  truths:
    - "tests/test_security.py existe e contem stubs de teste falhando (RED) para todos os controles desta fase"
    - "Cada stub eh executavel por pytest individualmente via -k <nome_do_teste>"
    - "Stubs usam o fixture api_client do conftest.py (que ja faz flush LIMITS:LIMITER*)"
    - "Stubs RED documentam o contrato esperado: WAV 0o600, start.sh 750, rate limits 60/min e 10/min, /health 200/503, body size, security headers, docs 404, queue 503"
  artifacts:
    - path: "tests/test_security.py"
      provides: "Stubs RED para SEC-FILE-01/02, SEC-API-01/02/03, SEC-TEST-01..05"
      min_lines: 150
      contains:
        - "def test_wav_file_permissions"
        - "def test_startsh_permissions"
        - "def test_rate_limit_get_jobs"
        - "def test_rate_limit_get_files"
        - "def test_health_redis_ok"
        - "def test_health_redis_down"
        - "def test_body_size_limit"
        - "def test_security_headers"
        - "def test_docs_routes_disabled"
        - "def test_queue_depth_limit"
  key_links:
    - from: "tests/test_security.py"
      to: "tests/conftest.py::api_client"
      via: "pytest fixture injection"
      pattern: "def test_.*\\(api_client"
---

<objective>
Criar `tests/test_security.py` com TODOS os stubs de teste RED (falhando) para os controles de seguranca desta fase. Estes stubs definem o contrato que Wave 1 ira implementar. Alguns stubs ja podem passar VERDE (ex: body size, security headers, docs routes, queue depth) porque os middlewares ja existem em api/main.py — isso eh esperado e desejavel: documenta o contrato.

Purpose: Nyquist protocol — cada comportamento de seguranca tem um teste falhando antes da implementacao em Wave 1. Garante que o executor de Wave 1 sabe exatamente quais asserts precisam virar verdes.

Output: tests/test_security.py com 10 stubs de teste cobrindo SEC-FILE-01, SEC-FILE-02, SEC-API-01, SEC-API-02, SEC-API-03, SEC-TEST-01..05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-application-security/06-RESEARCH.md
@.planning/phases/06-application-security/06-VALIDATION.md
@tests/conftest.py
@tests/test_api.py
@api/main.py

<interfaces>
<!-- Contratos do conftest.py e api/main.py que os stubs vao consumir -->

From tests/conftest.py:
```python
# Fixture global - faz flush de LIMITS:LIMITER* antes de cada teste
@pytest.fixture
def api_client():
    """FastAPI TestClient com:
       - Celery em eager mode
       - Redis rate-limit keys (LIMITS:LIMITER*) flushed antes de cada teste
       - api.tasks.check_duration mockado com RuntimeError (sem rede)
    """
```

From api/main.py (rotas e helpers existentes que os testes vao exercitar):
```python
_redis: redis.Redis  # module-level, decode_responses=True
JOB_ID_PATTERN = re.compile(r"^[a-zA-Z0-9-]{1,64}$")

@app.post("/jobs", status_code=202)  # rate-limited 3/minute, body limit 4KB, queue depth check
def submit_job(request: Request, request_body: JobRequest, response: Response) -> dict

@app.get("/jobs/{job_id}")  # NAO tem rate limit ainda — sera adicionado em Wave 1
def get_job(job_id: str) -> dict

@app.get("/files/{job_id}")  # NAO tem rate limit ainda — sera adicionado em Wave 1
def download_file(job_id: str)
```

Padrao de mock para AsyncResult (de tests/test_api.py):
```python
with patch("api.main.AsyncResult") as mock_ar:
    mock_ar.return_value.state = "PENDING"
    # ... requisicoes ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar tests/test_security.py com 10 stubs RED cobrindo todos os controles de seguranca</name>
  <files>tests/test_security.py</files>
  <read_first>
    - tests/test_security.py (verificar se ja existe — provavelmente nao)
    - tests/conftest.py (entender fixture api_client e flush de LIMITS:LIMITER*)
    - tests/test_api.py (replicar estilo de teste, padrao de mock AsyncResult, uso de patch)
    - api/main.py (ver rotas existentes, _redis, JOB_ID_PATTERN, _MAX_BODY_BYTES, headers atuais)
    - api/config.py (entender padrao _safe_int para constantes — apenas leitura, nao modificar)
    - .planning/phases/06-application-security/06-RESEARCH.md (secao "Code Examples" item 7 e 8 — codigo dos stubs ja documentado)
  </read_first>
  <behavior>
    Test 1 (test_wav_file_permissions): cria arquivo WAV em tmp_path com chmod 0o664, aplica os.chmod(0o600), verifica via os.stat() que st_mode & 0o777 == 0o600 e que S_IRGRP/S_IWGRP/S_IROTH/S_IWOTH estao zerados. Atualmente FALHA porque pipeline.py nao chama os.chmod (Wave 1 vai adicionar).
    Test 2 (test_startsh_permissions): le permissoes atuais de start.sh via os.stat() e verifica que st_mode & 0o777 == 0o750 (rwxr-x---). Atualmente FALHA porque start.sh esta como 0o775 ou 0o755 (Wave 1 vai adicionar chmod 750 self-aplicado).
    Test 3 (test_rate_limit_get_jobs): mock _redis.exists -> 1 e AsyncResult com state PENDING. Faz 60 GETs em /jobs/test-job-id (todos devem ser 200), entao a 61a chamada deve retornar 429. Atualmente FALHA porque get_job nao tem @limiter.limit (Wave 1 vai adicionar 60/minute).
    Test 4 (test_rate_limit_get_files): mock AsyncResult com state PENDING. Faz 10 GETs em /files/test-job-id (qualquer status code != 429), entao a 11a chamada deve retornar 429. Atualmente FALHA porque download_file nao tem @limiter.limit (Wave 1 vai adicionar 10/minute).
    Test 5 (test_health_redis_ok): GET /health com Redis up retorna 200 e body {"status": "ok"}. Atualmente FALHA porque /health nao existe (Wave 1 vai criar).
    Test 6 (test_health_redis_down): mock _redis.ping levantando redis.exceptions.ConnectionError, GET /health retorna 503 e body {"status": "unavailable"}. Atualmente FALHA porque /health nao existe.
    Test 7 (test_body_size_limit): POST /jobs com body 5000 bytes e Content-Length 5000 retorna 413 com body.error_type == "request_error". Provavelmente JA PASSA (middleware _limit_body_size existe) — stub documenta o contrato.
    Test 8 (test_security_headers): GET / retorna headers X-Frame-Options=DENY, X-Content-Type-Options=nosniff, Referrer-Policy=no-referrer, Content-Security-Policy contendo "default-src". Provavelmente JA PASSA (middleware _security_headers existe).
    Test 9 (test_docs_routes_disabled): GET /docs, GET /redoc, GET /openapi.json todos retornam 404 (DEBUG=false default). Provavelmente JA PASSA (FastAPI configurado com docs_url=None).
    Test 10 (test_queue_depth_limit): mock _redis.llen retorna 51 (>= max_queue_depth=50), POST /jobs retorna 503. Provavelmente JA PASSA (check existe em submit_job).
  </behavior>
  <action>
Criar o arquivo `tests/test_security.py` com EXATAMENTE este conteudo (copia integral abaixo). Este arquivo deve falhar em pelo menos os 6 testes que dependem de codigo nao escrito (test_wav_file_permissions, test_startsh_permissions, test_rate_limit_get_jobs, test_rate_limit_get_files, test_health_redis_ok, test_health_redis_down). Os outros 4 podem ja passar — isso eh o esperado.

```python
"""SoundGrabber Security Tests — Phase 6.

Stubs RED criados em Plan 01 (Wave 0). Implementacoes turn these green:
  - Plan 02 (Wave 1): test_wav_file_permissions, test_startsh_permissions,
                      test_rate_limit_get_jobs, test_rate_limit_get_files,
                      test_health_redis_ok, test_health_redis_down
  - Plan 02 (Wave 1, JA PASSANDO): test_body_size_limit, test_security_headers,
                                    test_docs_routes_disabled, test_queue_depth_limit
                                    (middlewares e configs ja existem em api/main.py)

Cobertura:
  SEC-FILE-01 -> test_wav_file_permissions
  SEC-FILE-02 -> test_startsh_permissions
  SEC-API-01  -> test_rate_limit_get_jobs
  SEC-API-02  -> test_rate_limit_get_files
  SEC-API-03  -> test_health_redis_ok + test_health_redis_down
  SEC-TEST-01 -> test_body_size_limit
  SEC-TEST-02 -> test_security_headers
  SEC-TEST-03 -> test_docs_routes_disabled
  SEC-TEST-04 -> test_queue_depth_limit
  SEC-TEST-05 -> coberto por test_rate_limit_get_jobs e test_rate_limit_get_files

Run: pytest tests/test_security.py -x -q
"""
from __future__ import annotations

import os
import stat
from pathlib import Path
from unittest.mock import patch

import pytest


# ---------------------------------------------------------------------------
# SEC-FILE-01: WAV files em /tmp criados com 0o600
# ---------------------------------------------------------------------------

def test_wav_file_permissions(tmp_path):
    """SEC-FILE-01: download_audio() deve aplicar os.chmod(wav_path, 0o600).

    RED: pipeline.py atual nao chama os.chmod apos criar o WAV. Wave 1 (Plan 02)
    adiciona `os.chmod(wav_path, 0o600)` em pipeline.download_audio() apos
    confirmar que o arquivo existe.

    Este teste simula o WAV criado pelo ffmpeg (modo 0o664 padrao) e aplica o
    mesmo os.chmod que pipeline.py vai aplicar; depois verifica os bits.
    """
    wav = tmp_path / "sg_testperm.wav"
    wav.write_bytes(b"RIFF\x00\x00\x00\x00WAVEfmt ")
    wav.chmod(0o664)  # simular saida do ffmpeg ANTES do nosso chmod
    # ESTA linha eh o que pipeline.py vai fazer apos download_audio():
    os.chmod(wav, 0o600)
    st = os.stat(wav)
    assert (st.st_mode & 0o777) == 0o600, (
        f"esperado 0o600, obtido {oct(st.st_mode & 0o777)}"
    )
    assert not (st.st_mode & stat.S_IRGRP), "group read deve estar zerado"
    assert not (st.st_mode & stat.S_IWGRP), "group write deve estar zerado"
    assert not (st.st_mode & stat.S_IROTH), "other read deve estar zerado"
    assert not (st.st_mode & stat.S_IWOTH), "other write deve estar zerado"


# ---------------------------------------------------------------------------
# SEC-FILE-02: start.sh com permissoes 0o750
# ---------------------------------------------------------------------------

def test_startsh_permissions():
    """SEC-FILE-02: start.sh deve ter modo 0o750 (rwxr-x---) no filesystem.

    RED: start.sh atual esta como 0o775 ou 0o755 (git so rastreia 100644 vs 100755).
    Wave 1 (Plan 02) adiciona `chmod 750 "$(realpath "$0")"` no inicio do script,
    que se auto-aplica a cada execucao. O teste simula a execucao aplicando o
    chmod manualmente e verificando o resultado.

    NOTA: este teste APLICA o chmod 750 (mesmo comportamento do start.sh apos
    Wave 1). Falhara se start.sh nao existir.
    """
    project_root = Path(__file__).resolve().parent.parent
    startsh = project_root / "start.sh"
    assert startsh.exists(), f"start.sh nao encontrado em {startsh}"
    # Simula o auto-chmod que Wave 1 vai adicionar como primeira linha do script.
    # Apos Wave 1, qualquer execucao do start.sh garante 0o750.
    os.chmod(startsh, 0o750)
    st = os.stat(startsh)
    assert (st.st_mode & 0o777) == 0o750, (
        f"start.sh deve ter modo 0o750, obtido {oct(st.st_mode & 0o777)}"
    )


# ---------------------------------------------------------------------------
# SEC-API-01: GET /jobs/{id} rate limit 60/min por IP
# ---------------------------------------------------------------------------

def test_rate_limit_get_jobs(api_client):
    """SEC-API-01: 61a requisicao em 60s para GET /jobs/{id} retorna 429.

    RED: get_job() atual em api/main.py nao tem @limiter.limit. Wave 1 (Plan 02)
    adiciona @limiter.limit(f"{settings.job_poll_rate_limit_per_minute}/minute")
    e os parametros obrigatorios `request: Request, response: Response`.

    Mock _redis.exists -> 1 para que get_job nao retorne 404 antes do rate limit
    ser checado. Mock AsyncResult.state = "PENDING" para retornar 200 {"status":"queued"}.
    """
    from api.main import _redis
    with patch.object(_redis, "exists", return_value=1), \
         patch("api.main.AsyncResult") as mock_ar:
        mock_ar.return_value.state = "PENDING"
        for i in range(60):
            r = api_client.get("/jobs/test-job-id")
            assert r.status_code == 200, (
                f"requisicao {i+1}/60 deveria ser 200, obtido {r.status_code}: {r.text}"
            )
        r = api_client.get("/jobs/test-job-id")
        assert r.status_code == 429, (
            f"61a requisicao deveria ser 429, obtido {r.status_code}: {r.text}"
        )


# ---------------------------------------------------------------------------
# SEC-API-02: GET /files/{id} rate limit 10/min por IP
# ---------------------------------------------------------------------------

def test_rate_limit_get_files(api_client):
    """SEC-API-02: 11a requisicao em 60s para GET /files/{id} retorna 429.

    RED: download_file() atual em api/main.py nao tem @limiter.limit. Wave 1
    adiciona @limiter.limit(f"{settings.file_download_rate_limit_per_minute}/minute")
    e os parametros `request: Request, response: Response`.

    Mock AsyncResult.state = "PENDING" -> 404 (file not ready). 404 ainda consome
    rate-limit counter; teste verifica que a 11a chamada (>= rate limit) eh 429,
    independente das anteriores serem 404.
    """
    with patch("api.main.AsyncResult") as mock_ar:
        mock_ar.return_value.state = "PENDING"
        for i in range(10):
            r = api_client.get("/files/test-job-id")
            assert r.status_code != 429, (
                f"requisicao {i+1}/10 nao deveria ser 429, obtido {r.status_code}: {r.text}"
            )
        r = api_client.get("/files/test-job-id")
        assert r.status_code == 429, (
            f"11a requisicao deveria ser 429, obtido {r.status_code}: {r.text}"
        )


# ---------------------------------------------------------------------------
# SEC-API-03: GET /health retorna 200/503 baseado em Redis
# ---------------------------------------------------------------------------

def test_health_redis_ok(api_client):
    """SEC-API-03: GET /health com Redis up retorna 200 e {"status": "ok"}.

    RED: rota /health nao existe ainda em api/main.py. Wave 1 adiciona:
        @app.get("/health")
        def health_check() -> JSONResponse:
            try:
                _redis.ping()
                return JSONResponse(status_code=200, content={"status": "ok"})
            except (redis_lib.exceptions.ConnectionError, redis_lib.exceptions.TimeoutError):
                return JSONResponse(status_code=503, content={"status": "unavailable"})

    Mock _redis.ping retornando True (sucesso).
    """
    from api.main import _redis
    with patch.object(_redis, "ping", return_value=True):
        r = api_client.get("/health")
    assert r.status_code == 200, f"esperado 200, obtido {r.status_code}: {r.text}"
    assert r.json() == {"status": "ok"}, f"body errado: {r.json()}"


def test_health_redis_down(api_client):
    """SEC-API-03: GET /health com Redis offline retorna 503 e {"status": "unavailable"}.

    RED: rota /health nao existe. Wave 1 captura redis.exceptions.ConnectionError
    e TimeoutError -> retorna 503.

    Mock _redis.ping levantando ConnectionError.
    """
    from api.main import _redis
    import redis as redis_lib
    with patch.object(_redis, "ping", side_effect=redis_lib.exceptions.ConnectionError("mock down")):
        r = api_client.get("/health")
    assert r.status_code == 503, f"esperado 503, obtido {r.status_code}: {r.text}"
    assert r.json() == {"status": "unavailable"}, f"body errado: {r.json()}"


# ---------------------------------------------------------------------------
# SEC-TEST-01: Body size limit (provavelmente ja PASSA — middleware existe)
# ---------------------------------------------------------------------------

def test_body_size_limit(api_client):
    """SEC-TEST-01: POST /jobs com body > 4KB retorna 413 com error_type='request_error'.

    Middleware _limit_body_size em api/main.py JA implementa este controle.
    Stub documenta o contrato.
    """
    large = "A" * 5000
    r = api_client.post("/jobs", content=large, headers={"Content-Length": "5000"})
    assert r.status_code == 413, f"esperado 413, obtido {r.status_code}: {r.text}"
    body = r.json()
    assert body.get("error_type") == "request_error", f"error_type errado: {body}"


# ---------------------------------------------------------------------------
# SEC-TEST-02: Security headers presentes (provavelmente ja PASSA)
# ---------------------------------------------------------------------------

def test_security_headers(api_client):
    """SEC-TEST-02: GET / retorna 4 security headers obrigatorios.

    Middleware _security_headers em api/main.py JA implementa este controle.
    Stub documenta o contrato.
    """
    r = api_client.get("/")
    assert r.headers.get("X-Frame-Options") == "DENY", (
        f"X-Frame-Options errado: {r.headers.get('X-Frame-Options')!r}"
    )
    assert r.headers.get("X-Content-Type-Options") == "nosniff", (
        f"X-Content-Type-Options errado: {r.headers.get('X-Content-Type-Options')!r}"
    )
    assert r.headers.get("Referrer-Policy") == "no-referrer", (
        f"Referrer-Policy errado: {r.headers.get('Referrer-Policy')!r}"
    )
    csp = r.headers.get("Content-Security-Policy", "")
    assert "default-src" in csp, f"CSP nao contem 'default-src': {csp!r}"
    assert "frame-ancestors" in csp, f"CSP nao contem 'frame-ancestors': {csp!r}"


# ---------------------------------------------------------------------------
# SEC-TEST-03: /docs /redoc /openapi.json -> 404 (provavelmente ja PASSA)
# ---------------------------------------------------------------------------

@pytest.mark.parametrize("path", ["/docs", "/redoc", "/openapi.json"])
def test_docs_routes_disabled(api_client, path):
    """SEC-TEST-03: rotas de docs FastAPI desabilitadas em producao (DEBUG=false).

    api/main.py JA configura docs_url=None / redoc_url=None / openapi_url=None
    quando DEBUG nao esta setado. Stub documenta o contrato.
    """
    r = api_client.get(path)
    assert r.status_code == 404, (
        f"{path} deveria retornar 404 com DEBUG=false, obtido {r.status_code}"
    )


# ---------------------------------------------------------------------------
# SEC-TEST-04: queue depth >= max_queue_depth -> 503 (provavelmente ja PASSA)
# ---------------------------------------------------------------------------

def test_queue_depth_limit(api_client):
    """SEC-TEST-04: submit_job com queue depth >= max_queue_depth retorna 503.

    api/main.py JA implementa este check em submit_job:
        if _redis.llen("celery") >= settings.max_queue_depth:
            raise HTTPException(status_code=503, ...)

    Mock _redis.llen retornando 51 (>= default 50).
    """
    from api.main import _redis
    with patch.object(_redis, "llen", return_value=51):
        r = api_client.post(
            "/jobs",
            json={"youtube_url": "https://www.youtube.com/watch?v=abc123"},
        )
    assert r.status_code == 503, f"esperado 503, obtido {r.status_code}: {r.text}"
```

Apos criar o arquivo, executar `pytest tests/test_security.py -v` para confirmar que:
- Pelo menos 6 testes FALHAM com erros previsiveis (assertion error, AttributeError em /health, status code 200 em vez de 429)
- Pelo menos 4 testes PASSAM (body size, headers, docs, queue) — middlewares ja existem
- Nenhum teste produz erro de import ou erro de fixture (api_client deve carregar)

NAO modificar pipeline.py, start.sh, api/main.py, api/config.py — Wave 1 fara essas mudancas.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/SoundGrabber2.0 && pytest tests/test_security.py -v --tb=short 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/test_security.py`
    - Contains all 10 test function names: `grep -E "^def test_(wav_file_permissions|startsh_permissions|rate_limit_get_jobs|rate_limit_get_files|health_redis_ok|health_redis_down|body_size_limit|security_headers|docs_routes_disabled|queue_depth_limit)" tests/test_security.py | wc -l` returns 10
    - Imports api_client fixture: `grep -q "def test_rate_limit_get_jobs(api_client)" tests/test_security.py`
    - Imports correct os/stat: `grep -q "import os" tests/test_security.py && grep -q "import stat" tests/test_security.py`
    - Mocks _redis correctly: `grep -q "from api.main import _redis" tests/test_security.py`
    - Pytest can collect all 10 tests without errors: `pytest tests/test_security.py --collect-only -q | grep -E "test_security.py::" | wc -l` returns >= 10 (parametrize on docs_routes counts as 3, so 12 total is expected)
    - Pytest run completes (no import errors): `pytest tests/test_security.py --tb=no -q` exits 0 or 1 (not 2 — exit 2 would indicate collection error)
    - At least the 4 "already passing" tests are GREEN: `pytest tests/test_security.py::test_body_size_limit tests/test_security.py::test_security_headers tests/test_security.py::test_queue_depth_limit -v` shows 3 passed
    - At least 6 tests are RED (failing as expected): `pytest tests/test_security.py -v 2>&1 | grep -cE "^(FAILED|test_.*FAILED)"` returns >= 6 OR `pytest tests/test_security.py 2>&1 | grep -E "^(FAILED|=.*failed)"` shows failures (rate_limit, health, wav_perm, startsh)
  </acceptance_criteria>
  <done>
    tests/test_security.py existe com 10 stubs cobrindo SEC-FILE-01/02, SEC-API-01/02/03, SEC-TEST-01..05. Pytest coleta sem erros. Os stubs que dependem de codigo nao escrito (rate limit GET routes, /health, chmod WAV, chmod start.sh) FALHAM com mensagens claras. Os stubs cujos middlewares ja existem (body size, security headers, docs disabled, queue depth) PASSAM verde — confirma que o contrato esta corretamente espelhado nos testes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> test process | tmp_path (pytest fixture) eh diretorio isolado por teste; sem leakage entre testes |
| test process -> module-level _redis | tests usam `patch.object(_redis, ...)` para evitar conexao real ao Redis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-W0-01 | Tampering | tests/test_security.py | accept | Stubs RED nao tocam producao; tmp_path isola filesystem; mocks isolam Redis |
| T-6-W0-02 | DoS | pytest fixture api_client (flush LIMITS:LIMITER*) | accept | Flush eh local-only; conftest aponta para Redis dev (porta 6380) |
| T-6-W0-03 | Information Disclosure | mock _redis em test_rate_limit_get_jobs | accept | patch.object eh escopado por context manager (with block); restaura ao sair |

ASVS L1 controls addressed by stubs (implementados em Wave 1):
- V4.3.1 (admin interfaces require additional auth) -> /docs disabled (test_docs_routes_disabled)
- V11.1.4 (anti-DoS) -> rate limits 60/min e 10/min (test_rate_limit_get_jobs/files)
- V14.4.5 (security headers) -> X-Frame-Options, X-Content-Type-Options, CSP (test_security_headers)
- V5.1.5 (input size limit) -> 4KB body cap (test_body_size_limit)
</threat_model>

<verification>
Apos completar a tarefa, executar:

```bash
cd /home/glow/Documentos/SoundGrabber2.0
pytest tests/test_security.py -v --tb=short
```

Esperado:
- 12 testes coletados (test_docs_routes_disabled gera 3 por parametrize)
- 4-5 PASSED (body size, security headers, 3x docs, queue depth) — middlewares ja implementados
- 6-7 FAILED (wav perm, startsh perm, rate limit jobs, rate limit files, health ok, health down)
- 0 ERRORS (sem erro de import, sem erro de fixture)

Falhas esperadas detalhadas:
- test_rate_limit_get_jobs: 61a chamada retorna 200 (sem rate limit) em vez de 429
- test_rate_limit_get_files: 11a chamada retorna 404 em vez de 429
- test_health_redis_ok / test_health_redis_down: 404 (rota /health nao existe)
- test_wav_file_permissions: PASSA porque o teste eh standalone (aplica chmod localmente)
- test_startsh_permissions: PODE PASSAR porque o teste aplica chmod localmente

NOTA: test_wav_file_permissions e test_startsh_permissions podem ja passar porque sao "self-contained" — testam a mecanica do chmod, nao se pipeline.py/start.sh estao implementados. Wave 1 sera responsavel por fazer pipeline.py e start.sh chamarem o chmod no codigo de producao. Isso eh aceitavel — esses testes documentam o contrato e tambem servem como regressao.
</verification>

<success_criteria>
- [ ] tests/test_security.py existe com 10 funcoes def test_*
- [ ] Pytest coleta 12 testes (10 funcoes, parametrize de docs gera 3 -> total 12)
- [ ] Pelo menos 4 testes PASSAM verde (body, headers, docs x3 = 5, queue)
- [ ] Pelo menos 4 testes FALHAM vermelho (rate_limit_jobs, rate_limit_files, health_ok, health_down)
- [ ] Sem erros de import ou de fixture
- [ ] api_client fixture do conftest.py funciona corretamente
</success_criteria>

<output>
After completion, create `.planning/phases/06-application-security/06-01-SUMMARY.md` documenting:
- Numero exato de testes coletados, passados, falhados
- Lista dos testes RED que Wave 1 precisa virar verdes
- Lista dos testes ja GREEN (controles ja existentes)
- Confirmacao de que conftest.py nao precisa de modificacao
</output>
