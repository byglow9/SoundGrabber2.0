---
phase: 06-application-security
plan: 03
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - README.md
  - CLAUDE.md
  - .planning/SECURITY-CHECKLIST.md
autonomous: true
requirements:
  - SEC-TEST-06
  - SEC-POLICY-01
  - SEC-POLICY-02

must_haves:
  truths:
    - "README.md contem secao documentando pip-audit como verificacao obrigatoria pre-deploy com comando exato"
    - "CLAUDE.md contem secao '## Security Gate' descrevendo regras obrigatorias de seguranca para novas funcionalidades"
    - ".planning/SECURITY-CHECKLIST.md existe e cobre todos os controles da Phase 6 com checklist verificavel"
    - "Comando pip-audit documentado: pip install pip-audit && pip-audit -r requirements.txt"
    - "SECURITY-CHECKLIST.md referencia todos os controles SEC-FILE-*, SEC-API-*, SEC-TEST-*, SEC-POLICY-*"
    - "Nenhum codigo de aplicacao (api/, pipeline.py, start.sh) eh modificado neste plan — apenas documentacao"
    - "Nenhum teste anterior quebra (suite pytest tests/ continua verde)"
  artifacts:
    - path: "README.md"
      provides: "Secao pip-audit pre-deploy com comando exato"
      contains:
        - "pip-audit"
        - "pip install pip-audit"
        - "pip-audit -r requirements.txt"
    - path: "CLAUDE.md"
      provides: "Secao '## Security Gate' com regras de seguranca obrigatorias"
      contains:
        - "## Security Gate"
        - "pip-audit"
        - "tests/test_security.py"
    - path: ".planning/SECURITY-CHECKLIST.md"
      provides: "Checklist de seguranca abrangente cobrindo todos os controles Phase 6"
      min_lines: 50
      contains:
        - "SEC-FILE-01"
        - "SEC-FILE-02"
        - "SEC-API-01"
        - "SEC-API-02"
        - "SEC-API-03"
        - "SEC-TEST-01"
        - "SEC-TEST-02"
        - "SEC-TEST-03"
        - "SEC-TEST-04"
        - "SEC-TEST-05"
        - "SEC-TEST-06"
        - "SEC-POLICY-01"
        - "SEC-POLICY-02"
        - "pip-audit"
  key_links:
    - from: "CLAUDE.md::Security Gate"
      to: ".planning/SECURITY-CHECKLIST.md"
      via: "Referencia explicita ao checklist como fonte de verdade"
      pattern: "SECURITY-CHECKLIST\\.md"
    - from: "README.md::Pre-Deploy Security"
      to: "pip-audit"
      via: "Comando exato documentado para auditoria de dependencias"
      pattern: "pip-audit -r requirements\\.txt"
    - from: ".planning/SECURITY-CHECKLIST.md"
      to: "tests/test_security.py"
      via: "Checklist referencia testes automatizados como evidencia"
      pattern: "tests/test_security\\.py"
---

<objective>
Documentar a politica de seguranca do projeto em tres artefatos: (1) secao pip-audit no README como verificacao obrigatoria pre-deploy (SEC-TEST-06), (2) secao "## Security Gate" no CLAUDE.md tornando os controles de seguranca regra obrigatoria para qualquer nova funcionalidade (SEC-POLICY-01), e (3) `.planning/SECURITY-CHECKLIST.md` consolidando todos os controles da Phase 6 em um checklist verificavel (SEC-POLICY-02).

Purpose: Transformar os controles tecnicos implementados em Wave 1 em politica permanente do projeto. Sem documentacao explicita, qualquer feature futura pode regredir os controles silenciosamente. O Security Gate em CLAUDE.md eh lido por Claude antes de cada nova fase, garantindo que novas funcionalidades sejam planejadas com seguranca por default.

Output: 2 arquivos modificados (README.md, CLAUDE.md) + 1 arquivo novo (.planning/SECURITY-CHECKLIST.md). Zero modificacoes em codigo de producao. Zero impacto em testes existentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-application-security/06-RESEARCH.md
@.planning/phases/06-application-security/06-01-PLAN.md
@.planning/phases/06-application-security/06-02-PLAN.md
@README.md
@CLAUDE.md
@requirements.txt

<interfaces>
<!-- Estado ATUAL dos arquivos que serao modificados/criados -->

README.md atual: tem secoes "Install", "User Setup", "Usage", "Tests", "Rodando localmente", "File Layout", "Known Limitations", "Architecture", "License". A secao pip-audit deve ser adicionada ANTES de "License" e DEPOIS de "Architecture" — perto do final, mas com proeminencia (nivel ##).

CLAUDE.md atual: tem secoes "What This Is", "Stack", "GSD Workflow", "Fases", "Restricoes criticas", "Proximo passo". A secao "## Security Gate" deve ser adicionada ENTRE "Restricoes criticas" e "Proximo passo" — antes do bloco de comando de proximos passos.

.planning/SECURITY-CHECKLIST.md: NAO existe. Criar do zero.

Coverage map (Phase 6 requisitos -> SECURITY-CHECKLIST.md sections):
- SEC-FILE-01 -> "Filesystem permissions" / item "WAV files chmod 0o600"
- SEC-FILE-02 -> "Filesystem permissions" / item "start.sh chmod 750"
- SEC-API-01 -> "API rate limiting" / item "GET /jobs/{id} 60/min"
- SEC-API-02 -> "API rate limiting" / item "GET /files/{id} 10/min"
- SEC-API-03 -> "Health endpoint" / item "GET /health 200/503"
- SEC-TEST-01 -> "Automated test coverage" / item "body size limit"
- SEC-TEST-02 -> "Automated test coverage" / item "security headers"
- SEC-TEST-03 -> "Automated test coverage" / item "/docs disabled"
- SEC-TEST-04 -> "Automated test coverage" / item "queue depth"
- SEC-TEST-05 -> "Automated test coverage" / item "rate limits"
- SEC-TEST-06 -> "Pre-deploy audit" / item "pip-audit"
- SEC-POLICY-01 -> Self-referential: este checklist eh o resultado da policy
- SEC-POLICY-02 -> Self-referential: este arquivo

NOTA SOBRE FORMATACAO DESTE PLAN: blocos de codigo markdown embutidos abaixo usam fences explicitos (com markdown ou bash). Linhas separadoras horizontais dentro do conteudo embutido usam `===` em vez de `---` para evitar confundir o parser de frontmatter. Ao criar os arquivos REAIS (README.md, CLAUDE.md, SECURITY-CHECKLIST.md), o executor DEVE usar `---` (a sintaxe markdown correta para horizontal rule), conforme o exemplo final em cada Action block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar secao pip-audit pre-deploy ao README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (TODO arquivo — entender layout atual e onde inserir)
    - requirements.txt (verificar quais deps serao auditadas)
    - .planning/phases/06-application-security/06-RESEARCH.md (secao "Standard Stack > Dev > pip-audit" para confirmar comando)
  </read_first>
  <action>
**EDIT 1 — README.md: adicionar secao "## Pre-Deploy Security Audit" antes de "## License":**

Localizar a secao "## License" no final do README.md (linha ~176). INSERIR a nova secao IMEDIATAMENTE ANTES dela, com este conteudo (substitua `{TRES-BACKTICKS}` pelo literal de tres backticks no momento de escrever no arquivo real):

Heading da secao: `## Pre-Deploy Security Audit`

Paragrafo 1: "**Obrigatorio antes de cada deploy** — auditoria de dependencias para CVEs conhecidos via [pip-audit](https://github.com/pypa/pip-audit) (PyPA, integra OSV + PyPI Advisory DB)."

Bloco de codigo bash (entre `{TRES-BACKTICKS}bash` e `{TRES-BACKTICKS}`) com EXATAMENTE estas duas linhas:
- `pip install pip-audit`
- `pip-audit -r requirements.txt`

Paragrafo 2: "Se houver vulnerabilidades reportadas:"

Lista numerada (3 itens):
1. "Atualizar a dependencia afetada para a versao patcheada (ex: `pip install -U <pkg>` e atualizar pin em `requirements.txt`)."
2. "Se nao houver patch disponivel, avaliar workaround ou aceitar risco com justificativa em `.planning/SECURITY-CHECKLIST.md`."
3. "**NAO fazer deploy** com vulnerabilidades de severidade HIGH ou CRITICAL sem decisao explicita."

Paragrafo final: "**Nota:** `pip-audit` eh ferramenta de auditoria, NAO runtime — nao precisa estar em `requirements.txt` de producao. Instalar sob demanda na maquina de deploy ou em CI."

NAO modificar nenhuma outra secao do README.md. NAO mover "## License" — apenas inserir a nova secao acima dela. Usar duas linhas em branco para separar a nova secao da secao seguinte (estilo padrao do resto do README).

EXEMPLO LITERAL do bloco a inserir (use isto como guia exato — substitua os tres backticks no comeco e fim do bloco bash pelos backticks reais):

```text
## Pre-Deploy Security Audit

**Obrigatorio antes de cada deploy** — auditoria de dependencias para CVEs conhecidos via [pip-audit](https://github.com/pypa/pip-audit) (PyPA, integra OSV + PyPI Advisory DB).

(BLOCO BASH AQUI: tres backticks + "bash", duas linhas:
pip install pip-audit
pip-audit -r requirements.txt
tres backticks de fechamento)

Se houver vulnerabilidades reportadas:

1. Atualizar a dependencia afetada para a versao patcheada (ex: `pip install -U <pkg>` e atualizar pin em `requirements.txt`).
2. Se nao houver patch disponivel, avaliar workaround ou aceitar risco com justificativa em `.planning/SECURITY-CHECKLIST.md`.
3. **NAO fazer deploy** com vulnerabilidades de severidade HIGH ou CRITICAL sem decisao explicita.

**Nota:** `pip-audit` eh ferramenta de auditoria, NAO runtime — nao precisa estar em `requirements.txt` de producao. Instalar sob demanda na maquina de deploy ou em CI.
```
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/SoundGrabber2.0 && grep -c "## Pre-Deploy Security Audit" README.md && grep -c "pip install pip-audit" README.md && grep -c "pip-audit -r requirements.txt" README.md && grep -B1 "^## License$" README.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Pre-Deploy Security Audit" README.md` returns 1
    - `grep -c "pip install pip-audit" README.md` returns 1
    - `grep -c "pip-audit -r requirements.txt" README.md` returns 1
    - Secao aparece ANTES de "## License": `awk '/## Pre-Deploy Security Audit/{a=NR} /^## License$/{b=NR} END{exit !(a > 0 && b > 0 && a < b)}' README.md` exits 0
    - Conteudo menciona PyPA: `grep -q "PyPA" README.md`
    - Conteudo menciona OSV: `grep -q "OSV" README.md`
    - README continua com pelo menos 195 linhas (era ~178 + ~16 novas): `[ $(wc -l < README.md) -ge 190 ]` exits 0
    - Secao "## License" continua existindo: `grep -c "^## License$" README.md` returns 1
    - Todas as 11 secoes pre-existentes continuam: `grep -cE "^## (What This Phase Provides|Requirements|Install|User Setup|Usage|Tests|Rodando localmente|File Layout|Known Limitations|Architecture|License)" README.md` returns 11
  </acceptance_criteria>
  <done>
    README.md tem nova secao "## Pre-Deploy Security Audit" entre "## Architecture" e "## License", contendo o comando exato `pip install pip-audit && pip-audit -r requirements.txt`, justificativa do uso, e politica de tratamento de vulnerabilidades. Todas as outras secoes pre-existentes intactas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar secao "## Security Gate" ao CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (TODO arquivo — entender layout atual e ponto de insercao)
    - .planning/REQUIREMENTS.md (linhas SEC-* para garantir alinhamento da policy)
    - .planning/phases/06-application-security/06-02-PLAN.md (controles concretos referenciados)
  </read_first>
  <action>
**EDIT 1 — CLAUDE.md: adicionar secao "## Security Gate" entre "## Restricoes criticas" e "## Proximo passo":**

Localizar a secao "## Proximo passo" no final do CLAUDE.md (linha ~55). Antes dela existe a secao "## Restricoes criticas". INSERIR a nova secao "## Security Gate" ENTRE essas duas. A nova secao deve ter exatamente esta estrutura (subsecoes em `###`):

Heading: `## Security Gate`

Paragrafo introdutorio: "**Regra obrigatoria para qualquer nova feature ou modificacao.** Toda mudanca em codigo de producao DEVE passar por este gate antes do merge. Esta secao tem precedencia sobre velocidade de entrega."

Subsecao 1: `### Controles obrigatorios em qualquer novo endpoint HTTP` — lista numerada de 4 itens:
1. "**Rate limiting** — qualquer rota nova (GET, POST, PUT, DELETE) DEVE ter `@limiter.limit(\"<N>/minute\")`. Default conservador: 60/min para reads, 10/min para writes/downloads. Justificar se omitir."
2. "**Request body validation** — POST/PUT DEVEM usar Pydantic `BaseModel` com `field_validator`. Sem `request.json()` cru."
3. "**Body size limit** — middleware `_limit_body_size` (4KB) ja cobre globalmente. Se uma rota legitimamente precisar mais, justificar e aumentar `_MAX_BODY_BYTES` com nota explicita."
4. "**Sync routes com slowapi** — SEMPRE adicionar `request: Request, response: Response` na assinatura. Sem isso, slowapi sync_wrapper levanta Exception em runtime."

Subsecao 2: `### Controles obrigatorios em qualquer novo arquivo gerado em /tmp` — lista numerada de 3 itens:
1. "**Permissoes 0o600** — `os.chmod(path, 0o600)` apos confirmar que o arquivo existe. Bloqueia leitura por outros usuarios do sistema."
2. "**Prefixo `sg_`** — todos os arquivos do projeto em /tmp comecam com `sg_` para o sweeper limpar e para path-traversal defense."
3. "**Path traversal defense** — qualquer endpoint que retorne arquivo DEVE validar `path.resolve().relative_to(Path(\"/tmp\").resolve())` antes de servir."

Subsecao 3: `### Controles obrigatorios em qualquer novo script shell` — lista numerada de 3 itens:
1. "**`set -e`** na primeira linha apos shebang."
2. "**Auto-chmod restritivo** — `chmod 750 \"$(realpath \"$0\")\"` para scripts de operacao; `chmod 600` para scripts contendo segredos."
3. "**Sem `eval` de input externo** — uso de `eval` requer justificativa explicita."

Subsecao 4: `### Testes obrigatorios` — lista numerada de 3 itens:
1. "**Cada novo endpoint DEVE ter teste em `tests/test_security.py`** confirmando rate limit e validacoes."
2. "**`pytest tests/test_security.py` DEVE estar verde** antes de qualquer commit em main."
3. "**`pip-audit -r requirements.txt`** DEVE ser executado antes de cada deploy (ver `.planning/SECURITY-CHECKLIST.md`)."

Subsecao 5: `### Documentacao obrigatoria` — lista numerada de 2 itens:
1. "**`.planning/SECURITY-CHECKLIST.md`** eh a fonte de verdade dos controles ativos. Nova feature que adiciona controle DEVE atualizar este arquivo."
2. "**REQUIREMENTS.md** deve ter REQ-ID novo (categoria SEC-*) para qualquer novo controle de seguranca."

Subsecao 6: `### Quando esta regra pode ser flexibilizada`

Paragrafo: "Nunca silenciosamente. Apenas com decisao explicita registrada em `STATE.md` (Key Decisions) e justificativa que sobreviva a revisao 6 meses depois. Default eh: aplicar o controle."

NAO modificar nenhuma outra secao do CLAUDE.md. A nova secao "## Security Gate" deve ficar EXATAMENTE entre "## Restricoes criticas" e "## Proximo passo". Manter linha em branco antes e depois da nova secao para legibilidade.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/SoundGrabber2.0 && grep -c "^## Security Gate$" CLAUDE.md && grep -c "tests/test_security.py" CLAUDE.md && grep -c "pip-audit" CLAUDE.md && grep -c "SECURITY-CHECKLIST.md" CLAUDE.md && awk '/^## Restricoes criticas$/{a=NR} /^## Security Gate$/{b=NR} /^## Proximo passo$/{c=NR} END{print "restricoes:", a, "security:", b, "proximo:", c}' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Security Gate$" CLAUDE.md` returns 1
    - `grep -c "tests/test_security.py" CLAUDE.md` returns >= 1
    - `grep -c "pip-audit" CLAUDE.md` returns >= 1
    - `grep -c "SECURITY-CHECKLIST.md" CLAUDE.md` returns >= 1
    - `grep -c "@limiter.limit" CLAUDE.md` returns >= 1
    - `grep -c "os.chmod" CLAUDE.md` returns >= 1
    - Secao "## Security Gate" aparece ENTRE "## Restricoes criticas" e "## Proximo passo": `awk '/^## Restricoes criticas$/{a=NR} /^## Security Gate$/{b=NR} /^## Proximo passo$/{c=NR} END{exit !(a > 0 && b > 0 && c > 0 && a < b && b < c)}' CLAUDE.md` exits 0
    - As 6 subsecoes obrigatorias aparecem: `grep -cE "^### (Controles obrigatorios em qualquer novo endpoint HTTP|Controles obrigatorios em qualquer novo arquivo gerado em /tmp|Controles obrigatorios em qualquer novo script shell|Testes obrigatorios|Documentacao obrigatoria|Quando esta regra pode ser flexibilizada)$" CLAUDE.md` returns 6
    - CLAUDE.md cresceu (era ~60 linhas, agora >= 95): `[ $(wc -l < CLAUDE.md) -ge 95 ]` exits 0
  </acceptance_criteria>
  <done>
    CLAUDE.md tem nova secao "## Security Gate" entre "## Restricoes criticas" e "## Proximo passo", documentando 5 conjuntos de controles obrigatorios (HTTP endpoints, /tmp files, shell scripts, testes, documentacao) + clausula sobre quando flexibilizar. Referencia explicita a `.planning/SECURITY-CHECKLIST.md`, `tests/test_security.py`, e `pip-audit`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar .planning/SECURITY-CHECKLIST.md cobrindo todos os controles Phase 6</name>
  <files>.planning/SECURITY-CHECKLIST.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (todos os SEC-* requirements)
    - .planning/phases/06-application-security/06-RESEARCH.md (secao "Security Domain" e "Phase Requirements" para mapping completo)
    - .planning/phases/06-application-security/06-02-PLAN.md (threat_model com controles especificos)
    - tests/test_security.py (apos Plan 01, para listar comandos pytest exatos)
    - README.md (apos Task 1 deste plan, para referenciar secao pip-audit)
  </read_first>
  <action>
**CREATE — .planning/SECURITY-CHECKLIST.md (arquivo NOVO):**

Criar o arquivo `.planning/SECURITY-CHECKLIST.md` cobrindo 8 secoes principais. Use markdown padrao: heading principal `# Security Checklist — SoundGrabber`, seguido de paragrafo introdutorio + secoes numeradas (`## 1. ...`, `## 2. ...`, etc) separadas por horizontal rules markdown (linha com tres tracos).

Estrutura completa (use exatamente estas secoes e itens):

**Heading 1:** `# Security Checklist — SoundGrabber`

**Paragrafo introdutorio:** "**Fonte de verdade dos controles de seguranca ativos.** Atualizar sempre que um novo controle for adicionado ou removido. Esta checklist eh referenciada por `CLAUDE.md > Security Gate` e deve estar verde antes de cada deploy de producao."

**Metadata em duas linhas separadas:**
- `**Ultima atualizacao:** Phase 6 (Application Security)`
- `**Proxima revisao:** Phase 7 (Infrastructure Security) — SEC-INFRA-01..04`

**Secao "## Como usar":** Lista numerada de 4 itens:
1. "Antes de cada deploy de producao, percorrer este checklist do topo ao fim."
2. "Cada item tem um comando de verificacao automatizada (quando aplicavel)."
3. "Se um item falhar, NAO fazer deploy ate corrigir ou registrar excecao em `.planning/STATE.md > Key Decisions`."
4. "Ao adicionar nova feature, conferir contra `CLAUDE.md > Security Gate` antes do merge."

**Secao "## 1. Filesystem Permissions":**

Subsecao "### SEC-FILE-01 — WAV files em /tmp criados com 0o600":
- [ ] item: "`pipeline.py::download_audio()` chama `os.chmod(wav_path, 0o600)` antes de `return wav_path`"
- "**Verificacao:** `pytest tests/test_security.py::test_wav_file_permissions -x`"
- "**Verificacao runtime:** apos rodar pipeline com URL valida, `stat -c '%a' /tmp/sg_*.wav` retorna `600`"
- "**Threat:** Information Disclosure — outros usuarios do OS poderiam ler WAVs sem este chmod"

Subsecao "### SEC-FILE-02 — start.sh com permissoes 750":
- [ ] item: "`start.sh` tem `chmod 750 \"$(realpath \"$0\")\"` apos `set -e`"
- "**Verificacao:** `pytest tests/test_security.py::test_startsh_permissions -x`"
- "**Verificacao runtime:** apos `bash start.sh`, `ls -l start.sh` mostra `-rwxr-x---`"
- "**Threat:** Elevation of Privilege — outros usuarios poderiam executar start.sh"

**Secao "## 2. API Rate Limiting":**

Subsecao "### SEC-API-01 — GET /jobs/{id} rate limit 60/min":
- [ ] "`api/main.py::get_job` decorado com `@limiter.limit(f\"{settings.job_poll_rate_limit_per_minute}/minute\")`"
- [ ] "Assinatura inclui `request: Request, response: Response`"
- [ ] "`JOB_POLL_RATE_LIMIT_PER_MINUTE` configuravel via env (default 60)"
- "**Verificacao:** `pytest tests/test_security.py::test_rate_limit_get_jobs -x`"
- "**Threat:** DoS via polling agressivo"

Subsecao "### SEC-API-02 — GET /files/{id} rate limit 10/min":
- [ ] "`api/main.py::download_file` decorado com `@limiter.limit(f\"{settings.file_download_rate_limit_per_minute}/minute\")`"
- [ ] "Assinatura inclui `request: Request, response: Response`"
- [ ] "`FILE_DOWNLOAD_RATE_LIMIT_PER_MINUTE` configuravel via env (default 10)"
- "**Verificacao:** `pytest tests/test_security.py::test_rate_limit_get_files -x`"
- "**Threat:** DoS via download bombing (custo de I/O alto)"

Subsecao "### SEC-API-03 — GET /health (liveness Redis)":
- [ ] "Rota `@app.get(\"/health\")` existe em `api/main.py`"
- [ ] "Retorna 200 `{\"status\": \"ok\"}` quando `_redis.ping()` succeeds"
- [ ] "Retorna 503 `{\"status\": \"unavailable\"}` em `redis.exceptions.ConnectionError` ou `TimeoutError`"
- [ ] "NAO esta atras de rate limit (intencional — health checks de monitoring)"
- "**Verificacao:** `pytest tests/test_security.py::test_health_redis_ok tests/test_security.py::test_health_redis_down -x`"
- "**Threat:** Information Disclosure — body retorna apenas status; nao expoe versao/host/metricas"

Subsecao "### POST /jobs rate limit 3/min (Phase 3 — pre-existente)":
- [ ] "`submit_job` decorado com `@limiter.limit(f\"{settings.rate_limit_per_minute}/minute\")`"
- "**Verificacao:** `pytest tests/test_api.py::test_rate_limit_returns_429 -x`"

**Secao "## 3. HTTP Hardening (controles ja implementados em api/main.py)":**

Subsecao "### SEC-TEST-01 — Body size limit 4KB":
- [ ] "Middleware `_limit_body_size` retorna 413 para `Content-Length > 4096`"
- "**Verificacao:** `pytest tests/test_security.py::test_body_size_limit -x`"
- "**Threat:** Memory exhaustion via body injection"

Subsecao "### SEC-TEST-02 — Security headers":
- [ ] "Middleware `_security_headers` injeta em TODAS as respostas:"
- "  - `X-Frame-Options: DENY`"
- "  - `X-Content-Type-Options: nosniff`"
- "  - `Referrer-Policy: no-referrer`"
- "  - `Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; frame-ancestors 'none';`"
- "**Verificacao:** `pytest tests/test_security.py::test_security_headers -x`"
- "**Nota:** `'unsafe-inline'` em style-src eh intencional (HTML Y2K usa inline styles); documentado em REQUIREMENTS.md > Out of Scope"

Subsecao "### SEC-TEST-03 — /docs /redoc /openapi.json desabilitados em producao":
- [ ] "FastAPI configurado com `docs_url=None`, `redoc_url=None`, `openapi_url=None` quando `DEBUG != \"true\"`"
- [ ] "`.env` de producao NAO tem `DEBUG=true`"
- "**Verificacao:** `pytest tests/test_security.py::test_docs_routes_disabled -x` (3 testes via parametrize)"

Subsecao "### SEC-TEST-04 — Queue depth limit (503)":
- [ ] "`submit_job` checa `_redis.llen(\"celery\") >= settings.max_queue_depth` (default 50)"
- [ ] "Retorna 503 `\"Service busy. Please try again later.\"` quando excede"
- "**Verificacao:** `pytest tests/test_security.py::test_queue_depth_limit -x`"
- "**Threat:** Queue exhaustion via job spam"

Subsecao "### SEC-TEST-05 — Rate limits GET /jobs e GET /files":
- [ ] "Coberto por SEC-API-01 e SEC-API-02 acima"
- "**Verificacao:** `pytest tests/test_security.py::test_rate_limit_get_jobs tests/test_security.py::test_rate_limit_get_files -x`"

**Secao "## 4. Pre-Deploy Audit":**

Subsecao "### SEC-TEST-06 — pip-audit antes de cada deploy":
- [ ] "Documentado em `README.md > Pre-Deploy Security Audit`"
- [ ] "Comando exato: `pip install pip-audit && pip-audit -r requirements.txt`"
- [ ] "Politica para vulnerabilidades:"
- "  - HIGH/CRITICAL sem patch -> NAO fazer deploy sem decisao explicita"
- "  - HIGH/CRITICAL com patch -> atualizar dependencia + bump em requirements.txt"
- "  - LOW/MEDIUM -> avaliar caso a caso"
- "**Verificacao:** rodar `pip-audit -r requirements.txt` e revisar output"
- "**Verificacao docs:** `grep -q \"pip install pip-audit\" README.md && grep -q \"pip-audit -r requirements.txt\" README.md`"

**Secao "## 5. Policy & Documentation":**

Subsecao "### SEC-POLICY-01 — Security Gate em CLAUDE.md":
- [ ] "Secao `## Security Gate` existe em `CLAUDE.md`"
- [ ] "Cobre: HTTP endpoints, /tmp files, shell scripts, testes, documentacao"
- [ ] "Referencia explicita a este checklist e a `tests/test_security.py`"
- "**Verificacao:** `grep -q \"^## Security Gate\\$\" CLAUDE.md`"

Subsecao "### SEC-POLICY-02 — Este arquivo":
- [ ] "`.planning/SECURITY-CHECKLIST.md` existe (este arquivo)"
- [ ] "Cobre todos os SEC-* da Phase 6"
- [ ] "Atualizado a cada nova feature que adicione controle"
- "**Verificacao:** `test -f .planning/SECURITY-CHECKLIST.md`"

**Secao "## 6. Threats NAO mitigados nesta fase (deferidos)":**

Paragrafo: "Estes itens estao escopados para Phase 7 (Infrastructure Security) ou v1.2:"

Lista bullet:
- "**SEC-INFRA-01** — Redis com senha obrigatoria em producao (Phase 7)"
- "**SEC-INFRA-02** — Uvicorn em 127.0.0.1 + nginx reverse proxy (Phase 7)"
- "**SEC-INFRA-03** — HTTPS via Let's Encrypt + redirect 301 HTTP -> HTTPS (Phase 7)"
- "**SEC-INFRA-04** — HSTS header `max-age=31536000` (Phase 7)"
- "**CSP sem `'unsafe-inline'`** — requer remover inline styles do HTML Y2K (v1.2)"
- "**Private /tmp directory por job** — `/tmp/sg_{id}/` com `os.mkdir(mode=0o700)` (v2)"
- "**Job cancellation endpoint** — DELETE /jobs/{id} com auth (v2)"

**Secao "## 7. Verificacao end-to-end (rodar antes de cada deploy)":**

Paragrafo introdutorio: "Bloco de comandos shell para validar a checklist completa antes de deploy. Cada bloco em fence bash."

Bloco bash com estes comandos (em fence ```bash ... ```):
- Comentario: `# 1. Suite completa de seguranca`
- `pytest tests/test_security.py -v`
- linha em branco
- Comentario: `# 2. Suite completa do projeto (sem regressao)`
- `pytest tests/ -v -m "not e2e and not integration"`
- linha em branco
- Comentario: `# 3. Audit de dependencias`
- `pip install pip-audit`
- `pip-audit -r requirements.txt`
- linha em branco
- Comentario: `# 4. Permissoes filesystem (apos primeira execucao do start.sh)`
- `ls -l start.sh                     # esperado: -rwxr-x---`
- `stat -c '%a' /tmp/sg_*.wav 2>/dev/null | sort -u  # esperado: 600`
- linha em branco
- Comentario: `# 5. Smoke test do health endpoint (com server up)`
- `curl -s http://localhost:8000/health`
- linha em branco
- Comentario: `# 6. Smoke test rate limit POST /jobs (com server up)`
- `for i in $(seq 1 4); do`
- `  curl -s -o /dev/null -w "%{http_code}\\n" -X POST http://localhost:8000/jobs \\`
- `    -H 'Content-Type: application/json' \\`
- `    -d '{"youtube_url":"https://www.youtube.com/watch?v=abc"}'`
- `done`
- `# esperado: 202, 202, 202, 429`

**Secao "## 8. Historico de mudancas":**

Tabela markdown com header `| Data | Phase | Controles adicionados |` e separator `|------|-------|----------------------|` e estas linhas:
- `| Phase 3 | Hardening | Body size, security headers, docs disabled, queue depth, rate limit POST /jobs |`
- `| Phase 6 | Application Security | WAV chmod 0o600, start.sh chmod 750, rate limit GET /jobs e /files, /health endpoint, pip-audit policy, este checklist |`
- `| Phase 7 (planned) | Infrastructure Security | Redis auth, nginx reverse proxy, HTTPS, HSTS |`

Use horizontal rule (linha com tres tracos) entre secoes principais para legibilidade. NAO criar arquivo em outro local. NAO alterar `.planning/REQUIREMENTS.md` ou outros arquivos. Apenas criar este unico arquivo novo.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/SoundGrabber2.0 && test -f .planning/SECURITY-CHECKLIST.md && wc -l .planning/SECURITY-CHECKLIST.md && for id in SEC-FILE-01 SEC-FILE-02 SEC-API-01 SEC-API-02 SEC-API-03 SEC-TEST-01 SEC-TEST-02 SEC-TEST-03 SEC-TEST-04 SEC-TEST-05 SEC-TEST-06 SEC-POLICY-01 SEC-POLICY-02; do grep -q "$id" .planning/SECURITY-CHECKLIST.md && echo "OK $id" || echo "MISSING $id"; done && grep -c "pip-audit" .planning/SECURITY-CHECKLIST.md && grep -c "tests/test_security.py" .planning/SECURITY-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/SECURITY-CHECKLIST.md` exits 0
    - File has at least 50 lines: `[ $(wc -l < .planning/SECURITY-CHECKLIST.md) -ge 50 ]` exits 0
    - All 13 SEC-* IDs appear: `for id in SEC-FILE-01 SEC-FILE-02 SEC-API-01 SEC-API-02 SEC-API-03 SEC-TEST-01 SEC-TEST-02 SEC-TEST-03 SEC-TEST-04 SEC-TEST-05 SEC-TEST-06 SEC-POLICY-01 SEC-POLICY-02; do grep -q "$id" .planning/SECURITY-CHECKLIST.md || exit 1; done` exits 0
    - References pip-audit at least 2 times: `[ $(grep -c "pip-audit" .planning/SECURITY-CHECKLIST.md) -ge 2 ]` exits 0
    - References test_security.py at least 5 times: `[ $(grep -c "tests/test_security.py" .planning/SECURITY-CHECKLIST.md) -ge 5 ]` exits 0
    - Has 8 main sections: `grep -cE "^## [0-9]\." .planning/SECURITY-CHECKLIST.md` returns 8
    - Has historico de mudancas: `grep -q "Historico de mudancas" .planning/SECURITY-CHECKLIST.md`
    - Has end-to-end script: `grep -q "Verificacao end-to-end" .planning/SECURITY-CHECKLIST.md`
    - Phase 7 deferrals listed: `grep -q "SEC-INFRA-01" .planning/SECURITY-CHECKLIST.md && grep -q "Phase 7" .planning/SECURITY-CHECKLIST.md`
    - Suite pytest continua verde apos as mudancas (esta task so cria docs): `pytest tests/ --tb=no -q -m "not e2e and not integration" 2>&1 | tail -5` mostra "passed" sem "failed"
  </acceptance_criteria>
  <done>
    `.planning/SECURITY-CHECKLIST.md` criado com 8 secoes principais cobrindo todos os 13 SEC-* requirements da Phase 6: Filesystem Permissions, API Rate Limiting, HTTP Hardening, Pre-Deploy Audit, Policy & Documentation, Threats deferidos, Verificacao end-to-end, Historico. Cada controle tem comando de verificacao automatizada e referencia ao threat correspondente. Pytest suite continua verde (este plan so adiciona docs).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositorio (docs) -> futuras decisoes | CLAUDE.md eh lido por Claude antes de cada nova fase; modificacoes maliciosas/erradas afetam plans futuros |
| README.md -> deploy operator | Operador humano segue instrucoes do README; documentacao errada -> deploy inseguro |
| .planning/SECURITY-CHECKLIST.md -> auditoria | Checklist eh fonte de verdade para revisoes pre-deploy; omissoes -> controles esquecidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-W2-01 | Repudiation | Decisoes de seguranca nao documentadas (silent rollback) | mitigate | CLAUDE.md > Security Gate exige decisao explicita em STATE.md para flexibilizar; SECURITY-CHECKLIST.md eh fonte de verdade rastreavel |
| T-6-W2-02 | Information Disclosure | pip-audit poderia expor versoes vulneraveis se nao for executado | mitigate | README.md documenta como mandatorio pre-deploy; SECURITY-CHECKLIST.md inclui no checklist |
| T-6-W2-03 | Tampering | CLAUDE.md poderia ser editado para remover Security Gate | accept | git diff revisao manual; sem mecanismo automatico — risco baixo dado projeto solo |
| T-6-W2-04 | DoS | pip-audit network call para PyPI durante deploy | accept | pip-audit eh sob demanda, nao runtime; falha nao impede app funcionar — apenas bloqueia deploy ate revisao |
| T-6-W2-05 | Spoofing | Operator humano pular checklist intencionalmente | accept | Sem enforcement automatico — gate eh cultural/processual; risco residual aceito para v1.1 |

ASVS L1 controls addressed:
- V1.1.6 (security architecture documented) — SECURITY-CHECKLIST.md
- V1.2.1 (security controls listed and reviewed) — Section 7 (verificacao end-to-end)
- V14.2.1 (third-party components inventoried + scanned for CVEs) — pip-audit policy
- V14.2.2 (vulnerability response plan) — SECURITY-CHECKLIST.md secao "Pre-Deploy Audit" tem politica de resposta
</threat_model>

<verification>
Apos completar as 3 tasks, executar:

```bash
cd /home/glow/Documentos/SoundGrabber2.0

# 1. README.md tem secao pip-audit
grep -A2 "## Pre-Deploy Security Audit" README.md
grep "pip install pip-audit" README.md
grep "pip-audit -r requirements.txt" README.md

# 2. CLAUDE.md tem Security Gate
grep -A1 "^## Security Gate$" CLAUDE.md

# 3. SECURITY-CHECKLIST.md existe e cobre todos os requisitos
test -f .planning/SECURITY-CHECKLIST.md && echo "EXISTS"
wc -l .planning/SECURITY-CHECKLIST.md
for id in SEC-FILE-01 SEC-FILE-02 SEC-API-01 SEC-API-02 SEC-API-03 \
          SEC-TEST-01 SEC-TEST-02 SEC-TEST-03 SEC-TEST-04 SEC-TEST-05 SEC-TEST-06 \
          SEC-POLICY-01 SEC-POLICY-02; do
  grep -q "$id" .planning/SECURITY-CHECKLIST.md && echo "OK $id" || echo "MISSING $id"
done

# 4. Suite continua verde (este plan so adiciona docs, mas confirmar)
pytest tests/ -v --tb=short -m "not e2e and not integration"

# 5. pip-audit funciona (smoke test do comando documentado — opcional)
pip install pip-audit
pip-audit -r requirements.txt
```

Esperado:
- README.md tem nova secao "## Pre-Deploy Security Audit" entre Architecture e License
- CLAUDE.md tem nova secao "## Security Gate" entre Restricoes criticas e Proximo passo
- .planning/SECURITY-CHECKLIST.md existe com 8 secoes principais cobrindo todos os 13 SEC-*
- pytest tests/ verde (nenhuma regressao porque este plan so adiciona docs)
- pip-audit roda e reporta vulnerabilidades (zero ou nao-zero — relevante para deploy, nao para o plan)
</verification>

<success_criteria>
- [ ] README.md tem secao "## Pre-Deploy Security Audit" com comando exato `pip install pip-audit && pip-audit -r requirements.txt`
- [ ] CLAUDE.md tem secao "## Security Gate" com 5 categorias de controles + clausula de excecao
- [ ] .planning/SECURITY-CHECKLIST.md existe com 8 secoes cobrindo SEC-FILE-01/02, SEC-API-01/02/03, SEC-TEST-01..06, SEC-POLICY-01/02
- [ ] Cada controle no checklist tem comando de verificacao automatizada
- [ ] Cross-references corretas: CLAUDE.md -> SECURITY-CHECKLIST.md, SECURITY-CHECKLIST.md -> README.md (pip-audit) e tests/test_security.py
- [ ] Threats deferidos para Phase 7 listados (SEC-INFRA-01..04)
- [ ] Historico de mudancas inclui Phase 3, Phase 6 e plano para Phase 7
- [ ] Pytest tests/ continua verde (zero regressoes — este plan so adiciona docs)
- [ ] Nenhum codigo de aplicacao (api/, pipeline.py, start.sh) modificado
</success_criteria>

<output>
After completion, create `.planning/phases/06-application-security/06-03-SUMMARY.md` documenting:
- Linhas exatas adicionadas em README.md (numero de linha de inicio + tamanho da secao)
- Linhas exatas adicionadas em CLAUDE.md (numero de linha de inicio + subsecoes criadas)
- Tamanho final de .planning/SECURITY-CHECKLIST.md (linhas + bytes)
- Confirmacao de que TODOS os 13 SEC-* da Phase 6 estao referenciados no checklist
- Output de `pip-audit -r requirements.txt` no momento da conclusao (zero ou N vulnerabilidades — informativo, nao gate)
- Confirmacao de que pytest tests/ continua verde
</output>
