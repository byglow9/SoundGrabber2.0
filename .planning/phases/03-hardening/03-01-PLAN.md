---
phase: 03-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - tests/test_api.py
autonomous: true
requirements:
  - UX-03
  - UX-04

must_haves:
  truths:
    - "pytest descobre os quatro novos stubs e os executa (mesmo que falhem por falta de implementacao)"
    - "slowapi==0.1.9 esta listado em requirements.txt"
    - "test_validation_error_format verifica body {error, error_type} na resposta 422"
    - "test_rate_limit_returns_429 verifica que a 4a requisicao retorna 429"
    - "test_rate_limit_retry_after_header verifica header Retry-After no 429"
    - "test_sweeper_deletes_partial_files verifica delecao de .part e .ytdl expirados"
  artifacts:
    - path: "requirements.txt"
      provides: "slowapi==0.1.9 pinado"
      contains: "slowapi==0.1.9"
    - path: "tests/test_api.py"
      provides: "quatro stubs de Phase 3"
      exports:
        - test_validation_error_format
        - test_rate_limit_returns_429
        - test_rate_limit_retry_after_header
        - test_sweeper_deletes_partial_files
  key_links:
    - from: "tests/test_api.py"
      to: "api.main.sweep_expired_wavs"
      via: "import direto em test_sweeper_deletes_partial_files"
      pattern: "from api\\.main import sweep_expired_wavs"
    - from: "tests/test_api.py"
      to: "POST /jobs"
      via: "api_client.post('/jobs', ...)"
      pattern: "api_client\\.post.*jobs"
---

<objective>
Instalar slowapi e criar os quatro stubs de teste exigidos pelo protocolo Nyquist antes de qualquer
implementacao. Os stubs falham (RED) agora e passam (GREEN) nos planos seguintes.

Purpose: Garantir que cada comportamento de Phase 3 tem um teste automatizado aguardando antes de o
codigo ser escrito. Planos 02 e 03 rodam pytest -x e sabem imediatamente se sua implementacao esta
correta.

Output: requirements.txt atualizado + quatro novos testes ao final de tests/test_api.py.
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/glow/Documentos/SoundGrabber2.0/.planning/PROJECT.md
@/home/glow/Documentos/SoundGrabber2.0/.planning/ROADMAP.md
@/home/glow/Documentos/SoundGrabber2.0/.planning/STATE.md

<interfaces>
<!-- Padroes existentes que os stubs devem seguir. Extraidos de tests/test_api.py. -->

Fixture api_client (tests/conftest.py):
- FastAPI TestClient com Celery em eager mode
- Redis apontado para redis://localhost:6380/0
- patch("api.tasks.check_duration") ativo durante yield

Fixture tmp_path: diretorio temporario isolado do pytest (nao usar /tmp diretamente)

Padroes de assert existentes:
  assert response.status_code == 422, f"URL {url!r} should be rejected; ..."
  assert response.status_code == 202, f"expected 202 ..."
  assert not old.exists(), "sweeper must delete WAVs older than ttl"
  assert new.exists(), "sweeper must NOT delete fresh WAVs"

Imports ja presentes em tests/test_api.py:
  import time
  from pathlib import Path
  from unittest.mock import patch, MagicMock
  import pytest
  import os  (usado dentro de funcoes, nao no topo)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar slowapi==0.1.9 em requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (ver ordem atual das dependencias para inserir slowapi no lugar correto)
  </read_first>
  <action>
Adicionar a linha `slowapi==0.1.9` em requirements.txt, logo apos `fastapi==0.136.1`.

Estado atual do arquivo (linhas 1-12):
```
yt-dlp==2026.3.17
librosa==0.11.0
soundfile==0.13.1
numpy>=2.0,<3.0
scipy>=1.10
pytest==9.0.3
pytest-subprocess>=1.5
fastapi==0.136.1
uvicorn==0.46.0
celery[redis]==5.6.3
redis==6.4.0
httpx>=0.27
```

Estado alvo (linha 9 inserida):
```
yt-dlp==2026.3.17
librosa==0.11.0
soundfile==0.13.1
numpy>=2.0,<3.0
scipy>=1.10
pytest==9.0.3
pytest-subprocess>=1.5
fastapi==0.136.1
slowapi==0.1.9
uvicorn==0.46.0
celery[redis]==5.6.3
redis==6.4.0
httpx>=0.27
```

Convencao do projeto: dependencias de aplicacao pinadas com `==`; slowapi e dependencia de aplicacao,
nao transitiva. Nao usar `>=`.

Apos editar o arquivo, instalar a dependencia:
```
pip install slowapi==0.1.9
```
  </action>
  <verify>
    <automated>grep "slowapi==0.1.9" /home/glow/Documentos/SoundGrabber2.0/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contem a linha exata `slowapi==0.1.9`
    - A linha esta posicionada apos `fastapi==0.136.1` e antes de `uvicorn==0.46.0`
    - `python -c "import slowapi"` nao retorna ImportError
  </acceptance_criteria>
  <done>requirements.txt contem `slowapi==0.1.9` e `import slowapi` funciona no ambiente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar quatro stubs de Phase 3 em tests/test_api.py</name>
  <files>tests/test_api.py</files>
  <read_first>
    - tests/test_api.py (ler completo — os stubs vao ao final, apos test_concurrent_jobs na linha 203)
    - tests/conftest.py (ver fixtures api_client e tmp_path disponiveis)
  </read_first>
  <action>
Adicionar os quatro novos testes ao final de tests/test_api.py, apos a funcao `test_concurrent_jobs`
(linha 203). Nao modificar nenhum teste existente.

Bloco a adicionar (copiar exatamente):

```python
# -----------------------------------------------------------------------
# UX-03: 422 body normalizado — Phase 3, Plan 02 torna verde
# -----------------------------------------------------------------------

def test_validation_error_format(api_client):
    """422 body segue formato {error, error_type: 'validation_error'} sem prefixo Pydantic v2 (D-07)."""
    response = api_client.post("/jobs", json={"youtube_url": "https://vimeo.com/12345"})
    assert response.status_code == 422
    body = response.json()
    assert "error" in body, f"body deve ter chave 'error': {body}"
    assert body.get("error_type") == "validation_error", f"error_type errado: {body}"
    assert "detail" not in body, f"chave 'detail' nao deve existir no body: {body}"
    assert not body["error"].startswith("Value error,"), (
        f"prefixo Pydantic v2 nao deve vazar no body: {body['error']!r}"
    )


# -----------------------------------------------------------------------
# UX-04: Rate limiting 429 — Phase 3, Plan 03 torna verde
# -----------------------------------------------------------------------

def test_rate_limit_returns_429(api_client):
    """4a requisicao em 60s pelo mesmo IP retorna 429 com error_type rate_limit_error (D-01/D-03)."""
    url = "https://www.youtube.com/watch?v=abc123"
    for i in range(3):
        r = api_client.post("/jobs", json={"youtube_url": url})
        assert r.status_code == 202, f"requisicao {i+1}/3 esperada 202: {r.status_code} {r.text}"
    r = api_client.post("/jobs", json={"youtube_url": url})
    assert r.status_code == 429, f"4a requisicao deve retornar 429: {r.status_code} {r.text}"
    body = r.json()
    assert body.get("error_type") == "rate_limit_error", f"error_type errado: {body}"
    assert "error" in body, f"body deve ter chave 'error': {body}"


def test_rate_limit_retry_after_header(api_client):
    """429 inclui header Retry-After com valor inteiro em segundos (D-04 / RFC 9110)."""
    url = "https://www.youtube.com/watch?v=abc123"
    for _ in range(3):
        api_client.post("/jobs", json={"youtube_url": url})
    r = api_client.post("/jobs", json={"youtube_url": url})
    assert r.status_code == 429
    assert "retry-after" in r.headers, (
        f"header Retry-After ausente no 429: {dict(r.headers)}"
    )
    retry_val = r.headers["retry-after"]
    assert retry_val.isdigit(), (
        f"Retry-After deve ser inteiro em segundos (ex: '60'): {retry_val!r}"
    )


# -----------------------------------------------------------------------
# SC-4 / D-05/D-06: Sweeper limpa .part e .ytdl — Phase 3, Plan 02 torna verde
# -----------------------------------------------------------------------

def test_sweeper_deletes_partial_files(tmp_path):
    """sweep_expired_wavs deleta sg_*.part e sg_*.ytdl mais velhos que ttl; nao deleta frescos (D-05/D-06)."""
    import os
    from api.main import sweep_expired_wavs

    old_part = tmp_path / "sg_abc.part"
    old_ytdl = tmp_path / "sg_abc.ytdl"
    new_part = tmp_path / "sg_xyz.part"

    old_part.write_bytes(b"partial")
    old_ytdl.write_bytes(b"state")
    new_part.write_bytes(b"inprogress")

    past = time.time() - 1200  # 20 min atras > ttl de 900s
    os.utime(old_part, (past, past))
    os.utime(old_ytdl, (past, past))
    # new_part: mtime atual (fresco — nao deve ser deletado)

    sweep_expired_wavs(tmp_path, ttl_seconds=900)

    assert not old_part.exists(), "sweeper deve deletar sg_*.part expirado"
    assert not old_ytdl.exists(), "sweeper deve deletar sg_*.ytdl expirado"
    assert new_part.exists(), "sweeper NAO deve deletar sg_*.part fresco"
```

Convencoes do projeto:
- `import os` dentro da funcao (nao no topo do arquivo) — mesmo padrao de `test_sweeper_deletes_expired_wavs` linha 162
- Assertivas com mensagem descritiva via f-string
- Sem `@pytest.mark` em testes unitarios (apenas `@pytest.mark.e2e` para testes de rede)
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/SoundGrabber2.0 && python -m pytest tests/test_api.py::test_validation_error_format tests/test_api.py::test_sweeper_deletes_partial_files --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_api.py contem as funcoes: test_validation_error_format, test_rate_limit_returns_429, test_rate_limit_retry_after_header, test_sweeper_deletes_partial_files
    - `grep -c "def test_" tests/test_api.py` retorna 14 (10 existentes + 4 novos)
    - `pytest tests/test_api.py --collect-only -q` lista os quatro novos testes sem erro de sintaxe
    - Os testes existentes nao foram modificados (os primeiros 203 linhas permanecem identicas)
  </acceptance_criteria>
  <done>
    Quatro novos testes presentes em tests/test_api.py. pytest --collect-only os lista sem erro.
    Os testes de rate limit falham com AssertionError (nao com ImportError ou SyntaxError) pois a
    implementacao ainda nao existe — esse e o estado RED correto do ciclo TDD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner -> api.main | Testes importam diretamente api.main; sem boundary de seguranca (ambiente de teste) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-hardening-setup-01 | Tampering | requirements.txt | accept | Arquivo de dependencias nao e trust boundary; mudanca e auditavel via git diff |
| T-hardening-setup-02 | Information Disclosure | tests/test_api.py | accept | Testes nao expõem dados de producao; ambiente de teste isolado |
</threat_model>

<verification>
```bash
cd /home/glow/Documentos/SoundGrabber2.0

# 1. slowapi instalado
python -c "import slowapi; print(slowapi.__version__)"

# 2. requirements.txt atualizado
grep "slowapi==0.1.9" requirements.txt

# 3. Quatro novos testes coletados
pytest tests/test_api.py --collect-only -q | grep -E "test_validation_error_format|test_rate_limit_returns_429|test_rate_limit_retry_after_header|test_sweeper_deletes_partial_files"

# 4. Suite existente nao quebrou
pytest tests/test_api.py -x -q --ignore-glob="*e2e*" -m "not e2e"
```
</verification>

<success_criteria>
- `grep "slowapi==0.1.9" requirements.txt` retorna match
- `python -c "import slowapi"` sai com codigo 0
- `pytest tests/test_api.py --collect-only -q` lista os 4 novos stubs sem erros de sintaxe
- Testes existentes (test_post_jobs_returns_job_id etc.) continuam passando
- `grep -c "def test_" tests/test_api.py` retorna 14 (10 existentes + 4 novos)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-hardening/03-01-SUMMARY.md` com:
- O que foi feito (slowapi instalado, 4 stubs adicionados)
- Estado RED confirmado nos testes de Phase 3
- Quaisquer desvios do plano
</output>
