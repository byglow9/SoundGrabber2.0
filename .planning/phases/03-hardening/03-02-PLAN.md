---
phase: 03-hardening
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - api/config.py
  - api/main.py
autonomous: true
requirements:
  - UX-03
  - UX-04

must_haves:
  truths:
    - "POST /jobs com URL invalida retorna 422 com body {error: '...', error_type: 'validation_error'} sem chave 'detail' e sem prefixo 'Value error,'"
    - "Killing do worker deixa arquivos .part e .ytdl; o sweeper os deleta dentro de 20 minutos"
    - "Settings tem o campo rate_limit_per_minute configuravel via env var RATE_LIMIT_PER_MINUTE"
  artifacts:
    - path: "api/config.py"
      provides: "campo rate_limit_per_minute: int = int(os.environ.get('RATE_LIMIT_PER_MINUTE', '3'))"
      contains: "rate_limit_per_minute"
    - path: "api/main.py"
      provides: "handler _validation_exception_handler + sweeper estendido para .part/.ytdl"
      exports:
        - _validation_exception_handler
        - sweep_expired_wavs (agora com sg_*.part e sg_*.ytdl)
  key_links:
    - from: "api/main.py._validation_exception_handler"
      to: "fastapi.exceptions.RequestValidationError"
      via: "@app.exception_handler(RequestValidationError)"
      pattern: "exception_handler.*RequestValidationError"
    - from: "api/main.py.sweep_expired_wavs"
      to: "patterns tuple"
      via: "for pattern in ('sg_*.wav', 'sg_*.part', 'sg_*.ytdl')"
      pattern: "sg_\\*\\.part.*sg_\\*\\.ytdl"
---

<objective>
Implementar dois dos tres hardening items de Phase 3: (1) normalizar o formato dos erros sincronos
422 via exception handler customizado (D-07), e (2) estender o sweeper para deletar arquivos
parciais de yt-dlp (.part, .ytdl) deixados por workers SIGKILL'd (D-05/D-06). Tambem adicionar
o campo de configuracao rate_limit_per_minute em api/config.py (necessario para Plan 03).

Purpose: UX-03 exige que erros de URL invalida retornem mensagem humana sem internals do Pydantic.
O success criterion 4 do ROADMAP exige que .part e .ytdl sejam limpos dentro de 20 minutos.

Output: api/config.py com rate_limit_per_minute, api/main.py com handler 422 e sweeper estendido.
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/glow/Documentos/SoundGrabber2.0/.planning/PROJECT.md
@/home/glow/Documentos/SoundGrabber2.0/.planning/ROADMAP.md
@/home/glow/Documentos/SoundGrabber2.0/.planning/STATE.md
@/home/glow/Documentos/SoundGrabber2.0/.planning/phases/03-hardening/03-01-SUMMARY.md

<interfaces>
<!-- Extraido de api/config.py (completo) -->

```python
@dataclass(frozen=True)
class Settings:
    redis_url: str = os.environ.get("REDIS_URL", "redis://localhost:6379/0")
    cookies_path: str = os.environ.get("YTDLP_COOKIES_FILE", "")
    po_token: str = os.environ.get("YTDLP_PO_TOKEN", "")
    wav_ttl: int = int(os.environ.get("WAV_TTL_SECONDS", "900"))

settings = Settings()
```

<!-- Extraido de api/main.py (secoes relevantes) -->

Imports atuais de fastapi:
```python
from fastapi import FastAPI, HTTPException
from fastapi.responses import FileResponse
```

Funcao sweep_expired_wavs atual (linhas 47-62):
```python
def sweep_expired_wavs(directory: Path, ttl_seconds: int) -> int:
    """Delete sg_*.wav files in `directory` older than `ttl_seconds`. Returns count deleted."""
    deleted = 0
    now = time.time()
    for wav in Path(directory).glob("sg_*.wav"):
        try:
            if now - wav.stat().st_mtime > ttl_seconds:
                wav.unlink(missing_ok=True)
                deleted += 1
        except OSError:
            continue
    return deleted
```

Linha de criacao do app (linha 89):
```python
app = FastAPI(title="SoundGrabber API", version="0.2.0", lifespan=lifespan)
```

Assinatura atual de submit_job (linhas 92-94):
```python
@app.post("/jobs", status_code=202)
def submit_job(request: JobRequest) -> dict:
    task = process_job.delay(request.youtube_url)
```

Formato de erro canonico do projeto (de api/tasks.py JobFailure):
```python
{"error": "<mensagem humanizada>", "error_type": "<snake_case>"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar rate_limit_per_minute em api/config.py</name>
  <files>api/config.py</files>
  <read_first>
    - api/config.py (ler completo — 17 linhas — para ver o padrao exato antes de editar)
  </read_first>
  <action>
Adicionar o campo `rate_limit_per_minute` na classe Settings de api/config.py, logo apos `wav_ttl`
(linha 13 atual), seguindo o mesmo padrao de cast inteiro.

Estado alvo da classe Settings:
```python
@dataclass(frozen=True)
class Settings:
    redis_url: str = os.environ.get("REDIS_URL", "redis://localhost:6379/0")
    cookies_path: str = os.environ.get("YTDLP_COOKIES_FILE", "")
    po_token: str = os.environ.get("YTDLP_PO_TOKEN", "")
    wav_ttl: int = int(os.environ.get("WAV_TTL_SECONDS", "900"))
    rate_limit_per_minute: int = int(os.environ.get("RATE_LIMIT_PER_MINUTE", "3"))
```

Regras obrigatorias:
- Cast `int(os.environ.get("RATE_LIMIT_PER_MINUTE", "3"))` — default como string, cast fora
- Tipo explicitamente anotado como `int`
- Nao usar `int(os.environ.get(...) or "3")` — mascara variavel vazia
- `dataclass(frozen=True)` permanece inalterado
- `settings = Settings()` na linha final permanece inalterado
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/SoundGrabber2.0 && python -c "from api.config import settings; assert hasattr(settings, 'rate_limit_per_minute'); assert settings.rate_limit_per_minute == 3; print('OK:', settings.rate_limit_per_minute)"</automated>
  </verify>
  <acceptance_criteria>
    - api/config.py contem a linha `rate_limit_per_minute: int = int(os.environ.get("RATE_LIMIT_PER_MINUTE", "3"))`
    - `python -c "from api.config import settings; print(settings.rate_limit_per_minute)"` imprime `3`
    - `RATE_LIMIT_PER_MINUTE=5 python -c "from api.config import Settings; s = Settings(); print(s.rate_limit_per_minute)"` imprime `5`
    - Nenhum outro campo da classe foi alterado
  </acceptance_criteria>
  <done>settings.rate_limit_per_minute == 3 por padrao e configuravel via env var RATE_LIMIT_PER_MINUTE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler 422 normalizado + sweeper estendido em api/main.py</name>
  <files>api/main.py</files>
  <read_first>
    - api/main.py (ler completo — 186 linhas — antes de qualquer edicao)
    - tests/test_api.py (linhas 44-55: test_invalid_url_rejected; linhas finais: test_validation_error_format e test_sweeper_deletes_partial_files — sao os alvos RED deste task)
  </read_first>
  <behavior>
    - test_validation_error_format: POST /jobs com URL vimeo.com retorna 422, body tem {error, error_type: "validation_error"}, sem chave "detail", sem prefixo "Value error,"
    - test_sweeper_deletes_partial_files: sweep_expired_wavs deleta sg_abc.part e sg_abc.ytdl com mtime 20min atras; nao deleta sg_xyz.part fresco
    - test_invalid_url_rejected: continua passando (sem regressao nos 6 parametros do parametrize)
  </behavior>
  <action>
Realizar TRES mudancas independentes em api/main.py. Ler o arquivo completo antes de qualquer edit.

--- MUDANCA 1: Novos imports ---

Expandir os imports de fastapi e adicionar imports de slowapi no bloco correto.
Substituir as linhas de import de fastapi (atualmente linhas 12-16) para:

```python
from fastapi import FastAPI, HTTPException, Request
from fastapi.exceptions import RequestValidationError
from fastapi.responses import FileResponse, JSONResponse
```

Adicionar logo apos o bloco `from fastapi*` (antes de `from api.config import settings`):
```python
from slowapi.errors import RateLimitExceeded
```

Nota: `Limiter` e `get_ipaddr` ainda NAO devem ser importados neste plano — eles serao adicionados
no Plan 03 junto com o decorator de rate limit. Importar apenas o que e usado neste plano.

--- MUDANCA 2: Handler 422 normalizado (D-07) ---

Inserir logo apos a linha `app = FastAPI(title="SoundGrabber API", ...)` (linha 89 atual),
ANTES do primeiro `@app.post`:

```python
@app.exception_handler(RequestValidationError)
async def _validation_exception_handler(
    request: Request, exc: RequestValidationError
) -> JSONResponse:
    """Converte 422 Pydantic para formato {error, error_type} unificado (D-07).

    Pydantic v2 prefixa mensagens de field_validator com "Value error, " automaticamente.
    removeprefix() remove esse prefixo sem alterar o resto da mensagem.
    """
    errors = exc.errors()
    msg = errors[0].get("msg", "Invalid request") if errors else "Invalid request"
    msg = msg.removeprefix("Value error, ")
    return JSONResponse(
        status_code=422,
        content={"error": msg, "error_type": "validation_error"},
    )
```

--- MUDANCA 3: Sweeper estendido para .part e .ytdl (D-05/D-06) ---

Substituir a funcao `sweep_expired_wavs` existente (linhas 47-62) pela versao estendida:

```python
def sweep_expired_wavs(directory: Path, ttl_seconds: int) -> int:
    """Delete sg_*.wav, sg_*.part, sg_*.ytdl in `directory` older than `ttl_seconds`.

    Returns count deleted. Pure function — testable without threads.
    D-01: TTL matches the 15-minute WAV lifecycle decision.
    D-05/D-06 (Phase 3): also cleans yt-dlp partial files left by SIGKILL'd workers.
    Extensoes corretas confirmadas em yt_dlp/downloader/common.py:
      .part = arquivo de download parcial (temp_name)
      .ytdl = arquivo de estado/progresso (ytdl_filename)
    """
    deleted = 0
    now = time.time()
    for pattern in ("sg_*.wav", "sg_*.part", "sg_*.ytdl"):
        for f in Path(directory).glob(pattern):
            try:
                if now - f.stat().st_mtime > ttl_seconds:
                    f.unlink(missing_ok=True)
                    deleted += 1
            except OSError:
                continue
    return deleted
```

Notas criticas:
- Variavel interna renomeada de `wav` para `f` (agora lida com multiplos tipos de arquivo)
- Loop externo `for pattern in (...)` itera sobre tupla de 3 strings (nao lista)
- Logica de comparacao `now - f.stat().st_mtime > ttl_seconds` permanece identica
- `_run_sweeper_loop` (linha 65) e `lifespan` (linha 77) NAO sao modificados
- Assinatura da funcao permanece identica: `(directory: Path, ttl_seconds: int) -> int`
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/SoundGrabber2.0 && python -m pytest tests/test_api.py::test_validation_error_format tests/test_api.py::test_sweeper_deletes_partial_files tests/test_api.py::test_invalid_url_rejected -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep "RequestValidationError" api/main.py` retorna match (import + handler)
    - `grep "_validation_exception_handler" api/main.py` retorna match
    - `grep "removeprefix" api/main.py` retorna match
    - `grep "sg_\*.part" api/main.py` retorna match dentro de sweep_expired_wavs
    - `grep "sg_\*.ytdl" api/main.py` retorna match dentro de sweep_expired_wavs
    - `pytest tests/test_api.py::test_validation_error_format -x -q` sai com codigo 0
    - `pytest tests/test_api.py::test_sweeper_deletes_partial_files -x -q` sai com codigo 0
    - `pytest tests/test_api.py::test_invalid_url_rejected -x -q` sai com codigo 0 (sem regressao)
    - `grep "\"detail\"" api/main.py` NAO encontra nenhum body de resposta com chave "detail" nos novos handlers
  </acceptance_criteria>
  <done>
    test_validation_error_format e test_sweeper_deletes_partial_files passam (GREEN).
    test_invalid_url_rejected continua passando.
    test_rate_limit_returns_429 e test_rate_limit_retry_after_header ainda falham (aguardam Plan 03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP -> POST /jobs | Input nao confiavel; validado pelo Pydantic field_validator antes do handler |
| /tmp filesystem -> sweeper | Sweeper usa glob com prefixo sg_ para nao deletar arquivos de outros processos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-hardening-02-01 | Information Disclosure | _validation_exception_handler | mitigate | removeprefix("Value error, ") remove o prefixo Pydantic v2; exc.errors()[0]["msg"] contem apenas a mensagem do field_validator, nao stack trace |
| T-hardening-02-02 | Denial of Service (disco) | sweep_expired_wavs | mitigate | Glob sg_*.part e sg_*.ytdl com prefixo sg_ garantem que apenas arquivos do SoundGrabber sao deletados; arquivos frescos (mtime < ttl) sao preservados |
| T-hardening-02-03 | Tampering | sweep_expired_wavs / /tmp | accept | Prefixo sg_ e suficiente para v1; path traversal em /tmp ja e protegido pelo validation em GET /files/{id} (Phase 2) |
</threat_model>

<verification>
```bash
cd /home/glow/Documentos/SoundGrabber2.0

# 1. Config com novo campo
python -c "from api.config import settings; print('rate_limit_per_minute:', settings.rate_limit_per_minute)"

# 2. Handler 422 normalizado
pytest tests/test_api.py::test_validation_error_format -x -q

# 3. Sweeper estendido
pytest tests/test_api.py::test_sweeper_deletes_partial_files -x -q

# 4. Sem regressao nos testes existentes
pytest tests/test_api.py -x -q -m "not e2e"
```
</verification>

<success_criteria>
- `python -c "from api.config import settings; assert settings.rate_limit_per_minute == 3"` sai 0
- `pytest tests/test_api.py::test_validation_error_format -x -q` PASSED
- `pytest tests/test_api.py::test_sweeper_deletes_partial_files -x -q` PASSED
- `pytest tests/test_api.py -x -q -m "not e2e"` sem novas falhas (test_rate_limit_* podem falhar — aguardam Plan 03)
- `grep "sg_\*.part" api/main.py` e `grep "sg_\*.ytdl" api/main.py` retornam match
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-hardening/03-02-SUMMARY.md` com:
- Mudancas realizadas em api/config.py e api/main.py
- Resultados dos testes (quais passaram, quais ainda estao RED)
- Quaisquer desvios do plano
</output>
