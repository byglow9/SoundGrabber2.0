---
phase: 4
plan: 2
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - static/index.html
autonomous: true
requirements: [CORE-01, UX-01, UX-02]

must_haves:
  truths:
    - "static/index.html exists with all required IDs from UI-SPEC"
    - "HTML has zero CSS — no <style> tags, no inline style= attributes, no CSS files referenced"
    - "All 16 required element IDs are present exactly as specified in UI-SPEC"
    - "static/ directory is created, unblocking Plan 04's StaticFiles mount"
  artifacts:
    - path: "static/index.html"
      provides: "HTML structure for the full frontend UI"
      contains: "id=\"url-input\""
  key_links:
    - from: "static/index.html"
      to: "static/app.js"
      via: "<script src=\"/static/app.js\"></script> at end of body"
      pattern: "src=\"/static/app.js\""
---

<objective>
Create `static/index.html` — the complete HTML structure for Phase 4 with all IDs and classes from the approved UI-SPEC, zero CSS.

Purpose: Provides the DOM structure that `app.js` manipulates. All IDs must match exactly so Plan 03's JavaScript can find elements by ID. Creating this file also creates the `static/` directory, which Plan 04 requires before mounting StaticFiles.

Output: `static/index.html` — valid HTML5 with all 16 required IDs, zero CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend/04-UI-SPEC.md
@.planning/phases/04-frontend/04-CONTEXT.md

<interfaces>
<!-- All IDs and classes required — from 04-UI-SPEC.md "ID/Class Reference Table" -->
<!-- These are the exact strings tests/test_frontend.py checks for -->

Required IDs (all must appear as id="..." in the HTML):
  url-input, submit-btn, progress-area, progress-label,
  result-card, bpm-value, bpm-half-value, bpm-double-value,
  key-value, camelot-value, size-value, download-link,
  error-area, error-message, retry-btn, validation-error

Required classes (for Phase 5 to style):
  sg-app, sg-header, sg-title, sg-tagline, sg-form-area, sg-hint,
  sg-input-group, sg-url-input, sg-submit-btn, sg-error sg-error--inline,
  sg-progress-area, sg-progress-label, sg-result-card, sg-result-section,
  sg-result-value sg-result-value--primary, sg-result-value,
  sg-download-area, sg-download-btn, sg-error-area, sg-error-message, sg-retry-btn

Static copy strings (from 04-UI-SPEC.md Copywriting Contract):
  - Site title: "SoundGrabber"
  - Site tagline: "Cole um link do YouTube. Baixe o beat em WAV."
  - URL input placeholder: "https://www.youtube.com/watch?v=..."
  - Submit button: "Baixar Beat"
  - Duration hint: "Vídeos com mais de 15 minutos não são aceitos."
  - Download button: "Baixar WAV"

Hidden elements at load (controlled by JS via element.hidden):
  - id="validation-error" → hidden attribute present
  - id="progress-area" → hidden attribute present
  - id="result-card" → hidden attribute present
  - id="error-area" → hidden attribute present
  - id="retry-btn" → hidden attribute present (inside error-area)

Script tag (at end of body, before </body>):
  <script src="/static/app.js"></script>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static/index.html per UI-SPEC HTML Structure</name>
  <files>static/index.html</files>
  <read_first>
    - .planning/phases/04-frontend/04-UI-SPEC.md (sections: "HTML Structure", "ID/Class Reference Table", "Copywriting Contract — Static Copy")
    - .planning/phases/04-frontend/04-CONTEXT.md (D-10: zero CSS; D-11: div-based structure)
  </read_first>
  <action>
Create the `static/` directory and write `static/index.html`.

The file must be a complete, valid HTML5 document. Follow the HTML structure from 04-UI-SPEC.md section "HTML Structure" EXACTLY — do not deviate from element IDs, class names, or nesting structure.

CRITICAL CONSTRAINTS (D-10):
- NO `<style>` tags anywhere
- NO `style="..."` attributes on any element
- NO `<link rel="stylesheet">` references
- NO CSS file of any kind
- Zero CSS. The page will look completely unstyled — that is correct and expected.

DOCTYPE and head:
```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SoundGrabber</title>
</head>
<body>
```

Body structure: Follow the HTML structure from 04-UI-SPEC.md exactly. Key elements:

1. Root: `<div id="app" class="sg-app">`
2. Header: `<div id="header" class="sg-header">` containing site-title and site-tagline divs
3. Form area: `<div id="form-area" class="sg-form-area">` containing:
   - `<div id="duration-hint" class="sg-hint">` — always visible
   - `<div id="input-group" class="sg-input-group">` containing the `<input>` and `<button>`
   - `<div id="validation-error" class="sg-error sg-error--inline" hidden>` (hidden at load)
4. Progress: `<div id="progress-area" class="sg-progress-area" hidden>` with inner progress-label div
5. Result card: `<div id="result-card" class="sg-result-card" hidden>` containing:
   - result-bpm section with bpm-value, bpm-half-value, bpm-double-value
   - result-key section with key-value, camelot-value
   - result-size section with size-value
   - download-area with `<a id="download-link" class="sg-download-btn" download>Baixar WAV</a>`
6. Error area: `<div id="error-area" class="sg-error-area" hidden>` with:
   - error-message div
   - `<button type="button" id="retry-btn" class="sg-retry-btn" hidden>Tentar novamente</button>`

Input element attributes (exact):
```html
<input
  type="text"
  id="url-input"
  class="sg-url-input"
  placeholder="https://www.youtube.com/watch?v=..."
  autocomplete="off"
  spellcheck="false"
/>
```

Submit button:
```html
<button type="button" id="submit-btn" class="sg-submit-btn">Baixar Beat</button>
```

Labels in result-bpm section use the copywriting from UI-SPEC:
- BPM section label: "BPM"
- Half-time label: "Metade (÷2)"
- Double-time label: "Dobro (×2)"
- Key section label: "Tonalidade"
- Camelot label: "Camelot"
- File size label: "Tamanho estimado"

The value divs (bpm-value, bpm-half-value, etc.) are EMPTY in HTML — JS populates them via textContent.

Close body with:
```html
  <script src="/static/app.js"></script>
</body>
</html>
```

XSS note: Value divs are intentionally empty in HTML. JS will set textContent (never innerHTML) per RESEARCH.md security domain.
  </action>
  <verify>
    <automated>grep -c 'id=' /home/glow/Documentos/SoundGrabber2.0/static/index.html && grep -L 'style' /home/glow/Documentos/SoundGrabber2.0/static/index.html && python3 -c "
ids = ['url-input','submit-btn','progress-area','progress-label','result-card','bpm-value','bpm-half-value','bpm-double-value','key-value','camelot-value','size-value','download-link','error-area','error-message','retry-btn','validation-error']
html = open('static/index.html').read()
missing = [i for i in ids if f'id=\"{i}\"' not in html]
assert not missing, f'Missing IDs: {missing}'
assert '<style' not in html, 'CSS found in HTML'
assert 'style=' not in html, 'Inline style found'
print('ALL CHECKS PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - `static/index.html` exists (creates `static/` directory)
    - `grep 'id="url-input"' static/index.html` exits 0
    - `grep 'id="submit-btn"' static/index.html` exits 0
    - `grep '<style' static/index.html` exits 1 (no matches — zero CSS)
    - `grep 'style=' static/index.html` exits 1 (no inline styles)
    - `grep 'src="/static/app.js"' static/index.html` exits 0
    - Python check: all 16 required IDs present — `test_html_required_ids_present` stub turns GREEN
    - HTML is valid DOCTYPE html5 with lang="pt-BR"
  </acceptance_criteria>
  <done>
    static/index.html created with: valid HTML5 structure, all 16 required IDs, all sg-* classes, zero CSS, script tag pointing to /static/app.js, hidden attributes on initially-hidden elements.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→HTML | HTML served to browser; no user-controlled content in HTML itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | static/index.html value divs | mitigate | Value divs are empty in HTML; JS uses textContent (never innerHTML) per RESEARCH.md XSS mitigation — enforced in Plan 03 |
| T-04-03 | Tampering | download-link href | mitigate | href is not set in HTML; JS sets it from data.download_url (verified to start with /files/) — enforced in Plan 03 |
</threat_model>

<verification>
```bash
cd /home/glow/Documentos/SoundGrabber2.0
# 1. All 16 IDs present
python3 -c "
ids = ['url-input','submit-btn','progress-area','progress-label','result-card','bpm-value','bpm-half-value','bpm-double-value','key-value','camelot-value','size-value','download-link','error-area','error-message','retry-btn','validation-error']
html = open('static/index.html').read()
missing = [i for i in ids if f'id=\"{i}\"' not in html]
print('Missing:', missing if missing else 'NONE')
"
# 2. Zero CSS
grep -c '<style' static/index.html || echo "0 style tags — PASS"
grep -c 'style=' static/index.html || echo "0 inline styles — PASS"
# 3. App.js reference
grep 'src="/static/app.js"' static/index.html && echo "script tag — PASS"
```
</verification>

<success_criteria>
- `static/` directory created
- `static/index.html` contains all 16 required IDs
- Zero CSS anywhere in the file (no `<style>`, no `style=`, no `.css` references)
- `<script src="/static/app.js">` present at end of body
- `test_html_required_ids_present` will pass when GET / is mounted in Plan 04
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend/04-02-SUMMARY.md`
</output>
